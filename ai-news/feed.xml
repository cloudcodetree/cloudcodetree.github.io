<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0"
  xmlns:content="http://purl.org/rss/1.0/modules/content/"
  xmlns:dc="http://purl.org/dc/elements/1.1/"
  xmlns:atom="http://www.w3.org/2005/Atom"
  xmlns:media="http://search.yahoo.com/mrss/">
  <channel>
    <title>AI News · CloudCodeTree</title>
    <link>https://cloudcodetree.com/ai-news/</link>
    <atom:link href="https://cloudcodetree.com/ai-news/feed.xml" rel="self" type="application/rss+xml" />
    <description>Daily field notes on AI-assisted engineering.</description>
    <language>en-us</language>
    <generator>cloudcodetree generate-feeds.mjs</generator>
    <lastBuildDate>Sat, 08 Aug 2026 12:11:04 GMT</lastBuildDate>
  <item>
    <title><![CDATA[Automate Your Team's Agentic Workflows with a Comment: GitHub Copilot Cloud Agent Automations]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-08-06-github-copilot-comment-automations-agentic-trigger/</link>
    <guid isPermaLink="false">2026-08-08-06-github-copilot-comment-automations-agentic-trigger</guid>
    <pubDate>Sat, 08 Aug 2026 12:05:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Workflow]]></category>
    <category><![CDATA[Agents]]></category>
    <category><![CDATA[Developer Tools]]></category>
    <description><![CDATA[GitHub Copilot can now run a full cloud agent task whenever someone posts a matching comment on an issue or PR — type /docs on a PR and Copilot writes the documentation and opens a follow-up PR.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-06-github-copilot-comment-automations-agentic-trigger.jpg" alt="" /></p>
<p><strong>Type <code>/docs</code> on any PR comment and GitHub's cloud agent writes the documentation, opens a follow-up PR, and closes the loop — no buttons, no dashboard navigation required.</strong></p>
<p>GitHub shipped comment-triggered Copilot cloud agent automations on Aug 3. Define an automation with a trigger phrase; whenever anyone posts that phrase in an issue or PR comment, Copilot runs the agent task automatically. The result lands as a new PR, a follow-up issue, or a reply comment — depending on what your automation is configured to do.</p>
<p>## Set up a comment trigger</p>
<p>1. Go to your repo → <strong>Agents</strong> → <strong>Automations</strong> → <strong>New automation</strong>
2. Set the trigger type to &quot;Issue or PR comment&quot; and enter the trigger phrase (e.g. <code>/docs</code>)
3. Write the agent instructions for what it should do when triggered
4. Save and enable</p>
<p>Now anyone with repo access can fire the automation by commenting the trigger phrase.</p>
<p>## Three workflows worth wiring up now</p>
<p><strong>Auto-docs on PR merge:</strong>
``<code>
Trigger: /docs
Instructions: Read the diff in this PR. Update or write the relevant
docstrings, README sections, and inline comments to reflect the changes.
Open a new PR targeting this branch with the documentation updates only.
</code>``</p>
<p><strong>Bug investigation on demand:</strong>
``<code>
Trigger: /investigate
Instructions: Reproduce the reported issue from this issue description.
Search the codebase for the likely root cause. Post a comment with 3
likely causes, the relevant files, and suggested first steps.
</code>``</p>
<p><strong>Tech-debt tracker:</strong>
``<code>
Trigger: /track-debt
Instructions: Review the changes in this PR. Identify shortcuts, hardcoded
values, missing tests, and deferred work. Create a GitHub issue for each
one, labeled tech-debt, with a clear description and suggested fix.
</code>``</p>
<p>## Also shipping Aug 3: adjustable reasoning level</p>
<p>The same release adds a reasoning level control (low / medium / high) when you start a Copilot cloud agent task. Use <code>low</code> for fast, mechanical tasks (renaming, formatting); <code>high</code> for open-ended investigation or cross-file refactors that benefit from deeper planning.</p>
<p>Comment automations are available on Copilot Pro, Pro+, Max, Business, and Enterprise. Business/Enterprise requires the Copilot cloud agent policy enabled by an admin.</p>
<p><strong>Sources:</strong> <a href="https://github.blog/changelog/2026-08-03-trigger-copilot-automations-with-comments/">Trigger Copilot automations with comments — GitHub Changelog</a> · <a href="https://github.blog/changelog/2026-08-03-customize-the-reasoning-level-for-copilot-cloud-agent/">Customize reasoning level for Copilot cloud agent — GitHub Changelog</a> · <a href="https://docs.github.com/en/copilot/how-tos/use-copilot-agents/cloud-agent">Copilot cloud agent docs — GitHub Docs</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-06-github-copilot-comment-automations-agentic-trigger.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-06-github-copilot-comment-automations-agentic-trigger.jpg" />
  </item>
  <item>
    <title><![CDATA[Claude Code v2.1.225: Gateway Spend Warnings Now Name the Cap, Agent Trust Prompt Added]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-08-05-claude-code-v2125-gateway-spend-workspace-trust/</link>
    <guid isPermaLink="false">2026-08-08-05-claude-code-v2125-gateway-spend-workspace-trust</guid>
    <pubDate>Sat, 08 Aug 2026 12:04:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[News]]></category>
    <category><![CDATA[Claude Code]]></category>
    <category><![CDATA[Developer Tools]]></category>
    <description><![CDATA[Claude Code v2.1.225 names the specific spend cap and reset time when a gateway limit is hit, and now prompts before claude agents runs in untrusted directories. v2.1.226 is bug-fix only.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-05-claude-code-v2125-gateway-spend-workspace-trust.jpg" alt="" /></p>
<p><strong>Claude Code v2.1.225 makes gateway spend-limit messages actionable — you now see which cap was hit, when it resets, and the operator's note — instead of a generic &quot;limit reached.&quot;</strong></p>
<p>Two additions in v2.1.225 (Aug 7), no feature changes in v2.1.226 (Aug 8):</p>
<p><strong>Gateway spend-limit messages.</strong> When a Claude Apps Gateway spend cap is reached, the usage warning now displays the cap name, its reset time, and any message the operator configured — so a CI job that stalls on a budget limit tells you exactly what hit and when it clears, rather than sending you to the gateway dashboard to find out. Requires gateway version 2.1.225+.</p>
<p><strong>Workspace trust prompt for <code>claude agents</code>.</strong> The <code>claude agents</code> command now asks whether to trust an untrusted directory before running, matching the existing behavior of <code>claude</code>. This prevents an agent session from silently operating on code outside its intended scope when launched from an unfamiliar or shared directory.</p>
<p><strong>Why it matters:</strong> If you're running Claude Code through the Claude Apps Gateway (which adds SSO and per-user spend caps for teams on AWS or Google Cloud), the improved spend message removes the main friction point when a budget cap trips in CI: you no longer need to context-switch to the gateway dashboard to understand what happened and when you can retry.</p>
<p><strong>Sources:</strong> <a href="https://code.claude.com/docs/en/changelog">Claude Code changelog — code.claude.com</a> · <a href="https://github.com/anthropics/claude-code/releases/tag/v2.1.225">Release v2.1.225 — github.com/anthropics/claude-code</a> · <a href="https://byteiota.com/claude-apps-gateway-sso-spend-caps-claude-code/">Claude Apps Gateway — byteiota.com</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-05-claude-code-v2125-gateway-spend-workspace-trust.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-05-claude-code-v2125-gateway-spend-workspace-trust.jpg" />
  </item>
  <item>
    <title><![CDATA[From Text to Vectors: Compute Your First Text Embeddings with sentence-transformers]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-08-04-sentence-transformers-text-embeddings-foundations/</link>
    <guid isPermaLink="false">2026-08-08-04-sentence-transformers-text-embeddings-foundations</guid>
    <pubDate>Sat, 08 Aug 2026 12:03:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Tutorial]]></category>
    <category><![CDATA[Embeddings]]></category>
    <category><![CDATA[HuggingFace]]></category>
    <description><![CDATA[Three lines of Python turn any text into a 384-dimensional vector — then cosine similarity shows you exactly how closely two sentences mean the same thing, even when they share no words.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-04-sentence-transformers-text-embeddings-foundations.jpg" alt="" /></p>
<p><strong>Three lines of Python turn any text into a 384-dimensional vector — and cosine similarity tells you exactly how closely two sentences mean the same thing, even when they share no words.</strong></p>
<p><strong>What you'll be able to do after this:</strong>
- Turn raw text into dense vector representations using a pre-trained model — no training required, runs on CPU
- Measure semantic similarity between any two strings using cosine similarity, even when they share no vocabulary
- Understand what an embedding actually <em>is</em> and why the geometric distance between vectors captures meaning — the foundation for every RAG pipeline, semantic search engine, and recommendation system</p>
<p>## Install</p>
<p>``<code>bash
pip install sentence-transformers
</code>``</p>
<p>The model used here is <code>all-MiniLM-L6-v2</code>: 22 million parameters, 384-dimensional output, roughly 14,000 sentences per second on a standard CPU. It's the standard starting point — small enough to run anywhere, good enough for most retrieval and similarity tasks.</p>
<p>## Compute your first embeddings</p>
<p>```python
from sentence_transformers import SentenceTransformer</p>
<p>model = SentenceTransformer(&quot;all-MiniLM-L6-v2&quot;)</p>
<p>sentences = [
    &quot;The weather is lovely today.&quot;,
    &quot;It's so sunny outside!&quot;,
    &quot;He drove to the store yesterday.&quot;,
]</p>
<p>embeddings = model.encode(sentences)
print(embeddings.shape)  # (3, 384)
```</p>
<p>Each sentence becomes a 384-dimensional float array. That's it. The model has already learned — from over a billion training pairs — how to map semantically similar text to nearby regions of the vector space.</p>
<p>## Measure similarity</p>
<p>``<code>python
similarities = model.similarity(embeddings, embeddings)
print(similarities)
</code>``</p>
<p>Output (approximate):
``<code>
tensor([[1.00, 0.67, 0.10],
        [0.67, 1.00, 0.09],
        [0.10, 0.09, 1.00]])
</code>``</p>
<p>The two weather sentences score 0.67 — semantically close, despite sharing no words. &quot;He drove to the store&quot; scores ~0.10 against both — unrelated. A cosine score of 1.0 means identical direction in vector space; 0.0 means orthogonal (unrelated); negative means opposite.</p>
<p>## Encode a query against a corpus</p>
<p>The most common real-world use: given a user query, find the most relevant passages from a set of documents.</p>
<p>```python
corpus = [
    &quot;Puppies are adorable little dogs.&quot;,
    &quot;The stock market fell sharply today.&quot;,
    &quot;Kittens are playful young cats.&quot;,
    &quot;Interest rates affect bond prices.&quot;,
]
query = &quot;cute baby animals&quot;</p>
<p>corpus_embeddings = model.encode(corpus)
query_embedding = model.encode([query])</p>
<p>scores = model.similarity(query_embedding, corpus_embeddings)[0]
ranked = sorted(zip(scores, corpus), reverse=True)</p>
<p>for score, sentence in ranked:
    print(f&quot;{score:.2f}  {sentence}&quot;)
```</p>
<p>Output:
``<code>
0.71  Puppies are adorable little dogs.
0.68  Kittens are playful young cats.
0.04  The stock market fell sharply today.
0.02  Interest rates affect bond prices.
</code>``</p>
<p>The model ranked &quot;cute baby animals&quot; correctly against passages it has never seen, with no keyword overlap required.</p>
<p>## What comes next</p>
<p>This is the foundation. The next step in the curriculum is storing these vectors in a vector database — Chroma, FAISS, or pgvector — so you can search across thousands or millions of them in milliseconds. The <code>encode()</code> + similarity pattern you just ran is exactly what happens inside every RAG retrieval step; knowing it hands-on makes the rest of the stack legible.</p>
<p>Over 10,000 pre-trained Sentence Transformer models are available on <a href="https://huggingface.co/models?library=sentence-transformers">Hugging Face</a> for specialized domains (code, scientific text, multilingual) — all use the same API.</p>
<p><strong>Sources:</strong> <a href="https://www.sbert.net/docs/quickstart.html">Quickstart — sbert.net</a> · <a href="https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2">all-MiniLM-L6-v2 — Hugging Face</a> · <a href="https://www.sbert.net/docs/sentence_transformer/usage/semantic_textual_similarity.html">Semantic Textual Similarity — sbert.net</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-04-sentence-transformers-text-embeddings-foundations.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-04-sentence-transformers-text-embeddings-foundations.jpg" />
  </item>
  <item>
    <title><![CDATA[Three Claude Code v2.1.224 Settings Every Team Should Know: Session Caps, Repo Permissions, and Archive Plugins]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-08-03-claude-code-session-caps-repo-permissions/</link>
    <guid isPermaLink="false">2026-08-08-03-claude-code-session-caps-repo-permissions</guid>
    <pubDate>Sat, 08 Aug 2026 04:09:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Workflow]]></category>
    <category><![CDATA[Claude Code]]></category>
    <category><![CDATA[Best Practices]]></category>
    <description><![CDATA[Claude Code v2.1.224 adds tunable session-wide safety caps, repo-root "Always allow" permission persistence, and zip-based archive plugin distribution — three settings that matter most in team and…]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-03-claude-code-session-caps-repo-permissions.jpg" alt="" /></p>
<p><strong>Claude Code v2.1.224 adds session-wide runaway-loop guards (tunable via env vars), repo-root &quot;Always allow&quot; persistence, and zip-based plugin distribution — three settings that matter most in team and CI contexts.</strong></p>
<p>## 1. Session-wide safety caps — and how to tune them</p>
<p>v2.1.224 introduces per-session limits to stop runaway agentic loops before they rack up large API bills or spin indefinitely:</p>
<p>| Cap | Default | Env var to override |
|---|---|---|
| WebSearch calls | 200 | <code>CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION</code> |
| Subagent spawns | 200 | <code>CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION</code> |</p>
<p>For most interactive sessions, 200 is far more than enough. For legitimate long-running CI jobs that fan out many subagents — a full-repo migration, a large test-suite analysis — raise the cap before the run:</p>
<p>``<code>bash
export CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION=500
claude -p &quot;Migrate all 380 route handlers to the new API format&quot; \
  --output-format json \
  --allowedTools &quot;Read,Edit,Bash(node:*)&quot;
</code>``</p>
<p>When a cap is hit, Claude Code logs a warning and terminates cleanly with exit code 1 — so your pipeline can catch it and retry with a higher limit if needed.</p>
<p>## 2. &quot;Always allow&quot; rules now save at the repo root</p>
<p>Previously, approving a tool action with &quot;Always allow&quot; stored the rule in your global <code>~/.claude/settings.json</code>. As of v2.1.224, it writes to <code>.claude/settings.json</code> in the repository root. Concretely:</p>
<p>- Project allowlists travel with the project (commit them, and teammates get them automatically)
- Approvals granted in a git worktree persist across sessions and other worktrees in the same repo
- Per-repo rules no longer bleed into unrelated projects</p>
<p>For teams: commit <code>.claude/settings.json</code> with your standard approved tool set. New developers who clone the repo inherit the team's allowlist without having to approve each action from scratch.</p>
<p>## 3. Archive plugin source</p>
<p>v2.1.224 adds a new plugin source type: install a plugin directly from a zip file served over HTTPS, no git repo or npm package required.</p>
<p>``<code>json
// .claude/settings.json
{
  &quot;pluginSources&quot;: [
    {
      &quot;type&quot;: &quot;archive&quot;,
      &quot;url&quot;: &quot;https://internal.example.com/plugins/company-standards.zip&quot;,
      &quot;sha256&quot;: &quot;a1b2c3d4e5f6...&quot;
    }
  ]
}
</code>``</p>
<p>The <code>sha256</code> field is optional but strongly recommended for any internal distribution — it pins the exact archive so a compromised URL can't silently swap in a different payload.</p>
<p><strong>Sources:</strong> <a href="https://code.claude.com/docs/en/changelog">Claude Code changelog — code.claude.com</a> · <a href="https://releasebot.io/updates/anthropic/claude-code">Claude Code Updates Aug 2026 — Releasebot</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-03-claude-code-session-caps-repo-permissions.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-03-claude-code-session-caps-repo-permissions.jpg" />
  </item>
  <item>
    <title><![CDATA[OpenAI Upgrades ChatGPT Free to GPT-5.6 Luna with Unlimited Text Chats]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-08-02-chatgpt-gpt56-luna-free-default/</link>
    <guid isPermaLink="false">2026-08-08-02-chatgpt-gpt56-luna-free-default</guid>
    <pubDate>Sat, 08 Aug 2026 04:08:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[News]]></category>
    <category><![CDATA[LLM]]></category>
    <category><![CDATA[Developer Tools]]></category>
    <description><![CDATA[OpenAI makes GPT-5.6 Luna the default for all free ChatGPT users and removes the daily text chat limit — 62% fewer factual errors than GPT-5.5 Instant, with a Think button for harder questions.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-02-chatgpt-gpt56-luna-free-default.jpg" alt="" /></p>
<p><strong>GPT-5.6 Luna — 62% fewer factual errors than GPT-5.5 Instant — is now the default model for all ChatGPT Free users, with text chats uncapped and a Think button for harder questions.</strong></p>
<p>OpenAI's <a href="https://openai.com/index/improving-gpt-5-6-sol-in-chatgpt/">August 6 update</a> promotes Luna from its previous position as a mid-tier option to the default experience for everyone on the free plan, replacing GPT-5.5 Instant. Unlimited text conversations are now available (image generation, file uploads, and voice still have limits). The same release upgrades GPT-5.6 Sol — the pro-tier model — with improved factual accuracy and instruction following.</p>
<p><strong>Why it matters:</strong> Luna's API price sits at $0.20/$1.20 per million input/output tokens. The model your users encounter for free is now the same model that, at the API, is the most cost-efficient frontier option for high-volume agentic pipelines. The free-tier baseline just rose significantly.</p>
<p><strong>Sources:</strong> <a href="https://openai.com/index/improving-gpt-5-6-sol-in-chatgpt/">OpenAI announcement</a> · <a href="https://techcrunch.com/2026/08/06/openai-brings-unlimited-chatgpt-text-chats-to-free-users/">TechCrunch</a> · <a href="https://thenextweb.com/news/chatgpt-free-unlimited-text-chats-gpt-5-6-luna-default">The Next Web</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-02-chatgpt-gpt56-luna-free-default.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-02-chatgpt-gpt56-luna-free-default.jpg" />
  </item>
  <item>
    <title><![CDATA[Measure Before You Improve: Evaluate Any RAG Pipeline with RAGAS]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-08-01-ragas-rag-evaluation-faithfulness/</link>
    <guid isPermaLink="false">2026-08-08-01-ragas-rag-evaluation-faithfulness</guid>
    <pubDate>Sat, 08 Aug 2026 04:07:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Tutorial]]></category>
    <category><![CDATA[RAG]]></category>
    <category><![CDATA[Best Practices]]></category>
    <description><![CDATA[RAGAS gives you four numbers that diagnose any RAG pipeline: faithfulness, context precision, context recall, and answer relevancy — each pointing to a different fix.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-01-ragas-rag-evaluation-faithfulness.jpg" alt="" /></p>
<p><strong><code>ragas</code> gives you four numbers that tell you exactly where your RAG pipeline breaks — before you touch the code, run a benchmark, or guess at what to fix.</strong></p>
<p><strong>What you'll be able to do after this:</strong>
- Run automated RAG evaluation with one function call and get per-metric scores for every query in your test set
- Distinguish retrieval failures (low context precision or recall) from generation failures (low faithfulness or answer relevancy) and know which layer to fix
- Drop evaluation into CI so regressions surface before deployment</p>
<p>## Install</p>
<p>``<code>bash
pip install ragas langchain_openai
</code>``</p>
<p>Set <code>OPENAI_API_KEY</code> — RAGAS uses an LLM as judge. Any OpenAI-compatible endpoint works.</p>
<p>## Build an evaluation dataset</p>
<p>You need three things per example: the user question, the retrieved context chunks (as a list of strings), and the model's answer. Start with 10–30 representative queries from your actual use case.</p>
<p>```python
from ragas import EvaluationDataset, SingleTurnSample</p>
<p>samples = [
    SingleTurnSample(
        user_input=&quot;What is the return policy?&quot;,
        retrieved_contexts=[
            &quot;Items can be returned within 30 days of purchase with a receipt.&quot;,
            &quot;Sale items are final and cannot be returned.&quot;,
        ],
        response=&quot;You can return items within 30 days with a receipt. Sale items are non-refundable.&quot;,
    ),
    # ... add more samples
]</p>
<p>dataset = EvaluationDataset(samples=samples)
```</p>
<p>## Run the evaluation</p>
<p>```python
from ragas import evaluate
from ragas.metrics import Faithfulness, AnswerRelevancy, ContextPrecision, ContextRecall
from ragas.llms import LangchainLLMWrapper
from langchain_openai import ChatOpenAI</p>
<p>evaluator_llm = LangchainLLMWrapper(ChatOpenAI(model=&quot;gpt-4o-mini&quot;))</p>
<p>results = evaluate(
    dataset=dataset,
    metrics=[Faithfulness(), AnswerRelevancy(), ContextPrecision(), ContextRecall()],
    llm=evaluator_llm,
)</p>
<p>print(results.to_pandas()[[&quot;faithfulness&quot;,&quot;answer_relevancy&quot;,&quot;context_precision&quot;,&quot;context_recall&quot;]])
```</p>
<p>## Reading the scores</p>
<p>| Metric | What it measures | Low score → fix |
|---|---|---|
| <strong>Faithfulness</strong> | Answer grounded in retrieved context? | Prompt: add citation enforcement; reduce hallucination |
| <strong>Context Precision</strong> | Right chunks ranked first? | Reranker, better embeddings, metadata filters |
| <strong>Context Recall</strong> | Did retrieval find all relevant chunks? | Increase <code>k</code>, better chunking, hybrid search |
| <strong>Answer Relevancy</strong> | Does the answer address the question? | Prompt engineering, filter off-topic retrieval |</p>
<p>A score below ~0.7 on any metric usually points to a specific, fixable issue. Fix precision before recall — surfacing the right chunk first is cheaper than retrieving more and hoping a reranker saves you.</p>
<p>## Add a CI gate</p>
<p>``<code>python
# In a CI script or conftest.py
results = evaluate(dataset, metrics=[Faithfulness(), ContextPrecision()], llm=evaluator_llm)
df = results.to_pandas()
assert df[&quot;faithfulness&quot;].mean() &gt; 0.75, f&quot;Faithfulness regression: {df['faithfulness'].mean():.2f}&quot;
assert df[&quot;context_precision&quot;].mean() &gt; 0.70, f&quot;Precision regression: {df['context_precision'].mean():.2f}&quot;
</code>``</p>
<p>RAGAS also accepts Hugging Face datasets as input, so you can version your eval set alongside your code and track scores across commits.</p>
<p><strong>Sources:</strong> <a href="https://docs.ragas.io/en/stable/getstarted/quickstart/">RAGAS Quickstart — docs.ragas.io</a> · <a href="https://docs.ragas.io/en/stable/tutorials/rag/">Evaluate a simple RAG system — docs.ragas.io</a> · <a href="https://github.com/vibrantlabsai/ragas">RAGAS GitHub — vibrantlabsai/ragas</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-01-ragas-rag-evaluation-faithfulness.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-08-01-ragas-rag-evaluation-faithfulness.jpg" />
  </item>
  <item>
    <title><![CDATA[Automate Claude Code with --print and --output-format json: Scripting and CI/CD Pipelines]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-07-08-claude-code-headless-mode-automation/</link>
    <guid isPermaLink="false">2026-08-07-08-claude-code-headless-mode-automation</guid>
    <pubDate>Fri, 07 Aug 2026 20:05:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Workflow]]></category>
    <category><![CDATA[Claude Code]]></category>
    <category><![CDATA[Best Practices]]></category>
    <description><![CDATA[Run Claude Code non-interactively with -p, get structured JSON output, restrict tools with --allowedTools, and drop AI steps into any CI/CD pipeline — no terminal UI required.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-08-claude-code-headless-mode-automation.jpg" alt="" /></p>
<p><strong><code>claude -p &quot;prompt&quot; --output-format json</code> turns Claude Code into a scriptable pipeline step — structured output, deterministic exit codes, and schema-constrained responses for CI/CD automation.</strong></p>
<p>Most Claude Code usage is interactive. But <code>-p</code> (non-interactive/print mode) is the bridge between the agent and your toolchain: one command in, parseable data out. Every workflow that runs on a schedule, inside GitHub Actions, or as part of a build pipeline benefits from this.</p>
<p>## The three flags you need</p>
<p><strong><code>-p</code> / <code>--print</code></strong> — non-interactive mode. Claude reads the prompt, runs the agent loop, and exits. No REPL, no UI.</p>
<p><strong><code>--output-format</code></strong> — controls what comes out on stdout:
- <code>text</code> (default) — plain text result
- <code>json</code> — structured object with <code>result</code>, <code>session_id</code>, <code>total_cost_usd</code>, and <code>duration_ms</code>
- <code>stream-json</code> — newline-delimited JSON, one event per line; useful for real-time streaming in long-running jobs</p>
<p><strong><code>--allowedTools</code></strong> — restrict which tools Claude can use. Critical for CI: only allow what the task actually needs.</p>
<p>## Minimal example</p>
<p>``<code>bash
# One-shot code review, JSON output
claude -p &quot;Review the staged diff for security issues. Be concise.&quot; \
  --output-format json \
  --allowedTools &quot;Bash(git diff:*),Read&quot; \
  | jq -r '.result'
</code>``</p>
<p>Exit code <code>0</code> = success; any non-zero code = error. Unix-standard, so <code>set -e</code> pipelines work correctly.</p>
<p>## GitHub Actions integration</p>
<p>``<code>yaml
- name: AI security scan
  run: |
    REVIEW=$(claude -p &quot;Scan the staged files for hardcoded secrets or SQL injection risks&quot; \
      --output-format json \
      --allowedTools &quot;Read,Bash(git diff:*)&quot; \
      | jq -r '.result')
    echo &quot;## Security Review&quot; &gt;&gt; $GITHUB_STEP_SUMMARY
    echo &quot;$REVIEW&quot; &gt;&gt; $GITHUB_STEP_SUMMARY
</code>``</p>
<p>## Structured output with <code>--json-schema</code></p>
<p>Force Claude to return a JSON object matching your schema:</p>
<p>``<code>bash
claude -p &quot;Analyze the test results in ./test-output.xml&quot; \
  --output-format json \
  --allowedTools &quot;Read&quot; \
  --json-schema '{
    &quot;type&quot;: &quot;object&quot;,
    &quot;properties&quot;: {
      &quot;passed&quot;: {&quot;type&quot;: &quot;boolean&quot;},
      &quot;failures&quot;: {&quot;type&quot;: &quot;array&quot;, &quot;items&quot;: {&quot;type&quot;: &quot;string&quot;}},
      &quot;summary&quot;: {&quot;type&quot;: &quot;string&quot;}
    },
    &quot;required&quot;: [&quot;passed&quot;, &quot;failures&quot;, &quot;summary&quot;]
  }' \
  | jq '.result'
</code>``</p>
<p>The schema-constrained response lands in the <code>result</code> field alongside <code>total_cost_usd</code> — ready to pipe into <code>jq</code>, a script, or a monitoring system.</p>
<p>## What to lock down in CI</p>
<p>- Always pass <code>--allowedTools</code> listing only what the task needs — omit Bash entirely for read-only analysis
- Store <code>ANTHROPIC_API_KEY</code> in your CI secrets; Claude Code picks it up from the environment
- Use <code>--max-turns N</code> to cap the agent loop length for predictable runtime</p>
<p><strong>Sources:</strong> <a href="https://code.claude.com/docs/en/headless">Run Claude Code programmatically — code.claude.com</a> · <a href="https://institute.sfeir.com/en/claude-code/claude-code-headless-mode-and-ci-cd/">Headless Mode and CI/CD — SFEIR Institute</a> · <a href="https://www.buildthisnow.com/blog/guide/development/claude-code-headless-mode">Claude Code Headless Mode guide — buildthisnow.com</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-08-claude-code-headless-mode-automation.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-08-claude-code-headless-mode-automation.jpg" />
  </item>
  <item>
    <title><![CDATA[Scale Any vLLM-Backed Model to Production with Ray Serve LLM]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-07-07-ray-serve-llm-vllm-autoscaling/</link>
    <guid isPermaLink="false">2026-08-07-07-ray-serve-llm-vllm-autoscaling</guid>
    <pubDate>Fri, 07 Aug 2026 20:04:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Tutorial]]></category>
    <category><![CDATA[Self-Hosting]]></category>
    <category><![CDATA[Developer Tools]]></category>
    <description><![CDATA[Ray Serve LLM wraps vLLM in a production harness — autoscaling that responds to queue depth, multi-model routing, and a built-in OpenAI-compatible API in under 30 lines of Python.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-07-ray-serve-llm-vllm-autoscaling.jpg" alt="" /></p>
<p><strong>Ray Serve LLM wraps vLLM in a production harness — queue-depth autoscaling, multi-model routing, and an OpenAI-compatible API endpoint in ~25 lines of Python.</strong></p>
<p>You've loaded a model with Ollama or spun up a raw vLLM server. That works for one machine and modest load. When you need autoscaling, rolling upgrades, multi-model routing, or true production reliability, Ray Serve LLM is the missing layer.</p>
<p><strong>What you'll be able to do after this:</strong>
- Deploy any Hugging Face model behind an OpenAI-compatible REST endpoint using Ray Serve LLM and vLLM
- Enable autoscaling that responds to request queue depth (not CPU) — spinning up replicas under load, scaling back down at idle
- Serve multiple models from one cluster and route traffic by model name, with no changes to the client</p>
<p>## Install</p>
<p>``<code>bash
pip install &quot;ray[serve]&quot; vllm
</code>``</p>
<p>## Deploy a model in ~20 lines</p>
<p>```python
# serve_llm.py
from ray import serve
from ray.serve.llm import LLMServer, LLMConfig</p>
<p>config = LLMConfig(
    model_loading_config={
        &quot;model_id&quot;: &quot;meta-llama/Llama-3.1-8B-Instruct&quot;,
    },
    deployment_config={
        &quot;autoscaling_config&quot;: {
            &quot;min_replicas&quot;: 1,
            &quot;max_replicas&quot;: 4,
            &quot;target_ongoing_requests&quot;: 5,
        }
    },
    accelerator_type=&quot;A10G&quot;,   # or &quot;T4&quot;, &quot;A100&quot;, etc.
)</p>
<p>llm_server = LLMServer.from_llm_config(config)
serve.run(llm_server)
```</p>
<p>```bash
# Start
serve run serve_llm.py</p>
<p># Query — standard OpenAI API
curl http://localhost:8000/v1/chat/completions \
  -H &quot;Content-Type: application/json&quot; \
  -d '{
    &quot;model&quot;: &quot;meta-llama/Llama-3.1-8B-Instruct&quot;,
    &quot;messages&quot;: [{&quot;role&quot;: &quot;user&quot;, &quot;content&quot;: &quot;Explain KV caching in one paragraph.&quot;}]
  }'
```</p>
<p>## Multi-model serving</p>
<p>Add a second <code>LLMConfig</code> entry with a different <code>model_id</code> — Ray Serve routes requests by the <code>model</code> field in each API call. No load balancer config needed; Ray handles it.</p>
<p>## Why queue depth, not CPU</p>
<p>Traditional autoscalers trigger on CPU or memory. LLMs are I/O-bound: a GPU can be 100% utilized but still have headroom for more requests in the batch. Ray Serve autoscales on <code>target_ongoing_requests</code> — the number of requests currently being processed per replica. This correctly captures backpressure and scales before the queue grows too long.</p>
<p>## What this covers beyond raw vLLM</p>
<p>| Feature | Raw <code>vllm serve</code> | Ray Serve LLM |
|---|---|---|
| OpenAI-compatible API | Yes | Yes |
| Autoscaling | No | Yes (queue-depth) |
| Multi-model routing | No | Yes |
| Rolling upgrades | No | Yes |
| Multi-node tensor parallel | Yes | Yes |
| Health checks | Basic | Enterprise-grade |</p>
<p><strong>Sources:</strong> <a href="https://docs.ray.io/en/latest/serve/llm/index.html">Serving LLMs — Ray 2.56.0 docs</a> · <a href="https://docs.ray.io/en/latest/serve/llm/quick-start.html">Ray Serve LLM Quickstart</a> · <a href="https://www.anyscale.com/blog/llm-apis-ray-data-serve">Announcing Native LLM APIs in Ray Data and Ray Serve — Anyscale blog</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-07-ray-serve-llm-vllm-autoscaling.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-07-ray-serve-llm-vllm-autoscaling.jpg" />
  </item>
  <item>
    <title><![CDATA[Claude Code Self-Hosted Environments Are Now in Public Beta: Run Agent Sessions on Your Own Compute]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-07-06-claude-code-self-hosted-environments-cross-session/</link>
    <guid isPermaLink="false">2026-08-07-06-claude-code-self-hosted-environments-cross-session</guid>
    <pubDate>Fri, 07 Aug 2026 12:05:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Workflow]]></category>
    <category><![CDATA[Claude Code]]></category>
    <category><![CDATA[Best Practices]]></category>
    <description><![CDATA[Claude Code's self-hosted environments (public beta) let Team/Enterprise teams run agent sessions on infrastructure they control — inside the firewall, next to internal services, with no source cod…]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-06-claude-code-self-hosted-environments-cross-session.jpg" alt="" /></p>
<p><strong>Claude Code's new self-hosted environments (public beta, Team/Enterprise) run agent sessions on your own compute — inside your firewall, next to internal APIs, keeping source code and artifacts off Anthropic's servers.</strong></p>
<p>Anthropic's cloud infrastructure covers most workloads, but regulated industries, internal-API-dependent codebases, and strict compliance requirements need agent execution on controlled compute. Self-hosted environments solve this while keeping the full Claude Code cloud UI.</p>
<p>## How it works</p>
<p>You deploy <em>runners</em> — long-lived processes that pick up session requests from Claude Code clients (web, mobile, desktop, routines) and start a local Claude Code process per session. Two modes:</p>
<p>- <strong>Fixed</strong>: a set number of runners stay running; sessions distribute across them
- <strong>On-demand</strong>: an orchestrator watches the queue and provisions runners as sessions arrive</p>
<p>Repository checkouts, build artifacts, secrets, and any files a session creates stay on your machines. The session UI runs through Anthropic's cloud; only the agent execution is local.</p>
<p>## Set up (Team or Enterprise plan)</p>
<p>1. Go to your organization's Claude settings → <strong>Environments</strong> → <strong>Self-hosted (beta)</strong>
2. Deploy runners on your infrastructure (the <a href="https://code.claude.com/docs/en/self-hosted-environments">docs cover Kubernetes and systemd deployments</a>)
3. Runners register with a token and start accepting sessions immediately</p>
<p>No changes to how developers start a session — they get the same web/desktop/mobile UI; the agent just executes on your hardware.</p>
<p>## Also shipping in this update: cross-session messaging + spawn cap gone</p>
<p>Two more agent coordination improvements alongside self-hosted environments:</p>
<p>- <strong>Cross-session messaging</strong>: <code>ListAgents</code> now discovers Claude Code sessions across machines (macOS and Linux); <code>SendMessage</code> delivers work between them without human relay. Background agents can hand off to other sessions directly.
- <strong>Subagent spawn cap removed</strong>: the previous 200-subagent ceiling per session is gone. Long autonomous workflows no longer hit a hard limit — concurrency and depth limits still apply, but sustained multi-hour agent runs are no longer bounded by a count.</p>
<p><strong>Sources:</strong> <a href="https://claude.com/blog/run-claude-code-sessions-on-your-own-compute">Self-hosted environments for Claude Code — Anthropic blog</a> · <a href="https://code.claude.com/docs/en/self-hosted-environments">Self-hosted environments docs — code.claude.com</a> · <a href="https://www.gradually.ai/en/changelogs/claude-code/">Claude Code changelog Aug 2026 — Gradually</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-06-claude-code-self-hosted-environments-cross-session.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-06-claude-code-self-hosted-environments-cross-session.jpg" />
  </item>
  <item>
    <title><![CDATA[xAI Ships Grok 4.6 Today: Improved Post-Training on the 1.5T Core, Grok 4.7 (2.1T) Next]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-07-05-grok-46-xai-launch/</link>
    <guid isPermaLink="false">2026-08-07-05-grok-46-xai-launch</guid>
    <pubDate>Fri, 07 Aug 2026 12:04:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[News]]></category>
    <category><![CDATA[LLM]]></category>
    <category><![CDATA[Developer Tools]]></category>
    <description><![CDATA[xAI releases Grok 4.6 today — same 1.5T V9 base as Grok 4.5 but significantly improved SFT and RL; 2.1T Grok 4.7 follows in weeks. The coding-agent race now has four serious competitors.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-05-grok-46-xai-launch.jpg" alt="" /></p>
<p><strong>xAI ships Grok 4.6 today — same 1.5T V9 foundation as Grok 4.5 but with significantly better post-training (SFT + RL); Grok 4.7 at 2.1T follows in weeks.</strong></p>
<p>Grok 4.6 doesn't increase parameter count — it reuses the 1.5T V9 core that xAI co-built with Cursor since that acquisition and delivers gains through improved supervised fine-tuning and reinforcement learning. API access typically follows X/SuperGrok consumer rollout by a few days. Independent benchmark scores (SWE-bench, LiveCodeBench) usually appear within a week of consumer launch.</p>
<p>Grok 4.7, a 2.1T model, is slated to follow Grok 4.6 by a few weeks and is projected to outperform it on every benchmark while trading slightly more latency for better token efficiency.</p>
<p>The practical context: the AI coding space now has four serious competitors — Claude Code, OpenAI Codex/GPT-5.6, Meta Muse Code (launched Aug 5), and xAI Grok — each shipping faster post-training iteration cycles than new architectures. For developers building model-agnostic tools, OpenRouter and LiteLLM routing matters more than model loyalty.</p>
<p><strong>Why it matters:</strong> Four competitive AI coding agents on rapid-release cycles means benchmark gaps close within weeks; routing flexibility beats picking a permanent winner.</p>
<p><strong>Sources:</strong> <a href="https://aitoolsreview.co.uk/insights/grok-4-6-grok-4-7-release-date">Grok 4.6 release confirmed — AIToolsReview</a> · <a href="https://kie.ai/blog/what-is-grok-4-6">Grok 4.6 specs — kie.ai</a> · <a href="https://tbreak.com/grok-4-6-4-7-xai-release-date-specs/">Grok 4.6 &amp; 4.7 roadmap — tbreak.com</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-05-grok-46-xai-launch.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-05-grok-46-xai-launch.jpg" />
  </item>
  <item>
    <title><![CDATA[Build Your First MCP Server in 20 Lines of Python with FastMCP]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-07-04-build-mcp-server-fastmcp-python/</link>
    <guid isPermaLink="false">2026-08-07-04-build-mcp-server-fastmcp-python</guid>
    <pubDate>Fri, 07 Aug 2026 12:03:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Tutorial]]></category>
    <category><![CDATA[MCP]]></category>
    <category><![CDATA[Agents]]></category>
    <description><![CDATA[Install FastMCP, decorate a Python function with @mcp.tool, and Claude Code discovers it automatically — no boilerplate, no manual JSON Schema. Build tools, resources, and prompts in one file.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-04-build-mcp-server-fastmcp-python.jpg" alt="" /></p>
<p><strong>Turn any Python function into an MCP tool Claude can call — FastMCP's decorator API eliminates all the protocol boilerplate so you ship a working server in under 20 lines.</strong></p>
<p><strong>What you'll be able to do after this:</strong>
- Write a Python function and expose it as an MCP tool that Claude Code discovers and calls automatically
- Add read-only resources (data the model can browse) and reusable prompt templates alongside your tools
- Connect the server to Claude Code locally (stdio) or run it remotely over HTTP for team-shared tooling</p>
<p>## Why build your own MCP server?</p>
<p>Every capability Claude Code reaches for — file reads, shell commands, web search — is served by an MCP server. Writing your own gives Claude direct, type-safe access to your internal APIs, databases, or proprietary tooling, with inputs validated and documentation auto-generated from Python type hints.</p>
<p>## A complete server in 20 lines</p>
<p>``<code>bash
pip install fastmcp
</code>``</p>
<p>```python
from fastmcp import FastMCP</p>
<p>mcp = FastMCP(&quot;my-tools&quot;)</p>
<p>@mcp.tool
def get_issue(repo: str, number: int) -&gt; dict:
    &quot;&quot;&quot;Fetch a GitHub issue by repo and number.&quot;&quot;&quot;
    import httpx
    r = httpx.get(f&quot;https://api.github.com/repos/{repo}/issues/{number}&quot;)
    return r.json()</p>
<p>@mcp.resource(&quot;config://defaults&quot;)
def defaults() -&gt; str:
    &quot;&quot;&quot;Return project-wide defaults.&quot;&quot;&quot;
    return &quot;timeout=30\nmax_retries=3&quot;</p>
<p>if __name__ == &quot;__main__&quot;:
    mcp.run()   # stdio by default; pass transport=&quot;http&quot; for remote
```</p>
<p>The <code>@mcp.tool</code> decorator reads the function's type hints to build the JSON Schema and uses the docstring as the tool description. No separate schema file, no serialization code.</p>
<p>## Wire it into Claude Code</p>
<p>Add the server to <code>.claude/settings.json</code>:</p>
<p>``<code>json
{
  &quot;mcpServers&quot;: {
    &quot;my-tools&quot;: {
      &quot;command&quot;: &quot;python&quot;,
      &quot;args&quot;: [&quot;/path/to/server.py&quot;]
    }
  }
}
</code>``</p>
<p>Claude Code discovers <code>get_issue</code> on the next session start. For a remote (HTTP) server shared across a team:</p>
<p>``<code>python
mcp.run(transport=&quot;http&quot;, host=&quot;0.0.0.0&quot;, port=8000)
</code>``</p>
<p>then update the settings to <code>&quot;type&quot;: &quot;http&quot;, &quot;url&quot;: &quot;http://your-server:8000/mcp&quot;</code>.</p>
<p>## The three MCP primitives</p>
<p>FastMCP covers all three things an MCP server can expose:</p>
<p>| Primitive | Decorator | What it does |
|---|---|---|
| Tool | <code>@mcp.tool</code> | Executable function the model can call |
| Resource | <code>@mcp.resource(&quot;uri://…&quot;)</code> | Read-only data the model can browse |
| Prompt | <code>@mcp.prompt</code> | Reusable instruction template in Claude Code's prompt picker |</p>
<p>The <a href="https://gofastmcp.com/getting-started/quickstart">FastMCP quickstart</a> builds all three in a single file — a good first project to clone and extend.</p>
<p><strong>Sources:</strong> <a href="https://gofastmcp.com/getting-started/quickstart">FastMCP quickstart — gofastmcp.com</a> · <a href="https://modelcontextprotocol.io/docs/develop/build-server">Official MCP build-server guide — modelcontextprotocol.io</a> · <a href="https://www.freecodecamp.org/news/how-to-build-your-first-mcp-server-using-fastmcp/">How to Build Your First MCP Server — freeCodeCamp</a> · <a href="https://github.com/PrefectHQ/fastmcp">FastMCP on GitHub — PrefectHQ/fastmcp</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-04-build-mcp-server-fastmcp-python.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-04-build-mcp-server-fastmcp-python.jpg" />
  </item>
  <item>
    <title><![CDATA[Give Claude Code the Entire HuggingFace Ecosystem: Three Commands to Unlock Model Search, Training Jobs, and Hub Pushes]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-07-03-huggingface-skills-claude-code-plugin/</link>
    <guid isPermaLink="false">2026-08-07-03-huggingface-skills-claude-code-plugin</guid>
    <pubDate>Fri, 07 Aug 2026 04:05:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Workflow]]></category>
    <category><![CDATA[Claude Code]]></category>
    <category><![CDATA[HuggingFace]]></category>
    <description><![CDATA[Three Claude Code commands wire in the entire HuggingFace Hub — search models, launch GPU fine-tuning jobs, push checkpoints, and manage datasets from a single plugin marketplace install.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-03-huggingface-skills-claude-code-plugin.jpg" alt="" /></p>
<p><strong>Three Claude Code commands wire in the entire HuggingFace Hub — search models, launch GPU fine-tuning jobs, push checkpoints, and manage datasets from a single plugin marketplace install.</strong></p>
<p>Claude Code is great at writing code. HuggingFace Skills makes it great at <em>running ML workloads</em> — searching the Hub, submitting training jobs to cloud GPUs, evaluating models, and pushing finished checkpoints, all without leaving your Claude Code session.</p>
<p>## Installing the HuggingFace Skills plugin</p>
<p>HuggingFace publishes a plugin marketplace at <code>huggingface/skills</code>. Add it to Claude Code and install the HuggingFace CLI skill:</p>
<p>```bash
# 1. Register the marketplace (one-time)
/plugin marketplace add huggingface/skills</p>
<p># 2. Install the hf-cli skill (teaches Claude every hf command)
/plugin install hf-cli@huggingface/skills</p>
<p># 3. (Optional) Install the fine-tuning skill
/plugin install hf-llm-trainer@huggingface/skills
```</p>
<p>The <code>hf-cli</code> skill is generated from your locally installed HuggingFace CLI, so it's always current. Once installed, Claude Code understands every <code>hf</code> subcommand — search, datasets, models, Spaces, jobs, and buckets.</p>
<p>## What Claude Code can now do</p>
<p>```
You: Search for Llama-3.2 fine-tunes that support 128K context and are &lt; 8B params</p>
<p>Claude: [searches Hub] Found 14 models matching those criteria:
        - meta-llama/Llama-3.2-8B-Instruct (8B, 128K) ✓
        - unsloth/Llama-3.2-8B-Instruct-bnb-4bit (4-bit quant) ✓
        ...</p>
<p>You: Fine-tune the 8B Instruct model on datasets/my-org/support-tickets
     with LoRA on a single H100, push to my-org/support-llama</p>
<p>Claude: [launches job on HuggingFace Jobs, monitors progress, pushes on completion]
```</p>
<p>With the <code>hf-llm-trainer</code> skill, Claude understands the full training decision space: which GPU tier to pick for a given model size, LoRA vs full fine-tuning, SFT vs DPO vs GRPO, how to configure Hub auth, and how to structure multi-stage pipelines.</p>
<p>## Available skills</p>
<p>| Skill | What it teaches Claude |
|---|---|
| <code>hf-cli</code> | All <code>hf</code> CLI commands: Hub search, datasets, models, Spaces, jobs |
| <code>hf-llm-trainer</code> | Fine-tuning from 0.5B to 70B params; SFT, DPO, RLVR; GGUF conversion |
| <code>hf-datasets</code> | Dataset loading, filtering, format conversion, push to Hub |</p>
<p>Install on demand with <code>/plugin install &lt;skill&gt;@huggingface/skills</code>. Each skill is a <code>SKILL.md</code> file with YAML frontmatter — you can read and customize them.</p>
<p>## Why this matters for your workflow</p>
<p>The HuggingFace ecosystem is where open-model AI engineering happens: model discovery, dataset curation, training, evaluation, and serving. Skills make all of that available inside the same Claude Code session where you're writing application code — no context switch to a notebook, no separate CLI window, no copy-paste of model IDs.</p>
<p>HuggingFace published a worked example of using this to have Claude fine-tune an open-source LLM end-to-end — from dataset prep to Hub push — without the developer writing a single training script: <a href="https://huggingface.co/blog/hf-skills-training">We Got Claude to Fine-Tune an Open Source LLM</a>.</p>
<p><strong>Sources:</strong> <a href="https://huggingface.co/docs/hub/en/agents-skills">HuggingFace Skills docs — huggingface.co/docs/hub/en/agents-skills</a> · <a href="https://github.com/huggingface/skills">huggingface/skills — GitHub</a> · <a href="https://huggingface.co/blog/hf-skills-training">We Got Claude to Fine-Tune an Open Source LLM — HuggingFace Blog</a> · <a href="https://deepwiki.com/huggingface/skills/3.1-claude-code-integration">Claude Code Integration — DeepWiki</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-03-huggingface-skills-claude-code-plugin.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-03-huggingface-skills-claude-code-plugin.jpg" />
  </item>
  <item>
    <title><![CDATA[Meta Enters the AI Coding Race With Muse Code: Parallel Worktree Subagents Built on Muse Spark 1.2]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-07-02-meta-muse-code-ai-coding-agent/</link>
    <guid isPermaLink="false">2026-08-07-02-meta-muse-code-ai-coding-agent</guid>
    <pubDate>Fri, 07 Aug 2026 04:04:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[News]]></category>
    <category><![CDATA[Developer Tools]]></category>
    <category><![CDATA[LLM]]></category>
    <description><![CDATA[Meta launched Muse Code on August 5 for macOS and Linux — an AI coding agent powered by Muse Spark 1.2 that fans out parallel subagents in isolated worktrees. Claude Code now has a fourth major rival.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-02-meta-muse-code-ai-coding-agent.jpg" alt="" /></p>
<p><strong>Meta's Muse Code, launched August 5 for macOS and Linux on Muse Spark 1.2, is Meta's first dedicated coding agent — parallel isolated-worktree subagents, one-command install, pay-as-you-go or contributor-tier pricing.</strong></p>
<p>Meta rolled out Muse Code beta on August 5, entering a space already occupied by Claude Code, OpenAI's Codex, and Cursor. The agent installs with a single command on macOS and Linux, connects to any git repo, and runs on Muse Spark 1.2 — a coding-optimized model variant trained specifically for the agent workload.</p>
<p>For large tasks, Muse Code fans out to parallel subagents working in isolated worktrees, so the main checkout is never touched mid-run. The pattern is structurally similar to Claude Code's workflow orchestration and Codex's parallel agent mode — all three converged on isolated-worktree subagents independently.</p>
<p>Pricing: pay-as-you-go at API rates, or a discounted contributor tier where developers consent to training data use.</p>
<p><strong>Why it matters:</strong> The coding-agent market now has four serious contenders competing on the same architecture — parallel subagents, worktree isolation, agentic workflows. Your CLAUDE.md conventions, context-engineering patterns, and custom slash commands are increasingly portable skills. A competitive field accelerates feature shipping and applies downward pressure on pricing.</p>
<p><strong>Sources:</strong> <a href="https://www.cnbc.com/2026/08/05/meta-debuts-muse-code-to-take-on-anthropic-and-openai-.html">Meta debuts Muse Code to rival Claude Code — CNBC</a> · <a href="https://thenextweb.com/news/meta-muse-code-ai-coding-agent-launch">Meta enters AI coding race — The Next Web</a> · <a href="https://dataconomy.com/2026/08/06/meta-muse-code-launch-ai-coding-agent/">Meta Muse Code beta — Dataconomy</a> · <a href="https://9to5mac.com/2026/08/05/meta-launches-muse-code-ai-coding-agent-for-macos-and-linux/">Meta Muse Code for macOS/Linux — 9to5Mac</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-02-meta-muse-code-ai-coding-agent.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-02-meta-muse-code-ai-coding-agent.jpg" />
  </item>
  <item>
    <title><![CDATA[Teach Any Model to Reason Step-by-Step: GRPO, the RL Algorithm Behind DeepSeek-R1, on a Free Colab T4]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-07-01-grpo-reasoning-model-fine-tuning-unsloth/</link>
    <guid isPermaLink="false">2026-08-07-01-grpo-reasoning-model-fine-tuning-unsloth</guid>
    <pubDate>Fri, 07 Aug 2026 04:03:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Tutorial]]></category>
    <category><![CDATA[Fine-Tuning]]></category>
    <category><![CDATA[HuggingFace]]></category>
    <description><![CDATA[GRPO trains reasoning by scoring groups of sampled responses and rewarding the above-average ones — no value model, no preference pairs. Unsloth's free Colab notebook runs it on a T4 in under 2 hours.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-01-grpo-reasoning-model-fine-tuning-unsloth.jpg" alt="" /></p>
<p><strong>GRPO is the RL algorithm behind DeepSeek-R1: score groups of responses, reward the better-than-average ones — no value model, no preference pairs. Unsloth's free Colab notebook trains a reasoning model on a T4 in under 2 hours.</strong></p>
<p><strong>What you'll be able to do after this:</strong>
- Explain why GRPO is cheaper and simpler than PPO for training reasoning models — and when it beats supervised fine-tuning
- Write a verifiable reward function for your domain (math, code, SQL, structured extraction) and plug it into Unsloth's GRPO trainer
- Run a full GRPO training run on a free Colab T4 to build a model that generates <code>&lt;think&gt;…&lt;/think&gt;</code> traces before answering</p>
<p>## What GRPO is</p>
<p>When you do supervised fine-tuning (SFT) on a reasoning dataset, the model learns to imitate the reasoning traces in your training data. GRPO (Group Relative Policy Optimization) does something fundamentally different: it <strong>explores</strong> by sampling multiple responses, <strong>evaluates</strong> each one against a reward function, and <strong>reinforces</strong> the above-average ones.</p>
<p>The key insight: instead of needing a value model to estimate expected reward (like PPO does), GRPO uses the <strong>group mean reward as the baseline</strong>. Sample G=8 completions for one prompt, score each, subtract the mean — the ones that did better than average get positive gradients, the rest get negative.</p>
<p>```
Prompt: &quot;Solve for x: 3x + 7 = 22&quot;</p>
<p>G=8 sampled completions:
  C1: &quot;x = 5&quot;          reward=1.0  (correct)
  C2: &quot;x = 5&quot;          reward=1.0  (correct)
  C3: &quot;x = 5&quot;          reward=1.0  (correct)
  C4: &quot;x = 4&quot;          reward=0.0  (wrong)
  C5: &quot;x = 5&quot;          reward=1.0  (correct)
  C6: &quot;x = 6&quot;          reward=0.0  (wrong)
  C7: &quot;x = 5&quot;          reward=1.0  (correct)
  C8: &quot;x = 4&quot;          reward=0.0  (wrong)</p>
<p>Group mean = 0.625
Advantages:
  C1: 0.375  (above mean → reinforce)
  C4: -0.625 (below mean → discourage)
```</p>
<p>No reference model scoring. No separate value head. No paired preference data. Just: sample, score, update.</p>
<p>## Running GRPO with Unsloth on a free Colab T4</p>
<p>Open Unsloth's <a href="https://unsloth.ai/docs/get-started/reinforcement-learning-rl-guide/tutorial-train-your-own-reasoning-model-with-grpo">GRPO notebook on Colab</a>. The key moving parts:</p>
<p>```python
from unsloth import FastLanguageModel
from trl import GRPOConfig, GRPOTrainer</p>
<p># Load a 1.5B model in 4-bit — fits in 16GB VRAM with GRPO overhead
model, tokenizer = FastLanguageModel.from_pretrained(
    model_name=&quot;unsloth/Qwen2.5-1.5B-Instruct&quot;,
    max_seq_length=2048,
    load_in_4bit=True,
)
model = FastLanguageModel.get_peft_model(model, r=16, lora_alpha=16)</p>
<p># Your reward function: the only thing GRPO needs you to write
def reward_correct_answer(completions, ground_truth, **kwargs):
    &quot;&quot;&quot;Return 1.0 if the completion contains the correct answer, else 0.0.&quot;&quot;&quot;
    return [
        1.0 if str(gt).strip() in c else 0.0
        for c, gt in zip(completions, ground_truth)
    ]</p>
<p>trainer = GRPOTrainer(
    model=model,
    reward_funcs=reward_correct_answer,  # plug in your domain's verifier
    args=GRPOConfig(
        num_generations=8,       # G — completions per prompt
        max_new_tokens=512,
        max_steps=500,
        learning_rate=5e-6,
        per_device_train_batch_size=1,
        gradient_accumulation_steps=4,
        output_dir=&quot;grpo-reasoning-model&quot;,
    ),
    train_dataset=dataset,       # needs a &quot;prompt&quot; column
)
trainer.train()
```</p>
<p>Unsloth patches the attention kernels to cut VRAM by ~2×, which is what makes GRPO feasible on a free T4.</p>
<p>## When to use GRPO instead of SFT</p>
<p>| Situation | Better choice |
|---|---|
| You have correct reasoning traces (CoT) | SFT on those traces |
| You can verify correctness automatically (math, code, SQL) | <strong>GRPO</strong> |
| You want the model to discover better reasoning strategies | <strong>GRPO</strong> |
| You're adapting writing style or domain vocabulary | SFT |
| Your task doesn't have a clear correct/incorrect signal | SFT or DPO |</p>
<p>GRPO excels at tasks with <strong>verifiable outcomes</strong>: the reward function is just a unit test. Code that passes tests, math answers that match, SQL that returns the right rows, structured output that satisfies a regex — all clean GRPO targets.</p>
<p><strong>Sources:</strong> <a href="https://unsloth.ai/docs/get-started/reinforcement-learning-rl-guide/tutorial-train-your-own-reasoning-model-with-grpo">Train Your Own Reasoning Model with GRPO — Unsloth Docs</a> · <a href="https://unsloth.ai/blog/r1-reasoning">Train your own R1 reasoning model locally — Unsloth Blog</a> · <a href="https://huggingface.co/docs/trl/grpo_trainer">GRPOTrainer — TRL / Hugging Face</a> · <a href="https://arxiv.org/abs/2501.12948">DeepSeek-R1 Technical Report — arXiv</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-01-grpo-reasoning-model-fine-tuning-unsloth.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-07-01-grpo-reasoning-model-fine-tuning-unsloth.jpg" />
  </item>
  <item>
    <title><![CDATA[What Did Claude Actually Do? Auditing Unattended Runs With JSONL Session Logs]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-06-09-claude-code-jsonl-session-audit-trail/</link>
    <guid isPermaLink="false">2026-08-06-09-claude-code-jsonl-session-audit-trail</guid>
    <pubDate>Thu, 06 Aug 2026 20:04:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Workflow]]></category>
    <category><![CDATA[Claude Code]]></category>
    <category><![CDATA[Best Practices]]></category>
    <description><![CDATA[Claude Code writes every tool call to JSONL logs in ~/.claude/projects/. One PostToolUse hook or three open-source tools turn those logs into a full audit trail for any background run.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-09-claude-code-jsonl-session-audit-trail.jpg" alt="" /></p>
<p><strong>Claude Code writes every tool call to JSONL logs in ~/.claude/projects/. One PostToolUse hook or three open-source tools turn those logs into a full audit trail for any background run.</strong></p>
<p>When Claude Code finishes a background task — a cron-triggered publish run, a CI pipeline, an overnight refactor — you want to know: which files did it touch? Which commands did it run? The answer lives in ~/.claude/.</p>
<p>## Where the logs are</p>
<p>Claude Code writes session transcripts to:</p>
<p>``<code>
~/.claude/projects/&lt;url-encoded-project-path&gt;/&lt;session-id&gt;.jsonl
</code>``</p>
<p>Each <code>.jsonl</code> file is one session, one JSON object per line, appended as the session runs. Crash-safe: a mid-session abort leaves a valid truncated file.</p>
<p>Each line is one event. Tool events look like:</p>
<p>``<code>json
{&quot;type&quot;:&quot;tool_use&quot;,&quot;id&quot;:&quot;toolu_01XYZ&quot;,&quot;name&quot;:&quot;Write&quot;,&quot;input&quot;:{&quot;file_path&quot;:&quot;/project/src/foo.ts&quot;,&quot;content&quot;:&quot;...&quot;}}
{&quot;type&quot;:&quot;tool_result&quot;,&quot;tool_use_id&quot;:&quot;toolu_01XYZ&quot;,&quot;content&quot;:[{&quot;type&quot;:&quot;text&quot;,&quot;text&quot;:&quot;File written&quot;}]}
</code>``</p>
<p>## Quick audit with jq</p>
<p>```bash
# All files written in the last session
cd ~/.claude/projects
LATEST=$(ls -t <em>/</em>.jsonl | head -1)
jq -r 'select(.type == &quot;tool_use&quot; and .name == &quot;Write&quot;) | .input.file_path' &quot;$LATEST&quot;</p>
<p># All bash commands run
jq -r 'select(.type == &quot;tool_use&quot; and .name == &quot;Bash&quot;) | .input.command' &quot;$LATEST&quot;</p>
<p># Token spend per turn
jq 'select(.usage != null) | .usage' &quot;$LATEST&quot;
```</p>
<p>## Real-time audit hook (PostToolUse)</p>
<p>Add this to <code>.claude/settings.json</code> to log every tool call as it happens — no after-the-fact parsing:</p>
<p>``<code>json
{
  &quot;hooks&quot;: {
    &quot;PostToolUse&quot;: [
      {
        &quot;matcher&quot;: &quot;.*&quot;,
        &quot;hooks&quot;: [{
          &quot;type&quot;: &quot;command&quot;,
          &quot;command&quot;: &quot;jq -c '{ts: now|todate, tool: .tool_name, input: .tool_input}' &gt;&gt; ~/.claude/session-logs/$(date +%Y-%m-%d).jsonl&quot;
        }]
      }
    ]
  }
}
</code>``</p>
<p>This produces one JSON line per tool call in <code>~/.claude/session-logs/YYYY-MM-DD.jsonl</code>:</p>
<p>``<code>json
{&quot;ts&quot;:&quot;2026-08-06T20:00:00Z&quot;,&quot;tool&quot;:&quot;Write&quot;,&quot;input&quot;:{&quot;file_path&quot;:&quot;/project/content/feed.xml&quot;}}
{&quot;ts&quot;:&quot;2026-08-06T20:00:01Z&quot;,&quot;tool&quot;:&quot;Bash&quot;,&quot;input&quot;:{&quot;command&quot;:&quot;node scripts/ingest-feed.mjs&quot;}}
</code>``</p>
<p>## Graphical viewers</p>
<p>- <strong><a href="https://github.com/delexw/claude-code-trace">claude-code-trace</a></strong> — TUI/web/desktop viewer. Browse sessions, inspect tool call trees, live-tail an active session. <code>npx @delexw/claude-code-trace</code>
- <strong><a href="https://github.com/yurukusa/cc-audit-log">cc-audit-log</a></strong> — scans all sessions under ~/.claude/projects/, classifies actions (file writes, edits, git, bash), outputs a human-readable timeline. <code>npx cc-audit-log</code>
- <strong><a href="https://github.com/daaain/claude-code-log">claude-code-log</a></strong> — converts a JSONL transcript to readable HTML or Markdown for sharing a session trace with your team.</p>
<p>For teams running Claude Code in CI or as a scheduled background agent, the PostToolUse hook is a five-minute change that pays off the first time something unexpected happens.</p>
<p><strong>Sources:</strong> <a href="https://dev.to/boucle2026/how-to-see-everything-claude-code-does-audit-trail-hook-1g9j">How to See Everything Claude Code Does — DEV Community</a> · <a href="https://github.com/delexw/claude-code-trace">claude-code-trace — GitHub</a> · <a href="https://github.com/yurukusa/cc-audit-log">cc-audit-log — GitHub</a> · <a href="https://claude-dev.tools/docs/jsonl-format">Claude Code JSONL format — claude-dev.tools</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-09-claude-code-jsonl-session-audit-trail.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-09-claude-code-jsonl-session-audit-trail.jpg" />
  </item>
  <item>
    <title><![CDATA[Google's AI Leadership Fractures: Hassabis to Chairman, Jeff Dean Exits to Found Discovery Loop]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-06-07-google-deepmind-leadership-exodus/</link>
    <guid isPermaLink="false">2026-08-06-07-google-deepmind-leadership-exodus</guid>
    <pubDate>Thu, 06 Aug 2026 20:03:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[News]]></category>
    <category><![CDATA[LLM]]></category>
    <category><![CDATA[Developer Tools]]></category>
    <description><![CDATA[Demis Hassabis steps back from Google DeepMind CEO to chairman/Alphabet chief scientist; Jeff Dean, Sanjay Ghemawat, Oriol Vinyals, and Quoc Le exit to co-found Discovery Loop.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-07-google-deepmind-leadership-exodus.jpg" alt="" /></p>
<p><strong>Two Google AI exits announced August 5: Demis Hassabis steps back as DeepMind CEO to become Alphabet Chief Scientist; Jeff Dean, Sanjay Ghemawat, Oriol Vinyals, and Quoc Le depart to co-found Discovery Loop.</strong></p>
<p>Two announcements on August 5 signal a structural shift at Google's AI operation.</p>
<p><strong>Hassabis out as day-to-day CEO.</strong> Demis Hassabis — who co-founded DeepMind in 2010 and has led it through the Gemini era — steps back from the CEO role. Koray Kavukcuoglu, a 13-year DeepMind veteran who served as its CTO and Google's chief AI architect, takes over as CEO. Hassabis becomes Alphabet's Chief Scientist and Chairman of Google DeepMind, focusing on longer-horizon AGI research.</p>
<p><strong>Jeff Dean exits after 27 years.</strong> Dean — author of the MapReduce and BigTable papers, lead architect of TensorFlow, and Google's chief scientist — is co-founding Discovery Loop with Sanjay Ghemawat (Google Senior Fellow, GFS co-author), Oriol Vinyals (VP of research at Google DeepMind, a technical lead on Gemini), and Quoc Le (Google Brain co-founder, AutoML-Zero). Discovery Loop's mission: automate the experimental loop of scientific research — proposing experiments, running them, evaluating results, iterating. Google is a founding investor and cloud partner.</p>
<p><strong>Why it matters:</strong> four of Google's most influential AI infrastructure and research figures departing in a single announcement is not routine churn. For teams building on Google infrastructure or Gemini APIs, the leadership transition signals a shift in priorities worth tracking — new AI leadership influences where platform investment goes.</p>
<p><strong>Sources:</strong> <a href="https://fortune.com/2026/08/05/demis-hassabis-steps-down-google-deepmind-ai-shakeup/">Hassabis steps down — Fortune</a> · <a href="https://techcrunch.com/2026/08/05/jeff-dean-and-other-top-ai-researchers-are-leaving-google-to-launch-their-own-startup/">Jeff Dean launches Discovery Loop — TechCrunch</a> · <a href="https://www.bloomberg.com/news/articles/2026-08-05/google-deepmind-boss-hassabis-moves-to-chair-role-in-shakeup/">Kavukcuoglu named CEO — Bloomberg</a> · <a href="https://www.bloomberg.com/news/articles/2026-08-06/google-shifts-ai-power-to-california-in-race-against-anthropic-openai">Google shifts AI to California — Bloomberg</a> · <a href="https://www.axios.com/2026/08/06/googles-ai-leadership-shuffle">Axios</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-07-google-deepmind-leadership-exodus.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-07-google-deepmind-leadership-exodus.jpg" />
  </item>
  <item>
    <title><![CDATA[Guaranteed JSON From Claude: Two Native API Patterns That Never Need a Retry Loop]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-06-08-claude-api-structured-outputs-tool-use/</link>
    <guid isPermaLink="false">2026-08-06-08-claude-api-structured-outputs-tool-use</guid>
    <pubDate>Thu, 06 Aug 2026 20:02:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Tutorial]]></category>
    <category><![CDATA[Agents]]></category>
    <category><![CDATA[Best Practices]]></category>
    <description><![CDATA[The Claude API has two orthogonal structured-output patterns: output_config JSON schema mode constrains Claude's text response; strict:true on tool definitions guarantees schema-valid tool inputs.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-08-claude-api-structured-outputs-tool-use.jpg" alt="" /></p>
<p><strong>The Claude API offers two orthogonal structured-output patterns — output_config JSON schema mode for constrained text responses and strict:true tool definitions for schema-valid tool inputs — that together eliminate JSON parse failures from agentic pipelines.</strong></p>
<p><strong>What you'll be able to do after this:</strong>
- Get parseable JSON from Claude every time without try/except retry loops
- Guarantee tool call inputs match your schema exactly via grammar-constrained sampling
- Combine both patterns to build reliable agentic pipelines where Claude calls tools AND returns structured data</p>
<p>## The two patterns</p>
<p>Pattern 1 controls what Claude <em>says</em> (its text response). Pattern 2 controls what Claude <em>does</em> (its tool call parameters). Both are orthogonal — use either or both together.</p>
<p><strong>Pattern 1: output_config JSON schema (constrained text responses)</strong></p>
<p>Pass an <code>output_config</code> block with a <code>json_schema</code> format. The API compiles your schema into a grammar and samples tokens only from schema-valid continuations — the response is always valid JSON matching your schema.</p>
<p>```python
import anthropic, json</p>
<p>client = anthropic.Anthropic()</p>
<p>schema = {
    &quot;type&quot;: &quot;object&quot;,
    &quot;properties&quot;: {
        &quot;sentiment&quot;: {&quot;type&quot;: &quot;string&quot;, &quot;enum&quot;: [&quot;positive&quot;, &quot;negative&quot;, &quot;neutral&quot;]},
        &quot;confidence&quot;: {&quot;type&quot;: &quot;number&quot;, &quot;minimum&quot;: 0, &quot;maximum&quot;: 1},
        &quot;key_phrases&quot;: {&quot;type&quot;: &quot;array&quot;, &quot;items&quot;: {&quot;type&quot;: &quot;string&quot;}, &quot;maxItems&quot;: 5}
    },
    &quot;required&quot;: [&quot;sentiment&quot;, &quot;confidence&quot;, &quot;key_phrases&quot;],
    &quot;additionalProperties&quot;: False
}</p>
<p>response = client.messages.create(
    model=&quot;claude-sonnet-4-6-20260901&quot;,
    max_tokens=512,
    output_config={
        &quot;format&quot;: {
            &quot;type&quot;: &quot;json_schema&quot;,
            &quot;json_schema&quot;: {
                &quot;name&quot;: &quot;sentiment_result&quot;,
                &quot;schema&quot;: schema
            }
        }
    },
    messages=[{&quot;role&quot;: &quot;user&quot;, &quot;content&quot;: &quot;Analyze sentiment: 'The new API is incredibly fast and docs are clear.'&quot;}]
)</p>
<p>result = json.loads(response.content[0].text)
# {&quot;sentiment&quot;: &quot;positive&quot;, &quot;confidence&quot;: 0.95, &quot;key_phrases&quot;: [&quot;incredibly fast&quot;, &quot;docs are clear&quot;]}
# sentiment is always &quot;positive&quot;, &quot;negative&quot;, or &quot;neutral&quot; — no parse error possible
```</p>
<p><strong>Tradeoff:</strong> complex schemas with many nested objects produce larger grammars that take longer to compile on the first call. Keep schemas focused; subsequent calls with the same schema benefit from server-side caching.</p>
<p><strong>Pattern 2: strict:true tool use (guaranteed tool call parameters)</strong></p>
<p>Add <code>&quot;strict&quot;: true</code> as a top-level property in a tool definition. When Claude calls that tool, its <code>input</code> always matches your <code>input_schema</code> — no coercion, no extra keys, no missing required fields.</p>
<p>```python
tool = {
    &quot;name&quot;: &quot;create_task&quot;,
    &quot;description&quot;: &quot;Create a task in the task management system&quot;,
    &quot;strict&quot;: True,
    &quot;input_schema&quot;: {
        &quot;type&quot;: &quot;object&quot;,
        &quot;properties&quot;: {
            &quot;title&quot;: {&quot;type&quot;: &quot;string&quot;, &quot;maxLength&quot;: 100},
            &quot;priority&quot;: {&quot;type&quot;: &quot;integer&quot;, &quot;minimum&quot;: 1, &quot;maximum&quot;: 5},
            &quot;assignee_id&quot;: {&quot;type&quot;: &quot;string&quot;, &quot;pattern&quot;: &quot;^usr_[a-z0-9]+$&quot;}
        },
        &quot;required&quot;: [&quot;title&quot;, &quot;priority&quot;],
        &quot;additionalProperties&quot;: False
    }
}</p>
<p>response = client.messages.create(
    model=&quot;claude-sonnet-4-6-20260901&quot;,
    max_tokens=256,
    tools=[tool],
    messages=[{&quot;role&quot;: &quot;user&quot;, &quot;content&quot;: &quot;Create a high-priority task: 'Fix the auth bug', assigned to usr_abc123.&quot;}]
)</p>
<p>for block in response.content:
    if block.type == &quot;tool_use&quot; and block.name == &quot;create_task&quot;:
        task_data = block.input
        # task_data[&quot;priority&quot;] is always 1-5, title always ≤ 100 chars — guaranteed
```</p>
<p><strong>Combining both patterns</strong></p>
<p>You can set <code>strict: true</code> on tools AND pass <code>output_config</code> in the same request. Claude calls tools with guaranteed-valid parameters and returns a structured final response — the right shape for multi-step agentic workflows.</p>
<p>``<code>python
response = client.messages.create(
    model=&quot;claude-sonnet-4-6-20260901&quot;,
    max_tokens=1024,
    tools=[tool],           # strict: True
    output_config={
        &quot;format&quot;: {
            &quot;type&quot;: &quot;json_schema&quot;,
            &quot;json_schema&quot;: {&quot;name&quot;: &quot;summary&quot;, &quot;schema&quot;: summary_schema}
        }
    },
    messages=[{&quot;role&quot;: &quot;user&quot;, &quot;content&quot;: &quot;Create tasks for the auth bug fix and return a summary.&quot;}]
)
# Claude calls create_task (guaranteed-valid inputs) then returns structured summary JSON
</code>``</p>
<p>This pattern is available on Claude Sonnet 4.5+, Opus 4.5+, Haiku 4.5+, and later.</p>
<p><strong>Sources:</strong> <a href="https://platform.claude.com/docs/en/build-with-claude/structured-outputs">Structured outputs — Claude Platform Docs</a> · <a href="https://platform.claude.com/docs/en/agents-and-tools/tool-use/strict-tool-use">Strict tool use — Claude Platform Docs</a> · <a href="https://platform.claude.com/docs/en/agents-and-tools/tool-use/implement-tool-use">How to implement tool use — Claude Platform Docs</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-08-claude-api-structured-outputs-tool-use.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-08-claude-api-structured-outputs-tool-use.jpg" />
  </item>
  <item>
    <title><![CDATA[Skip Permissions Safely: Run Claude Code in Full-Autonomy Mode Inside a Locked-Down Docker Container]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-06-06-claude-code-docker-devcontainer-sandbox/</link>
    <guid isPermaLink="false">2026-08-06-06-claude-code-docker-devcontainer-sandbox</guid>
    <pubDate>Thu, 06 Aug 2026 12:06:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Workflow]]></category>
    <category><![CDATA[Claude Code]]></category>
    <category><![CDATA[Best Practices]]></category>
    <description><![CDATA[A deny-all Docker container makes --dangerously-skip-permissions safe: Claude gets full autonomy inside an isolated filesystem and network wall, never touching your host.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-06-claude-code-docker-devcontainer-sandbox.jpg" alt="" /></p>
<p><strong>A deny-all Docker container makes <code>--dangerously-skip-permissions</code> safe: Claude gets full autonomy inside an isolated filesystem and network wall, never touching your host machine.</strong></p>
<p><code>--dangerously-skip-permissions</code> removes every approval dialog -- Claude can read, write, and execute without asking. On your laptop that means it can touch all your files and make any network request. Inside a container it can only touch what you explicitly mounted, and only reach the network endpoints you allow.</p>
<p>This matters even more after today's v2.1.223 patches, which fixed two permission-bypass bugs. Container isolation is defense-in-depth: even if a future bypass exists, a sandboxed Claude cannot escape to your host.</p>
<p>## Three paths to a sandboxed session</p>
<p><strong>Option 1: Docker Desktop sbx launcher (fastest)</strong></p>
<p>Docker Desktop 4.40+ ships a built-in sandbox launcher. One command gives you an isolated Claude Code environment:</p>
<p>``<code>bash
docker sbx run anthropic/claude-code@latest --dangerously-skip-permissions
</code>``</p>
<p>The launcher mounts your current directory into /workspace, installs a deny-all outbound firewall that permits only the Anthropic API and npm registry, and removes the container on exit.</p>
<p><strong>Option 2: Anthropic official devcontainer feature (best for VS Code teams)</strong></p>
<p>Add <code>.devcontainer/devcontainer.json</code> to any project:</p>
<p>``<code>json
{
  &quot;name&quot;: &quot;Claude Code Sandbox&quot;,
  &quot;image&quot;: &quot;mcr.microsoft.com/devcontainers/base:ubuntu-24.04&quot;,
  &quot;features&quot;: {
    &quot;ghcr.io/anthropics/claude-code-features/claude-code:latest&quot;: {}
  },
  &quot;postCreateCommand&quot;: &quot;npm install --prefix /workspace&quot;,
  &quot;remoteEnv&quot;: {
    &quot;ANTHROPIC_API_KEY&quot;: &quot;${localEnv:ANTHROPIC_API_KEY}&quot;
  }
}
</code>``</p>
<p>Open in VS Code with the Dev Containers extension and run <code>claude --dangerously-skip-permissions</code> in the integrated terminal. Your project files are mounted read-write; everything else on your host is unreachable.</p>
<p><strong>Option 3: Manual Dockerfile with firewall (most control)</strong></p>
<p>For tighter control -- allow npm and GitHub but block everything else:</p>
<p>``<code>dockerfile
FROM ubuntu:24.04
RUN apt-get update &amp;&amp; apt-get install -y nodejs npm nftables curl
RUN npm install -g @anthropic-ai/claude-code
COPY firewall.sh /usr/local/bin/firewall.sh
RUN chmod +x /usr/local/bin/firewall.sh
ENTRYPOINT [&quot;/usr/local/bin/firewall.sh&quot;]
CMD [&quot;claude&quot;, &quot;--dangerously-skip-permissions&quot;]
</code>``</p>
<p>``<code>bash
# firewall.sh -- allow only Anthropic + npm, deny all else
nft add table inet filter
nft add chain inet filter output '{ type filter hook output priority 0 ; policy drop ; }'
nft add rule inet filter output ip daddr 18.157.0.0/16 accept
nft add rule inet filter output ip daddr 104.16.0.0/12 accept
nft add rule inet filter output ct state established accept
</code>``</p>
<p>Run it with:</p>
<p>``<code>bash
docker build -f Dockerfile.claude-sandbox -t claude-sandbox .
docker run -it --rm   -v &quot;$(pwd)&quot;:/workspace   -e ANTHROPIC_API_KEY=&quot;$ANTHROPIC_API_KEY&quot;   --cap-add NET_ADMIN   claude-sandbox
</code>``</p>
<p>## What the container buys you</p>
<p>| Threat | Without container | With container |
|---|---|---|
| Claude reads ~/.ssh | Possible | Blocked -- not mounted |
| Claude exfiltrates to attacker server | Possible | Blocked -- deny-all firewall |
| Permission bypass bug | Exploitable | Contained to workspace only |
| Runaway file deletions | Host filesystem | Only mounted project dir |</p>
<p>The setup takes under 10 minutes. For any team running Claude Code in auto-approve mode on production code, it is the right foundation.</p>
<p><strong>Sources:</strong> <a href="https://www.docker.com/blog/docker-sandboxes-run-claude-code-and-other-coding-agents-unsupervised-but-safely/">Docker Sandboxes blog -- Docker.com</a> . <a href="https://github.com/trailofbits/claude-code-devcontainer">Trail of Bits devcontainer for Claude Code</a> . <a href="https://dev.to/wartzarbee/how-to-run-claude-code-sandboxed-containers-network-walls-and-secret-isolation-2jkn">Run Claude Code Sandboxed -- DEV Community</a> . <a href="https://code.claude.com/docs/en/claude-code-on-the-web">Claude Code on the web docs</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-06-claude-code-docker-devcontainer-sandbox.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-06-claude-code-docker-devcontainer-sandbox.jpg" />
  </item>
  <item>
    <title><![CDATA[Claude Code v2.1.223 Patches Two Permission-Layer Flaws That Let Commands Hide From You]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-06-05-claude-code-v2123-permission-security/</link>
    <guid isPermaLink="false">2026-08-06-05-claude-code-v2123-permission-security</guid>
    <pubDate>Thu, 06 Aug 2026 12:05:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[News]]></category>
    <category><![CDATA[Claude Code]]></category>
    <category><![CDATA[Security]]></category>
    <description><![CDATA[Today v2.1.223 closes a Bash compound-statement bypass and an invisible Unicode trick that let crafted commands hide parts of themselves from Claude Code approval dialogs.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-05-claude-code-v2123-permission-security.jpg" alt="" /></p>
<p><strong>Today v2.1.223 closes a Bash compound-statement bypass and invisible Unicode tab-padding that let crafted commands hide parts of themselves from Claude Code approval dialogs -- upgrade immediately if you run any auto-approve hooks.</strong></p>
<p>Two security fixes shipped in today's release, both targeting the permission layer that decides whether Claude Code needs user approval before running a command.</p>
<p><strong>Bash compound-statement bypass:</strong> a crafted command using Bash compound-statement syntax could make the permission analyzer see a benign-looking command while Bash executed something else. The fix makes the analyzer fail closed on file-descriptor redirect forms it parses differently than Bash does.</p>
<p><strong>Invisible Unicode / tab padding:</strong> commands padded with tab characters or invisible Unicode code points could visually hide part of the command in the approval dialog. You would approve what looked like &quot;git status&quot; but Bash would run something more. This closes the same class of bypass that v2.1.211 patched for whitespace characters in June.</p>
<p>These join a string of permission-hardening fixes since June (v2.1.211, v2.1.214, v2.1.216, now v2.1.223). The pattern suggests targeted fuzzing of the permission layer.</p>
<p><strong>Other changes in v2.1.223:</strong>
- Added /teleport hint in cloud sessions showing the CLI handoff command
- Added owner/* wildcard support in managed marketplace settings
- Added a warning when a background agent's requested model is restricted by policy</p>
<p><strong>What to do:</strong> run <code>claude update</code> or <code>npm update -g @anthropic-ai/claude-code</code> to get v2.1.223. If you have allowedTools or auto-approve hooks configured, this update is particularly relevant -- the bypass classes fixed today affect exactly that mode.</p>
<p><strong>Why it matters:</strong> as Claude Code agents run unattended in CI and cloud sessions, the permission layer is the last defense between Claude and unintended system commands. Keeping it bypass-proof is foundational to safe agentic automation.</p>
<p><strong>Sources:</strong> <a href="https://code.claude.com/docs/en/changelog">Claude Code changelog -- code.claude.com</a> . <a href="https://releasebot.io/updates/anthropic/claude-code">Releasebot Anthropic Claude Code August 2026</a> . <a href="https://www.petefreitag.com/blog/claude-code-permissions/">Claude Code permission guide -- petefreitag.com</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-05-claude-code-v2123-permission-security.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-05-claude-code-v2123-permission-security.jpg" />
  </item>
  <item>
    <title><![CDATA[Your Code and My Code Don't Cost the Same: How BPE Tokenization Shapes Every LLM API Bill]]></title>
    <link>https://cloudcodetree.com/ai-news/2026-08-06-04-bpe-tokenization-llm-vocab/</link>
    <guid isPermaLink="false">2026-08-06-04-bpe-tokenization-llm-vocab</guid>
    <pubDate>Thu, 06 Aug 2026 12:04:00 GMT</pubDate>
    <dc:creator><![CDATA[Chris Harper]]></dc:creator>
    <category><![CDATA[AI]]></category>
    <category><![CDATA[Tutorial]]></category>
    <category><![CDATA[LLM]]></category>
    <category><![CDATA[HuggingFace]]></category>
    <description><![CDATA[BPE tokenization is why code costs 2x more than prose and why different models see different context limits for the same text. Run the HuggingFace NLP course Colab notebook to see it work.]]></description>
    <content:encoded><![CDATA[<p><img src="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-04-bpe-tokenization-llm-vocab.jpg" alt="" /></p>
<p><strong>BPE tokenization is why code often costs 2x more than prose, why different models see different context limits for the same text, and why fine-tuning on padding-heavy data wastes compute -- it is the one foundational concept that makes everything else in AI engineering make sense.</strong></p>
<p><strong>What you'll be able to do after this:</strong>
- Explain how BPE builds a vocabulary from scratch and tokenizes any string into integers
- Use the HuggingFace tokenizers library to inspect and compare tokenization across any open model
- Predict when your prompts will be expensive or hit context limits before you send them</p>
<p>## What tokens actually are</p>
<p>An LLM never sees your text. It sees a sequence of integers -- token IDs -- drawn from a fixed vocabulary. &quot;Hello, world!&quot; might become [9906, 11, 1917, 0]. The model learns probability distributions over these integers. Everything about cost, context limits, and generation speed is downstream of how many integers your text becomes.</p>
<p>## How BPE builds a vocabulary</p>
<p>BPE (Byte-Pair Encoding) starts with a vocabulary of individual bytes (256 entries). It then iteratively finds the most frequent adjacent pair of tokens in a training corpus and merges them into a new single token. Repeat ~50,000 times and you have a 50,257-token vocabulary (GPT-2's size). Common English words become single tokens (&quot;the&quot; -&gt; token 0) while rare combinations split into multiple tokens.</p>
<p>```python
from tokenizers import Tokenizer
from tokenizers.models import BPE
from tokenizers.trainers import BpeTrainer</p>
<p># Train a BPE tokenizer from scratch on your own corpus
tokenizer = Tokenizer(BPE(unk_token=&quot;[UNK]&quot;))
trainer = BpeTrainer(vocab_size=5000, special_tokens=[&quot;[UNK]&quot;, &quot;[CLS]&quot;, &quot;[SEP]&quot;])
tokenizer.train([&quot;my_corpus.txt&quot;], trainer)</p>
<p>output = tokenizer.encode(&quot;Hello, world! def calculate():&quot;)
print(output.tokens)
# ['Hello', ',', 'world', '!', 'def', 'cal', 'culate', '(', ')', ':']
print(len(output.tokens))  # 10 tokens for 30 characters
```</p>
<p>## Why this changes your cost math</p>
<p>Code tokenizes more expensively than English prose. Special characters like <code>(</code>, <code>[</code>, <code>{</code>, <code>-&gt;</code> rarely appear adjacent to alphanumeric chars in English, so they do not get merged into efficient tokens. A Python function of 100 characters may cost 50-80 tokens. An English paragraph of 100 characters might be 25-35 tokens.</p>
<p>Different model vocabularies (GPT-4's cl100k_base vs Llama 3's SentencePiece vs Claude's BPE variant) tokenize the same string to different lengths. Before choosing a model for a code-heavy workload, compare token counts:</p>
<p>```python
import tiktoken  # pip install tiktoken</p>
<p>enc = tiktoken.get_encoding(&quot;cl100k_base&quot;)
text = &quot;def factorial(n):
    return 1 if n &lt;= 1 else n * factorial(n-1)&quot;
print(f&quot;GPT-4 token count: {len(enc.encode(text))}&quot;)  # ~22</p>
<p>from transformers import AutoTokenizer
llama_tok = AutoTokenizer.from_pretrained(&quot;meta-llama/Llama-3.1-8B&quot;)
print(f&quot;Llama-3 token count: {len(llama_tok(text)['input_ids'])}&quot;)  # may differ
```</p>
<p>## Practical rules for AI engineers</p>
<p>- <strong>Budget context windows per-token, not per-character.</strong> Rule of thumb: 1 token ~ 4 English chars, but 1 token ~ 2-3 code chars.
- <strong>Check vocabulary coverage before fine-tuning.</strong> If your domain has many OOV-ish terms, they split into many subword tokens and inflate sequence lengths.
- <strong>Padding adds compute cost.</strong> During fine-tuning, sequences are padded to the batch max length -- shorter, more uniform sequences mean less wasted GPU time.
- <strong>JSON and YAML structures are expensive.</strong> Structural punctuation (<code>{</code>, <code>&quot;</code>, <code>:</code>, <code>}</code>) resists merging and inflates token counts fast.
- <strong>Use count_tokens before sending large prompts.</strong> Anthropic's SDK has <code>client.messages.count_tokens()</code> -- call it before a large structured prompt to confirm fit and estimate cost.</p>
<p>## Go deeper</p>
<p>The interactive chapter at <a href="https://huggingface.co/learn/llm-course/en/chapter6/5">HuggingFace LLM Course Chapter 6</a> walks through BPE step by step with a live Colab notebook you can run in your browser -- no GPU needed. Chapter 6.8 builds a complete tokenizer from scratch on a small corpus so you can watch the merge algorithm work in real time.</p>
<p><strong>Sources:</strong> <a href="https://huggingface.co/learn/llm-course/en/chapter6/5">BPE tokenization -- HuggingFace LLM Course Ch. 6</a> . <a href="https://colab.research.google.com/github/huggingface/notebooks/blob/master/course/en/chapter6/section5.ipynb">Colab: Tokenizer training from scratch</a> . <a href="https://huggingface.co/docs/transformers/tokenizer_summary">Tokenization algorithms -- HuggingFace docs</a></p>]]></content:encoded>
    <media:content url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-04-bpe-tokenization-llm-vocab.jpg" medium="image" type="image/jpeg" />
    <media:thumbnail url="https://github.com/cloudcodetree/cloudcodetree.github.io/releases/download/blog-images/2026-08-06-04-bpe-tokenization-llm-vocab.jpg" />
  </item>
  </channel>
</rss>
