<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0"
  xmlns:content="http://purl.org/rss/1.0/modules/content/"
  xmlns:dc="http://purl.org/dc/elements/1.1/"
  xmlns:atom="http://www.w3.org/2005/Atom"
  xmlns:media="http://search.yahoo.com/mrss/">
  <channel>
    <title>AI News — cloudcodetree</title>
    <link>https://cloudcodetree.com/blog/</link>
    <atom:link href="https://cloudcodetree.com/feed.xml" rel="self" type="application/rss+xml" />
    <description>Daily field notes on AI-assisted engineering.</description>
    <language>en-us</language>
    <lastBuildDate>Fri, 21 Aug 2026 04:05:00 +0000</lastBuildDate>

    <item>
      <title>Generated Components Don't Know Your Breakpoints: A Three-Step Responsive Review Before Claude Code Handoff</title>
      <link>https://support.claude.com/en/articles/14604416-get-started-with-claude-design</link>
      <guid isPermaLink="false">2026-08-21-04-generated-components-responsive-review</guid>
      <pubDate>Fri, 21 Aug 2026 04:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Design</category>
      <category>Claude Design</category>
      <description>AI-generated UI is built at a fixed canvas size. Three concrete checks — pixel widths, missing mobile layout, text overflow — to run in Claude Design before sending the handoff bundle to Claude Code.</description>
      <content:encoded><![CDATA[**Design tools generate components at the canvas size you give them. They don't know your Tailwind breakpoints. Here are three responsive failures to catch before the handoff bundle lands in Claude Code.**

Sending a component straight to Claude Code with unresolved responsive issues means Claude Code tries to implement a layout that can't scale — and you spend the cleanup time you were hoping to skip.

**Check 1: Fixed pixel widths where relative units belong.**

Generated components commonly hard-code widths like `width: 480px`. In Claude Design, select each container element and look at the layout panel. Any explicit pixel width that should be fluid — a card, a content column, a grid cell — swap to `%`, `fr`, or a named token from your design system. The rule is simple: if the element should grow when the viewport grows, it needs a relative unit. If Claude Design locked it to pixels, it was working from what the canvas implied, not what your system requires.

Before handing off, every container that crosses a layout boundary should be relative. Fixed widths are fine for icons, avatars, and constrained inline elements.

**Check 2: No mobile layout rule.**

A horizontal card grid looks correct on a 1280px canvas. It has no rule for what happens below 640px — it just overflows. In Claude Design, duplicate the frame to a mobile viewport (375 × 812 is the standard reference size), apply the same component, and specify explicitly what changes:

- Grid collapses from three columns to one.
- Card image stacks above text.
- Font sizes drop one step in the type scale.
- The CTA button goes full-width.

Each breakpoint behavior you define in the canvas is a decision Claude Code doesn't have to guess. Whatever you don't specify, Claude Code invents — and it will invent something.

**Check 3: Text overflow on real content.**

Generated copy fills the design neatly because it was written to fit. Real content is longer. Before handing off, manually extend the text in two or three text nodes to 2× their current length and observe what breaks: truncation without ellipsis, overflow past the card boundary, a heading that wraps awkwardly. Add explicit handling in the canvas — `line-clamp`, `overflow: hidden`, or a `min-height` — for every case you find.

**The handoff note that pays off.**

When you pass the bundle to Claude Code, include a one-paragraph note alongside it:

> "Grid collapses to 1 column below 640px (`sm:grid-cols-1`). All container widths use `w-full` or design-system `max-w-*` tokens — no hardcoded px widths. Text nodes use `clamp()` from the typography scale; long values clamp at two lines."

Claude Code reads this note and implements to your spec instead of reconstructing intent from the component structure alone. The note costs 30 seconds and saves a review cycle.

**What this pass doesn't cover:** accessibility (color contrast, ARIA roles, keyboard focus) — that's a separate checklist, covered in the [August 18 post](https://cloudcodetree.com/ai-news/2026-08-18-08-generated-ui-accessibility-review-pass).

**Sources:** [Get started with Claude Design — Anthropic Help](https://support.claude.com/en/articles/14604416-get-started-with-claude-design) · [Set up your design system — Anthropic Help](https://support.claude.com/en/articles/14604397-set-up-your-design-system-in-claude-design) · [Responsive design testing — Smashing Magazine](https://www.smashingmagazine.com/2024/09/responsive-design-testing-tools-techniques/)
]]></content:encoded>
    </item>

    <item>
      <title>Catch What Claude Just Changed: A PostToolUse Hook That Validates Every File in Your Session</title>
      <link>https://code.claude.com/docs/en/hooks-guide</link>
      <guid isPermaLink="false">2026-08-21-03-claude-code-posttooluse-validation-hook</guid>
      <pubDate>Fri, 21 Aug 2026 04:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>A PostToolUse hook in .claude/settings.json runs a shell command after every Write or Edit. 12 lines of JSON catches lint errors, type failures, or schema violations before the session ends.</description>
      <content:encoded><![CDATA[**A PostToolUse hook in `.claude/settings.json` runs your validator after every Write or Edit call. Claude sees the output, understands if something broke, and fixes it — without you prompting again.**

Claude Code's hook system lets you intercept every tool call. `PostToolUse` fires after a tool succeeds. It is different from a pre-commit check: it runs in the middle of a session, while Claude still has context, can still fix the problem, and hasn't moved on to the next task.

**The minimal pattern — 12 lines**

In `.claude/settings.json`:

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/validate-edit.sh"
          }
        ]
      }
    ]
  }
}
```

In `.claude/hooks/validate-edit.sh`:

```bash
#!/bin/bash
FILE=$(jq -r '.file_path // empty' -)
[[ -z "$FILE" ]] && exit 0
pnpm eslint --quiet "$FILE" 2>&1 | head -30
```

Exit codes determine what Claude does next:
- **Exit 0** — nothing; Claude continues.
- **Exit 1** — Claude sees the output and attempts to fix the error before continuing.
- **Exit 2** — Claude stops and asks you before doing anything else.

**What to validate:**

- `pnpm eslint --quiet "$FILE"` — catch lint before the conversation ends.
- `pnpm tsc --noEmit` — surface type errors immediately instead of at build time.
- `jq empty` on any JSON file — structural validation; a corrupted JSON write is caught before it propagates.
- A custom schema check — this blog's `.claude/hooks/validate-blog.sh` runs after every write to `public/blog/` and blocks commits if `posts.json` is inconsistent. That is PostToolUse in production.

**What to watch out for:**

Hook commands inherit the session's environment, not a login shell. If your validator requires `PATH` entries that only exist in a login shell (e.g., `nvm`-managed node, Homebrew tools), add `export PATH=...` at the top of the hook script.

**The one gap:** hooks run in interactive sessions and the main loop. They do not fire inside Workflow SDK subagents. If your validation is critical in a multi-agent workflow, add it as an explicit `agent()` step after the write agent, not as a hook.

**Sources:** [Automate actions with hooks — Claude Code Docs](https://code.claude.com/docs/en/hooks-guide) · [Hooks reference — Claude Code Docs](https://code.claude.com/docs/en/hooks) · [Intercept and control agent behavior — Claude Code SDK Docs](https://code.claude.com/docs/en/agent-sdk/hooks)
]]></content:encoded>
    </item>

    <item>
      <title>Fable 5 Biology Queries Mostly Unblocked: Anthropic Reports 85% Drop in False Positives</title>
      <link>https://www.anthropic.com/news/improving-fable-5-s-biology-safeguards</link>
      <guid isPermaLink="false">2026-08-21-02-fable5-biology-safeguards-update</guid>
      <pubDate>Fri, 21 Aug 2026 04:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>LLM</category>
      <category>Security</category>
      <description>Anthropic retrained Fable 5's biology classifier, cutting false-positive fallbacks by ~85%. Everyday health and clinical queries now go through; virology and toxicology remain restricted to Opus 5.</description>
      <content:encoded><![CDATA[**Anthropic reports ~85% fewer biology-related fallbacks on Fable 5 after retraining the classifier. Everyday health, clinical, and educational biology queries now reach the model. Dual-use research on pathogens and toxins remains restricted.**

When Fable 5 launched, Anthropic blocked nearly all biology queries as a precaution — which meant interpreting lab results, explaining drug mechanisms, or answering clinical questions hit the same refusal wall as genuinely dangerous requests. The false-positive rate was high by design, accepted as the cost of a cautious launch.

That tradeoff has now been recalibrated. Anthropic rewrote the classifier's rule set to carve out detailed exceptions for benign use cases, gathered feedback from medical and scientific reviewers, and retrained the model. Per Anthropic's announcement, biology-related fallbacks dropped by approximately 85% across product surfaces. The August 2026 Risk Report, published alongside this update, details the broader classifier framework: real-time blocking, jailbreak remediation, and a formal distinction between universal jailbreaks and targeted attacks.

**For developers building on Fable 5:**

If you added fallback handling (`model_fallback` credit logic, graceful degradation to Sonnet 5 for biology-adjacent queries), re-test your critical paths — most should now return a useful response. The fallback credit mechanism still applies for anything that does reach a block.

**What remains restricted:** virology research, toxicology at the synthesis level, and similar dual-use areas. Per The Decoder's coverage, those capabilities remain gated to Opus 5, which carries stricter access controls. If your application depends on Fable 5 answering these queries, it still won't.

**The underlying caveat:** classifiers don't publish precise boundaries, intentionally. Test your specific prompts — don't assume a topic category is safe because a neighboring category is. The boundaries will continue to shift as Anthropic gathers data on what the recalibrated classifier misses.

**Why it matters:** developers who avoided Fable 5 for health, science, and clinical applications because of the fallback rate now have a substantially lower floor. The model is genuinely more usable for these workloads — though Opus 5 remains the right choice when dual-use biological research is the core task.

**Sources:** [Improving Fable 5 Safeguards — Anthropic](https://www.anthropic.com/news/improving-fable-5-s-biology-safeguards) · [August 2026 Risk Report — Anthropic](https://www.anthropic.com/aug-2026-risk-report) · [Anthropic loosens Fable 5's biology restrictions — The Decoder](https://the-decoder.com/anthropic-loosens-fable-5s-biology-restrictions-but-keeps-the-guardrails-on-for-virology-and-toxicology/)
]]></content:encoded>
    </item>

    <item>
      <title>Your Retriever Doesn't Know Which Documents Are Relevant — Metadata Filters Tell It Before Similarity Search Runs</title>
      <link>https://python.langchain.com/docs/how_to/self_query/</link>
      <guid isPermaLink="false">2026-08-21-01-rag-self-query-metadata-filter</guid>
      <pubDate>Fri, 21 Aug 2026 04:02:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>RAG</category>
      <category>Best Practices</category>
      <description>Metadata filters narrow your vector search to the right source, date, or category before similarity scoring runs. LangChain's SelfQueryRetriever builds those filters from natural language automatically.</description>
      <content:encoded><![CDATA[**What you'll be able to do after this:** query your vector store with natural language like "show me Python tutorials from 2024" and have the retriever automatically apply a structured filter — `language=Python AND year=2024` — before similarity search runs inside that subset.

**Why cosine similarity alone isn't enough**

Cosine similarity compares embedding vectors. It does not understand dates, categories, or document sources. A query like "what were the Q3 revenue numbers?" can return semantically close content from Q1 or Q2 if those happen to be nearest in embedding space. Metadata filtering adds an explicit pre-filter step: apply a WHERE clause first, then run similarity search inside the filtered subset.

**Three takeaways**

- Metadata filters run *before* similarity scoring — they narrow the haystack, they don't re-rank it.
- You can mix structured filters (`year > 2023`, `category = "finance"`) with free-text semantic similarity in the same query.
- LangChain's `SelfQueryRetriever` uses a small LLM call to translate natural language into filter predicates automatically — your users write plain queries, not structured filters.

**Walk-through with Chroma and LangChain**

First, store documents with metadata at index time:

```python
from langchain_community.vectorstores import Chroma
from langchain_openai import OpenAIEmbeddings

# Each doc.metadata carries fields like:
# {"source": "blog", "year": 2024, "topic": "rag", "language": "python"}
vectorstore = Chroma.from_documents(docs, OpenAIEmbeddings())
```

Declare the metadata fields the retriever is allowed to filter on:

```python
from langchain.chains.query_constructor.base import AttributeInfo
from langchain.retrievers.self_query.base import SelfQueryRetriever
from langchain_openai import ChatOpenAI

metadata_field_info = [
    AttributeInfo(name="source",   description="Document source: blog, docs, or wiki",        type="string"),
    AttributeInfo(name="year",     description="Year the document was published",               type="integer"),
    AttributeInfo(name="topic",    description="Primary topic tag, e.g. rag, embeddings, llm", type="string"),
    AttributeInfo(name="language", description="Programming language featured, if any",         type="string"),
]

retriever = SelfQueryRetriever.from_llm(
    llm=ChatOpenAI(model="gpt-4o-mini"),   # or claude-haiku-4-5 — filter construction is cheap
    vectorstore=vectorstore,
    document_contents="Technical articles about building AI applications",
    metadata_field_info=metadata_field_info,
)
```

Query with plain English — the LLM translates it into a Chroma filter automatically:

```python
docs = retriever.invoke("Python RAG articles from 2024")
# Chroma filter applied: {"year": {"$eq": 2024}, "topic": {"$eq": "rag"}, "language": {"$eq": "python"}}
```

**Three limits to know before you ship**

1. **Latency:** the filter-construction LLM call adds 100–500 ms. For latency-critical paths, build filters explicitly from structured query parameters (a date picker, a category dropdown) and skip the LLM translation entirely.

2. **Comparator support varies by store:** Chroma supports `$eq`, `$ne`, `$gt`, `$gte`, `$lt`, `$lte`, `$in`, `$nin`. Qdrant, Pinecone, and Weaviate support overlapping but non-identical sets. Check your store's comparator list before designing the metadata schema — a filter your store can't execute silently falls back to full scan.

3. **Filter accuracy depends on field descriptions:** the LLM generates filters from the `AttributeInfo` descriptions. Ambiguous descriptions produce wrong filters at the edges. Test queries that cross multiple fields before going to production.

**Sources:** [LangChain self-querying retrieval — official docs](https://python.langchain.com/docs/how_to/self_query/) · [How to Build a RAG System with a Self-Querying Retriever — Towards Data Science](https://towardsdatascience.com/how-to-build-a-rag-system-with-a-self-querying-retriever-in-langchain-16b4fa23e9ad/) · [Advanced RAG techniques Part 7 — Medium](https://medium.com/@roberto.g.infante/advanced-rag-techniques-with-langchain-part-7-843ecd3199f0)
]]></content:encoded>
    </item>




    <item>
      <title>Search Hugging Face Without Leaving Claude Code: Wire Up the Official HF MCP Server in One Command</title>
      <link>https://huggingface.co/docs/hub/en/hf-mcp-server</link>
      <guid isPermaLink="false">2026-08-20-07-hf-mcp-server-hub-from-claude-code</guid>
      <pubDate>Thu, 20 Aug 2026 20:09:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>MCP</category>
      <category>HuggingFace</category>
      <description>The official Hugging Face MCP server plugs the Hub into Claude Code with one command. Its hf_fs tool covers repos, docs, and semantic search in about 1,000 tokens — no more tab-hopping to hunt a model.</description>
      <content:encoded><![CDATA[**One `claude mcp add` command wires the official Hugging Face MCP server into Claude Code — its `hf_fs` tool covers Hub search, semantic docs lookup, and dataset discovery without leaving your editor.**

**The setup — one line**

```bash
claude mcp add hf-mcp-server -t http https://huggingface.co/mcp?login
```

Then start a Claude Code session and follow the browser prompt to sign in with your Hugging Face account (a READ-scope token is all it needs). Cursor, VS Code, Zed, and Claude Desktop all get client-specific snippets from [huggingface.co/settings/mcp](https://huggingface.co/settings/mcp) — that page generates the exact configuration your client expects; don't hand-write it.

**What `hf_fs` gives your agent**

`hf_fs` is the primary built-in tool, and one interface covers most Hub tasks: repos, storage, documentation, papers, Spaces. It includes semantic search over docs and Spaces, and HF's docs put its whole footprint at roughly 1,000 tokens — small enough to leave enabled by default without eating your context. Real prompts that work:

- "Search Hugging Face for Qwen 3.6 quantizations under 5B parameters."
- "Find a Space that transcribes audio."
- "How do I use LoRA adapters with PEFT?" (semantic docs search)
- "List datasets tagged `weather` and `time-series`."

Optional add-on tools — Contribute Repos, Sandboxes, Run and Manage Jobs — you turn on per-server in the settings page when you actually need them.

**Where it fits in a real workflow**

The point is not "chat with Hugging Face." It's that when Claude Code is picking a model, hunting a dataset, or clarifying a library API mid-task, the answer is a tool call instead of a context-switch to a browser tab. Combined with skills, it composes cleanly: a skill teaches Claude *when* to reach for the Hub; the MCP server gives it the *access*.

**Limits — name them upfront**

- **You give the agent your account's read access.** That includes your private repos if any exist. Use a fresh READ-only token if you want a clean audit trail.
- **`hf_fs` is search, not download.** It surfaces the right resource; pulling weights, running inference, or fine-tuning still happens through the normal Hub/transformers/vLLM path.
- **Restart the client after adding Spaces.** New Space-backed tools don't appear until the MCP client reloads.
- **Rate limits apply to your HF token**, not to Claude Code — heavy loops can hit them.

**Sources:** [Hugging Face MCP Server docs](https://huggingface.co/docs/hub/en/hf-mcp-server) · [MCP Server changelog + client snippets](https://huggingface.co/changelog/hf-mcp-server) · [Claude Code Beyond Basics: Skills, MCP, and Open Models — Adam Bernard](https://adambernard.com/kb/claude-code-beyond-basics-skills-mcp-and-open-models/)
]]></content:encoded>
      <media:content url="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/hf-mcp-vscode.png" medium="image" />
      <media:thumbnail url="https://huggingface.co/datasets/huggingface/documentation-images/resolve/main/hf-mcp-vscode.png" />
    </item>

    <item>
      <title>The Chunker Underneath Almost Every RAG Tutorial: LangChain's RecursiveCharacterTextSplitter, With Sizes That Actually Work</title>
      <link>https://docs.langchain.com/oss/python/integrations/splitters/recursive_text_splitter</link>
      <guid isPermaLink="false">2026-08-20-06-langchain-recursive-splitter-chunking-foundations</guid>
      <pubDate>Thu, 20 Aug 2026 20:08:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>RAG</category>
      <category>Best Practices</category>
      <description>Chunking is the least-glamorous, highest-leverage decision in a RAG pipeline. RecursiveCharacterTextSplitter is the sensible default; 512 chunks with ~64 overlap is the sensible starting size.</description>
      <content:encoded><![CDATA[**What you'll be able to do after this:** split any document into chunks that survive the "which paragraph was that in?" question, using LangChain's `RecursiveCharacterTextSplitter` with sizes benchmarks actually justify.

**The three-bullet takeaway:**
- **Recursive beats fixed-length.** The splitter tries paragraph breaks first, then lines, then spaces, then characters — so a chunk almost always ends at a boundary that makes sense to a reader.
- **512 characters with 64 overlap is the practical default.** Independent [chunking benchmarks](https://www.firecrawl.dev/blog/best-chunking-strategies-rag) put the sweet spot for general prose at 256–512 tokens with 10–20% overlap; use those knobs to open a bug report against your own retrieval, not to guess forever.
- **Metadata is the piece most people skip.** Every chunk needs `source`, `page` or heading, and `chunk_index`. Without them, "cite where this came from" is impossible after the fact.

**Walk-through — under 30 lines of Python**

```python
from langchain_text_splitters import RecursiveCharacterTextSplitter

with open("handbook.md") as f:
    doc = f.read()

splitter = RecursiveCharacterTextSplitter(
    chunk_size=512,       # target characters per chunk
    chunk_overlap=64,     # ~12% overlap keeps context across the seam
    separators=["\n\n", "\n", ". ", " ", ""],  # tried in order
    length_function=len,  # swap for a tokenizer for token-accurate sizes
)

chunks = splitter.create_documents(
    [doc],
    metadatas=[{"source": "handbook.md"}],
)

for i, c in enumerate(chunks):
    c.metadata["chunk_index"] = i

print(len(chunks), "chunks")
print(chunks[0].page_content[:200])
print(chunks[0].metadata)
```

**Why "recursive"?** The splitter walks the separator list top-down. Paragraph breaks come first because they're the strongest semantic seams; single newlines and sentences are fallbacks. It only reaches for spaces or raw characters when nothing better fits. That's why paragraphs stay whole for short documents and split gracefully when they don't.

**Common gotchas**

- **Character length ≠ token length.** `chunk_size=512` with `length_function=len` is 512 characters, roughly 100–150 tokens. If you want token-accurate sizes, swap in `tiktoken` or your model's tokenizer via `length_function`.
- **Code and markdown want different splitters.** `RecursiveCharacterTextSplitter.from_language(Language.PYTHON, ...)` uses code-aware separators (`\nclass `, `\ndef `) instead of prose ones. `MarkdownHeaderTextSplitter` splits by heading level first, then hands chunks off — worth it when heading structure matters more than length.
- **Overlap has diminishing returns.** Below ~10% you lose cross-chunk context; above ~25% you're paying to embed and store the same sentences repeatedly.

**Before you commit to a size, benchmark it.** Assemble 20–30 real questions your users ask, run each through your retriever, and measure recall@3 (does the correct passage appear in the top three?). Change `chunk_size` and re-run; the "correct" default is the one your queries actually recover. Skipping this and picking `chunk_size=1000` because a tutorial did is why so many RAG systems get to production with mysteriously bad recall.

**When to reach for something fancier:** semantic chunking (splitting where embedding similarity drops) is one option — Firecrawl's benchmark reports it lifts recall by roughly 70% over naïve fixed-size baselines while running ~14× slower than recursive. Reach for it after you have a baseline and a benchmark, never before.

**Sources:** [Splitting recursively — LangChain docs](https://docs.langchain.com/oss/python/integrations/splitters/recursive_text_splitter) · [Best chunking strategies for RAG (2026) — Firecrawl](https://www.firecrawl.dev/blog/best-chunking-strategies-rag) · [Chunk-size intuition — LangChain issue #2026](https://github.com/langchain-ai/langchain/issues/2026)
]]></content:encoded>
      <media:content url="https://python.langchain.com/img/brand/theme-image.png" medium="image" />
      <media:thumbnail url="https://python.langchain.com/img/brand/theme-image.png" />
    </item>

    <item>
      <title>Running /design-sync in Claude Code: What Your Codebase Feeds Into Claude Design (and Where the Round-Trip Gets Rough)</title>
      <link>https://support.claude.com/en/articles/14604397-set-up-your-design-system-in-claude-design</link>
      <guid isPermaLink="false">2026-08-20-05-design-sync-codebase-round-trip</guid>
      <pubDate>Thu, 20 Aug 2026 12:12:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Design</category>
      <category>Claude Design</category>
      <description>Run /design-sync in your Claude Code session and Claude Design builds prototypes from your actual components, not generic ones. The code-to-design direction works cleanly; the full project round-trip is still rough.</description>
      <content:encoded><![CDATA[**Run `/design-sync` in Claude Code and Claude Design builds your next prototype from your actual components instead of defaults. The code→design direction works cleanly; the design→code return trip still demands active attention.**

**What the command reads from your codebase**

`/design-sync` accepts a GitHub repo, a local directory, design files, or raw uploads. From your code it extracts:
- Component APIs and structure
- Color system, spacing scale, typography, CSS approach (custom properties, utility classes, etc.)
- Framework patterns: hooks, state, file organization

What it misses: conventions that live outside the code itself — custom `data-testid` patterns, undocumented a11y attributes, animation contracts. Before running `/design-sync` over a real codebase, write a short `DESIGN-SYSTEM.md` documenting those and include it in the upload. The day's earlier post on [understanding Claude Design's output format](https://cloudcodetree.com/ai-news/2026-08-20-02-claude-design-output-format-codebase-landing) covers what comes back.

**Running it**

```bash
/design-sync   # in any Claude Code session
```

The sync is incremental — one component at a time — and presents a plan you approve before anything is written. First sync over a large library means multiple approval rounds.

**The code→design direction: mostly solid**

Practitioners report that once the design system is loaded, Claude Design generates prototypes that look like your system — your color tokens, your component variants — rather than Tailwind defaults. Deeply nested slot structures sometimes get flattened, but the extraction is reliable for standard component APIs.

**The project round-trip: still rough**

The design→code leg produces a handoff bundle Claude Code picks up directly, but practitioners flag it as still awkward: the moments that demanded human judgment were the permission approvals, catching when Claude read the wrong component version, and reviewing the output before accepting it. [One detailed walkthrough](https://paulkuo.tw/en/articles/claude-design-sync-codebase/) describes it as "I wish it were a little easier" — the direction works, but it needs more active shepherding than the code→design leg.

**Requires:** Claude Pro, Max, or Team.

**Sources:** [Set up your design system — Anthropic Help](https://support.claude.com/en/articles/14604397-set-up-your-design-system-in-claude-design) · [Claude Design overhaul — VentureBeat](https://venturebeat.com/technology/anthropic-ships-major-claude-design-overhaul-with-design-system-imports-code-round-trips-and-a-fix-for-its-token-burning-problem) · [I Fed My Entire Design System to AI — Paul Kuo](https://paulkuo.tw/en/articles/claude-design-sync-codebase/)
]]></content:encoded>
    </item>

    <item>
      <title>Stop Re-Prompting for the Same Voice Every Session: Claude Code's Five Built-In Output Styles</title>
      <link>https://code.claude.com/docs/en/output-styles</link>
      <guid isPermaLink="false">2026-08-20-04-claude-code-output-styles-guide</guid>
      <pubDate>Thu, 20 Aug 2026 12:11:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Claude Code ships five built-in output styles — including Concise (added today) — each wired into the system prompt. Pick the one that matches the session job so you stop asking for the same voice every turn.</description>
      <content:encoded><![CDATA[**Claude Code ships five built-in output styles that change how it responds — tone, detail, execution posture — without changing what it's allowed to do. Concise was added today in v2.1.237. If you keep re-prompting "be brief" or "explain your choices", you want a style instead.**

**The five styles and when each fits:**

| Style | What it changes | Best session type |
|---|---|---|
| **Default** | Standard Claude Code | Routine engineering |
| **Concise** (new today) | Leads with results, skips narration | Experienced users, fast iteration |
| **Proactive** | Executes immediately, assumes reasonable choices | Hands-off sessions, long background runs |
| **Explanatory** | Adds "Insights" explaining choices | Learning a new codebase, audit trail |
| **Learning** | Insights + `TODO(human)` markers | Active pair-programming, onboarding |

**Proactive vs. auto mode:** Proactive adds *stronger* autonomous-execution guidance than auto mode does — Claude decides faster and narrates less — but it doesn't change your permission mode. Your permission settings still gate what runs without a prompt. If you want auto mode AND less narration, Proactive gives you that without opening new permissions.

**Set it:**
```bash
# Interactive: run in any session
/config   # → Output style → pick from menu

# Or set in .claude/settings.json for a project default
{ "outputStyle": "concise" }
```
Takes effect after `/clear` or a new session.

**Token impact:** Explanatory and Learning produce noticeably longer responses — the trade-off is worth it when you need the reasoning, wasteful when you don't. Styles inject a few hundred tokens into the system prompt, but prompt caching handles that overhead after the first request in a session.

**Limits:** One style per session — you can't blend them. Custom styles aren't in the built-in menu (you'd need a hook or SDK system-prompt edit). Independent practitioners note Proactive can miss edge cases that Default would flag; it's best for sessions where you've already validated the approach.

**Sources:** [Output styles — code.claude.com](https://code.claude.com/docs/en/output-styles) · [Pair programming with Claude Code output styles — Shipyard](https://shipyard.build/blog/claude-code-output-styles-pair-programming/) · [Output styles practical reference — ClaudeKit](https://getclaudekit.com/blog/guide/mechanics/output-styles-reference)
]]></content:encoded>
    </item>

    <item>
      <title>Anthropic Files API Is Now Generally Available: Upload Once, Reuse Across Calls</title>
      <link>https://docs.anthropic.com/en/docs/build-with-claude/files</link>
      <guid isPermaLink="false">2026-08-20-03-anthropic-files-api-ga</guid>
      <pubDate>Thu, 20 Aug 2026 12:10:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Agents</category>
      <category>Developer Tools</category>
      <description>Anthropic's Files API exits beta: upload a file once, reference it by ID across many Messages calls. No beta header needed. 1 TB per org, expires_at field added, new pagination on list endpoint.</description>
      <content:encoded><![CDATA[**Anthropic's Files API is now generally available — no beta header required. Upload PDFs, images, and other documents once, then pass a `file_id` to any Messages call instead of re-uploading each time.**

The GA release adds an `expires_at` field on file objects (so you can manage expiry proactively), pagination on the list endpoint (`page`/`next_page` + `ids[]` filter), and removes the `files-api-2025-04-14` beta header requirement.

Storage is 1 TB per organization at 500 requests per minute. Files do expire, so long-lived agents need to detect and re-upload when `expires_at` passes — it's not zero-maintenance. The API stores raw bytes; it doesn't embed or index content, so it isn't a retrieval layer on its own.

**Why it matters:** Multi-turn document agents — code review over a spec, analysis across a knowledge base — no longer pay re-upload latency and input-token overhead on every turn.

**Sources:** [Files API — Anthropic Docs](https://docs.anthropic.com/en/docs/build-with-claude/files) · [Claude API release notes — Anthropic](https://docs.anthropic.com/en/release-notes/api)
]]></content:encoded>
    </item>





    <item>
      <title>Set Your Claude Code Sessions to Run Without You: ANTHROPIC_DEFAULT_MODEL, Auto-Resume, and notify_when_idle</title>
      <link>https://code.claude.com/docs/en/changelog</link>
      <guid isPermaLink="false">2026-08-20-01-claude-code-unattended-session-defaults</guid>
      <pubDate>Thu, 20 Aug 2026 04:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Three Claude Code defaults from weeks 33-34 that let long-running sessions finish without supervision: pin your startup model, auto-resume after a usage pause, and get pinged when a background session goes idle.</description>
      <content:encoded><![CDATA[**Three Claude Code settings from the weeks 33–34 changelog that together cover the "start it and walk away" workflow: a persistent default model, a session that restarts itself, and a ping when a background session goes idle.**

**`ANTHROPIC_DEFAULT_MODEL` — pin your startup model system-wide**

Export it in your shell profile:

```bash
export ANTHROPIC_DEFAULT_MODEL=claude-sonnet-5-20260801
```

Unlike `ANTHROPIC_MODEL` (which applies only to the session you launch with the flag), `ANTHROPIC_DEFAULT_MODEL` sets the model every new session starts on. A `/model` pick in the session overrides it and persists across restarts — so per-project overrides still work. Use it if you have a preferred model you want without passing `--model` every time.

**Auto-resume after a usage-limit pause**

Claude Code now restarts your session automatically when a claude.ai usage limit resets, rather than dropping the session at the ceiling. If you're running a long-running job and step away, the session picks back up when your quota refills. Disable it in `/config` → "Continue session when usage limit resets" if you'd rather decide manually each time.

**`notify_when_idle` in cross-session SendMessage**

When you launch a background session, you can ask it to send a desktop notification the next time it goes idle:

```json
{"to": "my-review-session", "message": "notify_when_idle"}
```

One-shot and opt-in — the session sends one notification when it goes idle, then stops. Runs on macOS and Linux only (same-machine).

**What these don't cover**

- `notify_when_idle` is same-machine only — cloud sessions or remote runners can't ping back.
- Auto-resume applies to claude.ai time-based usage limits, not API token quotas.
- `ANTHROPIC_DEFAULT_MODEL` takes effect at session start, not mid-session.

**Sources:** [Claude Code changelog — code.claude.com](https://code.claude.com/docs/en/changelog) · [Cross-session messaging — code.claude.com](https://code.claude.com/docs/en/cross-session-messaging) · [Model configuration — code.claude.com](https://code.claude.com/docs/en/model-config) · [Claude Code August changelog summary — releasebot.io](https://releasebot.io/updates/anthropic/claude-code)
]]></content:encoded>
    </item>

    <item>
      <title>What Claude Design Hands You: Understanding the Output So You Can Land It in Your Codebase</title>
      <link>https://newsletter.victordibia.com/p/how-good-is-anthropics-claude-design</link>
      <guid isPermaLink="false">2026-08-20-02-claude-design-output-format-codebase-landing</guid>
      <pubDate>Thu, 20 Aug 2026 04:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Design</category>
      <category>Claude Design</category>
      <description>Claude Design outputs standard HTML/CSS or React — here's what the handoff bundle contains, how giving it your CSS changes extraction quality, and where you'll still need a review pass.</description>
      <content:encoded><![CDATA[**Claude Design outputs standard HTML/CSS/JS or React components — not a proprietary format. Giving it your existing CSS before generating changes the output quality significantly; here's how the extraction works and where it still falls short.**

**What you'll be able to do after this:**
- Know exactly what format Claude Design outputs, and what the handoff bundle passed to Claude Code actually contains
- Prepare your existing CSS or component library so Claude Design starts from your design system rather than generic defaults
- Know where automatic integration works and where you'll always need a review pass

## What Claude Design generates

Claude Design outputs HTML, CSS, and JavaScript for static prototypes, or React components when you ask for them. The output uses Tailwind or inline styles unless you specify otherwise. When you hand off to Claude Code, what passes across is the generated code plus context about design decisions — not a Figma file, not a design token spec, not a proprietary format. It's standard web code you can drop into version control.

[Victor Dibia's independent review of Claude Design](https://newsletter.victordibia.com/p/how-good-is-anthropics-claude-design) tested it on a real production task: adding an interactive globe to a live dashboard, wired to Google Analytics and Stripe data. His finding on output quality: **Claude Design ingests your existing CSS to understand your design system, so output respects your design tokens rather than guessing at them.** That one sentence captures the entire setup step worth doing.

## How to prep your codebase before generating (the step that changes output quality)

**Step 1 — Upload your CSS or design system file at project start.** In Claude Design, go to Settings → Design System (or use `/design-sync` in the Claude Code handoff path). Claude extracts color tokens, typography scales, and component patterns from the file. The more semantically named your CSS variables are — `--color-brand-primary` rather than `--c1` — the better the extraction.

**Step 2 — Upload your component library if you're working in React.** Provide the `components/` directory or a Storybook static build. Claude reads prop interfaces and naming conventions, so `<Button variant="primary">` comes out as your component, not a generic `<button className="bg-blue-500">`.

**Step 3 — Scope what you upload.** Claude Design reads the first ~50 KB of CSS it receives and the top-level components it finds. In a monorepo, give it the specific package path (`packages/ui/src/components`), not the repo root — otherwise it extracts from whatever file it encounters first.

## Where it still breaks

- **Prop signature mismatches.** Generated code won't exactly match custom component APIs. Your `size="sm"` may come out as `className="text-sm"`. Budget a light review pass.
- **Undocumented conventions.** If your system uses patterns that aren't expressed in CSS variables or JSDoc (custom a11y attributes, data-testid patterns, animation conventions), Claude Design won't infer them. Document what matters in a concise `DESIGN-SYSTEM.md` and include it in the upload.
- **The handoff bundle reflects what you approved in the canvas.** Claude Code can't fix a design-system mismatch that made it through design review unchanged. The design tool review pass is where you catch it.

**Sources:** [How Good is Anthropic's Claude Design? — Victor Dibia, Designing with AI newsletter](https://newsletter.victordibia.com/p/how-good-is-anthropics-claude-design) · [Set up your design system in Claude Design — support.claude.com](https://support.claude.com/en/articles/14604397-set-up-your-design-system-in-claude-design) · [Get started with Claude Design — support.claude.com](https://support.claude.com/en/articles/14604416-get-started-with-claude-design)
]]></content:encoded>
    </item>

    <item>
      <title>claude agents: See Which Background Sessions Need Your Attention, From One Terminal</title>
      <link>https://code.claude.com/docs/en/agent-view</link>
      <guid isPermaLink="false">2026-08-19-06-claude-agents-view-session-dashboard</guid>
      <pubDate>Wed, 19 Aug 2026 20:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Run 'claude agents' to see every background session's state at a glance. 'Needs input' (yellow star) is your priority queue — Space to peek and reply without leaving the list.</description>
      <content:encoded><![CDATA[**`claude agents` shows every background Claude Code session grouped by state — "Needs input" (yellow ✻) is the priority queue; `Space` lets you peek and reply without leaving the list.**

When you're running multiple background sessions via `--bg`, `/fork`, or a Workflow fan-out, you can't tell which ones are blocked without reattaching to each one. `claude agents` solves that:

```bash
claude agents
# optional flags:
# --cwd ~/projects/my-app   scope to one directory
# --model opus              set dispatch defaults
```

Sessions group by state. The three that matter:

| State | Icon | What to do |
|-------|------|------------|
| **Needs input** | Yellow ✻ | Reply — Claude is blocked |
| **Working** | Animated | Leave it running |
| **Completed** | Green ✓ | Peek to read the result |

**Navigate without fully attaching:**
- `Space` opens a peek panel. Read the context, compose a reply, press `Enter` — Claude unblocks without you leaving the session list.
- `Enter` or `→` attaches to the full interactive conversation.
- `←` detaches and returns to the list.

**Dispatch from the view:** type a task description in the input bar and press `Enter`. A new background session spins up alongside the existing ones. Use `Shift+Enter` to dispatch and attach immediately.

**Other keybindings worth knowing:**
- `Ctrl+T` — pin a session (prevents the 1-hour idle shutdown)
- `Ctrl+R` — rename a session
- `Ctrl+X` — stop; press twice to delete
- `?` — full shortcut list

**Stated limits:** Each background session draws on your rate-limit quota independently — 10 parallel sessions use roughly 10× your normal per-session rate. Non-pinned idle sessions stop after ~1 hour. The interface is still labeled research preview; shortcuts may change between releases.

The practical pattern: keep `claude agents` open in a split pane during a multi-agent run. Every yellow ✻ is a decision waiting for you; everything else is running fine.

**Sources:** [Agent view docs — code.claude.com](https://code.claude.com/docs/en/agent-view), [Claude Code v2.1.234 changelog](https://code.claude.com/docs/en/changelog)
]]></content:encoded>
    </item>

    <item>
      <title>Claude Code Weekly Quota +50% Extended to August 31; Anthropic Signals It May Become Permanent</title>
      <link>https://x.com/ClaudeDevs/status/2089798442306711646</link>
      <guid isPermaLink="false">2026-08-19-05-claude-code-weekly-limit-aug31</guid>
      <pubDate>Wed, 19 Aug 2026 20:06:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Claude Code</category>
      <category>Developer Tools</category>
      <description>Claude Code's 50% weekly quota boost (Pro, Max, Team, Enterprise) has been extended through August 31. First time Anthropic explicitly says they hope to make the increase permanent.</description>
      <content:encoded><![CDATA[**Claude Code's 50% weekly quota boost (Pro, Max, Team, and Enterprise) was extended through August 31 — and for the first time, Anthropic said they want to make it a permanent plan change.**

The 50% weekly usage increase was originally due to expire today. On August 18, @ClaudeDevs announced another extension through August 31 and said they "hope to make this a permanent change to our plans." Previous extension announcements were framed as capacity-driven one-offs; this one explicitly names permanence as the goal, though Anthropic adds that strong model demand means they cannot commit to a timeline.

In practice: your weekly token budget remains 1.5× documented plan limits through at least month-end. If you were pacing large multi-agent pipeline runs against the weekly cap, that constraint is lifted.

**Limit:** The five-hour per-session rate limits (permanently doubled in May) are a separate constraint this extension does not affect. Permanence is not confirmed — the next decision point is August 31.

**Why it matters:** Multi-agent Claude Code workflows you've been throttling against weekly quota limits have two more weeks — and this bump may become the new baseline.

**Sources:** [@ClaudeDevs on X (Aug 18, 2026)](https://x.com/ClaudeDevs/status/2089798442306711646), [Claude Code limits timeline — explainx.ai](https://explainx.ai/blog/claude-usage-limits-2026-timeline-explained)
]]></content:encoded>
    </item>

    <item>
      <title>Your AI Agent Ignores Your Design System. DTCG Tokens + AGENTS.md Tells It to Stop.</title>
      <link>https://styledictionary.com/info/dtcg/</link>
      <guid isPermaLink="false">2026-08-19-04-design-tokens-dtcg-agents-md-ai</guid>
      <pubDate>Wed, 19 Aug 2026 12:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Design</category>
      <category>Design-to-Code</category>
      <description>Export Figma variables as W3C DTCG JSON, transform to CSS with Style Dictionary, and add one line to AGENTS.md — agents then generate components that match your actual design tokens.</description>
      <content:encoded><![CDATA[**Export your design system as W3C DTCG JSON, build CSS custom properties with Style Dictionary, and add one line to AGENTS.md — Claude Code will then use your actual tokens instead of generic defaults in every component it generates.**

**What you'll be able to do after this:** make Claude Code (and other agents) generate components that use your actual design tokens — brand colors, spacing, typography — rather than the plausible-looking defaults they fall back on when you haven't told them where to look.

**Three things you'll take away:**
- How to export your design system as W3C DTCG JSON — the format agents can parse without special tooling
- How to compile those tokens to CSS custom properties your agent can find and use
- The one AGENTS.md line that makes the whole pipeline stick

**Why agents ignore your tokens without this.** Agents parse your codebase but don't know which values represent deliberate design decisions. Without explicit pointing, they pattern-match on whatever they see first — often Tailwind defaults or shadcn variables. A machine-readable token file plus an instruction in AGENTS.md fixes this without plugins.

**Step 1 — Export DTCG JSON from Figma.** *Plugins → Tokens Studio → Export → W3C DTCG format.* (Tokens Studio is free; DTCG export requires Figma with Variables, which is on paid plans.) This produces a `tokens.json` with `$value` and `$type` fields that any JSON-capable process can read:

```json
{
  "color": {
    "brand": {
      "primary": { "$value": "#3b82f6", "$type": "color" },
      "secondary": { "$value": "#06b6d4", "$type": "color" }
    }
  },
  "spacing": {
    "md": { "$value": "16px", "$type": "dimension" }
  }
}
```

Add this as `design-system/tokens.json` at the repo root and commit it.

**Step 2 — Build CSS custom properties with Style Dictionary.**

```bash
npm install --save-dev style-dictionary
```

`sd.config.json`:
```json
{
  "source": ["design-system/tokens.json"],
  "platforms": {
    "css": {
      "transformGroup": "css",
      "files": [{ "destination": "design-system/tokens.css", "format": "css/variables" }]
    }
  }
}
```

```bash
npx style-dictionary build --config sd.config.json
```

This writes `design-system/tokens.css` with entries like `--color-brand-primary: #3b82f6`. Commit it so the agent can read it at session start.

**Step 3 — Point Claude Code at the token file.** Add to `AGENTS.md` (or `.claude/CLAUDE.md`):

```markdown
## Design system
Always read `design-system/tokens.css` before generating or editing any component.
Use CSS custom properties from that file for all colors, spacing, and typography.
Do not invent color values or use Tailwind defaults when a token exists.
```

Claude Code reads AGENTS.md at session start and applies the instruction consistently.

**Where this breaks:** agents will still fall back to defaults wherever your token file has gaps — incomplete coverage in the spec means incomplete coverage in the output. Style Dictionary's default CSS naming (e.g. `--color-brand-primary`) may not match your existing component library's naming convention; if they diverge you'll need a custom formatter. DTCG export from Figma also requires the Variables feature, which is not on the free plan.

**Sources:** [Stop AI from Ignoring Your Design Tokens — atomize.tools](https://atomize.tools/blog/figma-design-tokens-vibe-coding/) · [Style Dictionary DTCG docs](https://styledictionary.com/info/dtcg/) · [W3C Design Tokens Community Group](https://www.designtokens.org/)
]]></content:encoded>
    </item>

    <item>
      <title>Cursor Launches Origin, a GitHub-Synced Code Host Designed for AI Agents</title>
      <link>https://cursor.com/changelog/origin-code-hosting</link>
      <guid isPermaLink="false">2026-08-19-03-cursor-origin-agent-native-git-host</guid>
      <pubDate>Wed, 19 Aug 2026 12:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Developer Tools</category>
      <category>Agents</category>
      <description>Cursor Origin launched for paid plans on Aug 18: a GitHub-synced code host designed for agent-scale workflows, with Vercel, Depot, and Buildkite integrations — agent-native features still coming.</description>
      <content:encoded><![CDATA[**Cursor launched Origin for paid plans on Aug 18 — a code host built for agent workloads that syncs with GitHub rather than replacing it.**

Origin is in early beta for Pro, Teams, and Enterprise plans. Initial features are basic: repos, pull requests, code browsing, and GitHub sync. Day-one integrations include Vercel (preview deployments per PR), Depot, and Buildkite (for existing GitHub Actions workflows). The "agent-native" features — per-agent identity and permissions, machine-readable PR descriptions, merge queues tuned for high-volume small diffs — are forthcoming.

Two things to know before moving anything:
- **No data terms published yet.** Cursor is now SpaceX-owned; what they can do with code stored on Origin isn't formally documented.
- **GitHub sync is one-way.** GitHub stays the source of truth; Origin pulls from it, not the other way around.

**Why it matters:** Version control designed for agent-scale commit rates and automated PRs is an unsolved gap in the toolchain. Origin is the first purpose-built attempt. Worth watching when the agent-native layer ships — per-agent identity in PRs would meaningfully change how you attribute and review Claude Code's work.

**Sources:** [Cursor Origin](https://cursor.com/changelog/origin-code-hosting) · [TechCrunch](https://techcrunch.com/2026/08/18/cursor-capitalizes-on-github-frustration-launches-rival-hosting-platform/) · [data concerns — TechTimes](https://www.techtimes.com/articles/324838/20260818/cursor-origin-ships-no-data-terms-spacex-now-holds-paid-developers-code.htm)
]]></content:encoded>
    </item>

    <item>
      <title>Dial Reasoning Up on Hard Subagents, Down on Cheap Ones: opts.effort in Claude Code Workflows</title>
      <link>https://code.claude.com/docs/en/workflows</link>
      <guid isPermaLink="false">2026-08-19-01-workflow-effort-per-agent</guid>
      <pubDate>Wed, 19 Aug 2026 04:02:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Set effort='low' on file scans and searches, effort='xhigh' on adversarial checks — same Claude Code workflow, 40-60% lower token cost where deep reasoning adds nothing.</description>
      <content:encoded><![CDATA[**Set `effort='low'` on scans, `effort='xhigh'` on verification — three reasoning tiers in one workflow cuts cost 40–60% without touching quality on hard steps.**

Every `agent()` call in a Claude Code workflow inherits the session's reasoning effort by default. The `opts.effort` parameter overrides it per call: `'low'` | `'medium'` | `'high'` | `'xhigh'` | `'max'`. The right tier depends on what kind of thinking the subagent actually needs.

```js
export const meta = { name: 'audit-and-fix', description: 'Security audit with tiered effort' }

// Stage 1: read files in parallel — no reasoning needed, just retrieval
const files = await parallel(paths.map(p => () =>
  agent(`Read and return the full contents of ${p}`, { effort: 'low' })
))

// Stage 2: implement fixes — medium effort (default) is fine for local edits
const fixes = await pipeline(files, f =>
  agent(`Fix any SQL injection risks in this file:\n\n${f}`)  // inherits session effort
)

// Stage 3: adversarial verify — xhigh: we want real scrutiny, not pattern-matching
const verdicts = await parallel(fixes.map(fix => () =>
  agent(`Try to find a bypass for this SQL injection fix. Default to bypassed=true if uncertain.`,
    { effort: 'xhigh', schema: VERDICT_SCHEMA })
))
```

## Decision table

| Stage type | Recommended effort | Reason |
|---|---|---|
| File reads, searches, grep | `low` | Output is deterministic; reasoning adds nothing |
| Straightforward implementation | `medium` (default) | Balanced cost/quality |
| Debugging, root-cause analysis | `high` | Benefits from reasoning steps |
| Adversarial verification, security review | `xhigh` | Needs genuine scrutiny |

## Limits to know

- `max` effort is not available on Haiku 4.5 — use `xhigh` as the ceiling there.
- Higher effort does not improve purely deterministic tasks (file reads, JSON parsing) — it just costs more tokens.
- Combine with `opts.model` for maximum control: `effort: 'low', model: 'haiku'` for cheap scans; `effort: 'xhigh', model: 'opus'` for the final judgment stage.
- Per-agent overrides are sticky within that agent only — the session's default effort is unaffected.

**Sources:** [Workflow SDK reference — code.claude.com](https://code.claude.com/docs/en/workflows) · [Model configuration — code.claude.com](https://code.claude.com/docs/en/model-configuration) · [claudefa.st — community practitioner notes](https://claudefa.st)
]]></content:encoded>
    </item>

    <item>
      <title>Regenerate in the Design Tool or Edit in Your Editor? The Decision Rule That Keeps Both From Fighting</title>
      <link>https://support.claude.com/en/articles/get-started-with-claude-design</link>
      <guid isPermaLink="false">2026-08-19-02-design-or-editor-decision-rule</guid>
      <pubDate>Wed, 19 Aug 2026 04:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Design</category>
      <category>Claude Design</category>
      <description>Three questions decide whether to regenerate in Claude Design or iterate in Claude Code — plus what post-handoff changes you'll need to re-apply after each re-export.</description>
      <content:encoded><![CDATA[**If it's exploratory or spans components, go back to the design tool. If direction is settled and the change is local, stay in the editor.**

The tension is real: Claude Design generates a fresh component tree on every export, so any change you made in code after the last export disappears on the next one. That makes "quick, I'll just tweak it in the tool" expensive — and "I'll just edit the code" increasingly wrong as soon as you need to explore a different layout direction.

## Three questions that decide

1. **Am I exploring directions, or executing one?**
   Still deciding between layouts, color schemes, or component structures → go back to Design. Direction is settled and you're implementing → stay in code.

2. **Does the change span more than one component?**
   A nav + hero + card consistency fix has to be done in the design tool — doing it piecemeal in code invites drift. A spacing fix inside one existing component is a one-liner in code.

3. **Will Claude Code know what "better" looks like?**
   Visual aesthetics and gestalt are things Claude Design reasons about holistically. Functional correctness (prop types, responsive breakpoints, a11y attributes) is what Claude Code is better positioned to verify.

## What's safe to edit directly in code (after handoff)

- Padding/margin/gap values within an existing component
- Prop types and TypeScript interfaces
- Responsive breakpoint tweaks (`sm:`, `md:` classes or MUI breakpoint objects)
- Animation timing and easing
- Accessibility attributes (`aria-label`, `role`, `tabIndex`)
- Integration-specific imports and wiring (`useRouter`, `useSession`, data hooks)

## What you'll lose on every re-export (the stateless re-export problem)

Claude Design has no memory of your codebase — it regenerates from its own model of the design. Every re-export is a clean slate from the tool's perspective. That means:

- Post-handoff a11y fixes
- TypeScript types you added
- Integration-specific imports
- Any custom hooks you wired in

**Recommendation:** maintain a `DIFF.md` (or a short comment block at the top of re-exported files) listing the changes you made post-handoff. Before every re-export, check it; after every re-export, re-apply it.

## The workflow that avoids the fight

1. Explore in Claude Design until direction is locked.
2. Export once. Record post-handoff changes in `DIFF.md`.
3. Iterate functionally in Claude Code from that point.
4. If you need to re-explore layout, go back to Design — but bring `DIFF.md` so you know what to re-apply.

**Sources:** [Get started with Claude Design — support.claude.com](https://support.claude.com/en/articles/get-started-with-claude-design) · [Claude Design handoff workflow — DEV Community](https://dev.to/claudeai/claude-design-to-code-handoff-workflow-2026) · [claudefa.st — community practitioner notes](https://claudefa.st)
]]></content:encoded>
    </item>

    <item>
      <title>Anthropic's First Operating Profit and the Path to a $2T IPO</title>
      <link>https://www.anthropic.com/news</link>
      <guid isPermaLink="false">2026-08-18-05-anthropic-first-profit-ipo-plan</guid>
      <pubDate>Tue, 18 Aug 2026 12:08:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>LLM</category>
      <description>Anthropic posted its first operating profit ($559M) in Q2 2026 on $11.5B in quarterly revenue — 14× year-over-year — with an annualized run-rate above $65B and an October IPO reportedly planned.</description>
      <content:encoded><![CDATA[**Anthropic's Q2 2026 results: $11.5B in revenue (14× year-over-year), first operating profit of ~$559M, annualized run-rate above $65B, and a reported $2T IPO in October.**

Q2 revenue rose 2.4× from Q1 ($4.73B) and the operating profit arrived roughly two years ahead of the timeline Anthropic had cited internally. The annualized run-rate figure — over $65B — reflects strong enterprise and API growth, primarily driven by Claude Code adoption and multi-seat API contracts.

**Why it matters:** The company behind Claude's API is now financially self-sustaining, which changes the calculus for teams building production workloads on it. A profitable Anthropic with IPO-level transparency is under more obligation to maintain API stability and pricing predictability than a burn-rate startup. Watch IPO filings for the first public breakdown of API vs. consumer revenue.

**Sources:** [Anthropic Q2 revenue $11.5B — CNBC](https://www.cnbc.com/2026/08/15/anthropic-revenue-jumps-to-over-11point5-billion-in-q2-report.html) · [Annualized run-rate surpasses $65B — Bloomberg](https://www.bloomberg.com/news/articles/2026-08-17/anthropic-revenue-run-rate-surpasses-65-billion-ahead-of-ipo) · [Plans for $2T IPO in October — Fortune](https://fortune.com/2026/08/13/anthropic-ipo-2-trillion-october-largest-ever-spacex/)
]]></content:encoded>
    </item>

    <item>
      <title>Three Claude Code Cloud Tasks Running in Parallel — and a PR Watcher That Fixes Itself</title>
      <link>https://code.claude.com/docs/en/claude-code-on-the-web</link>
      <guid isPermaLink="false">2026-08-18-06-cloud-parallel-tasks-autofix-pr</guid>
      <pubDate>Tue, 18 Aug 2026 12:09:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Three `claude --cloud` commands spawn three independent VMs that run in parallel. Pair with `/autofix-pr` and each resulting PR monitors and fixes itself while you keep coding.</description>
      <content:encoded><![CDATA[**One `--cloud` command per task spawns an independent cloud VM. Stack three in a row and all three run in parallel while you stay in your editor.**

```bash
# These three execute simultaneously in separate cloud VMs
claude --cloud "Fix the flaky test in auth.spec.ts"
claude --cloud "Update the OpenAPI docs in docs/api.yaml"
claude --cloud "Refactor the logger to use structured output"
```

Run `/tasks` inside any Claude Code session to monitor all three sessions at once, or check them at [claude.ai/code](https://claude.ai/code) from your phone.

## Auto-fix: the PR that patches itself

When each session opens a PR, you can turn on auto-fix: teleport into the session (or paste the PR URL into it) and run `/autofix-pr`. Claude subscribes to GitHub activity on that PR. When a check fails, Claude investigates and pushes a fix; when a reviewer leaves a comment, Claude resolves or responds to it. You don't need to babysit the PR.

From the terminal (while on the PR's branch):

```bash
/autofix-pr
# Claude detects the open PR with gh, spawns a web session,
# and turns on auto-fix in one step
```

## Two things to do before you `--cloud`

1. **Push first.** `--cloud` clones the remote at your current branch — local commits that aren't pushed won't be in the cloud session.
2. **Check rate limits.** Three parallel sessions consume proportionately more of your account's rate limit. For long-running tasks, stagger them by a few minutes if you're near your limit.

**When this pattern pays off:** large refactors you can decompose by file or subsystem, independent bug fixes on different features, or running the test-and-fix loop in the cloud while you continue writing new features locally.

**Sources:** [Claude Code on the web — code.claude.com](https://code.claude.com/docs/en/claude-code-on-the-web) · [Auto-fix pull requests — code.claude.com](https://code.claude.com/docs/en/claude-code-on-the-web#auto-fix-pull-requests)
]]></content:encoded>
    </item>

    <item>
      <title>Embed Sentences, Answer With Paragraphs: Parent-Document Retrieval Fixes the Chunk-Size Tradeoff</title>
      <link>https://dev.to/rushanksavant/sentence-window-retrieval-212d</link>
      <guid isPermaLink="false">2026-08-18-09-parent-document-retrieval-chunk-size</guid>
      <pubDate>Tue, 18 Aug 2026 16:26:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>RAG</category>
      <category>Embeddings</category>
      <description>Small chunks retrieve precisely but lack context; big chunks have context but retrieve poorly. Parent-document retrieval lets you stop choosing: embed the sentence, return the paragraph.</description>
      <content:encoded><![CDATA[
**Stop tuning chunk size. Embed small chunks for precise retrieval, then send the LLM the larger passage each chunk came from.**

Every RAG pipeline hits the same wall. Small chunks embed cleanly and match queries precisely, but hand the model a sentence with no surrounding context. Large chunks carry context but dilute the embedding, so retrieval gets vague. Tuning the number trades one failure for the other.

**What you'll be able to do after this:**
- Retrieve on precise, sentence-sized embeddings without starving the model of context
- Wire it up in LangChain with two splitters instead of one
- Know what it costs, so you can decide whether it's worth it

**How it works.** You store two representations. Small *child* chunks go in the vector store and are the only thing you search. Each child keeps the id of the larger *parent* passage it came from, and parents live in a separate docstore. At query time: similarity search hits a child → read its parent id → fetch the parent from the docstore → send the parent to the model. You search the needle and answer with the paragraph.

**In LangChain** this is `ParentDocumentRetriever`, built from a `child_splitter`, a `parent_splitter`, a `vectorstore`, and a `docstore`. [Rushank Savant's walk-through](https://dev.to/rushanksavant/sentence-window-retrieval-212d) uses a 100-character child splitter with no overlap against a 400-character parent splitter with 50-character overlap — a useful starting ratio. LlamaIndex does the same thing differently: `SentenceWindowNodeParser` embeds each sentence but stashes the neighbouring sentences in metadata, then `MetadataReplacementPostProcessor` swaps the window back in before the model sees it.

**What it costs.** That write-up is honest about the tradeoffs, and they're real: you store your corpus twice, the id lookup adds a hop of latency, overlapping parents mean you resend some tokens, and the vector store and docstore now have to stay in sync. A fixed window also can't help when the context you need sits outside it.

**Sources:** [Sentence window retrieval — Rushank Savant](https://dev.to/rushanksavant/sentence-window-retrieval-212d) · [LangChain ParentDocumentRetriever reference](https://reference.langchain.com/python/langchain-classic/retrievers/parent_document_retriever) · [Chunking for RAG — Vectara](https://www.vectara.com/blog/grounded-generation-done-right-chunking)
]]></content:encoded>
      <media:content url="https://media2.dev.to/dynamic/image/width=1200,height=627,fit=cover,gravity=auto,format=auto/https%3A%2F%2Fdev-to-uploads.s3.amazonaws.com%2Fuploads%2Farticles%2Fsal6acets6eqjs75qwg5.jpg" medium="image" />
      <media:thumbnail url="https://media2.dev.to/dynamic/image/width=1200,height=627,fit=cover,gravity=auto,format=auto/https%3A%2F%2Fdev-to-uploads.s3.amazonaws.com%2Fuploads%2Farticles%2Fsal6acets6eqjs75qwg5.jpg" />
    </item>

    <item>
      <title>A Better Prompt Won't Fix This: The Accessibility Failures That Survive AI UI Generation</title>
      <link>https://accessibe.com/accessibility-platform/reports/gen-ai-ecommerce/accessibility-audit</link>
      <guid isPermaLink="false">2026-08-18-08-generated-ui-accessibility-review-pass</guid>
      <pubDate>Tue, 18 Aug 2026 16:00:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Design</category>
      <category>Design-to-Code</category>
      <description>An audit of two AI-generated storefronts found the same four critical accessibility failures in both — a detailed prompt removed the minor issues and none of the critical ones.</description>
      <content:encoded><![CDATA[
**Two AI-built storefronts audited against WCAG 2.2 AA hit the same four critical failures despite very different prompts. Prompt harder isn't the fix — a review pass is.**

accessiBe [audited two Lovable-built storefronts](https://accessibe.com/accessibility-platform/reports/gen-ai-ecommerce/accessibility-audit) on 2026-06-16 against WCAG 2.2 AA, using screen readers and keyboard testing rather than a scanner alone. One was built from a one-line prompt, the other from a detailed, iterated brief. The detailed brief did help — 9 issues versus 15. But accessiBe reports **both sites shared the same four critical failures**. Better prompting moved the minor problems and left the serious ones untouched.

That result makes sense once you notice what the model is optimising for. ARIA roles, focus order, and accessible names are invisible in a rendered screenshot, so a model trained toward visually plausible output has no signal pushing it to get them right.

**The recurring four**, worth checking in any generated UI:

1. **Overlays** — modals, drawers, newsletter popups — with no `role="dialog"`, no accessible label, no focus trap, no Escape handler. Focus stays on the page behind.
2. **Inputs labelled only by placeholder text**, which disappears on typing and is not an accessible name.
3. **Contrast failures**, usually on sale badges and secondary text.
4. **Information carried only visually** — a struck-through price, a colour-coded state — with no text equivalent.

**Where to put the effort.** [master.dev's write-up](https://master.dev/blog/ai-generated-ui-is-inaccessible-by-default/) found ten distinct failures in a single generated sidebar, and puts prevention at 3–8 minutes per component against 45–90 minutes to remediate after launch. The cheap layers, in order: generate onto accessible primitives (Radix, React Aria — this is why v0's shadcn output starts ahead), then `eslint-plugin-jsx-a11y` for static checks, then `jest-axe` or `@axe-core/playwright` in CI.

**The limit worth knowing:** automated tooling catches roughly a third of WCAG criteria. Focus order, screen-reader narrative, and whether a flow makes sense without sight need a human keyboard pass — tab through the thing once before you ship it.

**Sources:** [accessiBe: AI storefront accessibility audit](https://accessibe.com/accessibility-platform/reports/gen-ai-ecommerce/accessibility-audit) · [master.dev: AI-generated UI is inaccessible by default](https://master.dev/blog/ai-generated-ui-is-inaccessible-by-default/) · [W3C WAI evaluation tools](https://www.w3.org/WAI/test-evaluate/tools/list/)
]]></content:encoded>
      <media:content url="https://master.dev/blog/wp-json/social-image-generator/v1/image/9221" medium="image" />
      <media:thumbnail url="https://master.dev/blog/wp-json/social-image-generator/v1/image/9221" />
    </item>



    <item>
      <title>Generate On-Brand React Components from Your Design System, Then Hand Them to Claude Code via MCP</title>
      <link>https://www.magicpatterns.com/docs/documentation/features/mcp-server/overview</link>
      <guid isPermaLink="false">2026-08-18-04-magic-patterns-mcp-claude-code-design-system</guid>
      <pubDate>Tue, 18 Aug 2026 04:11:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Design</category>
      <category>Design-to-Code</category>
      <description>Magic Patterns' MCP server connects your design system directly to Claude Code — generate on-brand React components and hand them off without copy-paste or manual token cleanup.</description>
      <content:encoded><![CDATA[**Magic Patterns MCP lets Claude Code pull AI-generated components straight from your design system spec — no copy-paste, no manual cleanup of token names or import paths.**

The workflow gap: you prototype a UI in an AI design tool, then manually translate the generated code into your codebase's conventions — fixing token names, swapping component aliases, correcting import paths. Magic Patterns' MCP server closes that gap by making the generated component visible to Claude Code as a machine-readable spec, not a blob of pasted JSX.

## Setup (~5 minutes)

1. In Magic Patterns, import your design system: upload your `@company/tokens` package or link your Figma library. Magic Patterns reads your primitives, components, and brand tokens.
2. Run `/mcp` inside Claude Code to trigger OAuth — the Magic Patterns Remote MCP Server connects automatically.
3. Add the Integration Skill via `/install magic-patterns`. This tells Claude Code to treat the Magic Patterns output as a spec and adapt it to your codebase's conventions rather than pasting the prototype verbatim.

## The workflow

Once connected, you describe the component you need inside Claude Code and it calls the MCP server, retrieves the component spec that matches your design system, and writes a file that uses your existing packages:

```bash
# In Claude Code:
Generate a FilterBar component using our design system tokens for the dashboard sidebar
# Claude Code calls Magic Patterns MCP, retrieves the spec,
# adapts it to your Button, Chip, and TextField components, and writes FilterBar.tsx
```

## Before merging, always check

- **Token usage:** does the component use `color.primary.600` from your tokens, or re-declare it as `#1d4ed8`?
- **Import paths:** are packages referenced in the file actually in your `package.json`?
- **Component aliases:** did it map Magic Patterns' `Tag` to your codebase's `Chip`, or introduce a new primitive?

The Integration Skill handles token mapping well; component-name aliases between your system and Magic Patterns still need a spot-check on the first few components until you've confirmed the mapping is right.

**Sources:** [Magic Patterns MCP Overview](https://www.magicpatterns.com/docs/documentation/features/mcp-server/overview) · [Design Systems — Magic Patterns](https://www.magicpatterns.com/product/design-systems)
]]></content:encoded>
      <media:content url="https://www.magicpatterns.com/og-image.png" medium="image" />
      <media:thumbnail url="https://www.magicpatterns.com/og-image.png" />
    </item>

    <item>
      <title>Don't Ctrl+Z Your AI Work — Claude Code's /rewind Menu Gives You Three Better Options</title>
      <link>https://code.claude.com/docs/en/checkpointing</link>
      <guid isPermaLink="false">2026-08-18-03-claude-code-rewind-checkpoints</guid>
      <pubDate>Tue, 18 Aug 2026 04:10:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Press Esc twice or type /rewind to independently revert code, conversation, or both — surgical rollback without running git reset or losing session context.</description>
      <content:encoded><![CDATA[**Press Esc twice or type /rewind to get a menu that independently rewinds code, conversation, or both — without losing the other, and without running git reset.**

Claude Code automatically snapshots your file state before each user prompt. When something goes sideways, most engineers reach for `git restore` or `git reset` and lose progress. `/rewind` is more surgical.

## What /rewind gives you

**Restore code, keep conversation.** Revert the files to the checkpoint, but keep the chat context. You can immediately try a different approach without re-explaining the task, the constraints, or the background.

**Restore conversation, keep code.** Go back to an earlier message while keeping whatever Claude just wrote. Useful when you want the code but want to re-ask a question differently — e.g., you got the output you wanted but the conversation drifted and you need to redirect.

**Summarize from here.** Compress the conversation backward from this point, freeing context window space without touching the code. Equivalent to a manual `/compact` targeted at one section.

## Activate it

```
Esc Esc    → opens the rewind menu (fastest)
/rewind    → same menu from the prompt
```

Navigate the checkpoint list, pick one, and choose which dimension to restore.

## What checkpoints do and don't capture

- **Captured:** every file Claude edits through its own file editing tools.
- **Not captured:** files modified by `Bash` commands or external processes.

For Bash-modified files, `git diff` is still your source of truth. Before a risky batch of edits, consider asking Claude to explain what it's about to do, then let it proceed. If the output is wrong: Esc+Esc → Restore code, keep conversation → redirect.

**Sources:** [Checkpointing — code.claude.com](https://code.claude.com/docs/en/checkpointing)
]]></content:encoded>
    </item>

    <item>
      <title>DeepSeek V4 Pricing Is Now Peak/Off-Peak — and Peak Can Be 10x the Old Flat Rate</title>
      <link>https://api-docs.deepseek.com/quick_start/pricing</link>
      <guid isPermaLink="false">2026-08-18-02-deepseek-v4-peak-offpeak-pricing</guid>
      <pubDate>Tue, 18 Aug 2026 04:09:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>LLM</category>
      <category>Developer Tools</category>
      <description>DeepSeek V4 Flash and V4 Pro switched to peak/off-peak pricing on August 16 — peak rates are up to 10x the old flat rate; off-peak is half of peak.</description>
      <content:encoded><![CDATA[**DeepSeek's new peak/off-peak pricing, live since August 16, replaces the flat V4 rates — peak can be up to 10x the old promotional price; off-peak is half of peak.**

The [August 14 post here](https://cloudcodetree.com/ai-news/2026-08-14-02-deepseek-v4-pro-0813-ga) flagged that DeepSeek had announced a price increase with no details. The numbers are now live:

| Model | Old flat | Off-peak | Peak |
|---|---|---|---|
| deepseek-v4-flash | $0.14 / $0.28 | $0.22 / $0.66 | $0.44 / $1.32 |
| deepseek-v4-pro | $0.435 / $0.87 | $0.66 / $1.98 | $1.32 / $3.96 |

*Per 1M input / output tokens.*

Peak hours run 01:00–04:00 UTC and 06:00–10:00 UTC daily. Everything else is off-peak.

**Why it matters:** If your pipeline is schedulable or batched, routing calls outside peak hours halves the cost. Budget projections built before August 16 need revision — V4 Flash at peak is now 3x the old rate, V4 Pro is 3x–4x.

**Sources:** [DeepSeek API Pricing](https://api-docs.deepseek.com/quick_start/pricing) · [qz.com](https://qz.com/deepseek-api-price-increase-v4-peak-off-peak-081326)
]]></content:encoded>
    </item>

    <item>
      <title>Your Retriever Is Looking at Your Data Through One Keyhole — Multi-Query RAG Gives It Five</title>
      <link>https://www.youtube.com/watch?v=LyMWMnguo1E</link>
      <guid isPermaLink="false">2026-08-18-01-multi-query-retriever-langchain-rag</guid>
      <pubDate>Tue, 18 Aug 2026 04:08:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>RAG</category>
      <category>Best Practices</category>
      <description>LangChain's MultiQueryRetriever generates 3-5 reformulations of every user question, runs them in parallel, and deduplicates results — surfacing relevant chunks a single query embedding would miss.</description>
      <content:encoded><![CDATA[**LangChain's MultiQueryRetriever generates 3–5 reformulations of every user question, runs them in parallel against your vector store, and deduplicates — surfacing relevant chunks that a single query embedding would miss.**

**What you'll be able to do after this:**
- Boost RAG recall without touching your embedding model, chunk size, or vector store
- Understand why a single query vector misses semantically related documents
- Wire up multi-query retrieval in ~15 lines of Python on top of any existing retriever

## The problem

Vector similarity search embeds your user's question into a point in space and returns the nearest neighbors. If the user asks "How does chunking affect embedding quality?", the embedding might miss a document titled "Optimal text splitting strategies for dense retrieval" — even though they cover the same idea — because the surface-form distance is large. One query = one angle of attack.

## The fix: generate multiple angles

MultiQueryRetriever calls a cheap LLM to rewrite the query three to five different ways, runs each one against the vector store, and returns the union (deduplicated). The union is almost always a superset of what any single query would return.

## Code

```python
from langchain.retrievers.multi_query import MultiQueryRetriever
from langchain_anthropic import ChatAnthropic
from langchain_chroma import Chroma

vectorstore = Chroma(collection_name="docs", embedding_function=embeddings)

retriever = MultiQueryRetriever.from_llm(
    retriever=vectorstore.as_retriever(search_kwargs={"k": 5}),
    llm=ChatAnthropic(model="claude-haiku-4-5-20251001"),  # cheap for query gen
)

docs = retriever.invoke("How does chunking affect embedding quality?")
# Returns union of k=5 results across 3–5 generated query variations
```

The LLM call for query generation is cheap — use Haiku or another small model, not Sonnet or Opus. The total latency overhead is roughly one extra LLM round-trip, which runs in parallel with the first vector search.

## Pair it with a reranker

Multi-query gives you high recall; a cross-encoder reranker (e.g., `cross-encoder/ms-marco-MiniLM-L-6-v2`) gives you high precision from the expanded candidate set. The combination is the standard recipe for production RAG that needs both.

**Resource:** [LangChain MultiQueryRetriever — YouTube tutorial #43](https://www.youtube.com/watch?v=LyMWMnguo1E) · [LangChain retrieval docs](https://docs.langchain.com/oss/python/langchain/retrieval) · [GitHub notebook with full examples](https://github.com/edumunozsala/langchain-rag-techniques/blob/main/Multiquery-retrieval.ipynb)

**Sources:** [LangChain MultiQueryRetriever — YouTube](https://www.youtube.com/watch?v=LyMWMnguo1E) · [LangChain docs](https://docs.langchain.com/oss/python/langchain/retrieval)
]]></content:encoded>
      <media:content url="https://img.youtube.com/vi/LyMWMnguo1E/maxresdefault.jpg" medium="image" />
      <media:thumbnail url="https://img.youtube.com/vi/LyMWMnguo1E/maxresdefault.jpg" />
    </item>

    <item>
      <title>Stop Reprompting the Whole Component: Figma Make Now Has a Properties Panel and In-Context Annotations</title>
      <link>https://www.figma.com/blog/properties-panel-and-annotations-now-in-figma-make/</link>
      <guid isPermaLink="false">2026-08-17-08-figma-make-properties-annotations</guid>
      <pubDate>Mon, 17 Aug 2026 20:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Design</category>
      <category>Design-to-Code</category>
      <description>Figma Make now has a properties panel for direct visual editing and in-context annotations so you mark exactly what you want the AI agent to change — no more reprompting the full component.</description>
      <content:encoded><![CDATA[**Figma Make's new properties panel brings familiar Figma Design controls into the AI editor; in-context annotations let you mark exactly what the agent should change — no more reprompting the whole component.**

When you iterate on a design in Figma Make, the default workflow is one big prompt: describe what you want and hope the AI scopes itself correctly. Two new tools shipped in late July change that.

**Properties panel.** The same spacing, typography, layout, and color controls from Figma Design are now live in the Make editor. Select any element and adjust it directly — Make stages the change in the prompt box for you to review before it lands in the underlying code. For pure visual adjustments, this beats a natural-language prompt every time.

**In-context annotations.** For changes that go beyond visual properties — an animation, a hover interaction, a conditional render — select "Annotate for agent" in the toolbar and mark the exact area on canvas. Write your instruction directly on the design. The agent sees precisely what you're pointing at without you having to describe the surrounding layout.

**The workflow in practice:**
1. Generate or open a component in Figma Make.
2. Fix visual properties (padding, font size, color) directly in the properties panel.
3. For behavior or animation changes, annotate the specific element — don't reprompt the full component.
4. Review each staged change before committing — Make updates the underlying React/Next.js code automatically.
5. Export or push to your repo when the component is right.

The practical gain: AI edits are scoped to exactly what you intended. Annotating one button's hover state on a complex dashboard is far less disruptive than reprompting the whole layout and reviewing a diff full of unrelated changes. Both tools work whether you're starting from scratch or editing existing Make-connected code.

**Sources:** [A Properties Panel and Annotations, Now in Figma Make — Figma Blog](https://www.figma.com/blog/properties-panel-and-annotations-now-in-figma-make/) · [Figma Release Notes — Releasebot](https://releasebot.io/updates/figma)
]]></content:encoded>
    </item>
    <item>
      <title>Connect v0 to Your Real GitHub Repo: AI-Generated Components That Go Through PRs, Not Around Them</title>
      <link>https://v0.app/docs/faqs</link>
      <guid isPermaLink="false">2026-08-17-07-v0-github-existing-repo-workflow</guid>
      <pubDate>Mon, 17 Aug 2026 12:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Design</category>
      <category>Design-to-Code</category>
      <description>v0's GitHub integration creates one branch per chat and never touches main — generated components land as a PR you review like any other code change.</description>
      <content:encoded><![CDATA[
**v0's GitHub integration creates one branch per chat and never commits to main — generated UI lands as a PR you review like any other code change, not as a mystery diff in your working tree.**

The common fear with v0: you prototype something useful and then face a pile of generated code with no clear path into an existing Next.js/React codebase. The February 2026 update added native GitHub integration that addresses this with a branch-per-chat workflow.

## Setup

1. In v0's project settings, connect your GitHub account.
2. Create a new project → + → "Import from GitHub" → select your repo.
3. For private repos: grant v0 access in your GitHub App settings.
4. v0 creates one branch per chat session and never commits to `main` by design.

## The workflow

- Every code change in v0 is an automatic commit on that branch.
- Create a PR directly from v0 — it goes through your normal review process.
- Pull the branch locally with `git fetch && git checkout <branch>` to inspect the generated code in your editor before merging.

## Before merging, check for

- Does the generated component use your existing design system, or does it shadow it with its own Tailwind/MUI classes?
- Are prop types and import paths consistent with the rest of the codebase?
- Are there `useEffect` calls with missing or empty deps arrays where the actual dependencies weren't inferred?

## After merge: hand off to Claude Code

The generated component is now in your repo and tracked by git. Run `/code-review` against the branch for a bug-finding pass, then iterate in your editor. v0 gets you to a workable shape; Claude Code closes the gap on correctness and integration.

**Sources:** [v0 docs: import from GitHub — v0.app](https://v0.app/docs/faqs) · [Integrating v0 with existing GitHub repos — Vercel Community](https://community.vercel.com/t/integrating-v0-with-existing-github-repositories-and-vercel-projects/34005) · [v0 complete guide 2026 — NxCode](https://www.nxcode.io/resources/news/v0-by-vercel-complete-guide-2026)
]]></content:encoded>
    </item>

    <item>
      <title>What /code-review Does That the GitHub App Doesn't: Effort Levels, --fix, and REVIEW.md</title>
      <link>https://code.claude.com/docs/en/code-review</link>
      <guid isPermaLink="false">2026-08-17-06-code-review-slash-command-effort-reviewmd</guid>
      <pubDate>Mon, 17 Aug 2026 12:06:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Developer Tools</category>
      <description>The /code-review slash command runs a bug-hunting subagent in the background without consuming your session context — tune effort, auto-apply fixes, and scope it with REVIEW.md.</description>
      <content:encoded><![CDATA[
**`/code-review` runs a background bug-hunting subagent that doesn't consume your session context — tune effort, auto-apply fixes, and give it a project-specific mandate with `REVIEW.md`.**

Claude Code's `/code-review` command is distinct from the GitHub App-based Code Review service. It runs locally as a forked background subagent — the findings arrive in your conversation when the review completes, without filling your context window with intermediate reasoning.

## Effort controls confidence vs. coverage

`low` and `medium` report only high-confidence findings, so you see fewer false positives but narrower coverage. `high` through `max` broaden coverage but include findings the review is less certain about. Claude remembers the last level you typed explicitly, so always pass one in scripts:

```
/code-review high          # background review, report findings
/code-review high --fix    # same, plus auto-apply findings to working tree
```

**Caveat:** `--fix` edits land outside your session checkpoints. `/rewind` won't undo them — use git if you want to inspect before applying.

## REVIEW.md gives the review a project mandate

A `REVIEW.md` file at your repo root is injected at highest priority into every agent in the pipeline — higher than your `CLAUDE.md`. Use it to define what "Important" means in your codebase, which paths to skip, and what checks to always run:

```markdown
## What Important means here
Reserve 🔴 for bugs that break behavior or leak data.
Style and naming are Nit at most.

## Always check
- New API routes have an integration test
- Log lines exclude email addresses and user IDs

## Skip
- Generated code under src/gen/
- Any *.lock file
```

Keep `REVIEW.md` focused: a long file dilutes the rules that matter most.

## For cloud-level review

`/code-review ultra` (Team/Enterprise) escalates to a multi-agent swarm on Anthropic infrastructure, with findings posted inline to your PR via `--post`. From CI scripts: `claude -p '/code-review ultra'` launches the review non-interactively.

**Sources:** [Claude Code Review — code.claude.com](https://code.claude.com/docs/en/code-review) · [REVIEW.md reference — code.claude.com](https://code.claude.com/docs/en/code-review#customize-reviews) · [Ultrareview — code.claude.com](https://code.claude.com/docs/en/ultrareview)
]]></content:encoded>
    </item>

    <item>
      <title>Stripe Buys OpenRouter for $7B+; Google Imagen 4 API Retires Today</title>
      <link>https://techcrunch.com/2026/08/16/stripe-will-reportedly-acquire-ai-gateway-startup-openrouter-for-7b/</link>
      <guid isPermaLink="false">2026-08-17-05-stripe-openrouter-imagen4-ai-infra</guid>
      <pubDate>Mon, 17 Aug 2026 12:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Developer Tools</category>
      <category>LLM</category>
      <description>Stripe finalizes a $7B+ deal for OpenRouter, the 400-model AI gateway; and Google retires three Imagen 4 model IDs today, breaking generate_images() with no grace period.</description>
      <content:encoded><![CDATA[
**Stripe buys the AI model routing layer that millions of developers depend on; Google's Imagen 4 `generate_images()` API shuts down today — two AI infra moves with immediate impact.**

## Stripe finalizes OpenRouter acquisition

Stripe has closed a deal to acquire OpenRouter — the AI model gateway that routes requests across 400+ models from 70+ providers — for more than $7 billion. OpenRouter's CEO described the company as "the Stripe for AI": one OpenAI-compatible endpoint, automatic provider failover, cost-based routing, and 8 million users. Service continues unchanged for now; no migration required, no pricing changes announced.

**Why it matters:** OpenRouter is load-bearing infrastructure for many production AI apps. The acquisition puts it inside Stripe's developer platform ecosystem — watch for pricing and API terms as the integration matures. If OpenRouter is in your critical path, now is a good time to review your fallback strategy.

## Google Imagen 4 API retires today

Three Imagen 4 model IDs shut down August 17: `imagen-4.0-generate-001`, `imagen-4.0-ultra-generate-001`, and `imagen-4.0-fast-generate-001`. The `generate_images()` method is removed entirely; migration requires switching to `gemini-3.1-flash-image` and calling `generate_content()` instead. Test your real prompts against the new model first — aspect ratios, safety filters, and output quality may differ.

**Why it matters:** Any application calling the Imagen 4 API without migration will return hard errors starting today.

**Sources:** [Stripe acquires OpenRouter — Bloomberg](https://www.bloomberg.com/news/articles/2026-08-16/stripe-nears-deal-to-buy-ai-firm-openrouter-for-over-7-billion) · [TechCrunch](https://techcrunch.com/2026/08/16/stripe-will-reportedly-acquire-ai-gateway-startup-openrouter-for-7b/) · [Imagen 4 shutdown details — kingy.ai](https://kingy.ai/ai-launch-tracker/google-will-shut-down-three-imagen-4-api-models-august-17/) · [Firebase migration guide](https://firebase.google.com/docs/ai-logic/imagen-models-migration)
]]></content:encoded>
    </item>


    <item>
      <title>One Field Cuts Your Claude API Bill 60-90%: Prompt Caching With cache_control</title>
      <link>https://platform.claude.com/docs/en/build-with-claude/prompt-caching</link>
      <guid isPermaLink="false">2026-08-17-03-claude-api-prompt-caching-cost</guid>
      <pubDate>Mon, 17 Aug 2026 04:06:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Add cache_control to any stable system prompt block and pay 10% of the normal input price on every repeated call — one field, no refactoring.</description>
      <content:encoded><![CDATA[
**Add `"cache_control": {"type": "ephemeral"}` to any stable content block and Claude reads it from cache on subsequent calls at roughly 10% of the normal input-token price.**

Any API workflow that sends the same system prompt, long instruction set, or reference document on every call is paying 100% of input cost each time. Prompt caching marks those stable blocks once; Claude caches them for an hour and charges cache-read rates on hits — about $0.30/M for Sonnet 5 instead of $3.00/M.

## How to add it

```python
import anthropic

client = anthropic.Anthropic()

system_prompt = """[Your stable system prompt — 1,024 tokens minimum to qualify]"""

response = client.messages.create(
    model="claude-sonnet-5-20260801",
    max_tokens=1024,
    system=[
        {
            "type": "text",
            "text": system_prompt,
            "cache_control": {"type": "ephemeral"}  # one field — this is all it takes
        }
    ],
    messages=[{"role": "user", "content": "What is the refund policy?"}],
)

# Inspect whether you got a cache hit
print(response.usage)
# cache_creation_input_tokens: N  (first call — writes the cache)
# cache_read_input_tokens: N      (subsequent calls — reads from cache)
```

The `cache_control` marker goes at the **end of the stable block** — Claude caches everything up to that point. Put volatile content (the user's question, the current date) after it. Never let timestamps or UUIDs drift into the cacheable prefix; that kills your hit rate.

## Where the savings are largest

| Workload | Stable block | Typical hit rate |
|---|---|---|
| Support bot | Full product docs + policy | 90-95% |
| Code review CI | Repo conventions + style guide | 85-90% |
| Eval suite | All test cases + rubric | ~100% |
| Multi-turn chat | System prompt + tool list | 60-80% |

## Cost math

A support bot sending a 10,000-token system prompt on 1,000 daily requests:

| Approach | Daily input cost (Sonnet 5) |
|---|---|
| No caching | 1,000 × 10K × $3/M = **$30.00** |
| Prompt caching | $3.00 (cache write) + 999 × 10K × $0.30/M = **$6.00** |

**80% saved, same responses, zero code refactoring beyond adding one field.**

Caching also works on tool definitions and large user-turn messages — add `cache_control` to any block 1,024+ tokens long that appears on multiple requests. The minimum is 1,024 tokens for standard models; 2,048 for extended context.

**Sources:** [Prompt caching — Claude Platform Docs](https://platform.claude.com/docs/en/build-with-claude/prompt-caching) · [Prompt caching cookbook — platform.claude.com](https://platform.claude.com/cookbook/prompt-caching) · [How to cut Claude API costs 90% with prompt caching — Iron Mind](https://iron-mind.ai/blog/prompt-caching-claude-production)
]]></content:encoded>
    </item>

    <item>
      <title>Agent Plugins 1.0: One Package Format That Works Across ChatGPT, Copilot, Cursor, VS Code, and Kiro</title>
      <link>https://github.blog/changelog/2026-08-12-agent-plugins-1-0-in-vs-code-copilot-cli-and-the-copilot-app/</link>
      <guid isPermaLink="false">2026-08-17-02-agent-plugins-1-0-open-standard</guid>
      <pubDate>Mon, 17 Aug 2026 04:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Developer Tools</category>
      <category>MCP</category>
      <description>Agent Plugins 1.0 is an open standard backed by GitHub, OpenAI, AWS, Google, and Microsoft: one plugin.json + skills/ + mcp.json installs into any compatible coding agent.</description>
      <content:encoded><![CDATA[
**A single `plugin.json` + `skills/` + `mcp.json` directory now installs into GitHub Copilot, VS Code, ChatGPT, Codex, Cursor, and Kiro — no separate manifest per client.**

[Agent Plugins 1.0](https://github.blog/changelog/2026-08-12-agent-plugins-1-0-in-vs-code-copilot-cli-and-the-copilot-app/) is an open standard published August 6 with AWS, Anysphere, Microsoft, OpenAI, Vercel, and Google as co-maintainers. It bundles agent skills and MCP servers into one installable format governed independently of any vendor. Build a deployment runbook skill with its MCP tool integration once; your whole team gets it on all their coding agents without you maintaining a separate manifest for each one.

**Why it matters:** Any team-specific skill or internal MCP integration you build now becomes portable — publish it to a skills registry or a shared repo directory and it works wherever your engineers happen to be.

**Sources:** [Agent Plugins 1.0 GA — GitHub Changelog](https://github.blog/changelog/2026-08-12-agent-plugins-1-0-in-vs-code-copilot-cli-and-the-copilot-app/) · [What the standard fixes — digitalapplied.com](https://www.digitalapplied.com/blog/agent-plugins-1-0-open-standard-portable-skills)
]]></content:encoded>
    </item>

    <item>
      <title>Your RAG Gives Answers. These Lines Make It Cite Its Work.</title>
      <link>https://www.youtube.com/watch?v=-wGzSnhQKPM</link>
      <guid isPermaLink="false">2026-08-17-01-rag-citations-source-attribution</guid>
      <pubDate>Mon, 17 Aug 2026 04:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>RAG</category>
      <category>Best Practices</category>
      <description>Tag retrieved chunks with IDs, ask Claude for inline citations, and return a sources list — your RAG answers become verifiable instead of just plausible. Walk-through included.</description>
      <content:encoded><![CDATA[
**Tag retrieved chunks with IDs, ask Claude to cite them inline, and return a sources list — your RAG answers become verifiable, not just plausible.**

**What you will be able to do after this:**
- Return `[1]` / `[2]` inline citations in any RAG response alongside a `sources` list
- Verify that cited documents actually support each claim using a structured `quote` field
- Build the citation layer in under 50 lines on top of your existing retriever

A RAG system that returns "Refunds take 5-7 days" is useful. One that returns "Refunds take 5-7 days [1]" with `[1]: Refund Policy, section 3.2` is trustworthy. That is the difference between a demo and a production tool.

**Resource:** [RAG with Verified Citations — Trelis Research (YouTube, 34 min)](https://www.youtube.com/watch?v=-wGzSnhQKPM)

## The pattern

Your retriever returns chunks. Tag each one with a sequential ID and include them in the user turn:

```python
import anthropic

client = anthropic.Anthropic()

def rag_with_citations(question: str, chunks: list[dict]) -> dict:
    # chunks = [{"id": 1, "text": "...", "source": "Refund Policy 3.2"}, ...]
    context = "

".join(f"[{c['id']}] {c['text']}" for c in chunks)

    response = client.messages.create(
        model="claude-sonnet-5-20260801",
        max_tokens=1024,
        system="""You are a helpful assistant. Answer using ONLY the provided context.
Cite every factual claim with a bracketed ID like [1] inline.
If the answer is not in the context, say so.""",
        messages=[{
            "role": "user",
            "content": f"Context:
{context}

Question: {question}"
        }]
    )
    return {
        "answer": response.content[0].text,
        "sources": [{"id": c["id"], "source": c["source"]} for c in chunks]
    }
```

## Structured output version (more reliable for parsing)

```python
import json

response = client.messages.create(
    model="claude-sonnet-5-20260801",
    max_tokens=1024,
    tools=[{
        "name": "answer_with_citations",
        "description": "Return an answer with inline citations",
        "input_schema": {
            "type": "object",
            "properties": {
                "answer": {
                    "type": "string",
                    "description": "Answer text with [N] inline citations"
                },
                "citations": {
                    "type": "array",
                    "items": {
                        "type": "object",
                        "properties": {
                            "id": {"type": "integer"},
                            "quote": {
                                "type": "string",
                                "description": "Exact text from document [N] that supports the claim"
                            },
                            "source": {"type": "string"}
                        },
                        "required": ["id", "quote", "source"]
                    }
                }
            },
            "required": ["answer", "citations"]
        }
    }],
    tool_choice={"type": "tool", "name": "answer_with_citations"},
    system="Answer using ONLY the provided context. Cite every factual claim inline.",
    messages=[{"role": "user", "content": f"Context:
{context}

Question: {question}"}]
)

result = json.loads(response.content[0].input)
# result["answer"]    = "Refunds take 5-7 days [1]."
# result["citations"] = [{"id": 1, "quote": "Refunds are processed in 5-7 business days", "source": "Refund Policy 3.2"}]
```

The `quote` field in each citation forces Claude to locate the exact supporting text — surfacing any hallucination where no supporting passage exists. If Claude cannot find a quote, it cannot fabricate a citation.

**Sources:** [RAG with Verified Citations — Trelis Research (YouTube)](https://www.youtube.com/watch?v=-wGzSnhQKPM) · [Citation-aware RAG — Tensorlake](https://www.tensorlake.ai/blog/rag-citations) · [Attributing sources in RAG — apxml.com](https://apxml.com/courses/getting-started-rag/chapter-4-rag-generation-augmentation/attributing-sources)
]]></content:encoded>
      <media:content url="https://img.youtube.com/vi/-wGzSnhQKPM/maxresdefault.jpg" medium="image" />
      <media:thumbnail url="https://img.youtube.com/vi/-wGzSnhQKPM/maxresdefault.jpg" />
    </item>

    <item>
      <title>Stop Letting Claude Skip the Tool: Four Modes of tool_choice for Reliable Agent Pipelines</title>
      <link>https://platform.claude.com/docs/en/agents-and-tools/tool-use/tool-reference</link>
      <guid isPermaLink="false">2026-08-16-08-tool-choice-force-specific-tool</guid>
      <pubDate>Sun, 16 Aug 2026 20:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Developer Tools</category>
      <category>Best Practices</category>
      <description>tool_choice: {type: "any"} forces Claude to call a tool; {type: "tool", name: "X"} forces a specific one — eliminating stochastic skips that break agent pipelines.</description>
      <content:encoded><![CDATA[
**`tool_choice: {type: "any"}` forces Claude to call a tool; `{type: "tool", name: "X"}` forces a specific one — eliminating the stochastic skips that break agent pipelines.**

By default (`tool_choice: {type: "auto"}`), Claude decides on its own whether to call a tool or answer directly. That's fine for chat — but in an agent pipeline it will occasionally surprise you: a research agent skips `save_findings` because the answer fits in a paragraph; a pricing step answers from training data instead of calling `get_current_price`. The fix is one field.

## The four modes

| Mode | What Claude does | Use when |
|------|-----------------|----------|
| `{type: "auto"}` | Decides on its own | Default; fine for open-ended chat |
| `{type: "any"}` | Must call *any* tool | Force live data retrieval; prevent stale-training answers |
| `{type: "tool", name: "X"}` | Must call tool X | Structured output, persisting state, required audit logging |
| `{type: "none"}` | No tool calls | Synthesis turn after data is gathered |

## Walk-through

```python
import anthropic

client = anthropic.Anthropic()

tools = [
    {
        "name": "save_findings",
        "description": "Persist research findings. Always call this before finishing.",
        "input_schema": {
            "type": "object",
            "properties": {
                "summary": {"type": "string"},
                "confidence": {"type": "number"},
            },
            "required": ["summary", "confidence"],
        },
    }
]

# Claude MUST call save_findings — no skipping, no text-only responses
response = client.messages.create(
    model="claude-sonnet-5-20251001",
    max_tokens=1024,
    tools=tools,
    tool_choice={"type": "tool", "name": "save_findings"},
    messages=[
        {"role": "user", "content": "Summarize this week's AI security news."}
    ],
)

# stop_reason == "tool_use" — guaranteed
tool_call = next(b for b in response.content if b.type == "tool_use")
print(tool_call.input)  # {"summary": "...", "confidence": 0.91}
```

**Pattern for multi-step loops:** use `{type: "any"}` on collection turns (force at least one lookup), then flip to `{type: "none"}` for the synthesis turn (synthesize what you have; no new fetches). Your agent loop becomes a predictable state machine instead of a probabilistic guess.

**Parallel guard:** add `"disable_parallel_tool_use": true` alongside `tool_choice` when your tools have ordering dependencies — forces one call at a time, deterministic sequence.

**Sources:** [Parallel tool use — Claude Platform Docs](https://platform.claude.com/docs/en/agents-and-tools/tool-use/parallel-tool-use) · [Tool choice — Claude Cookbook](https://platform.claude.com/cookbook/tool-use-tool-choice) · [Tool reference — Claude Platform Docs](https://platform.claude.com/docs/en/agents-and-tools/tool-use/tool-reference)
]]></content:encoded>
    </item>

    <item>
      <title>Run Any Open-Weight Model as a Private Local API: LM Studio in 3 Clicks and 2 Lines of Python</title>
      <link>https://lmstudio.ai/docs/developer</link>
      <guid isPermaLink="false">2026-08-16-07-lm-studio-local-llm-api-server</guid>
      <pubDate>Sun, 16 Aug 2026 20:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Self-Hosting</category>
      <category>Developer Tools</category>
      <description>LM Studio (free, Mac/Windows/Linux) turns any GGUF or MLX model into a private OpenAI-compatible server at localhost:1234 — no CLI, no cloud, one toggle in the app.</description>
      <content:encoded><![CDATA[
**LM Studio (free, Mac/Windows/Linux) turns any GGUF or MLX model into a private OpenAI-compatible server at localhost:1234 — no CLI, no cloud, one toggle in the app.**

**What you'll be able to do after this:** prototype AI features against local open-weight models without a cloud API key, swap any open-weight model into your existing OpenAI SDK code by changing one URL, and keep every token on your machine.

Ollama and vLLM are great — but they require a terminal. LM Studio gives you the same OpenAI-compatible local server with a GUI for browsing, downloading, and loading models. Three things that matter for engineers:

- **HuggingFace browser built in.** The Discover tab searches HuggingFace directly and filters GGUF files by compatibility with your hardware. Click Download — model lands in `~/.cache/lm-studio/models/`.
- **OpenAI-compatible server in one toggle.** Load a model → open the Developer tab → click **Start Server**. Binds to `http://localhost:1234/v1`. Serves `/v1/chat/completions`, `/v1/embeddings`, `/v1/models`, structured output, and tool calling.
- **Tool use + structured JSON output.** Any model that supports it gets JSON Schema-constrained outputs and tool calling — test your agent's tool-use logic locally before hitting the production API.

## Walk-through

1. [Download LM Studio](https://lmstudio.ai) — free for Mac, Windows, Linux.
2. **Discover tab** → search `qwen3-8b` → filter "Recommended for my hardware" → **Download**.
3. Click the downloaded model → **Load**. Then open the **Developer tab** → toggle **Start Server**.
4. Point your OpenAI SDK at the local endpoint — two lines change:

```python
from openai import OpenAI

client = OpenAI(
    base_url="http://localhost:1234/v1",
    api_key="lm-studio",  # any non-empty string; LM Studio ignores it
)

response = client.chat.completions.create(
    model="qwen3-8b",   # exact name shown in LM Studio's model list
    messages=[{"role": "user", "content": "Explain KV cache in one paragraph."}],
)
print(response.choices[0].message.content)
```

No tokens leave your machine. If you get a model-not-found error, call `GET /v1/models` to see the exact `id` string LM Studio uses.

**For teams:** LM Studio 0.4.0+ supports token-based auth (Server Settings → Require Authentication) and can bind to your local network — useful for shared dev access without standing up a full vLLM instance.

**Sources:** [Getting started with LM Studio — YouTube](https://www.youtube.com/watch?v=fTUGVA2EKq0) · [LM Studio developer docs](https://lmstudio.ai/docs/developer) · [OpenAI-compatible endpoints — lmstudio.ai](https://lmstudio.ai/docs/developer/openai-compat)
]]></content:encoded>
      <media:content url="https://img.youtube.com/vi/fTUGVA2EKq0/maxresdefault.jpg" medium="image"/>
      <media:thumbnail url="https://img.youtube.com/vi/fTUGVA2EKq0/maxresdefault.jpg"/>
    </item>

<item>
      <title>Sync Your Design System First: The Claude Design → Claude Code Handoff That Actually Lands</title>
      <link>https://support.claude.com/en/articles/14604416-get-started-with-claude-design</link>
      <guid isPermaLink="false">2026-08-16-09-claude-design-claude-code-handoff</guid>
      <pubDate>Sun, 16 Aug 2026 13:29:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Design</category>
      <category>Claude Design</category>
      <description>Run /design-sync before generating anything in Claude Design, then export a handoff bundle straight to Claude Code — plus the usage-pool and research-preview caveats worth planning around.</description>
      <content:encoded><![CDATA[
**Run `/design-sync` before you generate anything — it pulls your real design system in, so what Claude Code receives is built from your components instead of generic AI styling.**

[Claude Design](https://claude.com/product/design) shipped from Anthropic Labs on April 17, 2026 as a research preview for Pro, Max, Team, and Enterprise plans. It's a chat pane on the left and a canvas on the right, and the output isn't a picture — it's live HTML you can click through. The interesting part, if you already ship with Claude Code, is the last mile.

**The workflow that holds up:**

1. **Sync first, design second.** `/design-sync` imports a design system from a GitHub repo, design files, or a codebase. Do it at project creation. Skip it and you get plausible-looking work in someone else's design language — and you pay for that in review, not in the design tool.
2. **Iterate on the canvas, not in the prompt.** Inline comments, direct text editing, and the adjustment controls move faster than regenerating from a rewritten prompt.
3. **Hand off with the bundle.** When it's ready, use *send to local coding agent* or *Send to Claude Code Web*. Claude packages a handoff bundle you pass to Claude Code with a single instruction. That bundle — not a screenshot, not a description — is what makes generated UI land close to your codebase.

**Two things to know before you lean on it:**

- **It shares your usage budget.** Claude Design draws from the same pool as chat, Claude Code, and Cowork; there's no separate allowance. A long design session shortens your coding session that day.
- **It's a research preview and it shows.** Comment persistence is intermittent, simultaneous multi-person editing is unreliable, and design-system import is only as good as the source you point it at. Enterprise orgs get it **off by default** — an admin enables it in Organization settings.

Beyond the code handoff, exports include ZIP, PDF, PPTX, standalone HTML, plus Canva, Miro, and Vercel.

**Sources:** [Get started with Claude Design](https://support.claude.com/en/articles/14604416-get-started-with-claude-design) · [Introducing Claude Design](https://www.anthropic.com/news/claude-design-anthropic-labs) · [Claude Design product page](https://claude.com/product/design)
]]></content:encoded>
      <media:content url="https://www.anthropic.com/api/opengraph-illustration?name=Hand%20Quill&amp;backgroundColor=cactus" medium="image" />
      <media:thumbnail url="https://www.anthropic.com/api/opengraph-illustration?name=Hand%20Quill&amp;backgroundColor=cactus" />
    </item>

    <item>
      <title>Start Receiving Tool Arguments Immediately: Enable eager_input_streaming on Large-Output Tools</title>
      <link>https://platform.claude.com/docs/en/agents-and-tools/tool-use/fine-grained-tool-streaming</link>
      <guid isPermaLink="false">2026-08-16-06-eager-input-streaming-tool-latency</guid>
      <pubDate>Sun, 16 Aug 2026 12:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Agents</category>
      <category>Best Practices</category>
      <description>Add "eager_input_streaming": true to any tool definition and Claude streams partial JSON arguments instantly — first-byte latency drops from ~15s to ~3s for large parameters like generated documents.</description>
      <content:encoded><![CDATA[
**Add `"eager_input_streaming": true` to a tool definition and Claude streams partial JSON arguments to your client instantly — first-byte latency drops from ~15s to ~3s for large parameters.**

By default, the Claude API buffers a tool's entire `input` JSON until it is valid before delivering it. For a `write_file` tool producing a 2,000-word document, that means 15+ seconds of silence before your UI sees anything. Fine-grained tool streaming, now GA on all models and platforms (no beta header needed), removes that buffer per tool.

## One field, on the tool definition

```python
import anthropic

client = anthropic.Anthropic()

tools = [{
    "name": "write_file",
    "description": "Write content to a file",
    "input_schema": {
        "type": "object",
        "properties": {
            "path": {"type": "string"},
            "content": {"type": "string", "description": "File content — can be thousands of words"}
        },
        "required": ["path", "content"]
    },
    "eager_input_streaming": True   # <- the only change
}]

with client.messages.stream(
    model="claude-sonnet-5-20260130",
    max_tokens=4096,
    tools=tools,
    messages=[{"role": "user", "content": "Write a thorough README for a Python CLI tool."}]
) as stream:
    for event in stream:
        if hasattr(event, "delta") and event.delta.type == "input_json_delta":
            # Partial JSON string — write to UI as it arrives
            print(event.delta.partial_json, end="", flush=True)
```

## Three things to know before shipping

1. **User-defined tools only.** `eager_input_streaming` cannot be set on built-in tools (`web_search`, `code_execution`, etc.).

2. **Partial JSON is not valid JSON.** The SDK exposes a `snapshot` dict (partially parsed Python dict) on each delta — safe to read for real-time display. Wait for `content_block_stop` before passing the completed arguments to your actual tool executor.

3. **Different from Agent SDK streaming.** `include_partial_messages=True` in the Claude Agent SDK (covered Aug 1) signals when a tool call starts and stops. `eager_input_streaming` is lower-level: it streams the actual argument content *while Claude is generating it*, before the tool call completes. Use both together for the fastest possible UI.

**Sources:** [Fine-grained tool streaming — Claude Platform Docs](https://platform.claude.com/docs/en/agents-and-tools/tool-use/fine-grained-tool-streaming) · [Streaming messages — Anthropic Docs](https://docs.anthropic.com/en/docs/build-with-claude/streaming)
]]></content:encoded>
    </item>

    <item>
      <title>Anthropic's August Risk Report: Safety Benchmarks Saturate as Misalignment Risk Rises to 'Low'</title>
      <link>https://www.anthropic.com/aug-2026-risk-report</link>
      <guid isPermaLink="false">2026-08-16-05-anthropic-aug2026-risk-report-saturation</guid>
      <pubDate>Sun, 16 Aug 2026 12:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>LLM</category>
      <category>Best Practices</category>
      <description>Anthropic's August 2026 Risk Report (released Aug 14) upgrades misalignment risk from 'very low' to 'low' as its internal capability threshold benchmark saturates — can no longer register incremental gains.</description>
      <content:encoded><![CDATA[
**Anthropic's August 2026 Risk Report bumped misalignment risk to "low" and revealed a deeper structural problem: its internal safety benchmark for the most dangerous capability threshold has saturated and can no longer register capability gains.**

Released August 14, the report covers February–July 2026. The risk upgrade was driven by increased uncertainty following recent incident disclosures — Anthropic's underlying assessment still supports the lower "very low" rating, but the label was raised to reflect that confidence. An internal "Model 2" (stronger than Mythos 5) exists but is being withheld pending further safety review.

The more technically significant finding is benchmark saturation. The evaluations designed to detect when models cross the most dangerous capability threshold have stopped registering incremental gains at precisely the moment early signs of acceleration are appearing. The instrument built to sound the alarm can no longer differentiate between capability levels.

**Why it matters:** Benchmark saturation has a direct parallel for engineers building production AI systems — any single evaluation metric will eventually plateau while the underlying behavior keeps shifting. The lesson: run diverse, behavioral test suites alongside capability benchmarks and invest in evals that measure *what the model does*, not only *what score it achieves*. When your benchmark looks flat, that is the moment to add a new one.

**Sources:** [Anthropic August 2026 Risk Report](https://www.anthropic.com/aug-2026-risk-report) · [Benchmark Saturation — TechTimes](https://www.techtimes.com/articles/324573/20260815/anthropic-upgrades-misalignment-risk-key-safety-benchmarks-saturate.htm) · [Anthropic Raises Misalignment Risk — Unite.AI](https://www.unite.ai/anthropic-raises-misalignment-risk-to-low-and-shelves-internal-model-2/)
]]></content:encoded>
    </item>

    <item>
      <title>Orchestrate a Team of AI Agents in 25 Lines: The LangGraph Supervisor Pattern</title>
      <link>https://www.youtube.com/watch?v=BM39OouLNsM</link>
      <guid isPermaLink="false">2026-08-16-04-langgraph-supervisor-multi-agent-team</guid>
      <pubDate>Sun, 16 Aug 2026 12:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Agents</category>
      <category>MCP</category>
      <description>Use create_supervisor() from langgraph-supervisor and one LLM orchestrator routes tasks to specialist agents — no custom state machines or routing logic, and memory is one extra argument.</description>
      <content:encoded><![CDATA[
**`pip install langgraph-supervisor` and `create_supervisor()` gives you a routing orchestrator that delegates to specialist agents — 25 lines of Python, no custom routing logic.**

**What you'll be able to do after this:**

- Build a multi-agent system where a supervisor LLM reads each step and routes to the right specialist
- Use `create_supervisor()` to skip hand-coded state machines and transition logic
- Add memory and human-in-the-loop checkpointing to the same graph with two extra lines

**The pattern:** The supervisor is an LLM that reads the current conversation state and calls one of two built-in tools: `transfer_to_<agent_name>()` (delegate to a specialist) or returns a final response. Each specialist runs its own ReAct loop with its own tools. When the specialist finishes, control returns to the supervisor.

## Setup

```bash
pip install langgraph-supervisor langchain-anthropic
```

```python
from langgraph_supervisor import create_supervisor
from langgraph.prebuilt import create_react_agent
from langchain_anthropic import ChatAnthropic

model = ChatAnthropic(model="claude-sonnet-5-20260130")

# Specialist 1: web researcher
researcher = create_react_agent(
    model,
    tools=[web_search_tool],
    name="researcher",
    prompt="You search the web and return verified facts. Be concise."
)

# Specialist 2: Python analyst
analyst = create_react_agent(
    model,
    tools=[python_repl_tool],
    name="analyst",
    prompt="You run Python code and return computed results with the code used."
)

# Supervisor routes between them
app = create_supervisor(
    model,
    agents=[researcher, analyst],
    prompt=(
        "You manage researcher and analyst. "
        "Send information-gathering tasks to researcher, "
        "and computation tasks to analyst. "
        "When both are done, synthesize a final answer."
    )
).compile()

# Run it
result = app.invoke({
    "messages": [{"role": "user", "content": "What is the population of Tokyo? Calculate how many people that is per square kilometer (area: 2,194 km2)."}]
})
print(result["messages"][-1].content)
```

## Two things that make this practical

**State is shared.** The researcher's answer goes into the shared `messages` state, so the analyst can read it in the next step — no custom handoff code.

**Memory is one argument.** Pass `checkpointer=MemorySaver()` to `.compile()` and the graph persists state across invocations for the same `thread_id`. Multi-turn conversations and human-in-the-loop interrupts use the same API.

The video below walks through a complete version with MCP tool calls, guardrails, observability, and a Streamlit UI — worth watching once you have the basic pattern running.

**Sources:** [Build a Multi-Agent System with LangGraph, MCP, Supervisor, Guardrails — YouTube](https://www.youtube.com/watch?v=BM39OouLNsM) · [LangGraph Agent Supervisor tutorial — LangChain GitHub](https://github.com/langchain-ai/langgraph/blob/main/docs/docs/tutorials/multi_agent/agent_supervisor.md) · [langgraph-supervisor — PyPI](https://pypi.org/project/langgraph-supervisor/)
]]></content:encoded>
      <media:content url="https://img.youtube.com/vi/BM39OouLNsM/maxresdefault.jpg" medium="image" />
      <media:thumbnail url="https://img.youtube.com/vi/BM39OouLNsM/maxresdefault.jpg" />
    </item>

    <item>
      <title>Handle Claude Refusals in One Line: The API's fallbacks: 'default' Server-Side Fallback</title>
      <link>https://platform.claude.com/docs/en/build-with-claude/refusals-and-fallback</link>
      <guid isPermaLink="false">2026-08-16-03-claude-api-fallbacks-default-refusal-handling</guid>
      <pubDate>Sun, 16 Aug 2026 04:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Agents</category>
      <category>Best Practices</category>
      <description>Add fallbacks: "default" plus one beta header to your Claude API call and safety-classifier refusals automatically retry on Anthropic's recommended safe model — no try/catch loop required.</description>
      <content:encoded><![CDATA[
**Add `fallbacks: "default"` and one beta header to your `/v1/messages` call — when Claude's safety classifier declines, the API retries on Anthropic's recommended safe alternative in the same round trip.**

When a Claude model declines a request for safety reasons, your code normally receives a `stop_reason: "end_turn"` response with no useful content. Detecting that, deciding what to do, and retrying is extra logic in every agent that calls the API. The server-side fallback beta removes that loop for safety-classifier declines.

## The change

Add two things to any `/v1/messages` call:

```python
response = client.messages.create(
    model="claude-fable-5-20260201",
    max_tokens=1024,
    messages=[{"role": "user", "content": prompt}],
    fallbacks="default",                                         # <- add this
    extra_headers={
        "anthropic-beta": "server-side-fallback-2026-07-01"     # <- and this
    }
)

# Check which model actually served the response:
print(response.model)   # may differ from "claude-fable-5-20260201"
```

When the primary model's classifier declines, the API immediately retries on Anthropic's recommended alternative for that refusal category. You get one response — already answered — in the same round trip, with no error surfaced to your caller.

## Three things to know

1. **Only safety-classifier declines trigger the fallback.** Overload errors (429/529), token-limit hits, and network errors are not covered — handle those with your existing retry logic or `fallbackModel` in Claude Code config.

2. **Direct API only.** The `fallbacks` parameter is not available on Amazon Bedrock, Google Cloud Vertex, or Microsoft Foundry — only the Anthropic API.

3. **Track per-model usage.** The response's `model` field shows which model served it. Check `usage.iterations` for per-model token accounting when billing back to teams or cost-tracking by model.

You can also pass an explicit list of up to three fallback models instead of `"default"`:

```python
fallbacks=["claude-opus-5-20260201", "claude-sonnet-5-20260130"]
```

The `"default"` mode is the easier path for most teams — Anthropic updates the recommended fallback chain as models change, so your code stays current without maintenance.

**Sources:** [Refusals and Fallback — Claude Platform Docs](https://platform.claude.com/docs/en/build-with-claude/refusals-and-fallback) · [Fallback Credit — Claude Platform Docs](https://platform.claude.com/docs/en/build-with-claude/fallback-credit) · [Classifier Fallback and Billing — Claude Cookbook](https://platform.claude.com/cookbook/fable-5-fallback-billing-guide)
]]></content:encoded>
    </item>

    <item>
      <title>GitHub Copilot Upgrades Its Coding Model: MAI-Code-1.1-Flash Cuts Cost 73% and Adds Vision</title>
      <link>https://github.blog/changelog/2026-08-11-mai-code-1-1-flash-available-in-github-copilot/</link>
      <guid isPermaLink="false">2026-08-16-02-mai-code-11-flash-github-copilot-cheaper-vision</guid>
      <pubDate>Sun, 16 Aug 2026 04:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Developer Tools</category>
      <description>MAI-Code-1.1-Flash is now in GitHub Copilot: 73% cheaper than 1-Flash, 22% better on CLI benchmarks, 25% faster streaming, and native vision support. MAI-Code-1-Flash retires September 10.</description>
      <content:encoded><![CDATA[
**Microsoft's MAI-Code-1.1-Flash is now in GitHub Copilot: 73% cheaper than its predecessor, 22% better on Terminal-Bench CLI tasks, and it can now read images — MAI-Code-1-Flash retires September 10.**

Microsoft [released MAI-Code-1.1-Flash on August 11](https://github.blog/changelog/2026-08-11-mai-code-1-1-flash-available-in-github-copilot/) as the mandatory successor to MAI-Code-1-Flash across all Copilot surfaces. The upgrade is significant on every dimension that matters for repetitive agentic workflows:

- **73% lower list price** than MAI-Code-1-Flash
- **25% fewer tokens** for the same output (lower effective cost on top of the rate cut)
- **25% faster streaming**
- **22% better on Terminal-Bench 2.1** for Copilot CLI tasks; 15% better on .NET completions
- **Native vision** — the model accepts images, enabling screenshot-to-component, diagram-to-spec, and error-screenshot-to-fix directly in Copilot Chat

Paid individual, business, and enterprise customers can select MAI-Code-1.1-Flash now across Copilot CLI, Copilot Chat on GitHub.com, VS Code, Visual Studio, JetBrains, Eclipse, Xcode, and GitHub Mobile. MAI-Code-1-Flash retires on **September 10, 2026**.

**Why it matters:** The combined price and token-efficiency gains make 1.1-Flash the clear default for high-volume, repetitive Copilot CLI workflows — the kind where a coding agent calls the model hundreds of times per task. Vision support closes a gap that required pasting screenshot text into prompts; now you can pass the image directly.

**Sources:** [MAI-Code-1.1-Flash available in GitHub Copilot — GitHub Changelog](https://github.blog/changelog/2026-08-11-mai-code-1-1-flash-available-in-github-copilot/) · [Microsoft releases MAI-Code-1.1-Flash — Neowin](https://www.neowin.net/news/microsoft-releases-mai-code-11-flash-coding-model-to-better-compete-with-chinese-models/)
]]></content:encoded>
    </item>

    <item>
      <title>Fine-Tune Any LLM Without Writing Code: HuggingFace AutoTrain Does the Hard Part</title>
      <link>https://parlance-labs.com/education/fine_tuning/abhishek.html</link>
      <guid isPermaLink="false">2026-08-16-01-autotrain-no-code-llm-finetuning</guid>
      <pubDate>Sun, 16 Aug 2026 04:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Fine-Tuning</category>
      <category>HuggingFace</category>
      <description>Upload a JSONL file, pick a model, click train — HuggingFace AutoTrain handles LoRA, quantization, and chat-template formatting without any code. Walkthrough included.</description>
      <content:encoded><![CDATA[
**Upload a JSONL file, pick a model, click train — AutoTrain handles LoRA, quantization, and chat-template formatting for you. No Python required to run your first fine-tune.**

**What you'll be able to do after this:**
- Fine-tune any HuggingFace model (Llama 3.1, Qwen 3, Mistral, Gemma) using only a CSV or JSONL file — no training code
- Choose SFT, DPO, or ORPO from a dropdown and let AutoTrain configure LoRA hyperparameters
- Run a complete fine-tune on managed HuggingFace compute for under $1 for a small model

## Why AutoTrain

Every fine-tuning tutorial on this blog so far has used TRL, Unsloth, or Axolotl — all great tools, but all require writing Python. AutoTrain is HuggingFace's no-code path: it wraps those same training libraries in a UI and CLI so you can go from dataset to adapter without a training script. Abhishek Thakur (AutoTrain's creator) built it specifically for people who know what they want to train but don't want to spend hours on training boilerplate.

## Walk-through (UI path)

**1. Prepare your dataset.**

For SFT (most common), create a JSONL file with a single `text` column containing your formatted prompt+response pairs:

```jsonl
{"text": "<|im_start|>user\nWhat is LoRA?\n<|im_end|>\n<|im_start|>assistant\nLoRA (Low-Rank Adaptation)...<|im_end|>"}
```

For DPO, use `prompt`, `chosen`, `rejected` columns instead.

**2. Create a project** at [huggingface.co/autotrain](https://huggingface.co/autotrain) → "Create new project." AutoTrain spins up a Space for you.

**3. Upload your dataset** (CSV or JSONL), map columns, pick a base model from the Hub.

**4. Choose task and configuration.** Select "LLM SFT" and accept the LoRA defaults for your first run (rank=16, alpha=32 are sensible starting values).

**5. Click Train.** AutoTrain provisions a GPU, runs the fine-tune, and saves the LoRA adapter to your HuggingFace Hub as a private model.

## CLI path (scriptable, same results)

```bash
pip install autotrain-advanced

autotrain llm \
  --train \
  --model "meta-llama/Llama-3.1-8B" \
  --data-path ./data.csv \
  --text-column text \
  --lora-r 16 \
  --lora-alpha 32 \
  --batch-size 2 \
  --num-train-epochs 3 \
  --push-to-hub \
  --username your-hf-username \
  --token $HF_TOKEN
```

The CLI generates identical results to the UI and integrates naturally into CI pipelines or scheduled retraining jobs.

**Sources:** [Train (almost) any LLM using AutoTrain — Parlance Labs](https://parlance-labs.com/education/fine_tuning/abhishek.html) · [LLM Finetuning with AutoTrain Advanced — HuggingFace Docs](https://huggingface.co/docs/autotrain/tasks/llm_finetuning) · [Low-Code LLM Alignment — HuggingFace Blog](https://huggingface.co/blog/burtenshaw/ui-finetune-llm-autotrain) · [autotrain-advanced — GitHub](https://github.com/huggingface/autotrain-advanced)
]]></content:encoded>
    </item>

    <item>
      <title>Your Internal Tools, Claude's Reach: Private-Network MCP via Anthropic Tunnels</title>
      <link>https://platform.claude.com/docs/en/agents-and-tools/mcp-tunnels/overview</link>
      <guid isPermaLink="false">2026-08-15-06-mcp-tunnels-private-network-claude-agents</guid>
      <pubDate>Sat, 15 Aug 2026 20:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>MCP</category>
      <category>Agents</category>
      <category>Workflow</category>
      <description>MCP tunnels (research preview) let Claude Agents call tools inside your private network over an outbound-only connection — no open firewall ports, no public endpoints, no VPN required.</description>
      <content:encoded><![CDATA[
**MCP tunnels (research preview) give Claude Agents access to MCP servers inside your private network via an outbound-only connection — no public endpoints, no inbound firewall rules, no VPN required.**

If you're building Claude agents that need access to internal tooling — a private GitHub Enterprise instance, an internal Jira, a database behind a VPN, a proprietary REST API — you've had to either expose it publicly or build a custom proxy. MCP tunnels close that gap.

## How it works

Two components run inside your network:

- **cloudflared** (Cloudflare's open-source connector) opens a single outbound connection to the tunnel edge — no inbound ports, no IP allowlisting needed
- **Anthropic's routing proxy** terminates inner TLS and routes each request to the correct upstream MCP server by hostname

Anthropic's infrastructure only reaches your network through the tunnel your team controls. Three independent TLS layers protect every request; Cloudflare cannot read payload content because your proxy holds the inner certificate.

## Getting started (requires access request)

**Step 1 — Request access:** [claude.com/form/claude-managed-agents](https://claude.com/form/claude-managed-agents)

**Step 2 — Create a tunnel** in the Claude Console (Settings → MCP Tunnels → New Tunnel).

**Step 3 — Deploy the stack** on a VM (Docker Compose) or Kubernetes (Helm):

```bash
# Docker Compose quickstart — fill in TUNNEL_TOKEN and cert from Console
docker compose up -d
```

Each upstream MCP server you expose gets a subdomain under your tunnel domain (e.g. `docs.your-tunnel.anthropic-tunnels.com`).

**Step 4 — Attach to a session:** in the Console (Managed Agents → Sessions → + MCP Server → select your tunnel), or call it from the Messages API:

```python
response = client.beta.messages.create(
    model="claude-opus-5",
    max_tokens=1000,
    messages=[{"role": "user", "content": "Pull the latest sprint tickets."}],
    mcp_servers=[{
        "type": "url",
        "url": "https://jira.YOUR_TUNNEL_DOMAIN/mcp",
        "name": "jira",
    }],
    tools=[{"type": "mcp_toolset", "mcp_server_name": "jira"}],
    betas=["mcp-client-2025-11-20"],
)
```

The tunnel URL is just your private MCP server's assigned hostname — no changes to the server itself.

## What to watch for

- **Access is gated** — the tunnel won't work until your request is approved; plan for lead time
- **Research preview terms** — no uptime SLA; Cloudflare is the transport layer
- **MCP server auth is separate** — the tunnel carries traffic but doesn't authenticate to your server; add `authorization_token` in the `mcp_servers` block for that

**Sources:** [MCP tunnels overview — platform.claude.com](https://platform.claude.com/docs/en/agents-and-tools/mcp-tunnels/overview), [Request access form](https://claude.com/form/claude-managed-agents), [The New Stack coverage](https://thenewstack.io/anthropic-mcp-tunnels-sandboxes/)
]]></content:encoded>
    </item>

    <item>
      <title>Serve Open-Source Models Faster Than vLLM: SGLang's RadixAttention and Strict JSON Output</title>
      <link>https://docs.sglang.io/docs/get-started/quickstart</link>
      <guid isPermaLink="false">2026-08-15-07-sglang-fast-llm-serving-radix-attention</guid>
      <pubDate>Sat, 15 Aug 2026 20:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Self-Hosting</category>
      <category>LLM</category>
      <description>SGLang serves any HuggingFace model with an OpenAI-compatible API, reuses KV cache across requests sharing a prefix (3-5x throughput boost for agent loops), and enforces JSON Schema outputs natively.</description>
      <content:encoded><![CDATA[
**SGLang is an OpenAI-compatible LLM inference server that shares KV cache across requests with the same prefix (system prompts, tool lists) — giving 3–5x throughput on agent workloads — and enforces JSON Schema output at the token level.**

**What you'll be able to do after this:**
- Serve any HuggingFace model locally with a drop-in OpenAI-compatible API in under 5 minutes
- Get 3–5× higher throughput on agent workloads where multiple requests share a long system prompt or tool definition list
- Enforce strict JSON Schema output so your pipeline receives valid structured data every time — no retries, no post-processing

## Why SGLang instead of vLLM or Ollama?

Each tool has a lane:

| Tool | Best for |
|------|----------|
| **Ollama** | Developer convenience; one command, great for local chat |
| **vLLM** | Production serving; strong multi-LoRA adapter support |
| **SGLang** | Agent-first workloads; RadixAttention reuses KV cache across requests sharing a prefix |

The last row matters for agent loops: a 2,000-token system prompt plus tool definitions sent with every tool call is computed once in SGLang and amortized across every request in the batch. In benchmarks, SGLang runs 29% faster than vLLM on H100 GPUs and up to 6× faster in RAG scenarios with long shared context.

> Note: HuggingFace retired TGI (their older inference toolkit) in 2026 and now recommends SGLang alongside vLLM.

## Install and launch a model (5 minutes)

```bash
# GPU machine with CUDA 12.1 + Python 3.10+
pip install "sglang[all]" \
  --find-links https://flashinfer.ai/whl/cu121/torch2.3/flashinfer/

# Start the server
python -m sglang.launch_server \
  --model-path meta-llama/Llama-3.1-8B-Instruct \
  --port 30000 \
  --tp 1 \
  --mem-fraction-static 0.88
```

Or via Docker (no CUDA install needed):

```bash
docker run --gpus all \
  -v ~/.cache/huggingface:/root/.cache/huggingface \
  -p 30000:30000 \
  lmsysorg/sglang:latest \
  python -m sglang.launch_server \
    --model-path meta-llama/Llama-3.1-8B-Instruct \
    --port 30000 --tp 1
```

Smoke-test it:

```bash
curl http://localhost:30000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model": "meta-llama/Llama-3.1-8B-Instruct",
       "messages": [{"role": "user", "content": "Hello!"}],
       "max_tokens": 32}'
```

Point any existing OpenAI client at `http://localhost:30000/v1` — it's a drop-in replacement.

## Enforce strict JSON Schema output

This is where SGLang earns its place in an agent pipeline. Pass `response_format` with `json_schema` and `strict: true`, and the model is constrained at the token level — it physically cannot produce output that violates the schema:

```python
import requests, json

schema = {
    "type": "object",
    "properties": {
        "intent": {"type": "string", "enum": ["buy", "sell", "hold", "unknown"]},
        "confidence": {"type": "number", "minimum": 0, "maximum": 1}
    },
    "required": ["intent", "confidence"],
    "additionalProperties": False
}

resp = requests.post("http://localhost:30000/v1/chat/completions", json={
    "model": "meta-llama/Llama-3.1-8B-Instruct",
    "messages": [{"role": "user", "content": "Classify: 'NVIDIA beat earnings estimates by 40%'"}],
    "max_tokens": 64,
    "response_format": {
        "type": "json_schema",
        "json_schema": {"name": "signal", "schema": schema, "strict": True}
    }
})

result = json.loads(resp.json()["choices"][0]["message"]["content"])
# guaranteed to match schema — no try/except, no retries
print(result)  # {"intent": "buy", "confidence": 0.87}
```

## When to choose SGLang

Choose SGLang when your workload has long shared prefixes (agent system prompts, RAG context, few-shot examples) and you need structured output guarantees. Choose vLLM when you need production-grade multi-LoRA adapter hot-swapping. Use Ollama when you just need a fast local chat interface.

**Sources:** [SGLang official docs — docs.sglang.io](https://docs.sglang.io/docs/get-started/quickstart), [SGLang fast inference guide — markaicode.com](https://markaicode.com/sglang-fast-llm-inference-structured-generation/), [SGLang vs vLLM review — chatforest.com](https://chatforest.com/reviews/sglang-structured-generation-llm-serving/)
]]></content:encoded>
    </item>

    <item>
      <title>GitLab Teams: Claude Code's --worktree Flag Now Accepts Merge Request URLs</title>
      <link>https://code.claude.com/docs/en/changelog</link>
      <guid isPermaLink="false">2026-08-15-05-claude-code-gitlab-mr-worktree</guid>
      <pubDate>Sat, 15 Aug 2026 12:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Developer Tools</category>
      <description>Claude Code v2.1.233 extends --worktree to accept GitLab MR URLs, so GitLab teams get the same isolated-workspace code-review workflow GitHub users have had.</description>
      <content:encoded><![CDATA[
**Claude Code v2.1.233 adds GitLab MR URL support to --worktree — GitLab teams now get the same isolated code-review workflow GitHub PR users have had, plus memory and cache controls.**

If you're on a GitLab team, `--worktree` just became more useful. The flag previously accepted GitHub PR URLs and spun up an isolated git worktree scoped to that PR's branch. v2.1.233 extends that to **GitLab merge request URLs**, so you can point Claude Code at any MR and work against it without touching your main checkout. MRs now also display as `!N` in the `claude agents` view alongside any GitHub PRs.

## The workflow

**Review or test a merge request without switching branches:**

```bash
# Review an MR for correctness and security issues
claude --worktree https://gitlab.com/your-org/your-repo/-/merge_requests/417 \
  "Review this MR for correctness issues and any security concerns. Check that error handling covers all paths."

# Run the test suite against the MR's changes
claude --worktree https://gitlab.com/your-org/your-repo/-/merge_requests/417 \
  "Run pnpm test and report any new failures introduced by this MR's diff."
```

Claude checks out the MR's branch into a temporary worktree, scoped to those changes. Your working tree and branch are untouched. When the session ends, the worktree is cleaned up.

**Watch active MRs in the agents view:**

```bash
claude agents  # MRs appear as !N alongside GitHub PRs
```

## Two new environment controls also in v2.1.233

**`CLAUDE_CODE_TOOL_MEMORY_LIMIT`** (Linux, opt-in): caps memory for Bash tool commands via cgroup — useful on shared CI machines or self-hosted runners where a runaway build (`pnpm install` on a cold cache, `gradle build`, large Docker builds) can starve the session.

```bash
# Limit Bash tool commands to 4GB
export CLAUDE_CODE_TOOL_MEMORY_LIMIT=4g
claude ...
```

**`CLAUDE_CODE_WEBFETCH_CACHE_TTL_MS`**: controls how long the WebFetch tool caches page fetches within a session. Default is 15 minutes (900000 ms). Reduce it if your agent needs fresh data on each loop iteration:

```bash
export CLAUDE_CODE_WEBFETCH_CACHE_TTL_MS=60000  # 1-minute cache
```

## Why this matters for GitLab shops

Claude Code's `--worktree` pattern is the foundation for AI-assisted code review in CI/CD pipelines. Teams on GitHub have been running Claude as a code-review step in Actions since the agent-as-reviewer pattern emerged earlier this year. GitLab shops can now set up the equivalent in GitLab CI:

```yaml
# .gitlab-ci.yml
ai-review:
  stage: review
  script:
    - claude --worktree $CI_MERGE_REQUEST_PROJECT_URL/-/merge_requests/$CI_MERGE_REQUEST_IID
        "Review this MR for correctness and performance issues. Post a markdown summary."
  rules:
    - if: $CI_MERGE_REQUEST_ID
```

**Sources:** [Claude Code v2.1.233 release — GitHub](https://github.com/anthropics/claude-code/releases/tag/v2.1.233) · [Claude Code changelog — code.claude.com](https://code.claude.com/docs/en/changelog) · [Claude Code worktree guide — claudefa.st](https://claudefa.st/blog/guide/development/worktree-guide)
]]></content:encoded>
    </item>

    <item>
      <title>Your Fine-Tune Might Be Lying to You: Measure It With lm-evaluation-harness</title>
      <link>https://github.com/EleutherAI/lm-evaluation-harness</link>
      <guid isPermaLink="false">2026-08-15-04-evaluate-fine-tuned-model-lm-eval-harness</guid>
      <pubDate>Sat, 15 Aug 2026 12:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Fine-Tuning</category>
      <category>HuggingFace</category>
      <description>Training loss drops — but does the model actually work? Three layers of evaluation (held-out set, lm-eval-harness benchmarks, LLM-as-judge) tell you before production does.</description>
      <media:content url="https://opengraph.githubassets.com/1/EleutherAI/lm-evaluation-harness" medium="image" />
      <media:thumbnail url="https://opengraph.githubassets.com/1/EleutherAI/lm-evaluation-harness" />
      <content:encoded><![CDATA[
**Training loss drops — but your fine-tuned model can still quietly break on everything it wasn't tested on. Three layers of evaluation catch regressions before production does.**

**What you'll be able to do after this:**
- Detect catastrophic forgetting before shipping a fine-tuned model
- Run standard benchmarks (MMLU, ARC, GSM8K) on any HuggingFace model with one command using lm-evaluation-harness
- Know when to use benchmark evals vs task-specific evals vs LLM-as-judge

## The hidden trap

Training loss goes down. The model responds fluently. But did it improve on your task — without quietly regressing on everything else? Most devs skip evaluation and ship on vibes. The risk is **catastrophic forgetting**: a model fine-tuned on customer support data might score 10–15 points lower on math reasoning (GSM8K) after just two epochs, with no warning in the training logs.

Three evaluation layers catch this before users do.

## Layer 1: Your held-out eval set (task accuracy)

Before training, set aside 10–20% of your dataset as a held-out test set. Score your fine-tuned model against it after training:

```python
from datasets import load_dataset
from transformers import AutoModelForCausalLM, AutoTokenizer
import torch

model_id = "./outputs/my-fine-tuned-model"
tokenizer = AutoTokenizer.from_pretrained(model_id)
model = AutoModelForCausalLM.from_pretrained(
    model_id, torch_dtype=torch.float16, device_map="auto"
)

test_data = load_dataset("json", data_files="test.jsonl")["train"]

correct = 0
for example in test_data:
    inputs = tokenizer(example["prompt"], return_tensors="pt").to("cuda")
    with torch.no_grad():
        output = model.generate(**inputs, max_new_tokens=256)
    response = tokenizer.decode(output[0], skip_special_tokens=True)
    if example["expected"] in response:
        correct += 1

print(f"Task accuracy: {correct/len(test_data)*100:.1f}%")
```

For structured outputs (JSON, SQL, code), use exact-match. For freeform text, use ROUGE or BERTScore. This is your primary signal.

## Layer 2: Benchmark regression with lm-evaluation-harness

[lm-evaluation-harness](https://github.com/EleutherAI/lm-evaluation-harness) (EleutherAI) is the framework behind the HuggingFace Open LLM Leaderboard — the standard infrastructure for evaluating models at benchmark scale. One command runs your model on 200+ tasks. Run it on the **base model first**, save the results, then compare after fine-tuning.

```bash
pip install lm-eval

# Step 1: baseline the base model
lm_eval --model hf \
  --model_args pretrained=meta-llama/Llama-3.1-8B-Instruct \
  --tasks mmlu,gsm8k,arc_challenge \
  --device cuda:0 --batch_size 8 \
  --output_path ./eval-results/base

# Step 2: eval your fine-tuned model with the same tasks
lm_eval --model hf \
  --model_args pretrained=./outputs/my-fine-tuned-model \
  --tasks mmlu,gsm8k,arc_challenge \
  --device cuda:0 --batch_size 8 \
  --output_path ./eval-results/fine-tuned
```

Then diff the result JSON files. What to watch:
- **MMLU**: general knowledge across 57 subjects — a broad regression signal
- **GSM8K**: grade-school math — catches reasoning degradation fast
- **ARC-Challenge**: science questions requiring multi-step reasoning

**Interpreting the delta:**

| GSM8K / MMLU drop | Verdict |
|---|---|
| 0–3% | Normal — fine-tuning always trades some generality |
| 3–10% | Investigate — check training epochs, learning rate, data mix |
| 10%+ | Catastrophic forgetting — roll back; revisit LoRA rank, dataset balance, or add a regularization term |

## Layer 3: LLM-as-judge for generative quality

Automated metrics can't assess response quality for open-ended tasks. For that, use a frontier model as a judge:

```python
import anthropic, json

client = anthropic.Anthropic()

def judge_response(prompt: str, response: str, rubric: str) -> dict:
    result = client.messages.create(
        model="claude-opus-4-7",
        max_tokens=512,
        messages=[{
            "role": "user",
            "content": f"""Rate this response on a 1–5 scale.

Rubric: {rubric}

User prompt: {prompt}
Model response: {response}

Return JSON only: {{"score": 1-5, "reason": "one sentence"}}"""
        }]
    )
    return json.loads(result.content[0].text)

# Run on 50–100 held-out examples
results = [
    judge_response(ex["prompt"], generate(model, ex["prompt"]), rubric)
    for ex in eval_set
]
avg_score = sum(r["score"] for r in results) / len(results)
print(f"LLM-judge avg score: {avg_score:.2f}/5.0")
```

Run this against your base model too to get a baseline. A fine-tune that scores below the base on the judge is a regression.

## Which layer for which task

| Task type | Layer 1 | Layer 2 | Layer 3 |
|---|---|---|---|
| Structured output (JSON, SQL, code) | Exact-match | Always | Optional |
| Freeform generation (chat, summaries) | ROUGE | Always | Required |
| Any fine-tune | Required | Required | Recommended |

**Sources:** [lm-evaluation-harness — EleutherAI/GitHub](https://github.com/EleutherAI/lm-evaluation-harness) · [lm-eval-harness quickstart — readthedocs.io](https://lm-evaluation-harness.readthedocs.io/getting_started/quickstart/) · [Evaluate Fine-Tuned LLMs: MMLU, MT-Bench, Custom Evals — markaicode.com](https://markaicode.com/fine-tuning-evaluation-mmlu-mt-bench-custom-evals/)
]]></content:encoded>
    </item>

    <item>
      <title>Let Claude Think Between Tool Calls, Not Just Before: Interleaved Thinking in Your Agent Loop</title>
      <link>https://platform.claude.com/docs/en/build-with-claude/thinking-tool-workflows</link>
      <guid isPermaLink="false">2026-08-15-03-claude-api-interleaved-thinking-agent-loop</guid>
      <pubDate>Sat, 15 Aug 2026 04:09:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Agents</category>
      <category>Best Practices</category>
      <description>With adaptive thinking on Sonnet or Opus 4.6, Claude reasons after each tool result — not just once at the start — making multi-step agent loops sharper with no extra code.</description>
      <content:encoded><![CDATA[
**With adaptive thinking on Sonnet 4.6 or Opus 4.6, Claude automatically reasons after each tool result before calling the next one — no prompt engineering required.**

Standard tool use has a hidden flaw: Claude thinks once before the first tool call, then chains subsequent calls with no reasoning gap between them. When results are independent (parallel lookups, simple transformations), that's fine. But when each result should reshape the next query — competitive research, multi-source fact-checking, debugging flows where the error output determines which log to pull — you want the model reasoning at every step, not just at the start.

**Interleaved thinking** adds a reasoning block after each `tool_result` before the model decides its next action. The loop becomes: *think → call tool → receive result → think about result → call next tool → ...* On Sonnet 4.6 and Opus 4.6 with adaptive thinking, this is on by default.

```python
import anthropic

client = anthropic.Anthropic()

tools = [
    {
        "name": "web_search",
        "description": "Search the web for current information.",
        "input_schema": {
            "type": "object",
            "properties": {"query": {"type": "string"}},
            "required": ["query"],
        },
    }
]

messages = [
    {
        "role": "user",
        "content": "Compare React vs Vue popularity for new projects in 2026: check npm download stats, then cross-verify with recent job listings.",
    }
]

while True:
    response = client.messages.create(
        model="claude-sonnet-4-6",
        max_tokens=16000,
        thinking={"type": "adaptive"},   # interleaved thinking is automatic on 4.6
        tools=tools,
        messages=messages,
    )

    if response.stop_reason == "end_turn":
        print(next(b.text for b in response.content if hasattr(b, "text")))
        break

    tool_results = []
    for block in response.content:
        if block.type == "tool_use":
            result = dispatch_tool(block.name, block.input)   # your handler
            tool_results.append({
                "type": "tool_result",
                "tool_use_id": block.id,
                "content": result,
            })

    messages.append({"role": "assistant", "content": response.content})
    messages.append({"role": "user", "content": tool_results})
```

After each `tool_result` the response will contain a fresh `thinking` block before the next `tool_use`. You don't change the loop at all — adaptive thinking handles the interleaving.

**On other Claude 4 models** (not 4.6), opt in with a beta header:

```python
client.messages.create(
    ...,
    extra_headers={"anthropic-beta": "interleaved-thinking-2025-05-14"},
)
```

**When it makes a real difference:**

| Task type | Interleaved thinking benefit |
|---|---|
| Multi-source research (result A informs query B) | High — each result reshapes the next search |
| Sequential debugging (error → log → patch) | High — model reasons whether error narrows the search |
| Parallel independent lookups | Low — results don't depend on each other |
| Single-tool, single-call | None — only one turn |

**Why it matters:** Better multi-step judgment from the same model, same tokens, with no prompt engineering. The model's adaptive reasoning handles it — you just keep the same tool loop you already have.

**Sources:** [Thinking in tool and multi-turn workflows — platform.claude.com](https://platform.claude.com/docs/en/build-with-claude/thinking-tool-workflows) · [Extended thinking — platform.claude.com](https://platform.claude.com/docs/en/build-with-claude/extended-thinking) · [Build a tool-using agent — platform.claude.com](https://platform.claude.com/docs/en/agents-and-tools/tool-use/build-a-tool-using-agent)
]]></content:encoded>
    </item>

    <item>
      <title>Claude Code v2.1.233: Marketplace Skills Are Sandboxed, Opus 4.7 Leaves /fast, Workflows Default to Medium</title>
      <link>https://code.claude.com/docs/en/changelog</link>
      <guid isPermaLink="false">2026-08-15-02-claude-code-v2233-hardened-skills-fast-mode</guid>
      <pubDate>Sat, 15 Aug 2026 04:08:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Claude Code</category>
      <category>Developer Tools</category>
      <description>v2.1.233: skills from the claude.ai marketplace are sandboxed on your machine, /fast drops Opus 4.7, and dynamic workflows aim for fewer than 15 agents by default.</description>
      <content:encoded><![CDATA[
**Claude Code v2.1.233 (Aug 15) sandboxes marketplace skills so they can't shadow local commands, removes Opus 4.7 from /fast, and sets dynamic workflows to medium (< 15 agents) by default.**

**Marketplace skills are now sandboxed on your machine.** Skills synced from claude.ai no longer shadow local slash commands or MCP prompts. Their descriptions are sanitized and labeled. On your local machine, skill bodies can't run commands or expand `@` files — they're read-only reference material there; full execution only runs inside the managed session they're installed for. If you relied on marketplace skill `@` file expansion locally, rebuild that flow in a project skill instead.

**Opus 4.7 removed from /fast.** The July 24 migration deadline has passed. `/fast` now routes to Opus 5 and Opus 4.8 only. Run `/models` if you're seeing unexpected behavior after the change.

**Dynamic workflows default to medium.** Previously, the size guideline was unset unless you configured it. Now Claude Code aims for fewer than 15 agents per workflow by default — a guardrail against accidental cost spikes. Override with `/config workflow.size=large` (or `workflowSizeGuideline: "large"` in settings.json) when you need a comprehensive sweep.

**Why it matters:** The skill sandboxing closes a supply-chain risk — a poisoned marketplace skill could no longer inject behavior into your local command flow. The workflow default keeps routine runs from ballooning unexpectedly.

**Sources:** [Claude Code changelog — code.claude.com](https://code.claude.com/docs/en/changelog) · [Claude Code August 2026 updates — Releasebot](https://releasebot.io/updates/anthropic/claude-code)
]]></content:encoded>
    </item>

    <item>
      <title>Your Agent's Actions Are Python, Not JSON: Build a Code Agent With HuggingFace smolagents</title>
      <link>https://huggingface.co/learn/agents-course/en/unit2/smolagents/code_agents</link>
      <guid isPermaLink="false">2026-08-15-01-smolagents-code-agent-python</guid>
      <pubDate>Sat, 15 Aug 2026 04:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Agents</category>
      <category>HuggingFace</category>
      <description>smolagents gives your LLM a Python interpreter as its action space instead of JSON tool calls — start a code agent in 10 lines, with any HuggingFace or Claude model.</description>
      <media:content url="https://opengraph.githubassets.com/1/huggingface/smolagents" medium="image" />
      <media:thumbnail url="https://opengraph.githubassets.com/1/huggingface/smolagents" />
      <content:encoded><![CDATA[
**smolagents (HuggingFace) lets your LLM write actual Python to call tools instead of JSON blobs — in benchmarks, code agents complete complex multi-step tasks up to ~20% more often in fewer steps.**

**What you'll be able to do after this:**
- Understand why code agents outperform JSON tool-calling agents on multi-step tasks
- Build a working `CodeAgent` with real tools in under 30 lines of Python
- Know when to pick `CodeAgent` (default) vs `ToolCallingAgent` (structured API calls)

## Why code over JSON?

Most agent frameworks represent tool calls as JSON: `{"tool": "search", "args": {"query": "..."}}`. The model names the tool; the JSON is dispatched; repeat. smolagents instead gives the model a Python interpreter: it writes real code, with your tools available as callables. The model can loop, branch, compose multiple tools in one step, and define helper functions on the fly. Research shows this raises task success rates by up to ~20% on complex benchmarks and often reduces total steps.

smolagents ships two agent types:
- **`CodeAgent`** (default) — generates Python, executes it in a sandboxed subprocess
- **`ToolCallingAgent`** — generates JSON tool calls (backward-compatible with traditional tool-calling setups)

For most multi-step work, `CodeAgent` is the right choice.

## Quick start

```bash
pip install smolagents
```

```python
from smolagents import CodeAgent, HfApiModel, DuckDuckGoSearchTool

model = HfApiModel(model_id="Qwen/Qwen2.5-72B-Instruct")
agent = CodeAgent(tools=[DuckDuckGoSearchTool()], model=model)

result = agent.run(
    "Find the Python package for the largest open-source vector database "
    "by GitHub stars and show me the install command."
)
print(result)
```

To use **Claude Sonnet 4.6** instead:

```python
from smolagents import CodeAgent, LiteLLMModel, DuckDuckGoSearchTool

model = LiteLLMModel(model_id="anthropic/claude-sonnet-4-6")
agent = CodeAgent(tools=[DuckDuckGoSearchTool()], model=model)
```

smolagents uses [LiteLLM](https://docs.litellm.ai/) under the hood for provider routing, so any Anthropic model string works.

## Add a custom tool with a decorator

```python
from smolagents import tool

@tool
def count_words(text: str) -> int:
    """Counts the number of words in the given text."""
    return len(text.split())

agent = CodeAgent(tools=[count_words], model=model)
```

The function name becomes the tool name; the docstring becomes the model's description; type hints define the schema. No boilerplate.

## When to choose each agent type

| Scenario | Agent type |
|---|---|
| Multi-step research with branching | `CodeAgent` |
| Loop over a list until condition met | `CodeAgent` |
| Single structured API call | `ToolCallingAgent` |
| Can't execute arbitrary Python safely | `ToolCallingAgent` |

`CodeAgent` runs code in a sandboxed local interpreter by default. For production, swap it for the `DockerExecutor` or `E2BExecutor` to confine execution to a container. Tools are allowlisted — the model can only call functions you explicitly passed in.

## Where to go deeper

The **HuggingFace Agents Course Unit 2** ([huggingface.co/learn/agents-course](https://huggingface.co/learn/agents-course/en/unit2/smolagents/code_agents)) is the free, official walkthrough — covers CodeAgent, ToolCallingAgent, multi-agent orchestration, and the secure execution layer. The **DeepLearning.AI short course** (taught by Thomas Wolf and Aymeric Roucher, HuggingFace) goes from scratch to production in about two hours.

**Sources:** [Building Agents That Use Code — HuggingFace Agents Course](https://huggingface.co/learn/agents-course/en/unit2/smolagents/code_agents) · [Building Code Agents with HuggingFace smolagents — DeepLearning.AI](https://www.deeplearning.ai/courses/building-code-agents-with-hugging-face-smolagents) · [smolagents guided tour — HuggingFace Docs](https://huggingface.co/docs/smolagents/en/guided_tour) · [smolagents GitHub — huggingface/smolagents](https://github.com/huggingface/smolagents)
]]></content:encoded>
    </item>

    <item>
      <title>Fork Subagents Inherit Context by Default and @ Mentions Route Across Sessions: Two Claude Code Defaults Live Today</title>
      <link>https://code.claude.com/docs/en/changelog</link>
      <guid isPermaLink="false">2026-08-14-06-claude-code-fork-default-session-mentions</guid>
      <pubDate>Thu, 14 Aug 2026 12:14:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Claude Code</category>
      <category>Agents</category>
      <category>Workflow</category>
      <description>Starting August 14, fork subagents in Claude Code inherit the full conversation and prompt cache by default, and typing @ in a prompt routes a message directly to any named live session.</description>
      <content:encoded><![CDATA[
**Fork subagents now inherit the full conversation and prompt cache by default; @ in your prompt routes a message to any named live Claude Code session.**

Two defaults changed in Claude Code v2.1.232 (released today, August 14). Both affect multi-session workflows and are worth knowing before your next agentic run.

## Fork subagents: context inheritance on by default

A fork subagent inherits the parent session's full conversation history, system prompt, tool list, and model. Before today, you had to opt in. Now it's the default for subagents you want to hand context to, without re-briefing.

The practical payoff is **prompt cache reuse.** A fork's first request hits the same cached prefix as the parent — you build context once, the fork reads it at cache-read rates (roughly 4–10× cheaper than re-sending). For reviewers, auditors, or parallel variations that need your full context, this makes forks the clear first choice over fresh subagents.

```python
# Spawn a reviewer that inherits everything the main session has built up
result = await agent(
    "Audit the implementation above for edge cases the tests don't cover",
    subagent_type="fork"   # inherits conversation + cached prefix; no re-briefing needed
)
```

**Non-teammate subagent spawns in interactive sessions now background by default.** When you trigger a subagent in an interactive session, it runs without blocking your main window. Results arrive asynchronously; the main conversation keeps moving.

## When fork wins vs. a named subagent

| Use case | Pattern |
|---|---|
| Review / audit with full context | `subagent_type: "fork"` |
| Specialist agent (different model/tools) | Named subagent (starts cold, own cache) |
| One-off parallel variation | Fork — cache-sharing cuts cost |
| Long-running background session | Background agent + @ mention |

## @ session mentions

Type `@` followed by a session name in any Claude Code prompt — Claude routes the message via `SendMessage` without you writing the tool call explicitly. `SendMessage` now also resolves a bare name if exactly one live session matches, so you no longer need to append a `[ref]` suffix when the name is unambiguous:

```
@test-runner please run the auth spec suite and report any failures here
```

List all mentionable sessions with `ListAgents` or `/agents` in the TUI — only sessions that registered with a name are @-reachable. This is particularly useful when a background session (a running test harness, a long deploy watcher) is the right target for a side task.

## Practical pipeline: fork for review, mention for dispatch

1. Main session implements a feature, building rich context
2. At each milestone: `subagent_type: "fork"` spawns a reviewer — it inherits everything, costs cache-read rates
3. If a dedicated test session is live, `@test-runner` dispatches the test run without context-switching the main session

**Sources:** [Claude Code changelog v2.1.232 — code.claude.com](https://code.claude.com/docs/en/changelog) · [Cross-session messaging — code.claude.com](https://code.claude.com/docs/en/cross-session-messaging) · [Fork subagent updates — DevelopersIO](https://dev.classmethod.jp/en/articles/20260814-cc-updates-v2-1-232/)
]]></content:encoded>
    </item>

    <item>
      <title>Claude Code Platform Fixes This Week: SSE Keepalive for Vertex/Bedrock, MCP OAuth Fix, Usage Boost Through Aug 19</title>
      <link>https://code.claude.com/docs/en/changelog</link>
      <guid isPermaLink="false">2026-08-14-05-claude-code-aug14-platform-fixes</guid>
      <pubDate>Thu, 14 Aug 2026 12:13:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Claude Code</category>
      <category>News</category>
      <category>Developer Tools</category>
      <description>Claude Code v2.1.229-231 (Aug 12-13): SSE keepalive prevents mid-response disconnects on Vertex/Bedrock streaming; MCP OAuth redirect URI fixed for pre-registered clients; usage boost extended to Aug 19.</description>
      <content:encoded><![CDATA[
**Claude Code v2.1.229–231: SSE keepalive for Vertex/Bedrock streaming prevents mid-response disconnects, MCP OAuth redirect is fixed, and the usage boost runs through August 19.**

Three fixes from the past two days, each addressing a distinct failure mode.

**SSE keepalive during long thinking pauses (v2.1.229, Aug 12).** When Claude runs extended reasoning on Vertex AI or Amazon Bedrock with streaming on, the upstream gateway can time out if the model thinks for too long without emitting a token. The connection closes silently, and your code gets a truncated response or an error — with no obvious reason why. Claude Code now sends SSE keepalive pings during those pauses, preventing the idle-timeout from triggering. If you've been seeing intermittent mid-response disconnects on Claude Sonnet 5 or Haiku 4.5 through Vertex or Bedrock, this is the fix.

**MCP OAuth redirect URI mismatch (v2.1.231, Aug 13).** OAuth sign-in was silently failing for MCP servers that use a pre-registered OAuth client — Slack, and any server that registers specific redirect URIs in advance. The URI Claude Code sent didn't match the server's expected value. Fixed. If your Slack MCP or a similar OAuth-backed server was failing to authenticate, upgrade and retry.

**Usage boost extended through August 19.** The 50% weekly usage boost for Claude Code subscribers (Pro, Max, Team) runs five more days. If you have large multi-agent workflow runs queued, schedule them before August 20 when normal limits resume.

**Why it matters:** The Vertex/Bedrock SSE fix is the most impactful for production pipelines — enterprises routing Claude through cloud provider APIs were seeing silent failures on extended-reasoning calls. The MCP fix unblocks Slack and any OAuth server with pre-registered redirect URIs.

**Sources:** [Claude Code changelog — code.claude.com](https://code.claude.com/docs/en/changelog) · [Claude Code August updates — Releasebot](https://releasebot.io/updates/anthropic/claude-code)
]]></content:encoded>
    </item>

    <item>
      <title>Zero to Serverless GPU in 30 Lines: Deploy Any HuggingFace Model with Modal + vLLM</title>
      <link>https://modal.com/blog/how-to-deploy-vllm</link>
      <guid isPermaLink="false">2026-08-14-04-modal-vllm-serverless-gpu-deploy</guid>
      <pubDate>Thu, 14 Aug 2026 12:12:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Self-Hosting</category>
      <category>HuggingFace</category>
      <description>Modal is Python-native serverless GPU: annotate a function, name a model, and modal deploy gives you an OpenAI-compatible inference endpoint on a real H100 that scales to zero when idle.</description>
      <content:encoded><![CDATA[
**Modal is Python-native serverless GPU: annotate a function, name a model, and `modal deploy` gives you an OpenAI-compatible inference endpoint on a real H100 that scales to zero when idle.**

**What you'll be able to do after this:**

- Deploy a vLLM-backed LLM endpoint to a real GPU with ~30 lines of Python and no Dockerfile, Kubernetes, ECR push, or cluster management
- Get an OpenAI-compatible `/v1/chat/completions` endpoint that scales to zero when idle — you pay only for inference time (Modal bills per second of container uptime)
- Cache model weights in a Modal Volume so cold starts drop from several minutes to 10–30 seconds for a 7–8B model

Modal fills a gap between "local Ollama" (great for your machine, not shareable) and "Together/OpenRouter" (someone else's model). With Modal you control the model, the GPU tier, and the serving configuration — but you still write plain Python and let Modal handle provisioning.

## Install and authenticate

```bash
pip install modal
modal setup   # opens browser to authenticate with your modal.com account
```

Modal gives you $30/month of free credits — enough for several hours of H100 time per month for experiments.

## Write the inference server (inference.py)

```python
import modal
import subprocess

app = modal.App("vllm-server")

MODEL = "mistralai/Mistral-7B-Instruct-v0.3"   # swap any vLLM-supported HF model

vllm_image = (
    modal.Image.from_registry("nvidia/cuda:12.8.0-devel-ubuntu22.04", add_python="3.12")
    .uv_pip_install(
        "vllm==0.10.2",
        "huggingface_hub[hf_transfer]==0.35.0",
    )
    .env({"HF_HUB_ENABLE_HF_TRANSFER": "1"})   # faster HF downloads
)

weights = modal.Volume.from_name("llm-weights", create_if_missing=True)  # persists across deploys

@app.cls(
    image=vllm_image,
    gpu="H100",                    # or "A10G" for smaller/cheaper experiments
    volumes={"/models": weights},
    concurrency_limit=2,           # scale up replicas beyond this threshold
    timeout=3600,
)
@modal.web_server(8000)            # expose vLLM's HTTP server as a Modal web endpoint
class VLLMServer:
    @modal.enter()
    def run_server(self):
        subprocess.Popen([
            "vllm", "serve", MODEL,
            "--download-dir", "/models",
            "--host", "0.0.0.0",
            "--port", "8000",
        ])
```

## Deploy and call it

```bash
modal deploy inference.py
# → Deployed! URL: https://your-workspace--vllm-server.modal.run
```

```python
from openai import OpenAI

client = OpenAI(
    base_url="https://your-workspace--vllm-server.modal.run/v1",
    api_key="not-needed",
)
response = client.chat.completions.create(
    model=MODEL,
    messages=[{"role": "user", "content": "What is PagedAttention and why does it matter?"}],
)
print(response.choices[0].message.content)
```

The endpoint is identical to the OpenAI API — any code that calls `openai.chat.completions.create` works without modification, just swap the `base_url`.

## What happens when you hit the endpoint

1. If no container is running, Modal cold-starts one: CUDA image pulls (cached after the first deploy), vLLM process starts, model weights load from the Volume.
2. vLLM serves the request using PagedAttention for efficient KV-cache management.
3. After idle timeout, the container shuts down — zero cost while it's not running.

The weight Volume is the key optimization: model files are downloaded once on the first request and stored in Modal's networked storage, so subsequent cold starts skip the HuggingFace download and load directly from the Volume (10–30 seconds for a 7B model vs. 2–10 minutes for a fresh download).

## Swap models with one line change

Change `MODEL` to any model vLLM supports — `meta-llama/Llama-3.1-8B-Instruct`, `Qwen/Qwen2.5-7B-Instruct`, `google/gemma-2-9b-it` — and redeploy. For gated models (Llama), add a HuggingFace token as a Modal Secret and pass it to the container.

**Sources:** [How to deploy vLLM — Modal blog](https://modal.com/blog/how-to-deploy-vllm) · [Modal vLLM examples — modal.com/docs](https://modal.com/docs/examples/ministral3_inference) · [Running LLMs on Modal — Medium](https://pguso.medium.com/running-llms-on-modal-gpu-powered-inference-that-scales-to-zero-635854513557)
]]></content:encoded>
      <media:content url="https://modal.com/assets/modal-og.png" medium="image" type="image/png" />
      <media:thumbnail url="https://modal.com/assets/modal-og.png" />
    </item>

    <item>
      <title>Planner, Generator, Evaluator: Anthropic's Three-Agent Harness for Long-Running Agents That Never Forget</title>
      <link>https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents</link>
      <guid isPermaLink="false">2026-08-14-03-anthropic-harness-design-long-running-agents</guid>
      <pubDate>Thu, 14 Aug 2026 04:21:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Agents</category>
      <category>Best Practices</category>
      <category>Workflow</category>
      <description>When agents run for hours across context resets, Anthropic's three-role harness — Planner, Generator, Evaluator — with structured handoff files prevents context drift and compounding errors across sessions.</description>
      <content:encoded><![CDATA[
**When agents run for hours across context resets, Anthropic's three-role harness — Planner, Generator, Evaluator — with structured handoff files prevents context drift and compounding errors across sessions.**

Longer context windows are a footgun. When a long-running agent works in a single expanding window, it accumulates "context anxiety" — the model starts wrapping up prematurely and cutting corners as it senses the session ending. Anthropic's engineering blog addresses this directly with a harness built around *context resets, not compression*.

## The three roles

**Planner** — runs once at the start. Takes the high-level requirement and expands it into a structured product spec: a JSON feature list where every feature has a clear acceptance criterion. JSON over Markdown is intentional — models are less likely to accidentally overwrite or mangle a structured file than a freeform doc.

**Generator** — runs in each fresh session. Reads the feature list and progress file, picks one pending feature, implements it, commits to git, and updates the progress log. One feature per session, never a multi-feature sprint, to avoid the one-shot failure mode where the model tries to do everything and finishes nothing cleanly.

**Evaluator** — also runs each session, *separately from the Generator*. Opens a browser (Playwright or similar), exercises the just-committed feature end-to-end, and writes structured pass/fail feedback to a shared file the next Generator session reads. The separation is load-bearing: when a model self-evaluates its own output, it tends to confidently praise mediocre work. An external evaluator creates actionable feedback loops.

## The handoff artifacts

Three files are the project's memory across context resets:

```
feature_list.json    # Planner output; marks each feature passing / failing / pending
progress.md          # Per-session summary: what ran, what the next session picks up
CLAUDE.md            # Env setup, dev server commands, repo conventions
```

At the start of every Generator or Evaluator session, the harness prepends instructions to read these three files and check `git log --oneline -20` before writing a single line of code. The agent's memory is the files plus the commit history — not the conversation history.

## The sprint contract

Before each Generator session starts implementing, the harness asks Generator and Evaluator to agree on **testable success criteria for this session's feature**. This "sprint contract" bridges the gap between the Planner's abstract spec and the Evaluator's concrete browser tests. Without it, you get endless disagreement about what "working" means.

## Minimal implementation sketch

```python
def run_session(role: str, feature_list_path: str, progress_path: str):
    prompt = f"""
You are the {role}. Before doing anything else:
1. Read {feature_list_path} — find the next pending feature
2. Read {progress_path} — understand what the last session completed
3. Run `git log --oneline -20` and verify the dev server starts

Then: {'implement exactly one feature and commit' if role == 'Generator' else
       'test the last committed feature and write pass/fail to progress.md'}
"""
    response = client.messages.create(
        model="claude-opus-5",
        max_tokens=8096,
        messages=[{"role": "user", "content": prompt}],
        tools=[bash_tool, file_read_tool, file_write_tool],
    )
    # run agentic tool-use loop until stop_reason == "end_turn"
```

The critical point is what happens *between* sessions: the harness resets the conversation, re-reads artifacts from disk, and starts completely fresh — not from a compacted summary.

## When to strip components

Every component in a harness encodes an assumption about what the model *cannot* do on its own. As models improve, previously necessary scaffolding becomes overhead. If your model self-evaluates reliably on the task, collapse Generator and Evaluator into one. If the Planner simply restates the prompt, skip it. Audit your harness assumptions after every model upgrade — the right harness for Claude 3 is probably over-engineered for Claude 5.

**Sources:** [Effective Harnesses for Long-Running Agents — Anthropic Engineering](https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents) · [Harness Design for Long-Running Application Development — Anthropic Engineering](https://www.anthropic.com/engineering/harness-design-long-running-apps)
]]></content:encoded>
    </item>

    <item>
      <title>DeepSeek V4 Pro 0813 Goes GA: 1M-Context, Agent-Focused, Under a Dollar per Million Tokens</title>
      <link>https://www.unite.ai/deepseek-ships-v4-pro-as-its-flagship-model-leaves-preview/</link>
      <guid isPermaLink="false">2026-08-14-02-deepseek-v4-pro-0813-ga</guid>
      <pubDate>Thu, 14 Aug 2026 04:20:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>LLM</category>
      <category>News</category>
      <description>DeepSeek V4 Pro 0813 reached general availability August 13 — 1.6T-parameter MoE with 1M-token context, configurable reasoning levels, and sub-dollar pricing ahead of an announced (but undated) price increase.</description>
      <content:encoded><![CDATA[
**DeepSeek V4 Pro 0813 left preview on August 13 — 1.6T-parameter MoE with 1M-token context, agentic tool use, and sub-dollar pricing before a price hike lands.**

DeepSeek's V4 Pro (build 0813) reached general availability on August 13, becoming the lab's new flagship. The architecture is a mixture-of-experts system with **1.6 trillion total parameters** and roughly **49 billion active per forward pass**. The 0813 release adds DSpark speculative decoding for faster output and exposes configurable reasoning-effort levels (low / high / max) per request.

**Key specs:**
- Context window: **1 million tokens** (384K max output)
- Pricing: **$0.435/1M input** (cache miss) · **$0.003625/1M cached** · **$0.87/1M output**
- Native OpenAI Responses API support (one-click Codex-style setup)
- Reasoning effort: `low`, `high`, or `max` per request — same model, variable cost

The model is positioned around agentic workloads: multi-step tool use, long-horizon reasoning, and code generation. Vendor-reported SWE-bench scores are striking (up to ~91%), but no independent evaluator has replicated them as of publication.

**Why it matters:** At $0.435/1M input (and sub-cent cached), V4 Pro undercuts most closed frontier models for high-context agentic pipelines. The 1M-token window means entire codebases or document collections can fit in a single call.

**Heads up:** DeepSeek has announced a price increase is coming — no timeline, no figure, no affected tiers disclosed. Current pricing likely reflects a promotional discount. Factor in a conservative multiplier before baking these numbers into production cost projections.

Available via the DeepSeek API and through OpenRouter.

**Sources:** [DeepSeek Ships V4 Pro — Unite.AI](https://www.unite.ai/deepseek-ships-v4-pro-as-its-flagship-model-leaves-preview/) · [DeepSeek V4 Pro Review — CoderSera](https://codersera.com/blog/deepseek-v4-pro-review-benchmarks-pricing-2026/)
]]></content:encoded>
    </item>

    <item>
      <title>No Server, No Docker: LanceDB Is the Disk-Persisted Vector Store Your RAG Pipeline Has Been Missing</title>
      <link>https://lancedb.github.io/lancedb/</link>
      <guid isPermaLink="false">2026-08-14-01-lancedb-embedded-vector-store</guid>
      <pubDate>Thu, 14 Aug 2026 04:19:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Vectors</category>
      <category>RAG</category>
      <category>Tutorial</category>
      <description>LanceDB is an embedded vector database that persists to disk — no server, no Docker, native hybrid (vector + BM25 keyword) search built in. Build and query a local vector store in under 20 lines of Python.</description>
      <content:encoded><![CDATA[
**LanceDB is an embedded vector database that persists to disk like a file — no server, no Docker, and native hybrid (vector + BM25 keyword) search built in.**

You've seen the vector store spectrum already: [FAISS](https://cloudcodetree.com/ai-news/2026-08-11-07-faiss-local-vector-index-python) is blazing fast but resets on restart; [Chroma](https://cloudcodetree.com/ai-news/2026-08-13-01-chroma-local-vector-db-rag) persists to disk and has a great developer API; [pgvector](https://cloudcodetree.com/ai-news/2026-08-09-04-pgvector-supabase-vector-store) integrates with your existing Postgres. LanceDB fills a distinct gap: fully embedded (no server process), automatic disk persistence in the [Apache Lance](https://github.com/lancedb/lance) columnar format, and **hybrid search — vector similarity plus BM25 keyword — built in without extra infrastructure**.

Think of it as the DuckDB of vector stores: embed it in your process, close the connection, reopen it next week, and your indexed embeddings are exactly where you left them.

## Install

```bash
pip install lancedb sentence-transformers
```

## Connect and populate

```python
import lancedb
from sentence_transformers import SentenceTransformer

model = SentenceTransformer("all-MiniLM-L6-v2")   # 384-dim, no API key needed

# creates ./rag_db/ on first call; subsequent calls reopen the same store
db = lancedb.connect("./rag_db")

docs = [
    {"id": "doc-1", "text": "LanceDB stores vectors alongside any metadata column", "source": "docs"},
    {"id": "doc-2", "text": "Hybrid search blends vector similarity with BM25 keyword ranking", "source": "blog"},
    {"id": "doc-3", "text": "The Lance columnar format enables fast filtered scans over billions of rows", "source": "blog"},
]

rows = [{**d, "vector": model.encode(d["text"]).tolist()} for d in docs]

table = db.create_table("knowledge", data=rows, mode="overwrite")
```

## Vector search

```python
query = "how does column storage help retrieval?"
q_vec = model.encode(query).tolist()

results = (
    table.search(q_vec)
         .metric("cosine")
         .limit(3)
         .select(["id", "text", "_distance"])
         .to_pandas()
)
print(results)
```

## Hybrid search (vector + BM25)

```python
from lancedb.rerankers import RRFReranker

# build the BM25 full-text index once; persists to disk with the table
table.create_fts_index("text")

results = (
    table.search(
            "columnar storage retrieval",
            query_type="hybrid",
            vector_column_name="vector",
            fts_columns="text",
         )
         .rerank(RRFReranker())   # Reciprocal Rank Fusion blends both lists
         .limit(3)
         .to_pandas()
)
```

RRF promotes any result that ranks highly in *both* vector and keyword lists — a result that appears in position 3 of the vector results and position 2 of the BM25 results floats to the top.

## Metadata filtering

```python
# SQL WHERE clause on any column, combined with vector search
results = (
    table.search(q_vec)
         .where("source = 'docs'")   # prune before ranking
         .limit(5)
         .to_pandas()
)
```

## ANN index for large tables

Once your table exceeds ~100K rows, add an approximate nearest-neighbor index so search stays sub-millisecond:

```python
table.create_index(metric="cosine", vector_column_name="vector")
```

## Vector store comparison

| | FAISS | Chroma | LanceDB | pgvector |
|---|---|---|---|---|
| Embedded (no server) | Yes | Yes | Yes | No |
| Auto disk persistence | No (manual save) | Yes | Yes | Yes |
| Hybrid search built-in | No | No | Yes (BM25 + RRF) | Partial |
| Metadata filter | No | Yes | Yes (SQL) | Yes (SQL) |
| Production path | Self-manage | Chroma Cloud | LanceDB Cloud | Managed Postgres |

## From local to cloud

When you're ready to scale beyond a single machine, point at LanceDB Cloud — the same table, index, and search code works unchanged, you just swap the connection URI. No rewrite required.

**Sources:** [LanceDB documentation](https://lancedb.github.io/lancedb/) · [Full-Text Search — LanceDB docs](https://docs.lancedb.com/search/full-text-search) · [Hybrid Search — LanceDB docs](https://lancedb.com/docs/search/hybrid-search/) · [lancedb/lancedb — GitHub](https://github.com/lancedb/lancedb)
]]></content:encoded>
    </item>

    <item>
      <title>Think Less, Pay Less: Routing Agent Subworkloads by Thinking Level in Gemini 3.7 Flash</title>
      <link>https://ai.google.dev/gemini-api/docs/models/gemini-3.7-flash</link>
      <guid isPermaLink="false">2026-08-13-10-gemini-37-flash-thinking-levels-agent-routing</guid>
      <pubDate>Wed, 13 Aug 2026 20:09:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Agents</category>
      <category>Best Practices</category>
      <description>Gemini 3.7 Flash's thinking_level parameter (low/medium/high) lets you match reasoning effort to task complexity per request — same model, same endpoint, lower cost on simple steps.</description>
      <content:encoded><![CDATA[
**Gemini 3.7 Flash ships a per-request thinking_level parameter so your agent can dial reasoning up for hard steps and down for cheap lookups — one model, one endpoint, variable cost.**

Most agentic pipelines treat every step identically: every tool call, every lookup, and every complex code analysis goes to the same max-reasoning inference tier. Gemini 3.7 Flash (GA today) breaks that pattern with a `thinking_level` parameter in `generation_config` that lets you tune per call.

**Three levels:**

| Level | Best for | Cost profile |
|---|---|---|
| `low` | Quick lookups, file listing, simple tool calls | Lowest |
| `medium` | Default for most agentic steps, code analysis | ~1.5× low |
| `high` | Hard debugging, complex code gen, architectural design | Highest |

**Routing pattern:**

```python
from google import genai

client = genai.Client()

def step(task: str, thinking_level: str) -> str:
    return client.interactions.create(
        model="gemini-3.7-flash",
        input=task,
        generation_config={"thinking_level": thinking_level},
    ).output_text

# Cheap: simple context-prep steps
file_list = step(
    "List all Python files modified in the last 24h",
    thinking_level="low",
)

# Balanced: most analytical steps
plan = step(
    f"Given these files: {file_list}\n"
    "Identify which ones might contain auth logic.",
    thinking_level="medium",
)

# Full depth: complex debugging
fix = step(
    f"Review this auth module: {plan}\n"
    "Find any race conditions and rewrite the transaction lock safely.",
    thinking_level="high",
)
```

**Via OpenRouter** (OpenAI-compatible client):

```python
import openai

client = openai.OpenAI(
    base_url="https://openrouter.ai/api/v1",
    api_key="<openrouter-key>",
)

resp = client.chat.completions.create(
    model="google/gemini-3.7-flash",
    messages=[{"role": "user", "content": task}],
    extra_body={"generation_config": {"thinking_level": "high"}},
)
```

**Why this matters for Claude Code workflows:** On days when Anthropic quota is under pressure, Gemini 3.7 Flash is a solid background-task model — particularly for the compute-heavy "write a migration plan" or "find the bug" steps where you want real reasoning but don't need Claude's specific strengths. The thinking-level routing keeps those background costs proportional to actual task complexity.

**Sources:** [Gemini 3.7 Flash — ai.google.dev](https://ai.google.dev/gemini-api/docs/models/gemini-3.7-flash) · [Gemini thinking guide](https://ai.google.dev/gemini-api/docs/thinking) · [Gemini 3 Developer Guide](https://ai.google.dev/gemini-api/docs/gemini-3)
]]></content:encoded>
    </item>

    <item>
      <title>Two Speed Records Today: Gemini 3.7 Flash GA for Coding Agents, GPT-5.6 Sol Ultrafast at 750 Tok/s</title>
      <link>https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/</link>
      <guid isPermaLink="false">2026-08-13-09-gemini-37-flash-gpt56-ultrafast-model-speed</guid>
      <pubDate>Wed, 13 Aug 2026 20:08:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>LLM</category>
      <category>Developer Tools</category>
      <description>Gemini 3.7 Flash hits GA today for coding and agents at $0.75/1M tokens with tunable thinking levels; OpenAI previews GPT-5.6 Sol Ultrafast at 750 tokens/second via Cerebras.</description>
      <content:encoded><![CDATA[
**Two new top-tier inference options landed today: Gemini 3.7 Flash is GA for coding agents at $0.75/1M input tokens; GPT-5.6 Sol Ultrafast previews at 14× speed (750 tokens/sec) via Cerebras.**

**Gemini 3.7 Flash (GA, August 13)** is Google's new workhorse model for coding and agentic workloads, available now in the Gemini API and AI Studio. The headline improvements: better issue resolution, fewer failed agent loops, and higher-fidelity code generated directly from design mocks. Introductory pricing: $0.75/1M input, $3.75/1M output through December 31, 2026.

The standout feature is tunable reasoning per request via `thinking_level` in `generation_config`:
- **Low** — fast, lowest cost; tool calls, lookups, simple steps
- **Medium** (default) — recommended for complex code and multi-step agents
- **High** — maximum reasoning; hard problems, complex debugging

**GPT-5.6 Sol Ultrafast (limited preview):** OpenAI is previewing a Cerebras-powered tier that runs GPT-5.6 Sol at up to 750 output tokens/second — roughly 14× faster than Standard processing. Available now to a select group of API customers; targets real-time and highly interactive applications.

**Why it matters:** Per-request thinking-level routing inside a single model endpoint is a cleaner pattern than managing multiple models. And 750 tokens/second changes what "real-time agent UI" means — streaming a full code diff in under two seconds becomes viable.

**Sources:** [Gemini 3.7 Flash — blog.google](https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-gemini-3-7-flash/) · [Gemini 3.7 Flash API docs](https://ai.google.dev/gemini-api/docs/models/gemini-3.7-flash) · [Previewing Ultrafast — openai.com](https://openai.com/index/previewing-ultrafast/)
]]></content:encoded>
    </item>

    <item>
      <title>See Inside Every Agent Step: LLM Observability with Arize Phoenix</title>
      <link>https://arize.com/docs/phoenix/get-started/get-started-tracing</link>
      <guid isPermaLink="false">2026-08-13-08-arize-phoenix-llm-tracing-observability</guid>
      <pubDate>Wed, 13 Aug 2026 20:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Agents</category>
      <category>Best Practices</category>
      <description>Arize Phoenix is a free, open-source LLM observability platform — three lines of code give you a local dashboard showing every LLM call, tool span, and eval score from your agent.</description>
      <content:encoded><![CDATA[
**Arize Phoenix is a free, open-source observability platform — three lines of code give you a local dashboard showing every LLM call, tool span, and eval score from your agent, with no signup or data sent off-device.**

**What you'll be able to do after this:**

- Start Phoenix locally with one command and see structured traces of every LLM call, tool use, and retrieval step — no account or API key required
- Attach built-in LLM-judge evaluators (hallucination, relevance, faithfulness) to live traces to find exactly where your agent produces bad outputs
- Auto-instrument a Claude Agent SDK session in three lines and watch spans appear live in the Phoenix UI as your agent runs

You can score outputs with promptfoo (already covered) or build assertions into your CI pipeline. Observability is the complementary layer that answers a different question: "what actually happened during that run?" When an agent goes off the rails, tracing shows you the exact token sequence, tool call chain, and retrieval result that led there — without adding debug logs everywhere.

**Install:**

```bash
pip install arize-phoenix arize-phoenix-otel openinference-instrumentation-anthropic
```

**Start Phoenix and instrument your Anthropic client in three lines:**

```python
import phoenix as px
from phoenix.otel import register

# Start the local Phoenix server — opens a UI at http://localhost:6006
px.launch_app()

# Auto-instrument all Anthropic API calls in this process
register(project_name="my-agent", auto_instrument=True)

# From here, every call you make appears as a trace
import anthropic
client = anthropic.Anthropic()

response = client.messages.create(
    model="claude-sonnet-5",
    max_tokens=1024,
    messages=[{"role": "user", "content": "Summarize the quarterly financials."}],
)
```

Open http://localhost:6006 and you'll see one trace: an `LLM` span with the prompt, response, token counts, and latency. Add a retrieval step and it nests as a child span. Add a tool call and it appears as a sibling. Every agent run builds the trace tree automatically.

**Run built-in evals against your traces:**

```python
from phoenix.evals import (
    HallucinationEvaluator,
    QACorrectnessEvaluator,
    run_evals,
)
from phoenix.session.evaluation import get_qa_with_reference

# Pull live traces from the running Phoenix session
df = get_qa_with_reference(px.active_session())

# Score them — Phoenix uses an LLM judge internally
results = run_evals(
    dataframe=df,
    evaluators=[HallucinationEvaluator(), QACorrectnessEvaluator()],
    provide_explanation=True,
)
```

Scores attach to each trace span in the UI. Filter by `hallucination == True` and you immediately see the exact inputs that caused it — the retrieval context, the user query, and the model response, all in one view.

**Three deployment modes:**

| Mode | Command | When to use |
|---|---|---|
| Ephemeral (in-process) | `px.launch_app()` | Notebooks, quick debug runs |
| Persistent server | `phoenix serve` | Local dev — data survives restarts |
| Self-hosted (Docker) | `docker run -p 6006:6006 arizephoenix/phoenix` | Team-shared trace store |

All three modes expose the same UI and accept the same trace data — switch deployment modes without changing your instrumentation code.

**Sources:** [Arize Phoenix docs](https://arize.com/docs/phoenix) · [Claude Agent SDK integration](https://arize.com/docs/phoenix/integrations/python/claude-agent-sdk) · [Quickstart: Tracing (Python)](https://arize.com/docs/phoenix/tracing/llm-traces-1/quickstart-tracing-python) · [YouTube tutorial: Arize Phoenix Overview](https://www.youtube.com/watch?v=5PXRRXM8Iqo)
]]></content:encoded>
      <media:content url="https://img.youtube.com/vi/5PXRRXM8Iqo/hqdefault.jpg" medium="image"/>
      <media:thumbnail url="https://img.youtube.com/vi/5PXRRXM8Iqo/hqdefault.jpg"/>
    </item>

    <item>
      <title>Claude Code Skills That Fix Their Own Mistakes: Building Verification Loops</title>
      <link>https://claude.com/blog/building-verification-loops-in-claude-code-with-skills</link>
      <guid isPermaLink="false">2026-08-13-07-claude-code-verification-loops-skills</guid>
      <pubDate>Wed, 13 Aug 2026 12:09:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Package your build-test-fix cycle as a Claude Code skill and every session checks and corrects its own work automatically — standalone, chained, or wired to a PreToolUse hook.</description>
      <content:encoded><![CDATA[
**Package your build-test-fix cycle as a Claude Code skill and every session checks and corrects its own work automatically — without waiting for you to prompt it.**

A verification loop is a cycle Claude Code runs until the work actually passes: take action → run checks → if failures, fix them → recheck → repeat. Out-of-the-box Claude Code catches tool errors inline, but packaging the loop as a dedicated skill makes it automatic, consistent, and composable across any session.

**Adding verification to CLAUDE.md (simplest form):**

```markdown
## Verification commands
After any code change, always run:
1. `pnpm run lint` — must exit 0
2. `pnpm run test --changed` — must exit 0 with no failing tests
3. `pnpm run build` — must produce no TypeScript errors
Fix any failures before considering the task complete.
```

Claude reads these instructions and applies them every session — no explicit `/verify` call needed.

**Packaging as a reusable skill:**

```markdown
<!-- .claude/skills/verify-pr/prompt.md -->
Run these checks in order. If any fail, fix the failures, then re-run from step 1.
Stop only when all three exit 0. Report: rounds taken and what was fixed each round.

1. `pnpm run lint`
2. `pnpm run test`
3. `pnpm run build`
```

Call it explicitly or chain it after another task in one prompt:

```
Refactor the auth module to use the new TokenService interface, then /verify-pr
```

Claude does the refactor, then immediately enters the verification loop — no separate prompt.

**Four patterns:**

| Pattern | When to reach for it |
|---|---|
| Embedded in CLAUDE.md | Default — applies to every session automatically |
| Standalone skill (`/verify`) | Cross-cutting checks: security scans, a11y audits |
| Chained after a task | Keeps implementation and verification in one prompt |
| Tied to a hook | Runs automatically before `git commit`, impossible to skip |

**Hook-based automatic verification:**

```json
// .claude/settings.json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git commit'; then pnpm run lint && pnpm run test; fi"
          }
        ]
      }
    ]
  }
}
```

This fires lint + tests automatically before any `git commit` Bash call — not just when Claude remembers to check.

**Cost note:** Each verification round uses tokens for check output + Claude's analysis. On a fast suite (<10s) the overhead is negligible; on a slow one, limit to changed files (`--changed`) so loops stay quick.

**Sources:** [Building verification loops in Claude Code with skills — claude.com](https://claude.com/blog/building-verification-loops-in-claude-code-with-skills) · [Hooks reference — Claude Code Docs](https://code.claude.com/docs/en/hooks) · [Skills — Claude Code Docs](https://code.claude.com/docs/en/skills)
]]></content:encoded>
    </item>

    <item>
      <title>Unsloth Desktop Ships: One App to Run and Fine-Tune Open LLMs Locally, No Terminal Required</title>
      <link>https://unsloth.ai/</link>
      <guid isPermaLink="false">2026-08-13-06-unsloth-desktop-local-llm-train-run</guid>
      <pubDate>Wed, 13 Aug 2026 12:08:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Self-Hosting</category>
      <category>Fine-Tuning</category>
      <description>Unsloth Desktop (Aug 11) is the first GUI that combines local model inference and fine-tuning in one app — 500+ HuggingFace models, Mac/Windows/Linux, 2x faster training, no terminal required.</description>
      <content:encoded><![CDATA[
**Unsloth Desktop (Aug 11) is the first GUI to combine local LLM inference and fine-tuning in one app — Mac, Windows, and Linux, 500+ models, no Python environment required.**

Until now, running and training models locally meant managing Python environments, CUDA drivers, and shell scripts. Unsloth Desktop (free, open-source) packages all of that into a native GUI.

**What it includes:**

- **Model discovery and download** — browse and pull 500+ models from HuggingFace Hub in-app, no CLI required
- **Local inference** — run GGUF, MLX, and diffusion models; expose any local model as an OpenAI-compatible endpoint so Claude Code sessions can route inference to your fine-tuned model
- **Fine-tuning** — GUI for SFT and QLoRA on your own data, using Unsloth's optimized kernels: ~2x faster training, ~70% less VRAM than standard TRL
- **Coding-agent connections** — tool-calling support; Claude Code can call locally hosted model endpoints from within a session

**How the workflow changes:**

Before: `pip install unsloth` → write a training script → manage CUDA/MLX → run in a terminal → debug via logs.

Now: open Desktop, pick a model, upload your JSONL dataset, set LoRA rank with a slider, click Train. Metrics stream inline.

**Why it matters:** Fine-tuning with Unsloth previously assumed Python and CUDA fluency. Desktop extends Unsloth's training power to engineers who want to customize models without owning a training environment. And because it serves a local OpenAI-compatible endpoint, Claude Code sessions can route to a locally fine-tuned model — no cloud latency, no data leaving the machine.

**Sources:** [Unsloth Desktop — unsloth.ai](https://unsloth.ai/) · [Unsloth Desktop ships: first mainstream local app that trains and runs models — Enterprise DNA](https://enterprisedna.co/resources/ai-pulse/ai-pulse-2026-08-12-unsloth-desktop-ships-as-the-first-mainstream-local-app-that/) · [Unsloth Desktop brings local AI training to three OSes — implicator.ai](https://www.implicator.ai/unsloth-desktop-local-ai-training-mac-windows-linux/)
]]></content:encoded>
    </item>

    <item>
      <title>Your First Persistent Vector Store in 10 Minutes: Getting Started with Chroma</title>
      <link>https://docs.trychroma.com/docs/overview/getting-started</link>
      <guid isPermaLink="false">2026-08-13-05-chroma-persistent-vector-store-getting-started</guid>
      <pubDate>Wed, 13 Aug 2026 12:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Embeddings</category>
      <category>Vectors</category>
      <description>Chroma gives you a persistent, auto-embedding vector store in three commands — data survives restarts, grows with upserts, filters with metadata, and flips to server mode without changing your app code.</description>
      <content:encoded><![CDATA[
**Chroma gives you a persistent, auto-embedding vector store in three commands — data survives restarts, grows with upserts, filters with metadata, and flips to a shared server without changing your app code.**

**What you'll be able to do after this:**

- Run a persistent vector store on your laptop that survives restarts without re-indexing
- Add documents in plain text and query with natural language — Chroma handles embedding automatically using `sentence-transformers`
- Switch from a local disk store to a shared server mode when you need to scale, with zero changes to your application code

You've already seen [pgvector](https://cloudcodetree.com/ai-news/2026-08-09-04-pgvector-supabase-vector-store) (embedded in Postgres) and [FAISS](https://cloudcodetree.com/ai-news/2026-08-11-07-faiss-local-vector-index-python) (in-memory C++ index). Chroma sits between them for local development: it persists to disk, has built-in embedding, and exposes a higher-level collections API — without requiring a running Postgres server.

**Three client modes:**

| Mode | When to use |
|---|---|
| `chromadb.EphemeralClient()` | Testing, notebooks — data is in memory only |
| `chromadb.PersistentClient(path="./db")` | Local dev — data survives restarts |
| `chromadb.HttpClient(host=..., port=...)` | Production — shared store across services |

**Walk-through: build a persistent knowledge base**

```bash
pip install chromadb
```

```python
import chromadb

# Persistent client — data survives restarts
client = chromadb.PersistentClient(path="./knowledge-base")

# Create or open a collection
# Default embedding: sentence-transformers/all-MiniLM-L6-v2 (auto-downloaded)
collection = client.get_or_create_collection("docs")

# Add documents — Chroma embeds them automatically
collection.add(
    documents=[
        "Engineering owns the software development lifecycle and technical architecture.",
        "Product management defines the roadmap and prioritizes features based on user research.",
        "Design creates the visual language, interaction patterns, and component library.",
    ],
    ids=["eng", "pm", "design"],   # stable, unique ids required
)

print(collection.count())  # → 3
```

**Query with natural language:**

```python
results = collection.query(
    query_texts=["who decides what features to build next?"],
    n_results=2
)
print(results["documents"])
# → [["Product management defines the roadmap...", "Engineering owns..."]]
```

**Maintaining the knowledge base over time:**

```python
# Update a document (Chroma re-embeds automatically)
collection.update(
    ids=["pm"],
    documents=["Product management owns the roadmap and quarterly OKRs."]
)

# Upsert — add if new, update if exists
collection.upsert(
    ids=["security"],
    documents=["The security team owns threat modeling, pen testing, and incident response."]
)

# Delete a stale entry
collection.delete(ids=["design"])
```

**Metadata filtering (scope queries to a subset):**

```python
collection.add(
    documents=["Our API is rate-limited to 100 req/s per tenant."],
    metadatas=[{"team": "infra", "updated": "2026-08"}],
    ids=["api-limits"]
)

# Filter by metadata in the query
results = collection.query(
    query_texts=["rate limits"],
    where={"team": "infra"},
    n_results=1
)
```

**Switching to server mode:**

```bash
# Start the Chroma server — same ./knowledge-base directory
chroma run --path ./knowledge-base --port 8000
```

```python
# Switch the client — zero changes to the rest of your app code
client = chromadb.HttpClient(host="localhost", port=8000)
# The collection API is identical; app code doesn't need to know
```

The same `collection.add()`, `collection.query()`, and `collection.upsert()` calls work whether you're hitting an in-process store, a local disk, or a remote Chroma server — so you can prototype locally and deploy without a rewrite.

**Sources:** [Getting Started — Chroma Docs](https://docs.trychroma.com/docs/overview/getting-started) · [Usage Guide — Chroma Docs](https://docs.trychroma.com/guides) · [Chroma Integrations](https://docs.trychroma.com/integrations)
]]></content:encoded>
    </item>

    <item>
      <title>One Prompt, One Free GPU: Tell Your Agent to Fine-Tune a Model With the Google Colab CLI</title>
      <link>https://huggingface.co/blog/sergiopaniego/trl-colab-cli</link>
      <guid isPermaLink="false">2026-08-13-04-colab-cli-agent-gpu-finetuning</guid>
      <pubDate>Wed, 13 Aug 2026 04:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Agents</category>
      <category>Developer Tools</category>
      <description>The Google Colab CLI makes Colab GPUs scriptable from any terminal — hand an agent a dataset prompt and get a fine-tuned adapter pushed to the Hub, free, no notebook UI required.</description>
      <content:encoded><![CDATA[
**The Google Colab CLI makes Colab GPUs scriptable from any terminal — hand an agent a dataset prompt and get a fine-tuned adapter pushed to the Hub, free.**

[Google's Colab CLI](https://colab.research.google.com/notebooks/colab-cli.ipynb) (`pip install colab`) removes the notebook UI from the equation. Any process that can run shell commands — Claude Code, a LangChain agent, a Python script — can provision a free T4 GPU, run training code on it, stream live metrics, and push results, all without a browser.

HuggingFace published the proof of concept: an agent was given one instruction and handled every step.

**What the agent did, step by step:**

1. `colab start` — provisioned a free T4 runtime
2. Installed TRL, trackio, and bitsandbytes on the remote Colab session
3. Authenticated with HuggingFace Hub inside the runtime
4. Read TRL's SFTTrainer examples, then wrote a QLoRA training script from them
5. Ran training; wired `trackio` to stream the loss curve to a HuggingFace Space in real time
6. `push_to_hub=True` — LoRA adapter uploaded when training finished
7. `colab stop` — runtime cleaned up

Total cost: $0. Base model: `Qwen2.5-0.5B-Instruct`; the whole run finished in a few minutes on a free T4.

**Try it from a Claude Code session:**

```bash
pip install colab
colab login   # authenticates with your Google account
```

Then give Claude Code this prompt:

```
Fine-tune Qwen2.5-0.5B-Instruct on ./data/train.jsonl using TRL SFTTrainer
with QLoRA (r=16, alpha=32). Run on a Colab T4 via the Colab CLI, stream
metrics with trackio, push the adapter to <your-hf-username>/<repo-name>.
```

Claude Code reads the TRL docs, writes the script, provisions and runs the runtime, and reports back when the adapter is live on the Hub.

**Why this pattern matters:** agentic ML doesn't require you to own GPUs. The Colab CLI makes free compute programmable — combine it with an agent that knows the training APIs and you get a zero-infrastructure fine-tuning workflow that any developer can run today.

**Sources:** [I fine-tuned a model for free from one prompt, with TRL and the Google Colab CLI — HuggingFace Blog](https://huggingface.co/blog/sergiopaniego/trl-colab-cli) · [TRL SFTTrainer docs — HuggingFace](https://huggingface.co/docs/trl/sft_trainer)
]]></content:encoded>
    </item>

    <item>
      <title>TRL's GRPO Trainer Gets Continuous Batching: ~1.25x Throughput for Reasoning RL With No Extra Infrastructure</title>
      <link>https://huggingface.co/blog/sergiopaniego/cb-trl-grpo</link>
      <guid isPermaLink="false">2026-08-13-03-trl-grpo-continuous-batching</guid>
      <pubDate>Wed, 13 Aug 2026 04:06:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Fine-Tuning</category>
      <category>HuggingFace</category>
      <description>TRL's GRPOTrainer now uses continuous batching for rollout generation, recycling GPU slots as sequences finish — ~1.25x throughput on N≥32 reasoning workloads with no vLLM server required.</description>
      <content:encoded><![CDATA[
**TRL's GRPOTrainer now supports continuous batching for rollout generation, cutting idle GPU time during reasoning RL training with no server setup.**

GRPO training is generation-heavy: producing the N rollouts (completions the model judges itself on) is typically the bottleneck. Standard batching waits for all N sequences to finish; continuous batching fills idle GPU slots as sequences complete, keeping utilization high. On an A100 80GB with Llama-3.2-1B and GSM8K at N=32: **~1.25x throughput** versus standard batching, with lower peak memory at the same time.

It's a drop-in upgrade — set `use_vllm=False` in `GRPOConfig` (continuous batching is now the in-process default). TRL pre-allocates `max_memory_percent=0.5` of free VRAM for rollouts, leaving headroom for the backward pass. Async continuous batching — overlapping generation with the gradient update — is in progress (PR #5781).

**Why it matters:** Reasoning RL fine-tuning (GRPO, DAPO) is the fastest-growing method for building capable open models. Any free throughput gain in the rollout phase directly reduces GPU-hours per training run.

**Sources:** [Continuous batching for GRPO, now in TRL — HuggingFace Blog](https://huggingface.co/blog/sergiopaniego/cb-trl-grpo) · [GRPOTrainer — HuggingFace](https://huggingface.co/docs/trl/en/grpo_trainer)
]]></content:encoded>
    </item>

    <item>
      <title>Claude's Compliance API Now Covers Claude Code and Cowork Sessions — Full Audit Trail for Agentic Dev Tools</title>
      <link>https://claude.com/blog/compliance-api-cowork-and-claude-code</link>
      <guid isPermaLink="false">2026-08-13-02-compliance-api-claude-code-cowork</guid>
      <pubDate>Wed, 13 Aug 2026 04:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Claude Code</category>
      <category>Developer Tools</category>
      <description>Anthropic's Compliance API now covers Claude Code (CLI and desktop) and Cowork sessions in Enterprise beta — prompts, responses, and tool activity in one endpoint, same access key.</description>
      <content:encoded><![CDATA[
**Claude's Compliance API now returns full session transcripts for Claude Code (CLI and desktop) and Cowork (web, mobile, desktop) — one endpoint, same access key, no new integration.**

Announced August 11, the extension is in beta for Claude Enterprise customers. Security and compliance teams already using the Compliance API for regular Claude chat get Claude Code and Cowork coverage automatically — no separate access key, no changes to existing integrations.

**What each session record includes:**
- Full prompt and response text
- Tool call content: web browsing, MCP connector calls, skill runs, artifacts
- Metadata: verified user ID, organization ID, session and per-message IDs, timestamps

**Current beta scope:** Claude Code CLI and desktop app; Cowork desktop, web, and mobile. Not yet covered: Claude Code on the web, API-accessed Claude Code, or sessions on Bedrock, Vertex AI, or Foundry.

**Why it matters:** For HIPAA, FedRAMP, and SOC 2 teams, full audit trail coverage of agentic coding sessions closes the last gap between "we use Claude Code" and "we can prove what Claude Code did." This is the feature that unlocks Claude Code for regulated-industry engineering teams without a custom ATO process.

**Sources:** [Compliance API coverage extends to Claude Cowork and Claude Code — Anthropic](https://claude.com/blog/compliance-api-cowork-and-claude-code) · [Compliance API docs — Claude Platform](https://platform.claude.com/docs/en/manage-claude/compliance-api)
]]></content:encoded>
    </item>

    <item>
      <title>Fine-Tune Any LLM Without Writing Training Code: HuggingFace AutoTrain in 5 Minutes</title>
      <link>https://huggingface.co/docs/autotrain/tasks/llm_finetuning</link>
      <guid isPermaLink="false">2026-08-13-01-autotrain-no-code-llm-finetuning</guid>
      <pubDate>Wed, 13 Aug 2026 04:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Fine-Tuning</category>
      <category>HuggingFace</category>
      <description>HuggingFace AutoTrain lets you SFT, DPO, or ORPO any open LLM through a web UI or single CLI command — no training code, no server setup, LoRA adapter pushed to Hub automatically.</description>
      <content:encoded><![CDATA[
**HuggingFace AutoTrain lets you SFT, DPO, or ORPO any open model in a web UI or single `autotrain` command — no training code, no server, adapter pushed to Hub automatically.**

**What you'll be able to do after this:**

- Launch a supervised fine-tuning (SFT), DPO, or ORPO run on any open LLM without writing training code
- Prepare your dataset as JSONL and configure LoRA hyperparameters through AutoTrain's UI or a single CLI command
- Have the fine-tuned LoRA adapter automatically pushed to your HuggingFace Hub repo when training finishes

[AutoTrain Advanced](https://huggingface.co/autotrain) is HuggingFace's hosted fine-tuning platform. Pick a model, upload data, configure a few sliders — and it writes and runs the training loop for you. No `SFTTrainer` boilerplate, no CUDA debugging, no manual Hub upload.

**Step 1: Prepare your dataset**

AutoTrain expects JSONL with one training example per line. For SFT:

```json
{"prompt": "Classify the sentiment of this review:", "completion": "Positive"}
{"prompt": "Summarize in one sentence:", "completion": "A bear visited a bakery."}
```

For DPO, each row needs `prompt`, `chosen`, and `rejected` fields.

**Step 2: Create a project**

Go to [huggingface.co/autotrain](https://huggingface.co/autotrain) → **Create new project** → **LLM** → pick your base model (e.g. `Qwen/Qwen2.5-7B-Instruct` or `meta-llama/Llama-3.2-3B-Instruct`) → upload JSONL → pick hardware (ZeroGPU is free for small runs) → **Start Training**.

AutoTrain provisions compute, installs the right CUDA stack, and streams training logs in the Space UI.

**Or use the CLI (works locally or in CI):**

```bash
pip install autotrain-advanced

autotrain llm \
  --train \
  --model "Qwen/Qwen2.5-3B-Instruct" \
  --data-path ./data \
  --trainer sft \
  --lora-r 16 \
  --lora-alpha 32 \
  --lr 2e-4 \
  --batch-size 2 \
  --gradient-accumulation 4 \
  --num-train-epochs 3 \
  --push-to-hub \
  --username your-hf-username \
  --token $HF_TOKEN
```

When training ends the LoRA adapter lands in your Hub repo — ready to merge with `peft.merge_and_unload()` or serve directly via AutoTrain's inference endpoint.

**Safe starting defaults:**
- `--lora-r 16`, `--lora-alpha 32`: balanced expressivity vs. adapter size
- `--lr 2e-4`: standard for QLoRA SFT
- `--batch-size 2` + `--gradient-accumulation 4`: effective batch size 8, fits on a 16GB GPU

**Sources:** [AutoTrain LLM Fine-tuning — HuggingFace Docs](https://huggingface.co/docs/autotrain/tasks/llm_finetuning) · [Low-Code LLM Alignment with AutoTrain — HuggingFace Blog](https://huggingface.co/blog/burtenshaw/ui-finetune-llm-autotrain)
]]></content:encoded>
    </item>

    <item>
      <title>Warp Ships a Standalone Agent CLI: pty-Multiplexed Terminal Agent Runs Alongside Claude Code</title>
      <link>https://www.warp.dev/blog/introducing-the-warp-agent-cli-coding-agent</link>
      <guid isPermaLink="false">2026-08-12-10-warp-agent-cli-terminal-native-alongside-claude-code</guid>
      <pubDate>Wed, 12 Aug 2026 20:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Agents</category>
      <category>Developer Tools</category>
      <description>Warp Agent CLI (Aug 4) is a standalone coding agent that installs in any terminal via curl, wraps a pty multiplexer around your shell, and complements Claude Code for interactive terminal tasks.</description>
      <content:encoded><![CDATA[
**Warp shipped a standalone Agent CLI (installs in any terminal) on August 4 — pty-native control for interactive-terminal tasks that pure file-editing agents can't handle.**

[Warp Agent CLI](https://www.warp.dev/agent-cli) separates the agent from the Warp Terminal application. Install it in Ghostty, iTerm, VS Code, or any system shell with one line:

```bash
curl -fsSL https://app.warp.dev/download/agent-cli/install.sh | sh
# Windows: winget install Warp.WarpAgentCLI
```

Pricing: included in Warp subscriptions ($18/mo with $20 inference credits), ad-hoc credits from $10, or bring your own API key.

**What's architecturally different from Claude Code**

Claude Code reads files, writes code, and runs shell commands — it treats the terminal as an I/O pipe. Warp Agent CLI runs a pty (pseudoterminal) multiplexer between itself and your shell. That means the agent sees and drives full-screen terminal UIs:

- A `sqlite3` REPL — type SQL, read results interactively, step through a migration
- `gdb` with breakpoints set and stepped
- `htop` or a live log tail it monitors over time
- Any interactive prompt that requires keystrokes, not just piped command output

Claude Code can approximate some of these with enough prompting, but pty-native control is deterministic.

**A pattern that works: both in parallel**

```bash
# Tab 1 — Warp Agent watching a live test suite
warp-agent "run pytest in watch mode; alert me when coverage drops below 80%"

# Tab 2 — Claude Code doing a large refactor in the same repo
claude "rename every AuthService method from camelCase to snake_case and update all callers"
```

Each sends desktop notifications when it needs input. They don't block each other: Warp holds the interactive terminal loop, Claude Code holds the file-editing loop. Warp's own docs show this as a supported pattern — Claude Code runs as a CLI agent *inside* Warp tabs, with Warp adding native notification and workspace split.

**When to reach for which**

| Task | Reach for |
|---|---|
| Read files, write code, manage git | Claude Code |
| Interactive REPL / debugger / live monitor | Warp Agent CLI |
| Long-horizon autonomous task (hours) | Claude Code (`/goal` or `--continue`) |
| Multi-agent orchestration across terminals | Warp Agent CLI (built-in cloud handoff) |

**Why it matters:** The Warp Agent CLI fills the "interactive tool driving" gap in terminal-only setups. If your workflow has tasks that need pty control — database migrations, interactive test runners, debugger sessions — you now have a CLI-native option that works in whatever terminal you already use.

**Sources:** [Warp Agent CLI launch post](https://www.warp.dev/blog/introducing-the-warp-agent-cli-coding-agent) · [Warp Agent CLI docs](https://docs.warp.dev/cli/) · [Warp vs Claude Code — warp.dev](https://docs.warp.dev/guides/agent-workflows/warp-vs-claude-code/)
]]></content:encoded>
      <media:content url="https://img.youtube.com/vi/8jDz-1l88K8/maxresdefault.jpg" medium="image"/>
      <media:thumbnail url="https://img.youtube.com/vi/8jDz-1l88K8/maxresdefault.jpg"/>
    </item>

    <item>
      <title>One Command Launches Any Embedding Model as an OpenAI-Compatible API: Getting Started with Infinity</title>
      <link>https://github.com/michaelfeil/infinity</link>
      <guid isPermaLink="false">2026-08-12-09-infinity-embedding-server-self-hosted-openai-api</guid>
      <pubDate>Wed, 12 Aug 2026 20:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Self-Hosting</category>
      <category>Embeddings</category>
      <description>Self-host BAAI/bge, nomic-embed-text, or any sentence-transformer as an OpenAI-compatible embedding endpoint using Infinity — three commands, dynamic batching, CPU/CUDA/MPS.</description>
      <content:encoded><![CDATA[
**Self-host any sentence-transformer as an OpenAI-compatible embedding API in one command — Infinity adds dynamic batching and flash attention, your app changes nothing.**

If your RAG pipeline calls `openai.embeddings.create()`, swapping to a self-hosted model costs you nothing in application code. [Infinity](https://github.com/michaelfeil/infinity) speaks the OpenAI Embeddings wire format exactly — the only change is `base_url`.

**What you'll be able to do after this:**
- Replace `text-embedding-ada-002` in any RAG pipeline with a free BAAI/bge or nomic-embed model — no code changes in your app
- Serve embedding and reranking models together on a single port
- Run offline on CPU, Apple MPS, or CUDA for near-zero cost per batch

**Three commands to launch**

Pick whichever fits your environment:

```bash
# Option 1: pip — fastest to try
pip install "infinity-emb[all]"
infinity_emb v2 --model-id BAAI/bge-en-icl --port 7997

# Option 2: Docker with a CUDA GPU
docker run --gpus all -p 7997:7997 \
  michaelf34/infinity:latest v2 \
  --model-id BAAI/bge-en-icl --port 7997

# Option 3: CPU / Apple Silicon — drop the --gpus flag
docker run -p 7997:7997 \
  michaelf34/infinity:latest v2 \
  --model-id nomic-ai/nomic-embed-text-v1.5 --port 7997
```

The model downloads from HuggingFace Hub on first run and caches in `~/.cache/huggingface/`. Subsequent starts are instant.

**Zero code changes in your existing app**

```python
from openai import OpenAI

client = OpenAI(
    api_key="local",                    # Infinity ignores this; any string works
    base_url="http://localhost:7997/v1"
)

response = client.embeddings.create(
    model="BAAI/bge-en-icl",           # must match --model-id above
    input=["How does attention work?", "What is a transformer?"]
)
print(len(response.data[0].embedding))  # 4096 — same structure as the OpenAI response
```

LangChain's `OpenAIEmbeddings`, LlamaIndex, and LiteLLM all work with this endpoint unchanged — pass `openai_api_base="http://localhost:7997/v1"` in whichever client you use.

**Serve an embedder and a reranker on the same port**

Add a second `--model-id` to serve both at once:

```bash
infinity_emb v2 \
  --model-id BAAI/bge-en-icl \
  --model-id BAAI/bge-reranker-v2-m3 \
  --port 7997
```

Embeddings arrive at `/v1/embeddings`; reranking at `/v1/rerank` — same server, same port, no extra process.

**Why BAAI/bge-en-icl?**

It sits above `text-embedding-3-large` on the [MTEB leaderboard](https://huggingface.co/spaces/mteb/leaderboard) for most retrieval tasks, and it's free. With Infinity's dynamic batching, a 10k-token batch on a CPU-only `t3.medium` finishes in under two seconds. On a T4 GPU, it's milliseconds. At zero cost per call, the math is obvious once your evaluation dataset grows past a few thousand entries.

Infinity is MIT-licensed and actively maintained; it powers the embedding API behind Gradient.ai and several other hosted providers.

**Sources:** [GitHub — michaelfeil/infinity](https://github.com/michaelfeil/infinity) · [Infinity docs](https://michaelfeil.eu/infinity/0.0.72/) · [MTEB Leaderboard — HuggingFace](https://huggingface.co/spaces/mteb/leaderboard) · [Infinity on AMD — HuggingFace Blog](https://huggingface.co/blog/michaelfeil/infinity-amd)
]]></content:encoded>
    </item>

    <item>
      <title>Cut Claude API Costs 50% on Any Async Workload: The Message Batches API in 20 Lines</title>
      <link>https://platform.claude.com/docs/en/build-with-claude/batches</link>
      <guid isPermaLink="false">2026-08-12-07-anthropic-batch-api-50pct-async-workloads</guid>
      <pubDate>Wed, 12 Aug 2026 12:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Best Practices</category>
      <category>Developer Tools</category>
      <description>Any async workload — nightly evals, doc pipelines, bulk code analysis — can run at exactly 50% off standard Claude token prices using the Message Batches API. Submit up to 100K requests, retrieve results within hours.</description>
      <content:encoded><![CDATA[
**The Anthropic Message Batches API runs up to 100,000 Claude requests asynchronously at exactly 50% off standard token prices — submit, wait a few hours, retrieve.**

Any workload that doesn't need instant results — nightly evaluations, document pipelines, offline enrichment, bulk code analysis — is paying double. The [Message Batches API](https://platform.claude.com/docs/en/build-with-claude/batches) costs 50% less per token with zero quality difference.

## How it works

You submit a list of `MessageBatchRequest` objects, each with a `custom_id` and the same parameters as a standard `/messages` call. Anthropic processes them asynchronously (typically 1-2 hours, max 24h) and makes results available via the batch id. No streaming, no webhooks — you poll for status or come back later.

```python
import anthropic

client = anthropic.Anthropic()
docs = ["Article 1 text...", "Article 2 text...", "Article 3 text..."]

# Submit the batch
batch = client.messages.batches.create(
    requests=[
        {
            "custom_id": f"doc-{i}",
            "params": {
                "model": "claude-sonnet-5-20260801",
                "max_tokens": 256,
                "messages": [{"role": "user", "content": f"Summarize in 2 sentences: {docs[i]}"}]
            }
        }
        for i in range(len(docs))
    ]
)
print(f"Batch {batch.id} submitted — status: {batch.processing_status}")
```

**Poll until complete:**

```python
import time

while True:
    status = client.messages.batches.retrieve(batch.id)
    if status.processing_status == "ended":
        break
    print(f"Still processing... ({status.request_counts.processing} remaining)")
    time.sleep(60)

# Retrieve and print results
for result in client.messages.batches.results(batch.id):
    if result.result.type == "succeeded":
        print(result.custom_id, result.result.message.content[0].text[:120])
    else:
        print(result.custom_id, "FAILED:", result.result.error.type)
```

## Cost math

A nightly pipeline summarizing 1,000 documents (2,000 input + 500 output tokens each) on Sonnet 5:

| Approach | Input | Output | Total/day | Annual |
|---|---|---|---|---|
| Synchronous | $4.00 | $5.00 | $9.00 | $3,285 |
| Batch API | $2.00 | $2.50 | $4.50 | $1,642 |

Add prompt caching on a shared system prompt and the cached tokens cost another 90% less, pushing complex pipelines toward 65-70% total savings.

## Best uses

Batch is ideal when latency doesn't matter: code analysis across a full repo at PR-merge time, generating descriptions for 10K embeddings, running eval suites against 1,000 test cases, nightly newsletter digests, classifying a backlog of support tickets.

Not for: real-time chat, streaming responses, any user-facing flow where waiting hours isn't acceptable.

**Sources:** [Message Batches API — Claude Platform Docs](https://platform.claude.com/docs/en/build-with-claude/batches) · [Anthropic Batch API: process thousands of prompts at 50% cost — CodeWords](https://www.codewords.ai/blog/anthropic-batch-api) · [Claude Batch API Tutorial — AI for Anything](https://aiforanything.io/blog/claude-batch-api-tutorial-high-volume-processing-2026)
]]></content:encoded>
    </item>

    <item>
      <title>Claude Code v2.1.229: Agent-to-Agent Messages Now Permission-Screened; Managed Agents Get Budget Caps</title>
      <link>https://code.claude.com/docs/en/changelog</link>
      <guid isPermaLink="false">2026-08-12-06-claude-code-v2129-agent-security-managed-budget</guid>
      <pubDate>Wed, 12 Aug 2026 12:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Claude Code v2.1.229 now runs permission-classifier checks on inter-agent SendMessage calls, closing a prompt-injection vector in multi-agent pipelines. Managed Agents also ships budget caps and geo controls.</description>
      <content:encoded><![CDATA[
**Claude Code v2.1.229 screens agent-to-agent messages through the permission classifier before dispatch, closing a prompt-injection vector; Managed Agents adds per-session budget caps and geo controls.**

**Claude Code v2.1.229 (August 12)** adds a meaningful security layer for multi-agent workloads: any message dispatched to another Claude Code session via `SendMessage` now passes through the same permission classifier used for local tool calls before it reaches the target session. A compromised sub-agent can no longer silently instruct a parent session to run arbitrary bash or write to sensitive paths via a crafted message payload. The release also hardens synced skill/plugin directory naming on Windows, patches Remote Control session resume leaks, and restores CCR web-fetch/search proxy functionality after `/clear`.

**Managed Agents** — Anthropic's server-hosted agent sessions accessed via the API — now include per-session budget caps (set a token-spend ceiling in your API call; the agent stops and returns a structured error when reached) and geo controls (pin session processing to a specific AWS region for data residency compliance). GitHub-loaded skills — pointing a Managed Agent at a repo to load its `.claude/skills/` directory on startup — are also now available.

**Why it matters:** The `SendMessage` permission screen is the kind of hardening that separates a toy multi-agent project from a production one. If your pipeline has agents that talk to each other, update to v2.1.229 before shipping to prod.

**Sources:** [Claude Code changelog](https://code.claude.com/docs/en/changelog) · [Anthropic Release Notes — Releasebot](https://releasebot.io/updates/anthropic)
]]></content:encoded>
    </item>

    <item>
      <title>Give Your AI Agent a Memory: Persistent Context Across Sessions With Mem0</title>
      <link>https://docs.mem0.ai/open-source/python-quickstart</link>
      <guid isPermaLink="false">2026-08-12-05-mem0-agent-persistent-memory-python</guid>
      <pubDate>Wed, 12 Aug 2026 12:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Agents</category>
      <category>Best Practices</category>
      <description>Mem0 is an open-source memory layer that extracts, deduplicates, and retrieves facts from your agent conversations — add persistent cross-session memory to any Claude agent in 15 minutes.</description>
      <media:content url="https://mem0.ai/og.png" medium="image" />
      <media:thumbnail url="https://mem0.ai/og.png" />
      <content:encoded><![CDATA[
**Mem0 is an open-source memory layer that extracts, deduplicates, and surfaces facts from your agent conversations — add cross-session persistent memory to any Claude agent in 15 minutes.**

**What you'll be able to do after this:**
- Store and retrieve user-specific facts across any number of agent sessions
- Integrate Mem0 with a Claude agent using `m.add()` and `m.search()` in under 20 lines
- Self-host Mem0 with a local Qdrant vector store — no external API key required

Every stateless agent faces the same wall: the context window resets each session. Your user told it their preferred stack, their company's coding conventions, and their deadline — three conversations ago. Mem0 fixes this by sitting between your agent and each conversation, extracting discrete facts, deduplicating them against what's already stored, and surfacing the relevant ones at the start of every new session.

## Install and first memory

```bash
pip install mem0ai
```

The open-source version uses a local Qdrant vector store and your LLM of choice. Swap in Claude Haiku 4.5 as the extraction LLM for cost-efficient memory operations:

```python
from mem0 import Memory

config = {
    "llm": {
        "provider": "anthropic",
        "config": {
            "model": "claude-haiku-4-5-20251001",
            "api_key": "your-anthropic-key"
        }
    }
}

m = Memory.from_config(config)

# Add a conversation — Mem0 extracts and stores salient facts
result = m.add([
    {"role": "user",      "content": "I work on a Python monorepo with 4 microservices."},
    {"role": "assistant", "content": "Got it, I'll tailor examples to Python."}
], user_id="alice")

print(result)
# [{"event": "ADD", "memory": "Works on a Python monorepo with 4 microservices"}]
```

## Retrieve relevant memories before each call

```python
# In a new session — pull what's relevant before calling Claude
memories = m.search("what tech stack does this user prefer?", user_id="alice")
context = "\n".join(f"- {r['memory']}" for r in memories[:5])
print(context)  # - Works on a Python monorepo with 4 microservices
```

## Wire Mem0 into a Claude agent

```python
import anthropic
from mem0 import Memory

mem = Memory.from_config(config)
claude = anthropic.Anthropic()

def chat(user_message: str, user_id: str) -> str:
    # 1. Pull relevant memories
    memories = mem.search(user_message, user_id=user_id)
    ctx = "\n".join(f"- {r['memory']}" for r in memories[:5])

    # 2. Inject into system prompt
    system = f"Known context about this user:\n{ctx}" if ctx else "You are a helpful assistant."
    response = claude.messages.create(
        model="claude-sonnet-5-20260801",
        max_tokens=1024,
        system=system,
        messages=[{"role": "user", "content": user_message}]
    )
    answer = response.content[0].text

    # 3. Store the exchange so memory grows with each conversation
    mem.add([
        {"role": "user",      "content": user_message},
        {"role": "assistant", "content": answer}
    ], user_id=user_id)
    return answer
```

## Self-host with Docker (Qdrant backend)

No external API keys for storage:

```bash
git clone https://github.com/mem0ai/mem0
cd mem0
docker-compose up -d   # Qdrant on :6333, Mem0 REST API on :8000
```

Then point the SDK at your local server: `Memory(host="http://localhost:8000")`.

## How Mem0 V3 keeps memory coherent

Mem0 V3 (April 2026) uses a single-pass extraction pipeline: each new fact from a conversation is checked against existing memories and emits `ADD`, `UPDATE`, `DELETE`, or `NONE`. After 100 conversations the store stays clean and relevant — not bloated with contradictions or duplicates. Retrieval hits 92.5 on the LoCoMo long-context memory benchmark.

**Sources:** [Mem0 Python Quickstart — docs.mem0.ai](https://docs.mem0.ai/open-source/python-quickstart) · [The Easiest Way to Add Persistent Memory — Mem0 blog](https://mem0.ai/blog/the-easiest-way-to-add-persistent-memory-to-any-ai-agent) · [mem0ai/mem0 — GitHub](https://github.com/mem0ai/mem0)
]]></content:encoded>
    </item>

<item>
      <title>Three Models, One Pipeline: Claude Code's Per-Subagent Model Routing Cuts Costs 60-80%</title>
      <link>https://code.claude.com/docs/en/model-config</link>
      <guid isPermaLink="false">2026-08-12-04-claude-code-subagent-model-routing</guid>
      <pubDate>Wed, 12 Aug 2026 04:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Assigning Haiku 4.5, Sonnet 5, and Fable 5 to the right tasks in a Claude Code Workflow SDK pipeline cuts API costs 60-80% — decision framework and runnable SDK code.</description>
      <content:encoded><![CDATA[
**Assigning Haiku 4.5, Sonnet 5, and Fable 5 to the right tasks in a multi-agent workflow cuts API costs 60-80% with no quality loss — here's the decision framework with copy-paste SDK code.**

In Claude Code's Workflow SDK, every `agent()` call defaults to the session model (Sonnet 5). But Haiku 4.5 runs at $1/$5 per million tokens — half the cost of Sonnet 5 — and handles any task where reasoning depth isn't the bottleneck: file reads, searches, grep-equivalent scans, and simple classification. Reserving Fable 5 for decisions where a wrong answer is expensive keeps architectural reviews high-quality without applying that cost across every agent in the fan-out.

**Decision framework:**

| Task | Model | Why |
|---|---|---|
| Search, grep, file reads | Haiku 4.5 | Content retrieval — speed over depth |
| Implement, debug, write | Sonnet 5 (default) | Strong code reasoning at locked-in $2/$10 |
| Architectural decisions | Fable 5 | Complex judgment worth the extra cost |
| Binary checks (test pass/fail) | Haiku 4.5 | One-token output; waste to use Sonnet |

**SDK code:**

```javascript
export const meta = {
  name: 'routed-workflow',
  description: 'Model-routed multi-agent example',
  phases: [{ title: 'Search' }, { title: 'Implement' }, { title: 'Review' }],
}

// Fan out cheap searches with Haiku
const findings = await parallel(files.map(f => () =>
  agent(`List all auth-related functions in ${f}`, {
    model: 'claude-haiku-4-5-20251001',
    label: `scan:${f}`,
    phase: 'Search',
  })
))

// Implement on default Sonnet 5
const impl = await agent(
  `Implement auth changes based on: ${findings.filter(Boolean).join('\n')}`,
  { phase: 'Implement' }
)

// Escalate one architectural review to Fable
const review = await agent(
  `Review this auth implementation for security regressions and propose alternatives:\n${impl}`,
  { model: 'claude-fable-5', phase: 'Review' }
)
```

**Cost math example.** 20 files × 3K tokens (Haiku) + 1 implementation at 50K tokens (Sonnet 5) + 1 architectural review at 25K tokens (Fable):
- Haiku searches: 60K tokens × $1/M = **$0.06**
- Sonnet implementation: 50K × $2/M = **$0.10**
- Fable review: 25K × ~$15/M = **$0.38**
- Total: **$0.54** vs $1.50+ if everything ran on Fable

With Sonnet 5's pricing [now locked in permanently at $2/$10](https://x.com/claudeai/status/2086891169217122586), these economics are stable — build the routing now.

**Sources:** [Claude Code model config — code.claude.com](https://code.claude.com/docs/en/model-config) · [Claude model pricing — anthropic.com](https://www.anthropic.com/pricing) · [Multi-model routing guide — buildthisnow.com](https://www.buildthisnow.com/de/blog/guide/development/claude-code-model-routing-subagents)
]]></content:encoded>
      <media:content url="https://opengraph.githubassets.com/1/anthropics/claude-code" medium="image" />
      <media:thumbnail url="https://opengraph.githubassets.com/1/anthropics/claude-code" />
    </item>

    <item>
      <title>Sonnet 5 Stays at $2/$10 Per Million Tokens: Anthropic Cancels the September Price Increase</title>
      <link>https://x.com/claudeai/status/2086891169217122586</link>
      <guid isPermaLink="false">2026-08-12-03-sonnet5-pricing-permanent-anthropic</guid>
      <pubDate>Wed, 12 Aug 2026 04:06:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Developer Tools</category>
      <category>LLM</category>
      <description>Anthropic made Sonnet 5's $2/$10 per million token introductory pricing permanent on August 10 -- the planned 50% September 1 increase is officially canceled.</description>
      <content:encoded><![CDATA[
**Anthropic locked Sonnet 5 at $2/$10 per million tokens permanently — the planned 50% September 1 price increase is canceled.**

In [an August 10 announcement](https://x.com/claudeai/status/2086891169217122586), Anthropic made Claude Sonnet 5's introductory pricing permanent. The $2 input / $10 output per million token rate that launched in June will not step up to $3/$15 on September 1 — that planned increase is officially off.

One nuance remains: Sonnet 5's tokenizer produces ~30% more tokens from the same text as Sonnet 4.6, so effective cost comparisons across model generations still require accounting for that. But for anyone already running Sonnet 5 workloads, the rate is now stable indefinitely.

**Why it matters:** Any agent pipeline budgeted around a September 1 50% increase can drop that assumption. Lock in your model-tier routing now — $2/$10 is the permanent Sonnet 5 rate.

**Sources:** [@claudeai announcement](https://x.com/claudeai/status/2086891169217122586) · [Techmeme coverage](https://www.techmeme.com/260810/p42) · [Analysis — explainx.ai](https://explainx.ai/blog/anthropic-sonnet-5-permanent-pricing-august-2026)
]]></content:encoded>
    </item>

    <item>
      <title>OpenAI Ships GPT-5.6-Cyber: Specialized Security Model Lands Behind Daybreak Red Access Tier</title>
      <link>https://www.neowin.net/news/openai-launches-gpt-56-cyber-and-expands-daybreak-with-red-and-blue-access-tiers/</link>
      <guid isPermaLink="false">2026-08-12-02-gpt56cyber-daybreak-red-security</guid>
      <pubDate>Wed, 12 Aug 2026 04:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Security</category>
      <category>LLM</category>
      <description>GPT-5.6-Cyber completes 95% of security prompts that GPT-5.6 Sol refuses -- gated behind OpenAI's new Daybreak Red identity-verified access tier for vetted researchers.</description>
      <content:encoded><![CDATA[
**GPT-5.6-Cyber answers 95% of security prompts that GPT-5.6 Sol refuses — a purpose-built offensive research model gated behind OpenAI's new Daybreak Red vetted-access tier.**

OpenAI [launched GPT-5.6-Cyber on August 10](https://www.neowin.net/news/openai-launches-gpt-56-cyber-and-expands-daybreak-with-red-and-blue-access-tiers/), a variant of GPT-5.6 Sol trained for cybersecurity workflows: zero-day vulnerability research, exploit chain development, and red-team automation. It's only available via Daybreak Red, where security vendors (CrowdStrike, Palo Alto Networks, Cisco, Cloudflare, IBM) vouch for applicants' legitimate defensive use.

The companion Daybreak Blue tier gives approved defenders access to GPT-5.6 Sol with adjusted safeguards for vulnerability discovery, malware analysis, and patch validation — a lower access bar than Red.

**Why it matters:** Specialized security model tiers separate from consumer APIs is emerging as the industry pattern for dual-use AI. Most engineers won't qualify for Daybreak Red today, but this signals where agentic offensive security tooling is heading — expect other labs to follow.

**Sources:** [OpenAI Daybreak Red launch — Neowin](https://www.neowin.net/news/openai-launches-gpt-56-cyber-and-expands-daybreak-with-red-and-blue-access-tiers/) · [GPT-5.6-Cyber — The Hacker News](https://thehackernews.com/2026/08/openai-launches-gpt-56-cyber-with.html) · [Axios analysis](https://www.axios.com/2026/08/10/openai-gpt-astra-restrictions-safety-hacking-defenders)
]]></content:encoded>
    </item>

    <item>
      <title>Before You Instruction-Tune: Teach Your Model a New Domain With Continued Pre-Training in Unsloth</title>
      <link>https://unsloth.ai/blog/contpretraining</link>
      <guid isPermaLink="false">2026-08-12-01-domain-pretrain-unsloth-cpt</guid>
      <pubDate>Wed, 12 Aug 2026 04:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Fine-Tuning</category>
      <category>HuggingFace</category>
      <description>Continued pre-training (CPT) adapts a base model's vocabulary to your domain BEFORE instruction fine-tuning -- closing the context gap that forces SFT to work twice as hard.</description>
      <content:encoded><![CDATA[
**Continued pre-training adapts a base model's vocabulary and patterns to your domain BEFORE instruction fine-tuning — Unsloth's free Colab does it in minutes, and the gains compound through every downstream SFT task.**

**What you'll be able to do after this:**
- Know when CPT is worth doing (niche vocabulary, specialized formats, low-resource languages)
- Run Unsloth's QLoRA-based continued pre-training pipeline on raw domain text with a free Colab GPU
- Chain CPT → SFT for a model that understands your domain AND follows instructions

---

Every instruction fine-tune (SFT) fights the base model's priors. If your domain — medical notes, legal filings, an internal codebase, a language the model rarely saw at scale — has vocabulary and sentence patterns that are out-of-distribution for the base model, your SFT data has to teach both "what this domain is" AND "how to follow instructions." That's two jobs for one fine-tune.

Continued pre-training solves this with a "step 0": expose the base model to raw domain text as a next-token-prediction task. The model absorbs domain vocabulary and structure *without* changing output format. Then SFT your adapted model — you need fewer instruction pairs to hit the same quality, and domain-specific hallucinations drop significantly.

**Walk-through (Unsloth, free Colab T4):**

```python
from unsloth import FastLanguageModel, UnslothTrainer, UnslothTrainingArguments
from datasets import load_dataset

# 1. Load a base (not instruction-tuned) model
model, tokenizer = FastLanguageModel.from_pretrained(
    "unsloth/Llama-3.2-3B-bnb-4bit",
    max_seq_length=2048,
)

# 2. Prepare your domain corpus as raw text
dataset = load_dataset("text", data_files={"train": "my_domain_corpus.txt"})

# 3. CPT: raw text as next-token prediction, no instruction template
trainer = UnslothTrainer(
    model=model,
    tokenizer=tokenizer,
    train_dataset=dataset["train"],
    dataset_text_field="text",
    args=UnslothTrainingArguments(
        per_device_train_batch_size=2,
        gradient_accumulation_steps=4,
        warmup_ratio=0.1,
        num_train_epochs=1,
        learning_rate=5e-5,            # lower than SFT (2e-4)
        embedding_learning_rate=5e-6,  # 1/10th LR — keeps vocab stable
        output_dir="cpt-adapter/",
    ),
)
trainer.train()
model.save_pretrained("cpt-adapter/")
```

**Key settings:** The lower learning rate (5e-5 vs SFT's typical 2e-4) prevents catastrophic forgetting of general capabilities. `embedding_learning_rate` at 1/10th of the main LR keeps vocabulary embeddings stable while the transformer layers adapt. After CPT, merge the adapter and use the merged checkpoint as your SFT base — you'll need less instruction data to reach the same output quality.

**When CPT is worth it:** Medical/legal/scientific text, a low-resource language, a proprietary codebase with unique APIs or conventions. For general-purpose assistants built on general-domain data, skip CPT and go straight to SFT.

**Sources:** [Continued LLM Pretraining — Unsloth Blog](https://unsloth.ai/blog/contpretraining) · [Unsloth CPT Documentation](https://unsloth.ai/docs/basics/continued-pretraining) · [Continuing Pre-Training on Raw Text — Chris McCormick](https://mccormickml.com/2025/01/18/continuing-pre-training-on-raw-text/)
]]></content:encoded>
      <media:content url="https://opengraph.githubassets.com/1/unslothai/unsloth" medium="image" />
      <media:thumbnail url="https://opengraph.githubassets.com/1/unslothai/unsloth" />
    </item>

    <item>
      <title>Automated PR Review with claude-code-action: 10 Lines of YAML, Zero Manual Review Fatigue</title>
      <link>https://code.claude.com/docs/en/github-actions</link>
      <guid isPermaLink="false">2026-08-11-08-claude-code-action-github-pr-automation</guid>
      <pubDate>Tue, 11 Aug 2026 20:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Developer Tools</category>
      <category>Best Practices</category>
      <description>Drop 10 lines of YAML into .github/workflows/ and every PR gets a Claude Code review — reading the diff, understanding your codebase, and posting inline comments automatically.</description>
      <content:encoded><![CDATA[
**Drop `anthropics/claude-code-action@v1` into your GitHub Actions workflow and every PR gets a full Claude Code review — diff-aware, codebase-aware, and configurable to auto-fix.**

`claude-code-action` is Anthropic's official GitHub Action. It spins up a Claude Code session for each trigger, reads the PR diff alongside your repository, and posts review comments — or pushes fixes directly — all from a standard Actions YAML file.

**Minimal review setup** — add this to `.github/workflows/claude-review.yml`:

```yaml
name: Claude PR Review
on:
  pull_request:
    types: [opened, synchronize]
  issue_comment:
    types: [created]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          trigger_phrase: "@claude"
          mode: review
```

This fires on every new or updated PR and whenever someone comments `@claude review` — Claude reads the diff, checks against your codebase, and leaves inline review comments.

**Key options:**

| Option | What it does |
|---|---|
| `mode: fix` | Claude implements the fix and pushes commits |
| `allowed_tools: "Read,Bash"` | Least-privilege tool list (default: all tools) |
| `claude_md_path: ".claude/review.md"` | Custom review instructions per project |
| `trigger_phrase: "@claude"` | Comment-triggered only (saves cost on draft PRs) |

**Cost and safety notes.** Typical review runs $0.03–0.10 on Sonnet 5 — cheap enough to run on every commit. Pin to an audited SHA (`anthropics/claude-code-action@abc123`) rather than `@v1` for supply-chain safety; the Actions marketplace shows the latest verified SHA. Claude Code v2.1.227 (Aug 10) fixed a bug where all Bash commands would silently fail on GitHub-hosted runners when `allowed_non_write_users` was set — if you hit mysteriously empty tool results, that's the fix to pull.

**What it can't do automatically:** push to protected branches without relaxed ruleset exceptions, or merge PRs — those still require human approval. Use `mode: review` for feedback-only and `mode: fix` for branches where you want Claude to author commits.

**Sources:** [claude-code-action docs — Claude Code](https://code.claude.com/docs/en/github-actions) · [Complete CI/CD Integration Guide — The Prompt Shelf](https://thepromptshelf.dev/blog/claude-code-github-actions-cicd-complete-guide-2026/) · [v2.1.227 Bash fix — Claude Code Changelog](https://code.claude.com/docs/en/changelog)
]]></content:encoded>
      <media:content url="https://opengraph.githubassets.com/1/anthropics/claude-code-action" medium="image" />
      <media:thumbnail url="https://opengraph.githubassets.com/1/anthropics/claude-code-action" />
    </item>

    <item>
      <title>FAISS: Build a Lightning-Fast Local Vector Index in 15 Lines of Python</title>
      <link>https://www.pinecone.io/learn/series/faiss/</link>
      <guid isPermaLink="false">2026-08-11-07-faiss-local-vector-index-python</guid>
      <pubDate>Tue, 11 Aug 2026 20:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Vectors</category>
      <category>Embeddings</category>
      <category>HuggingFace</category>
      <description>FAISS is Meta's in-memory vector search library — no server, no SQL, just fast nearest-neighbor lookup over millions of embeddings. Learn IndexFlatL2 and IndexIVFFlat in 15 lines of Python.</description>
      <content:encoded><![CDATA[
**FAISS is Meta's open-source library for searching millions of embeddings in milliseconds — pure in-memory, no server required, and the backbone of many production RAG systems.**

**What you'll be able to do after this:**
- Build an exact-search vector index (`IndexFlatL2`) and query it in two lines of Python
- Switch to approximate-search (`IndexIVFFlat`) when your dataset grows past ~100K vectors, with configurable speed/recall trade-off
- Save and reload a FAISS index to disk so your embeddings survive restarts

---

**FAISS vs. the alternatives.** pgvector lives inside Postgres; Chroma is a persistent managed store. FAISS is neither — it's a raw, in-memory C++ library with Python bindings. That makes it the fastest option for search-heavy workloads where you control the lifecycle, and the best tool for learning how vector similarity actually works under the hood before adding a database layer.

**Two indexes every ML engineer should know:**

| Index | How it works | Best for |
|---|---|---|
| `IndexFlatL2` | Brute-force L2 distance over all vectors | <100K vectors; always exact |
| `IndexIVFFlat` | Partitions space into `nlist` cells; searches `nprobe` cells per query | 100K–100M vectors; ~1% recall trade-off for 10–100× speed |

---

**Walk-through**

Install:
```bash
pip install faiss-cpu sentence-transformers numpy
```

**Step 1: Exact search with `IndexFlatL2`**

```python
import faiss
import numpy as np
from sentence_transformers import SentenceTransformer

model = SentenceTransformer("all-MiniLM-L6-v2")

docs = [
    "Claude Code runs agentic sessions in your terminal",
    "FAISS is a C++ library for fast vector similarity search",
    "Embeddings encode meaning as high-dimensional float arrays",
]
doc_vecs = model.encode(docs, normalize_embeddings=True).astype("float32")
d = doc_vecs.shape[1]          # 384 for MiniLM

index = faiss.IndexFlatL2(d)   # exact L2 distance
index.add(doc_vecs)            # add all vectors
print(index.ntotal)            # 3

query = model.encode(["vector similarity library"], normalize_embeddings=True).astype("float32")
D, I = index.search(query, k=2)        # top-2 results
print([docs[i] for i in I[0]])         # → ["FAISS is a C++ library…", "Embeddings encode…"]
```

**Step 2: Approximate search with `IndexIVFFlat` for large datasets**

```python
nlist = 100                               # cluster count (rule of thumb: sqrt(N))
quantizer = faiss.IndexFlatL2(d)         # coarse-level quantizer
index_ivf = faiss.IndexIVFFlat(quantizer, d, nlist)

index_ivf.train(doc_vecs)                # required before adding
index_ivf.add(doc_vecs)
index_ivf.nprobe = 10                    # search 10 cells; raise for more recall

D, I = index_ivf.search(query, k=2)
print([docs[i] for i in I[0]])
```

Tune `nprobe` up to improve recall at the cost of speed. A value of 10 gives ~99% recall on most datasets; 1 gives maximum speed.

**Step 3: Save and reload**

```python
faiss.write_index(index_ivf, "my_index.faiss")

# --- later ---
index_ivf = faiss.read_index("my_index.faiss")
index_ivf.nprobe = 10                    # nprobe isn't saved — reset it
```

Note: FAISS doesn't store your original text — keep a parallel list or database to map integer indices back to documents (`docs[I[0][0]]` in the example above).

---

**Go deeper.** Pinecone's [FAISS: The Missing Manual](https://www.pinecone.io/learn/series/faiss/) series covers HNSW (graph-based, production-grade), Product Quantization (compress 97% of memory), and the Index Factory — and has runnable Colab notebooks for each.

**Sources:** [FAISS: The Missing Manual — Pinecone](https://www.pinecone.io/learn/series/faiss/) · [Nearest Neighbor Indexes — Pinecone](https://www.pinecone.io/learn/series/faiss/vector-indexes/) · [Official FAISS Python tutorials — GitHub](https://github.com/facebookresearch/faiss/tree/main/tutorial/python) · [FAISS Python API reference — GoLinuxCloud](https://www.golinuxcloud.com/faiss-python-api/)
]]></content:encoded>
      <media:content url="https://www.pinecone.io/learn/series/faiss/og-image.png" medium="image" />
      <media:thumbnail url="https://www.pinecone.io/learn/series/faiss/og-image.png" />
    </item>

    <item>
      <title>Claude API: Swap Tools Mid-Conversation Without Invalidating the Cache</title>
      <link>https://platform.claude.com/docs/en/release-notes/overview</link>
      <guid isPermaLink="false">2026-08-11-06-claude-api-mid-conversation-tools-beta</guid>
      <pubDate>Tue, 11 Aug 2026 12:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Agents</category>
      <category>Best Practices</category>
      <description>New Claude API beta header lets agentic apps swap the tool list between turns while preserving the prompt cache — no more latency penalty for tool-list changes mid-conversation.</description>
      <content:encoded><![CDATA[
**A Claude API beta header lets agents swap tools between turns while preserving the prompt cache — no more latency tax for tool-list changes.**

Multi-step agents often need different tools at different phases. Previously, changing the `tools` array between turns invalidated the entire prompt cache — paying full input cost again on every tool swap, plus re-processing the full conversation history.

The `mid-conversation-tool-changes-2026-07-01` beta header fixes this on Opus 5, Fable 5, Opus 4.8, and Mythos 5:

```python
import anthropic

client = anthropic.Anthropic()
BETA = ["mid-conversation-tool-changes-2026-07-01"]

# Phase 1: read-only tools
response = client.messages.create(
    model="claude-opus-5-20260724",
    betas=BETA,
    system="You are a code analysis agent.",
    messages=conversation,
    tools=[search_tool, read_file_tool],   # read-only
    max_tokens=4096,
)
conversation.extend(...)

# Phase 2: write tools — cache stays hot despite different tool list
response = client.messages.create(
    model="claude-opus-5-20260724",
    betas=BETA,
    system="You are a code analysis agent.",  # still cached
    messages=conversation,                    # history still cached
    tools=[write_file_tool, run_cmd_tool],    # different list — no cache bust
    max_tokens=4096,
)
```

**Why this matters for agent design.** You can match tool access to the task's risk level per turn — give the planner phase read-only tools, give the executor write access — without paying cache penalties. Least-privilege tool design is now practical in long conversations.

**Per-turn effort is also cache-friendly.** Opus 5 supports changing `budget_tokens` per turn without busting the cache. Start a planning pass at `effort="low"` and escalate to `effort="xhigh"` for a hard sub-problem, mid-conversation. One constraint: at `xhigh` or `max` effort, setting `thinking: {"type": "disabled"}` returns a 400 error — plan your effort ladder with this in mind.

**Sources:** [Claude Platform release notes — Anthropic](https://platform.claude.com/docs/en/release-notes/overview) · [What's new in Opus 5 — Claude Platform Docs](https://platform.claude.com/docs/en/about-claude/models/whats-new-opus-5) · [Effort parameter — Claude Platform Docs](https://platform.claude.com/docs/en/build-with-claude/effort)
]]></content:encoded>
    </item>

    <item>
      <title>Ollama v0.32.6: MLX Speculative Decoding Auto-Enabled + Streaming Format Corrected</title>
      <link>https://github.com/ollama/ollama/releases/tag/v0.32.6</link>
      <guid isPermaLink="false">2026-08-11-05-ollama-v0326-mlx-streaming</guid>
      <pubDate>Tue, 11 Aug 2026 12:06:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Self-Hosting</category>
      <category>LLM</category>
      <description>Ollama v0.32.6 (Aug 5) auto-enables MLX speculative decoding for Qwen3.5 on Apple Silicon and fixes /v1/chat/completions streaming to match OpenAI's wire format exactly.</description>
      <content:encoded><![CDATA[
**Ollama v0.32.6 (Aug 5) automatically enables MLX speculative decoding for Apple Silicon and corrects the `/v1/chat/completions` streaming format to match OpenAI's wire protocol exactly.**

**MLX speculative decoding is now automatic.** The MLX engine uses a model's Multi-Token Prediction (MTP) head for speculative decoding without any configuration. Qwen3.5 and other MTP-capable models run noticeably faster on M-series chips — no flag to set, no separate draft model to manage. If the model doesn't support speculative decoding, Ollama falls back silently to standard inference.

**OpenAI streaming wire format corrected.** The `/v1/chat/completions` SSE stream now behaves like the real OpenAI API: `role` appears only in the first chunk, `finish_reason` arrives on its own trailing chunk, and token usage arrives in a separate chunk when `stream_options.include_usage` is set. If you wrote streaming parsers that worked around Ollama's previous format quirks, test them after upgrading.

**Why it matters:** Speculative decoding is a free latency improvement for local inference with no accuracy tradeoff, and the streaming fix removes the last common friction point when substituting Ollama for the OpenAI SDK in existing code.

**Sources:** [Ollama v0.32.6 release — GitHub](https://github.com/ollama/ollama/releases/tag/v0.32.6) · [Ollama August 2026 updates — Releasebot](https://releasebot.io/updates/ollama)
]]></content:encoded>
    </item>

    <item>
      <title>Catch Prompt Regressions Before Users Do: LLM App Testing with Promptfoo</title>
      <link>https://www.promptfoo.dev/docs/configuration/guide/</link>
      <guid isPermaLink="false">2026-08-11-04-promptfoo-llm-eval-testing</guid>
      <pubDate>Tue, 11 Aug 2026 12:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Agents</category>
      <category>Best Practices</category>
      <category>Developer Tools</category>
      <description>Promptfoo runs every prompt variant against every model in parallel with YAML assertions — catch regressions and prompt-injection bugs in CI before users hit them.</description>
      <content:encoded><![CDATA[
**Promptfoo runs every prompt variant against every model in parallel with YAML-defined assertions — catch regressions and prompt injection in CI before users see them.**

**What you'll be able to do after this:**

- Define prompt variants and test cases in a single YAML file and evaluate all combinations in seconds
- Write deterministic and LLM-graded assertions that pass/fail like unit tests
- Run automated red-team scans with 67+ attack plugins to catch jailbreaks and prompt injection before shipping

## Setup

```bash
npx promptfoo@latest init
```

This creates `promptfooconfig.yaml`:

```yaml
prompts:
  - "Summarize the following text: {{text}}"
  - "In 3 bullet points, summarize: {{text}}"

providers:
  - anthropic:claude-haiku-4-5-20251001
  - openai:gpt-4o-mini

tests:
  - vars:
      text: "Anthropic builds AI that is safe and helpful."
    assert:
      - type: contains
        value: "Anthropic"
      - type: llm-rubric
        value: "Response is a concise summary, not a direct quote"
      - type: latency
        threshold: 5000

  - vars:
      text: "The meeting is at 3pm on Friday in room 4B."
    assert:
      - type: contains
        value: "meeting"
      - type: not-contains
        value: "I cannot"
```

## Run the eval

```bash
npx promptfoo eval              # runs every test × provider combo in parallel
npx promptfoo view              # opens local web UI — a grid of pass/fail results
npx promptfoo eval --ci         # exits code 1 if any assertion fails
```

The web UI shows a grid: rows are test cases, columns are prompt-provider combinations, cells show pass/fail with the model's actual output. You see at a glance which prompt wording wins across all your cases and providers.

**Assertion types:**
- **Deterministic** (`contains`, `not-contains`, `regex`, `latency`, `cost`) — free and instant, no LLM call needed
- **LLM-graded** (`llm-rubric`) — sends output to a judge model for subjective quality checks like tone or completeness
- **Custom** — Python or JavaScript functions for domain-specific checks

## Add to CI (GitHub Actions)

```yaml
- name: LLM regression test
  run: npx promptfoo eval --ci
  env:
    ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
    OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
```

Fails the build on prompt regressions exactly like a unit test suite — no more "we broke the prompt in last week's deploy."

## Red-teaming (optional, 2 commands)

```bash
npx promptfoo redteam init   # generates adversarial test cases for your app
npx promptfoo redteam run    # runs 67+ attack plugins: jailbreaks, injections, policy violations
```

Promptfoo is Apache 2.0 open source, runs 100% locally, and supports 90+ model providers. The evaluation framework was acquired by OpenAI in March 2026 but remains open source with first-party Anthropic support.

**Sources:** [Promptfoo configuration guide — promptfoo.dev](https://www.promptfoo.dev/docs/configuration/guide/) · [Promptfoo tutorial — DataCamp](https://www.datacamp.com/tutorial/promptfoo-tutorial) · [Red-team guide — promptfoo.dev](https://www.promptfoo.dev/docs/red-team/) · [CI/CD integration — promptfoo.dev](https://www.promptfoo.dev/docs/integrations/ci-cd/)
]]></content:encoded>
    </item>

    <item>
      <title>Run Claude Code Sessions on Your Own Machines: v2.1.224 Self-Hosted Runner</title>
      <link>https://github.com/anthropics/claude-code/releases/tag/v2.1.224</link>
      <guid isPermaLink="false">2026-08-11-03-claude-code-self-hosted-runner-cross-session</guid>
      <pubDate>Tue, 11 Aug 2026 04:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Claude Code v2.1.224 (Aug 7) adds self-hosted environments: run your Team/Enterprise cloud sessions on your own machines with one command, keeping code off Anthropic's servers.</description>
      <content:encoded><![CDATA[
**Claude Code v2.1.224 (Aug 7) lets Team/Enterprise teams run cloud sessions on their own hardware with a single command — keeping code, secrets, and tool execution on infrastructure you control.**

Before this release, Claude Code cloud sessions (from the web, mobile, or desktop app) ran on Anthropic's compute. Your code was transferred to Anthropic-managed infrastructure for the duration of the session. For many teams in regulated industries or behind network perimeters, that was a blocker.

## What changed

v2.1.224 introduces `claude self-hosted-runner`, which turns your own machine or container into the compute layer for Claude Code cloud sessions.

```bash
# On the machine you want Claude Code to run on:
claude self-hosted-runner

# That's it — Claude Code web/mobile/desktop sessions for your org
# now execute on this machine instead of Anthropic's servers
```

The command is available on **Team and Enterprise plans**. Sessions launched from any client (web, desktop, mobile) are routed to your self-hosted runner instead of Anthropic's compute.

## Three reasons to use it

**Network access.** Your Claude Code sessions run inside your network. The agent can reach internal APIs, databases, artifact registries, and dev services without exposing them to the internet or adding VPN complexity.

**Toolchain control.** Pre-install your compilers, SDKs, proprietary CLIs, and custom tools on the runner host. The agent finds them just as it would on a developer's local machine.

**Compliance.** Source code, environment variables, and intermediate artifacts never leave infrastructure you control. For teams in HIPAA, FedRAMP, or SOC 2 environments, this closes the biggest gap in cloud-based AI coding workflows.

## Cross-session messaging (same release)

v2.1.224 also ships cross-session messaging: agents in separate sessions can now send messages to each other by name. Combined with self-hosted runners, this enables multi-agent pipelines that run entirely on your own infra — one runner handles orchestration while others handle sub-tasks, all within your network perimeter.

```bash
# In one session, send a message to another named session:
# (from Claude Code's perspective, via the ListAgents / SendMessage tools)
# Sessions on the same runner can coordinate without API round-trips
```

## When to use self-hosted runners

- Your repo contains secrets that can't leave your network
- The agent needs to call internal microservices or hit a private artifact registry
- Your organization's security review prohibits code execution on third-party SaaS compute
- You need deterministic toolchain versions across all agent sessions

For teams without these constraints, Anthropic-managed compute is simpler and still the right default.

**Sources:** [Claude Code v2.1.224 release notes — GitHub](https://github.com/anthropics/claude-code/releases/tag/v2.1.224) · [Claude Code self-hosted runner overview — clauding.de](https://clauding.de/en/posts/claude-code-2-1-224-self-hosted-runner) · [Claude Code major updates v2.1.224 — dev.classmethod.jp](https://dev.classmethod.jp/en/articles/20260807-cc-updates-v2-1-224/) · [Claude Code changelog — code.claude.com](https://code.claude.com/docs/en/whats-new)
]]></content:encoded>
    </item>

    <item>
      <title>Claude Now Embeds Invisible Watermarks in All Generated Text</title>
      <link>https://support.claude.com/en/articles/16266773-how-claude-marks-ai-generated-content</link>
      <guid isPermaLink="false">2026-08-11-02-claude-content-watermark-c2pa-ai-act</guid>
      <pubDate>Tue, 11 Aug 2026 04:06:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Developer Tools</category>
      <category>Security</category>
      <description>Starting August 2, new Claude models weave imperceptible watermarks into generated text that survive copy-paste and light edits, and attach C2PA provenance metadata to generated files — globally.</description>
      <content:encoded><![CDATA[
**Starting August 2, Claude models weave an imperceptible watermark into generated text that survives copy-paste and light edits, and attach signed C2PA provenance metadata to supported file types — worldwide, not just in the EU.**

Anthropic rolled out two complementary provenance mechanisms in response to the EU AI Act's Article 50 transparency requirements, but chose to apply them globally:

- **Text watermarking:** an invisible signal baked into generated text at the model level. Users see nothing different; the signal persists through copy-paste and partial edits. Detection requires Anthropic's verification tooling, not available to end users directly.
- **File provenance (C2PA):** generated SVG, PNG, and JPG files receive signed Coalition for Content Provenance and Authenticity metadata — the same open standard used by Adobe Firefly, OpenAI image tools, and Google Imagen.

Coverage applies across Claude Platform (API), Claude, Claude Code, Claude Cowork, and Claude Tag.

**Why it matters:** If you're building a product on Claude, your users' output is now watermarked. This is largely invisible in practice, but worth knowing when: (1) you're integrating content-verification pipelines that check for AI-generated text; (2) your users paste Claude output into third-party tools that scan for watermarks; or (3) you're in a domain (journalism, legal, compliance) where AI provenance is actively scrutinized.

**Sources:** [How Claude marks AI-generated content — Claude Help Center](https://support.claude.com/en/articles/16266773-how-claude-marks-ai-generated-content) · [Anthropic embeds invisible watermarks globally — Crypto Briefing](https://cryptobriefing.com/anthropic-watermarks-claude-models-eu-ai-act/) · [Claude invisible watermarks — Interesting Engineering](https://interestingengineering.com/ai-robotics/anthropic-claude-text-invisible-watermarks)
]]></content:encoded>
    </item>

    <item>
      <title>Route Between 400+ LLMs Without Changing Your Code: A Practical OpenRouter Guide</title>
      <link>https://openrouter.ai/blog/insights/model-routing/</link>
      <guid isPermaLink="false">2026-08-11-01-openrouter-hosted-inference-routing-guide</guid>
      <pubDate>Tue, 11 Aug 2026 04:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Self-Hosting</category>
      <category>Developer Tools</category>
      <description>OpenRouter gives you one OpenAI-compatible endpoint for 400+ models across 70+ providers. Swap a URL, add fallbacks, and your app gets automatic provider failover and cost routing for free.</description>
      <content:encoded><![CDATA[
**OpenRouter gives you one OpenAI-compatible endpoint for 400+ models across 70+ providers — swap a URL, configure model fallbacks, and your app gets automatic provider routing and failover without touching your business logic.**

**What you'll be able to do after this:**
- Drop OpenRouter into any OpenAI SDK call with two lines of code and access every major model from a single API key
- Configure model fallbacks so your app survives provider outages and rate limits automatically
- Choose the right hosted inference platform — OpenRouter, Fireworks, or Together — for your workload

## The problem OpenRouter solves

Most LLM apps start with one provider: "send requests to Claude" or "send requests to GPT." That's fine until your provider has an outage, changes pricing, or releases a new model you want to compare. Updating every API call is mechanical work; more dangerously, a provider hiccup at 2am takes your app down.

OpenRouter is a reverse proxy that sits between your code and every inference provider. Your code always calls one endpoint. OpenRouter handles routing, load balancing, and failover.

## Step 1: Drop-in swap (two lines)

Get an API key at [openrouter.ai/keys](https://openrouter.ai/keys), then swap `base_url`:

```python
from openai import OpenAI

client = OpenAI(
    base_url="https://openrouter.ai/api/v1",
    api_key="sk-or-...",          # your OpenRouter key
)

response = client.chat.completions.create(
    model="anthropic/claude-sonnet-5-20250901",   # or any of 400+ models
    messages=[{"role": "user", "content": "Hello"}],
)
print(response.choices[0].message.content)
```

Model names use `provider/model-id` format. Every model available in OpenRouter's catalog is a valid value — browse the full list at [openrouter.ai/models](https://openrouter.ai/models).

## Step 2: Add model fallbacks

OpenRouter's `models` parameter lets you pass a prioritized list. If the first model fails (rate limit, outage, context-length error), OpenRouter tries the next one automatically:

```python
response = client.chat.completions.create(
    model="anthropic/claude-sonnet-5-20250901",   # primary
    extra_body={
        "models": [
            "anthropic/claude-sonnet-5-20250901",
            "google/gemini-2-flash",
            "meta-llama/llama-3.3-70b-instruct",  # cheap reliable floor
        ]
    },
    messages=[{"role": "user", "content": "Summarize this doc..."}],
)
```

Order the list with your preferred model first and a cheap reliable model last. OpenRouter walks the list sequentially until one succeeds; if all fail, it returns the last error.

## Step 3: Let the router pick (optional)

Use `openrouter/auto` to hand model selection entirely to OpenRouter. It routes to the cheapest capable model for each request:

```python
response = client.chat.completions.create(
    model="openrouter/auto",
    messages=[{"role": "user", "content": "Write a SQL query to..."}],
)
```

Useful for mixed workloads where some prompts need strong reasoning and others just need fast completion.

## How OpenRouter routes by default

When you specify a single model and that model has multiple providers, OpenRouter:
1. Excludes providers with outages in the last 30 seconds
2. Among healthy providers, picks from the lowest-cost candidates weighted by the inverse square of price (randomized toward cheaper)
3. Falls back to remaining providers if selected fails

The result: you always hit a healthy, cost-efficient provider without thinking about which one.

## Choosing between OpenRouter, Fireworks, and Together

| | **OpenRouter** | **Fireworks** | **Together** |
|---|---|---|---|
| **Model breadth** | 400+ across 70+ providers | 100+ open-source | Open-source focused |
| **Routing/failover** | Built-in | No | No |
| **Cost** | Provider rate + 5.5% fee | ~90% cheaper than closed-source | Competitive |
| **Latency** | Varies by provider | Fastest TTFT on open models | Good |
| **Fine-tuning** | No | Yes | Yes |
| **Best for** | Flexibility, multi-provider apps | Fastest open-model inference | Open-model + fine-tuning |

Rule of thumb: start with OpenRouter to validate your use case across models; migrate to Fireworks or Together when you've chosen a model and need the fastest inference or fine-tuning capability.

**Sources:** [How OpenRouter Model Routing Works — OpenRouter Blog](https://openrouter.ai/blog/insights/model-routing/) · [Model Fallbacks — OpenRouter Docs](https://openrouter.ai/docs/guides/routing/model-fallbacks) · [OpenRouter API in Python — Real Python](https://realpython.com/openrouter-api/) · [Fireworks AI: Best LLM API Providers 2026 — Fireworks Blog](https://fireworks.ai/blog/best-llm-api-providers) · [OpenRouter vs Together vs Fireworks 2026 — aibizhub.io](https://aibizhub.io/articles/openrouter-vs-together-vs-fireworks-2026/)
]]></content:encoded>
    </item>

    <item>
      <title>Prototype Agent Tool Loops Locally Before Paying for API Calls: Ollama's OpenAI Drop-In</title>
      <link>https://ollama.com/blog/openai-compatibility</link>
      <guid isPermaLink="false">2026-08-10-09-local-agent-dev-ollama-openai-compat</guid>
      <pubDate>Mon, 10 Aug 2026 20:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Agents</category>
      <category>Self-Hosting</category>
      <description>Ollama exposes an OpenAI-compatible /v1 endpoint — swap one URL and use your existing tool-calling code to iterate locally at zero API cost before promoting to Claude or GPT-5 in production.</description>
      <content:encoded><![CDATA[
**Ollama's OpenAI-compatible `/v1` endpoint lets you run your existing tool-calling agent code against local models — swap one URL to develop and debug at zero API cost, then swap back for production.**

Agent development is expensive in API calls, not just money — every iteration that changes the tool schema, system prompt, or dispatch logic requires real inference. Running against a local model eliminates that cost during the iteration phase.

## The drop-in swap

Ollama exposes an OpenAI-compatible endpoint at `http://localhost:11434/v1`. The OpenAI Python client accepts a `base_url` override, so the switch is two lines:

```python
from openai import OpenAI

# ── Local development ──────────────────────────────────────────────────
client = OpenAI(
    base_url="http://localhost:11434/v1",
    api_key="ollama",   # required by the client, ignored by Ollama
)
model = "muse-glimmer"        # or "qwen3:8b", "llama3.1:8b"

# ── Production ─────────────────────────────────────────────────────────
# client = OpenAI()                            # uses OPENAI_API_KEY
# model  = "claude-sonnet-5-20250901"          # or your Claude model via SDK
```

Your tool definitions, `tool_choice`, response parsing, and `tool_calls` dispatch code are identical in both environments. The only thing that changes is `base_url` and `model`.

## What to validate locally

Use the local loop for:
- **Schema correctness** — verifying Claude will receive tool definitions with the right types and required fields
- **Dispatch logic** — confirming your `if call.function.name == "..."` branches handle all tool names
- **Error handling** — testing your `is_error` paths and retry behavior
- **Message history** — catching append bugs before they cause API errors

## When to switch to Claude

Move to your production model when:
- Your tool schemas and dispatch logic are stable
- You need better reasoning on complex multi-step tasks
- Local model's function-calling accuracy drops on your real queries
- You're measuring latency or need longer context

Muse Glimmer (released today, Apache 2.0, 4-bit quantized to 18–20 GB) is the strongest local option for agent workloads right now: `ollama pull muse-glimmer`.

**Sources:** [Ollama OpenAI compatibility docs](https://ollama.com/blog/openai-compatibility) · [Muse Glimmer on Ollama via Unsloth GGUF](https://huggingface.co/unsloth/Muse-Glimmer-30B-GGUF) · [Ollama REST API reference](https://github.com/ollama/ollama/blob/main/docs/api.md)
]]></content:encoded>
    </item>

    <item>
      <title>Meta Open-Sources Muse Glimmer: A 30B Agentic Model That Runs on One Consumer GPU</title>
      <link>https://research.meta.ai/blog/introducing-muse-glimmer-open-agentic-model</link>
      <guid isPermaLink="false">2026-08-10-08-meta-muse-glimmer-30b-open-agentic-consumer-gpu</guid>
      <pubDate>Mon, 10 Aug 2026 20:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>LLM</category>
      <category>Agents</category>
      <category>Self-Hosting</category>
      <description>Meta releases Muse Glimmer today: a 30B open-weights agentic model (Apache 2.0) distilled from Muse Spark 1.2 that fits on a single consumer GPU at 18-20 GB with 4-bit quantization.</description>
      <media:content url="https://research.meta.ai/images/muse-glimmer-blog-header.png" medium="image" />
      <media:thumbnail url="https://research.meta.ai/images/muse-glimmer-blog-header.png" />
      <content:encoded><![CDATA[
**Meta releases Muse Glimmer today: 30B open-weights agentic model (Apache 2.0), distilled for tool use and failure recovery, fits on a single consumer GPU at 18-20 GB with 4-bit quantization.**

Muse Glimmer is a 30-billion-parameter dense multimodal model distilled from Muse Spark 1.2 and specifically trained for agentic workloads: multi-step tool use, function calling, LLM-as-a-judge evaluations, and task failure recovery. Full weights are 55 GB; Unsloth's GGUF 4-bit build (already on HuggingFace as of today) cuts that to 18–20 GB, within reach of a single RTX 4090 or a Mac with 24 GB+ unified memory.

Native integrations are available today for llama.cpp, MLX, ExecuTorch, and Ollama (`ollama pull muse-glimmer`). Text and image input supported.

**Why it matters:** Until today, every open model that could handle real agentic workloads reliably (Kimi K3, DeepSeek V3.2, the Muse Spark series) required multi-GPU infra or cloud APIs. Muse Glimmer is the first credible open agentic model that fits on prosumer hardware — making local agent development and self-hosted production deployments genuinely viable.

**Sources:** [Introducing Muse Glimmer — Meta AI Research](https://research.meta.ai/blog/introducing-muse-glimmer-open-agentic-model) · [meta-models/Muse-Glimmer-30B — HuggingFace](https://huggingface.co/meta-models/Muse-Glimmer-30B) · [unsloth/Muse-Glimmer-30B-GGUF — HuggingFace](https://huggingface.co/unsloth/Muse-Glimmer-30B-GGUF) · [Muse Glimmer on Phoronix](https://www.phoronix.com/news/Meta-Muse-Glimmer)
]]></content:encoded>
    </item>

    <item>
      <title>Fetch Multiple Data Points in One Claude Response: Parallel Tool Calls With asyncio.gather</title>
      <link>https://platform.claude.com/docs/en/agents-and-tools/tool-use/parallel-tool-use</link>
      <guid isPermaLink="false">2026-08-10-07-claude-api-parallel-tool-calls-asyncio</guid>
      <pubDate>Mon, 10 Aug 2026 20:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Agents</category>
      <category>Best Practices</category>
      <description>Claude 4 returns multiple independent tool calls in one response — dispatch them with asyncio.gather to cut latency by N times instead of running them sequentially.</description>
      <content:encoded><![CDATA[
**Claude 4 and later return multiple independent tool calls in a single response — dispatch them with `asyncio.gather` to cut latency by N× instead of running sequential round trips.**

**What you'll be able to do after this:**
- Understand how Claude signals parallel tool intent in a single response
- Dispatch multiple tool calls concurrently with `asyncio.gather` and return all results in one user message
- Know when to disable parallel tool use for sequential, side-effectful workflows

## The serial trap

Most agents are written as a simple while loop: call the API, get a tool request, execute it, return the result, repeat. If your agent needs to fetch a user profile, look up their orders, AND check their billing status, that is three sequential round trips — the total latency is T₁ + T₂ + T₃. Claude doesn't need them to be sequential; your code forces it.

## How Claude signals parallel intent

Claude 4 and later models return multiple `tool_use` blocks in a single assistant message when the calls are independent:

```python
# Claude returns ONE assistant message containing N tool_use blocks
response.content = [
    ToolUseBlock(id="toolu_01", name="get_user_profile", input={"user_id": "u123"}),
    ToolUseBlock(id="toolu_02", name="get_orders",       input={"user_id": "u123"}),
    ToolUseBlock(id="toolu_03", name="get_billing",      input={"user_id": "u123"}),
]
```

Your responsibility: run all three, then return ALL results in a single `user` message before calling the API again.

## The parallel dispatch pattern

```python
import asyncio
import anthropic

client = anthropic.Anthropic()

async def dispatch_parallel(tool_calls, tool_map):
    """Run all tool calls concurrently; return tool_result blocks."""
    tasks = [tool_map[tc.name](**tc.input) for tc in tool_calls]
    results = await asyncio.gather(*tasks, return_exceptions=True)
    return [
        {
            "type": "tool_result",
            "tool_use_id": tc.id,   # match by id, not by position
            "content": str(r) if not isinstance(r, Exception) else f"Error: {r}",
            "is_error": isinstance(r, Exception),
        }
        for tc, r in zip(tool_calls, results)
    ]

async def agent_loop(prompt, tools, tool_map):
    messages = [{"role": "user", "content": prompt}]
    while True:
        resp = client.messages.create(
            model="claude-sonnet-5-20250901",
            max_tokens=1024,
            tools=tools,
            messages=messages,
        )
        if resp.stop_reason == "end_turn":
            return next(b.text for b in resp.content if hasattr(b, "text"))

        tool_calls = [b for b in resp.content if b.type == "tool_use"]

        # 1. Append the full assistant message (keeps tool_use blocks intact)
        messages.append({"role": "assistant", "content": resp.content})

        # 2. Dispatch all calls in parallel
        results = await dispatch_parallel(tool_calls, tool_map)

        # 3. Return ALL results in a single user message
        messages.append({"role": "user", "content": results})
```

## Two rules that catch most bugs

**Match by `tool_use_id`, not list position.** Claude doesn't guarantee which tool appears first. Always pair calls and results by `id`.

**Append `response.content` in full — never strip `tool_use` blocks.** The next API call expects to see Claude's own requests echoed back. Stripping them causes an API error.

## Disable for sequential workflows

If your tools have side effects or ordering dependencies (e.g., `create_invoice` must succeed before `send_confirmation`):

```python
client.messages.create(
    ...,
    tool_choice={"type": "auto", "disable_parallel_tool_use": True},
)
```

**Sources:** [Parallel tool use — Claude Platform Docs](https://platform.claude.com/docs/en/agents-and-tools/tool-use/parallel-tool-use) · [Implement tool use — Claude Platform Docs](https://platform.claude.com/docs/en/agents-and-tools/tool-use/implement-tool-use) · [Build a tool-using agent — Claude Platform Docs](https://platform.claude.com/docs/en/agents-and-tools/tool-use/build-a-tool-using-agent)
]]></content:encoded>
    </item>


    <item>
      <title>Claude Code Auto Mode Becomes the Default on August 14 — What Changes and What to Configure</title>
      <link>https://claude.com/blog/auto-mode-default-in-claude-code</link>
      <guid isPermaLink="false">2026-08-10-06-claude-code-auto-mode-default-aug14</guid>
      <pubDate>Mon, 10 Aug 2026 12:15:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Starting August 14, new Claude Code sessions on Pro, Max, and Team plans run in auto mode by default — a safety classifier replaces per-action prompts with an 89% catch rate vs. 13.6% for manual review.</description>
      <content:encoded><![CDATA[
**Starting August 14, new Claude Code sessions on Pro, Max, and Team plans will skip per-action approval prompts — a safety classifier handles them automatically, catching 89% of dangerous commands vs. 13.6% for manual review.**

Auto mode has been opt-in since March. On August 14 it becomes the default for new sessions on Pro, Max, and Team plans. Anthropic's own testing explains why: users approve 97% of permission prompts reflexively — approval fatigue means manual oversight only catches 13.6% of dangerous commands. Auto mode's background classifier caught 89% of the same commands in the same study. Effective today, the classifier overhead is also free — no additional token charges.

## What changes

**Auto mode replaces per-action prompts with a classifier** that evaluates each tool call in real time. If it flags something irreversible or out-of-bounds, Claude tries a safer path or asks for permission. After repeated blocks, the session falls back to manual approval.

**Pinned defaults and managed admin settings are preserved.** Claude Enterprise, API, Bedrock, Vertex AI, and Foundry remain opt-in for now — Anthropic plans a broader rollout there separately.

## What to check before August 14

**1. If you rely on per-action prompts for a specific workflow**, pin your preference now:

```json
// ~/.claude/settings.json
{
  "disableAutoMode": true
}
```

**2. If your CLAUDE.md permission rules have drifted**, run:

```bash
claude auto-mode reset
```

This restores the default auto-mode classifier behavior without touching your other settings.

**3. Your `--allowlist-file` permissions are fully respected** — anything you've explicitly permitted stays permitted; auto mode only handles the undecided cases.

After August 14, the Claude Code session header shows the active mode so you always know which approval flow is running.

**Sources:** [Auto mode is now the default in Claude Code for Pro, Max, and Team plans — Anthropic](https://claude.com/blog/auto-mode-default-in-claude-code) · [TechCrunch](https://techcrunch.com/2026/08/09/anthropic-is-turning-claude-codes-auto-mode-on-by-default/) · [Simon Willison's notes](https://simonwillison.net/2026/Aug/8/auto-mode/)
]]></content:encoded>
    </item>

    <item>
      <title>Claude Code and Cowork Land in FedRAMP-High Government Desktop Beta</title>
      <link>https://claude.com/blog/bringing-claude-code-and-claude-cowork-to-government</link>
      <guid isPermaLink="false">2026-08-10-05-claude-for-government-desktop-beta</guid>
      <pubDate>Mon, 10 Aug 2026 12:14:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Claude Code</category>
      <category>Developer Tools</category>
      <description>Anthropic launches Claude for Government Desktop in public beta with FedRAMP High authorization — Claude Code and Cowork are now available to federal agencies, with tamper-evident audit logs and admin controls.</description>
      <content:encoded><![CDATA[
**Claude for Government Desktop is in public beta today — FedRAMP High authorized, with Claude Code and Cowork available to federal agencies and a $1/agency unlimited-seat price through August.**

Anthropic launched the public beta of Claude for Government Desktop today. It brings FedRAMP High authorization, Claude Code for software modernization, and Claude Cowork for document-heavy agency workflows (memo drafting, RFP reviews, casework). Admins get spend controls per department and tamper-evident audit logs that support the agency ATO process. A limited-time offer sets the price at $1 per agency with unlimited seats through August 2026.

**Why it matters:** Engineers at govtech firms and federal contractors can now use Claude Code in FedRAMP High environments without a custom authorization-to-operate process — the compliance lift just got much shorter.

**Sources:** [Bringing Claude Code and Cowork to government — Anthropic](https://claude.com/blog/bringing-claude-code-and-claude-cowork-to-government) · [Get started with Claude for Government — Anthropic Help Center](https://support.claude.com/en/articles/14503590-get-started-with-claude-for-government)
]]></content:encoded>
    </item>

    <item>
      <title>Why Pure Vector Search Fails on "React 18.3" and "GPT-4o" — and How BM25 + RRF Hybrid Search Fixes It</title>
      <link>https://www.youtube.com/watch?v=D8LmsEOJvPI</link>
      <guid isPermaLink="false">2026-08-10-04-hybrid-bm25-vector-search-rrf</guid>
      <pubDate>Mon, 10 Aug 2026 12:13:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>RAG</category>
      <category>Embeddings</category>
      <description>Vector search excels at meaning; BM25 excels at exact terms. Combining them with Reciprocal Rank Fusion lifts RAG recall from ~78% to ~91% and takes about 10 lines of LlamaIndex code.</description>
      <media:content url="https://img.youtube.com/vi/D8LmsEOJvPI/maxresdefault.jpg" medium="image" />
      <media:thumbnail url="https://img.youtube.com/vi/D8LmsEOJvPI/maxresdefault.jpg" />
      <content:encoded><![CDATA[
**Vector search excels at meaning; BM25 excels at exact terms. Combining them with Reciprocal Rank Fusion lifts RAG recall from ~78% to ~91% — and takes about 10 lines of LlamaIndex code.**

**What you'll be able to do after this:**
- Understand why exact-term failures happen in vector-only RAG and when hybrid search fixes them
- Implement a BM25 + dense vector retriever with RRF fusion using LlamaIndex in one runnable block
- Know when hybrid search is worth the added complexity (and when it isn't)

## The problem with vector-only retrieval

Dense embeddings encode *meaning* well — "GPT-4o" and "the latest OpenAI model" land close in embedding space. But that's also the problem: "GPT-4o" and "GPT-4.1" land close too, so version-number queries, product codes, API names, and identifiers confuse vector retrievers. A user searching "React 18.3 concurrent features" may miss the document that says exactly that, because the embedding for "React 18.3" overlaps with every React document.

**BM25 solves this.** BM25 is a keyword retrieval algorithm that scores documents by term frequency (TF) and inverse document frequency (IDF). A document containing "React 18.3" literally outscores a document that only discusses React in general — exact-term precision that embeddings trade away for semantic breadth.

## How Reciprocal Rank Fusion combines them

Run both retrievers in parallel, then merge their ranked lists with **RRF**:

```
score(doc) = Σ 1 / (k + rank_i(doc))
```

RRF works on *ranks*, not raw scores — which sidesteps the incompatibility between BM25's TF-IDF scores and cosine similarity values. No calibration needed. Benchmarks: hybrid + RRF reaches ~91% recall@10 vs ~78% for vector-only and ~65% for BM25-only.

## 10-line LlamaIndex implementation

```bash
pip install llama-index-core llama-index-retrievers-bm25
```

```python
from llama_index.core import VectorStoreIndex, SimpleDirectoryReader
from llama_index.core.retrievers import QueryFusionRetriever
from llama_index.retrievers.bm25 import BM25Retriever

docs = SimpleDirectoryReader("./data").load_data()
index = VectorStoreIndex.from_documents(docs)

vector_retriever = index.as_retriever(similarity_top_k=10)
bm25_retriever   = BM25Retriever.from_defaults(
    docstore=index.docstore, similarity_top_k=10
)

retriever = QueryFusionRetriever(
    [vector_retriever, bm25_retriever],
    similarity_top_k=5,
    mode="reciprocal_rerank",   # RRF
    num_queries=1,
    use_async=True,
)

nodes = retriever.retrieve("React 18.3 concurrent features")
```

For LangChain users: `EnsembleRetriever([bm25_retriever, vector_retriever], weights=[0.4, 0.6])` does the same thing with automatic RRF via its built-in `weighted_reciprocal_rank` implementation.

## When to add it

Add hybrid search when users query with specific names, codes, version numbers, or identifiers. Skip it if your corpus is purely conceptual (e.g., abstract policy FAQs) — pure vector search handles those fine, and the added complexity isn't worth it.

**Sources:** [BM25 Retriever — LlamaIndex official docs](https://developers.llamaindex.ai/python/examples/retrievers/bm25_retriever/) · [Hybrid Search in RAG Explained: BM25 + Semantic Search + RRF — YouTube](https://www.youtube.com/watch?v=D8LmsEOJvPI) · [Hybrid Search with Reranking — Qdrant official docs](https://qdrant.tech/documentation/tutorials-search-engineering/reranking-hybrid-search/)
]]></content:encoded>
    </item>

    <item>
      <title>Right-Size Your PR Reviews: GitHub Copilot Effort Levels (Lite vs. Balanced) Are Now GA</title>
      <link>https://github.blog/changelog/2026-08-07-copilot-code-review-effort-levels-are-generally-available/</link>
      <guid isPermaLink="false">2026-08-10-03-copilot-review-effort-levels-ga</guid>
      <pubDate>Mon, 10 Aug 2026 04:17:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Developer Tools</category>
      <category>Best Practices</category>
      <description>GitHub Copilot code review now offers Lite (fast, routine PRs) and Balanced (higher-reasoning, complex changes) effort levels GA — tune review depth per PR risk rather than one-size-fits-all.</description>
      <content:encoded><![CDATA[
**GitHub Copilot's code review now has two depth modes — Lite for straightforward changes and Balanced for complex ones — so review compute matches the actual risk of each pull request, not a fixed ceiling.**

GitHub shipped Lite and Balanced code review effort levels to general availability on August 7. Here's what each does and how to configure them.

## The problem they solve

Before effort levels, every PR triggered the same Copilot review depth — overkill for a documentation fix, and sometimes insufficient for a cross-service auth refactor. The levels let you express "routine vs. complex" intent and get a calibrated response.

## Lite vs. Balanced

**Lite** uses a lighter, faster model. Reach for it on:
- Documentation or copy fixes
- Config-only changes
- Formatting adjustments
- Narrow, well-isolated bug fixes

**Balanced** uses a higher-reasoning model for deeper analysis. Use it on:
- Security-sensitive changes (auth, permissions, cryptography)
- Cross-service or cross-API refactors
- New external-facing contracts or APIs
- Changes touching shared infrastructure or data models

Both levels work across Copilot Pro, Pro+, Max, Business, and Enterprise.

## Configuration

**Per-review**: Select the level when requesting a review. This overrides the org default for that one PR only — useful when a routine PR suddenly touches sensitive code.

**Org-wide default**: Go to **Organization Settings → Copilot → Copilot code review** and set the default. All repositories inherit it. Individual repos or reviewers can override it per-review.

**Audit trail**: The effort level now appears in PR timeline events and inline review comments, so you can always see which depth was applied to any historical review.

## Practical setup

A sensible starting configuration:
- Set org default to **Lite** — keeps baseline cost and latency low
- For repositories flagged as security-sensitive (auth service, billing, infra), configure **Balanced** as the repo-level default in repository settings
- Engineers can always request **Balanced** per-review on any PR that warrants it

The key insight: Copilot reviews have cost in two directions — compute and time. Defaulting to maximum depth universally wastes both; defaulting to minimum depth misses real issues. Effort levels give you the control to match depth to risk without per-PR manual triage.

**Sources:** [Copilot code review effort levels GA — GitHub Changelog](https://github.blog/changelog/2026-08-07-copilot-code-review-effort-levels-are-generally-available/) · [Configuring automatic code review — GitHub Docs](https://docs.github.com/en/copilot/how-tos/copilot-on-github/set-up-copilot/configure-automatic-review)
]]></content:encoded>
    </item>

    <item>
      <title>vLLM Hits 25K TPS/GPU and Ships 3x Long-Context Throughput with Decode Context Parallelism</title>
      <link>https://vllm.ai/blog/2026-08-07-decode-context-parallelism</link>
      <guid isPermaLink="false">2026-08-10-02-vllm-decode-parallelism-25k-tps</guid>
      <pubDate>Mon, 10 Aug 2026 04:16:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>LLM</category>
      <category>Self-Hosting</category>
      <description>Two vLLM milestones this week: 25K tokens/sec/GPU on Qwen3.5 with GB200 disaggregated serving; Decode Context Parallelism ships 3x throughput improvement for long-context agentic workloads.</description>
      <content:encoded><![CDATA[
**Two vLLM performance milestones this week signal how fast the inference stack is moving for long-context and agentic workloads.**

**25K total TPS/GPU on Qwen3.5 (Aug 6):** vLLM reached 25,000 tokens per second per GPU running Qwen3.5-397B-A17B-NVFP4 on NVIDIA GB200 NVL72 hardware. The result uses disaggregated prefill/decode serving (separate GPU pools for the two phases), Blackwell GDN kernels, HMA cache transfer, and async scheduling improvements. Most teams won't have GB200s yet — but disaggregated serving is available in vLLM today on standard hardware and the architectural pattern applies regardless of GPU generation.

**Decode Context Parallelism for long-context workloads (Aug 7):** For inference on 16k–128k token contexts, vLLM's new Decode Context Parallelism (DCP) shards the KV cache across GPUs by sequence dimension rather than by tensor layer. Result: 3× higher throughput on long-context agentic tasks compared to standard tensor parallelism. This matters directly if you're serving retrieval-augmented or multi-step agentic pipelines that regularly hit large context windows.

**Why it matters:** If you're designing a production vLLM deployment for an agentic system, both of these techniques reduce per-request latency and cost at scale. DCP is the more immediately practical one for teams already running vLLM — it activates for long-context workloads without requiring next-generation hardware.

**Sources:** [vLLM Reaches 25K TPS/GPU on Qwen3.5 — vLLM Blog](https://vllm.ai/blog/2026-08-06-qwen35-25k-tps) · [Efficient Decode Context Parallelism with vLLM — vLLM Blog](https://vllm.ai/blog/2026-08-07-decode-context-parallelism)
]]></content:encoded>
    </item>

    <item>
      <title>Run Any Open-Weight Model Locally: What GGUF Quantization Tags Actually Mean</title>
      <link>https://www.datacamp.com/tutorial/gguf-format-a-complete-guide</link>
      <guid isPermaLink="false">2026-08-10-01-gguf-quantization-ollama-levels</guid>
      <pubDate>Mon, 10 Aug 2026 04:15:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Self-Hosting</category>
      <category>LLM</category>
      <description>Q4_K_M is Ollama's default and the right pick for 99% of setups: 4.5GB for a 7B model, ~96% of full-precision quality. Here's what every GGUF tag means and how to choose.</description>
      <content:encoded><![CDATA[
**Q4_K_M is Ollama's default and covers 99% of local setups: 4.5GB for a 7B model, roughly 96% of full-precision quality. Here's what the letters and numbers in every GGUF model tag actually mean.**

**What you'll be able to do after this:**
- Read any GGUF model tag (Q4_K_M, Q8_0, IQ4_XS, Q5_K_M) and know what it means for RAM and quality
- Pull a specific quantized variant from Ollama or HuggingFace to match your hardware budget
- Make an informed decision about when to trade quality for size — and understand why bigger-at-lower-precision often wins

## What is GGUF?

When you run `ollama pull llama3.1`, Ollama downloads a GGUF file — a single portable bundle containing model weights, the tokenizer, and metadata. GGUF (GGML Unified Format) is the universal format for local LLM inference, used by Ollama, llama.cpp, LM Studio, and GPT4All.

GGUF files come in different quantization levels that compress the weights to reduce RAM and improve inference speed, at some quality cost. Think of it like JPEG compression: a Q4 quantization file is roughly 70% smaller than full-precision (fp16), while retaining ~96% of the original quality.

## Reading the tags

Ollama and HuggingFace model tags encode the quantization level directly. Here's a reference table for 7B-class models:

| Tag | Bits | ~RAM (7B model) | Quality |
|---|---|---|---|
| `fp16` | 16 | ~14 GB | Reference (100%) |
| `Q8_0` | 8 | ~8 GB | ≈99% |
| `Q5_K_M` | 5 (mixed) | ~5.5 GB | ≈98% |
| `Q4_K_M` | 4 (mixed) | ~4.5 GB | ≈96% |
| `IQ4_XS` | 4 (i-quant) | ~4.2 GB | ≈95% |
| `Q3_K_M` | 3 (mixed) | ~3.5 GB | ≈91% |
| `Q2_K` | 2 | ~2.8 GB | ≈82% |

**K-quants (Q4_K_M, Q5_K_S, Q3_K_L):** "K" means mixed precision. Attention layers (sensitive to precision loss) get slightly higher bit-width; feed-forward layers (less sensitive) get lower. M/S/L stand for Medium/Small/Large within the k-quant family — K_M is the balanced sweet spot.

**I-quants (IQ4_XS, IQ3_XS):** "I" means importance-based quantization, which allocates bits according to weight importance rather than uniformly across all layers. Slightly smaller files than k-quants at the same nominal bit level, with similar quality.

## How to pull a specific variant

Ollama defaults to Q4_K_M for official models. To override:

```bash
# Default pull — gets Q4_K_M automatically
ollama pull llama3.1

# Explicit variant by tag
ollama pull llama3.1:8b-instruct-q8_0       # higher quality, needs 8GB
ollama pull llama3.1:70b-instruct-q4_K_M    # 70B at Q4, ~40GB

# Pull GGUF models directly from HuggingFace
# bartowski maintains regularly updated, high-quality GGUF quants
ollama run hf.co/bartowski/Qwen3-8B-GGUF:Q4_K_M
ollama run hf.co/bartowski/Llama-3.2-3B-Instruct-GGUF:Q8_0
```

## Choosing a quantization level

Match the quantization to your available RAM:

- **≤8 GB RAM (laptop, Pi):** Q4_K_M or IQ4_XS for 7B models
- **16 GB RAM:** Q5_K_M or Q8_0 for 7B/8B; Q4_K_M for 13B
- **24 GB GPU (RTX 3090/4090):** Q8_0 for 13B; Q4_K_M for 70B
- **Apple Silicon (48 GB+):** Q8_0 for 70B models is practical

**Counter-intuitive rule:** A larger model at lower quantization almost always beats a smaller model at higher quantization. A 70B at Q4_K_M (~40 GB) will outperform an 8B at Q8_0 (~8 GB) for most reasoning tasks, if you have the RAM to run it at GPU speed.

The quality degradation below Q4 (Q3, Q2) becomes noticeable on reasoning-heavy tasks — responses get less coherent, facts less reliable. Q4_K_M is the floor for anything you'd put in front of a user.

## Why this matters for your stack

Every AI application that runs local inference — for cost, privacy, offline capability, or regulatory reasons — starts with this choice. Pick Q2 and you'll save RAM but degrade reasoning noticeably. Pick Q8 without enough RAM and the model spills to CPU, killing inference speed. Q4_K_M is the rational default because most hardware can run it fully on GPU, at a quality level where the degradation is invisible for real tasks.

**Sources:** [GGUF Format: A Complete Guide — DataCamp](https://www.datacamp.com/tutorial/gguf-format-a-complete-guide) · [Using Quantized Models with Ollama — Machine Learning Mastery](https://machinelearningmastery.com/using-quantized-models-with-ollama-for-application-development/) · [Ollama GGUF Quantization Guide — eastondev.com](https://eastondev.com/blog/en/posts/ai/20260422-ollama-gguf-quantization/) · [llama.cpp quantization types — GitHub](https://github.com/ggml-org/llama.cpp/blob/master/tools/quantize/README.md)
]]></content:encoded>
    </item>

    <item>
      <title>The History Collapse Pattern: Keep Long-Running Claude API Agents Focused and Affordable</title>
      <link>https://platform.claude.com/docs/en/build-with-claude/compaction</link>
      <guid isPermaLink="false">2026-08-09-08-claude-api-conversation-compression-pattern</guid>
      <pubDate>Sun, 09 Aug 2026 20:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>After N turns, inject one structured summary message and discard the raw history above it. Cost stays flat, the agent remembers what matters, and context never hits the wall mid-task.</description>
      <content:encoded><![CDATA[
**After N turns, inject one structured summary and truncate the raw message history above it — cost stays flat, the agent stays sharp, and you never hit the token wall mid-task.**

Claude Code has `/compact` to compress an interactive session. If you're building your own multi-turn agent with the Claude API, you need to wire this yourself. Here's the pattern.

## Why message history grows into a problem

Each API call sends the full `messages` array. A 10-turn coding agent with long file reads can easily accumulate 40,000–80,000 input tokens. At Claude Sonnet's current pricing, that's $0.12–$0.24 per turn *just in history* — and the longer the session runs, the worse it gets. Beyond cost, context quality degrades: early context crowds out attention for recent work.

The fix is not to blindly truncate (you lose decisions made 30 turns ago). The fix is to *compress semantically*: extract what matters and discard the rest.

## The compression call

```python
def compress_history(client, messages: list, model: str) -> list:
    """Collapses message history into one summary + system context."""
    if len(messages) < 2:
        return messages

    compression_prompt = """You are summarizing a coding agent's work session.
Produce a concise technical summary covering:
1. The task being worked on and its current state
2. Files created, modified, or deleted (with paths)
3. Key decisions made and why
4. Errors encountered and how they were resolved
5. Immediate next steps

Be specific and preserve exact file paths, function names, and error messages.
Format as structured markdown."""

    response = client.messages.create(
        model=model,
        max_tokens=2000,
        system=compression_prompt,
        messages=messages,
    )

    summary = response.content[0].text
    return [
        {"role": "user",
         "content": f"[SESSION CONTEXT — compressed from {len(messages)} messages]

{summary}"},
        {"role": "assistant",
         "content": "Understood. I'll continue from where we left off."},
    ]
```

## When to trigger it

```python
TOKEN_THRESHOLD = 60_000   # compress before you hit 80k+ expensive territory
TURN_THRESHOLD  = 20       # or every 20 turns

def should_compress(messages: list) -> bool:
    # Rough token estimate: ~4 chars per token
    approx_tokens = sum(len(str(m)) // 4 for m in messages)
    return approx_tokens > TOKEN_THRESHOLD or len(messages) > TURN_THRESHOLD * 2
```

## Full agent loop

```python
from anthropic import Anthropic

client = Anthropic()
MODEL  = "claude-sonnet-4-6-20261001"

messages = []

while True:
    user_input = input("You: ")
    if user_input.lower() == "exit":
        break

    messages.append({"role": "user", "content": user_input})

    if should_compress(messages[:-1]):          # compress everything except the new turn
        messages = compress_history(client, messages[:-1], MODEL) + [messages[-1]]
        print("[context compressed]")

    response = client.messages.create(
        model=MODEL,
        max_tokens=8096,
        messages=messages,
    )

    reply = response.content[0].text
    messages.append({"role": "assistant", "content": reply})
    print(f"Claude: {reply}")
```

## What to preserve in the summary

The compression prompt matters more than the timing. Include: current task state, file paths and their contents' purpose, key design decisions, open errors. Exclude: full file reads that can be re-read, exploratory dead ends.

For longer sessions, run two levels: compress every 20 turns into a "session chunk," then once per hour compress all chunks into a "project context." The pattern scales to sessions hours or days long.

**Sources:** [Compaction — Claude Platform Docs](https://platform.claude.com/docs/en/build-with-claude/compaction) · [Context Window Management — Claude Code Docs](https://code.claude.com/docs/en/context-management) · [Claude Code /compact Deep-Dive — claudefa.st](https://claudefa.st/blog/guide/mechanics/context-management)
]]></content:encoded>
    </item>

    <item>
      <title>Self-Host Your Content Moderation: Mistral Shieldstral 1.0 Is an Open-Weight 3B Guard Model</title>
      <link>https://www.marktechpost.com/2026/08/07/mistral-ai-releases-shieldstral-1-0-3b/</link>
      <guid isPermaLink="false">2026-08-09-07-mistral-shieldstral-safety-classifier</guid>
      <pubDate>Sun, 09 Aug 2026 20:06:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Security</category>
      <category>LLM</category>
      <description>Mistral released Shieldstral 1.0 (Aug 4, Apache 2.0) — a 3B multimodal safety classifier where you write moderation policies in plain English, run it on a 16GB GPU, and it matches models 7x its size.</description>
      <content:encoded><![CDATA[
**Mistral released Shieldstral 1.0 (Aug 4, Apache 2.0) — a 3B open-weight safety classifier where moderation policies are plain-English strings you pass at inference time, not categories baked into the model.**

Most guard models ship with fixed categories. Shieldstral inverts this: you write a natural-language policy ("block content promoting self-harm, harassment, or explicit material targeting minors") and the model scores against it on each call. Change what you're guarding against by changing a string — no retraining, no fine-tuning.

**Key specs:** 3B parameters, runs on a single 16GB GPU (A100, RTX 3090, or similar), covers text and images, 12 languages, Apache 2.0 on HuggingFace. Mistral reports parity with guard models up to 21B on text safety benchmarks and SOTA on multimodal moderation.

**For developers building agentic pipelines:** Shieldstral fits as a post-output filter — call it before any user-facing response leaves your system. Because the model is open-weight and runs locally, there's no outbound API, no data leaving your infrastructure, and updating your policy is a string change in your config.

**Why it matters:** Agentic systems that execute code, browse the web, and take real actions need a last-gate output filter that doesn't add a third-party API dependency. Shieldstral is the first open-weight option compact enough to serve on the same machine as your application server without a dedicated GPU node.

**Sources:** [Mistral AI Releases Shieldstral 1.0 3B — MarkTechPost](https://www.marktechpost.com/2026/08/07/mistral-ai-releases-shieldstral-1-0-3b/) · [Shieldstral 1.0 — Medium](https://medium.com/data-science-in-your-pocket/shieldstral-1-0-mistrals-policy-adaptive-3b-safety-classifier-ba3fe6e2f728) · [Mistral Releases Shieldstral for Multimodal Moderation — TestingCatalog](https://www.testingcatalog.com/mistral-releases-shieldstral-for-multimodal-moderation/)
]]></content:encoded>
    </item>

    <item>
      <title>The Adapter Trick That Makes LLM Fine-Tuning Free: LoRA and QLoRA Explained From the Ground Up</title>
      <link>https://unsloth.ai/docs/get-started/fine-tuning-llms-guide</link>
      <guid isPermaLink="false">2026-08-09-06-lora-qlora-mechanism-unsloth</guid>
      <pubDate>Sun, 09 Aug 2026 20:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Fine-Tuning</category>
      <category>HuggingFace</category>
      <description>LoRA adds two small matrices (B x A) to each weight layer and trains only those — about 1% of parameters. QLoRA freezes the base model in 4-bit so the whole thing fits on a free Colab T4 GPU.</description>
      <media:content url="https://unsloth.ai/images/unsloth.png" medium="image" />
      <media:thumbnail url="https://unsloth.ai/images/unsloth.png" />
      <content:encoded><![CDATA[
**LoRA freezes a model's weights and adds two small matrices (B×A) to each layer — only those train, roughly 1% of total parameters. QLoRA compresses the frozen base model to 4-bit so the whole thing fits on a free Colab GPU.**

Every other fine-tuning post in this curriculum has *used* LoRA. This one explains why it works and what the knobs actually do.

**What you'll be able to do after this:**
- Explain the B×A decomposition: why training a rank-r update captures meaningful behavioral change while touching a fraction of the weights
- Choose `r`, `lora_alpha`, and `target_modules` with intention instead of copying defaults
- Run a complete supervised fine-tune of Llama 3.1 8B on a free Colab T4 using Unsloth + QLoRA

## The problem LoRA solves

Full fine-tuning a 7B model requires storing gradients and optimizer states alongside the weights — roughly 80+ GB of memory. That's beyond any free GPU and barely fits on a single A100. You need to train *some* parameters without training all of them.

## The core mechanism

LoRA's insight: the updates a model needs during fine-tuning live in a low-dimensional subspace. Instead of updating weight matrix W (size d×k), you add an adapter ΔW = B×A where:

- **A** is (r × k) — initialized with random Gaussian values
- **B** is (d × r) — initialized to zero, so ΔW starts at zero and training begins from the pre-trained behavior
- **r** is the rank — your budget for adapter capacity

At inference time, the effective weight is W + (α/r) × B×A. Only B and A are trained; W is frozen.

## What r and alpha actually mean

| Hyperparameter | What it controls | Typical values |
|---|---|---|
| `r` (rank) | How many "directions of change" the adapter can represent — higher r = more capacity, more memory | 8, 16, 32, 64 |
| `lora_alpha` | Scales the adapter's contribution: effective scale = alpha/r | Set to r (1× scale) or 2r (2× scale) |
| `target_modules` | Which layer projections get adapters | Start with q_proj + v_proj; add k, o, gate, up, down_proj for stronger adaptation |

The *ratio* alpha/r matters more than either value alone. r=16, alpha=32 gives 2× scale — the adapter pushes harder relative to the base weights. r=64, alpha=64 gives 1× scale — more capacity but the same influence.

## QLoRA adds 4-bit compression

QLoRA loads the frozen base model in NF4 (normalized float 4-bit) format using `bitsandbytes`. An 8B model drops from ~16 GB to ~5–6 GB. Only the LoRA adapters (A and B matrices) are trained in BF16. Combined, a 7-8B QLoRA fine-tune fits in ~8-10 GB VRAM — well within a free Colab T4's 16 GB.

## Run it: the Unsloth SFT Colab

```python
# Install: pip install unsloth
from unsloth import FastLanguageModel

# Step 1 — load base model in 4-bit (QLoRA)
model, tokenizer = FastLanguageModel.from_pretrained(
    model_name="unsloth/Meta-Llama-3.1-8B-Instruct",
    max_seq_length=2048,
    load_in_4bit=True,   # NF4 quantization of the frozen base
)

# Step 2 — attach LoRA adapters
model = FastLanguageModel.get_peft_model(
    model,
    r=16,
    lora_alpha=16,
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj",
                    "gate_proj", "up_proj", "down_proj"],
    lora_dropout=0.05,
    bias="none",
    use_gradient_checkpointing="unsloth",  # 30% more VRAM savings
)

# Step 3 — SFTTrainer from TRL (standard HuggingFace fine-tuning loop)
from trl import SFTTrainer
from transformers import TrainingArguments

trainer = SFTTrainer(
    model=model,
    tokenizer=tokenizer,
    train_dataset=dataset,   # mlabonne/FineTome-100k or your own
    dataset_text_field="text",
    max_seq_length=2048,
    args=TrainingArguments(
        per_device_train_batch_size=2,
        gradient_accumulation_steps=4,
        warmup_steps=5,
        max_steps=60,         # ~30 min on a free T4
        learning_rate=2e-4,
        fp16=not is_bfloat16_supported(),
        bf16=is_bfloat16_supported(),
        output_dir="outputs",
    ),
)
trainer.train()
```

Unsloth's custom CUDA kernels train 2× faster and use 60% less VRAM than stock Hugging Face PEFT — critical on a 16 GB T4. The [Unsloth Llama 3.1 SFT Colab](https://colab.research.google.com/drive/1Ys44kVvmeZtnICzWz0xgpRnrIOjZAuxp?usp=sharing) includes the full loop: dataset loading, trainer config, evaluation, and GGUF export so you can run the result locally with Ollama.

**Sources:** [Fine-tuning LLMs Guide — Unsloth Docs](https://unsloth.ai/docs/get-started/fine-tuning-llms-guide) · [LoRA Hyperparameters Guide — Unsloth Docs](https://unsloth.ai/docs/get-started/fine-tuning-llms-guide/lora-hyperparameters-guide) · [Unsloth Llama 3.1 SFT Colab — Google Colab](https://colab.research.google.com/drive/1Ys44kVvmeZtnICzWz0xgpRnrIOjZAuxp?usp=sharing)
]]></content:encoded>
    </item>


    <item>
      <title>Your Agent Is Just a Python Class: NVIDIA's NOOA Collapses Tool Definitions, State, and Prompts Into One Object</title>
      <link>https://github.com/NVIDIA-NeMo/labs-OO-Agents</link>
      <guid isPermaLink="false">2026-08-09-05-nvidia-nooa-oop-agents</guid>
      <pubDate>Sun, 09 Aug 2026 12:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Agents</category>
      <category>Developer Tools</category>
      <description>NVIDIA's NOOA (open-sourced Aug 7, Apache 2.0) makes a Python class the entire agent: methods are tools, docstrings are prompts, and ... bodies complete at LLM runtime — model-agnostic via LiteLLM.</description>
      <content:encoded><![CDATA[
**NVIDIA's NOOA (open-sourced Aug 7, Apache 2.0) makes a Python class the entire agent — methods are tool calls, docstrings are prompts, and a `...` method body hands execution to the LLM at runtime.**

Most agent frameworks make you write tool schemas, register handlers, wire in a loop, and manage state separately. NOOA collapses all of that into one class. You write Python; the framework figures out the rest.

## The core idea

| Python construct | NOOA meaning |
|---|---|
| Instance field | Agent state the LLM can read and update |
| Class/method docstring | System prompt or tool description |
| Method body = `...` | LLM executes this at runtime |
| Method body = real code | Always deterministic, never LLM-completed |

Models plug in via LiteLLM — Claude, OpenAI, local Ollama, and any vLLM endpoint all work without changing your class.

## Quickstart

```bash
pip install nooa  # v0.0.8, Apache 2.0, Python 3.12-3.13
```

```python
from nooa import Agent

class FileOrganizer(Agent):
    """You are a file organization assistant."""

    working_dir: str = "."
    organized: list[str] = []

    def list_files(self) -> list[str]:
        """List all files in the working directory."""
        import os
        return os.listdir(self.working_dir)

    def move_file(self, filename: str, destination: str) -> str:
        """Move a file to a destination folder. Create the folder if needed."""
        ...  # LLM decides when and how to call this

    def summarize(self) -> str:
        """Summarize what was organized and why."""
        ...  # LLM writes the summary

agent = FileOrganizer(working_dir="./src")
result = agent.run(
    "Organize the Python files into subfolders by module.",
    model="anthropic/claude-sonnet-4-6"  # any LiteLLM model string
)
print(result)
```

Methods with `...` bodies are completed by the LLM-driven loop — it decides when to call them, with what arguments, and in what order. Methods with real Python bodies always run deterministically. You mix both freely in one class, which means you can test, trace, and refactor agent behavior exactly like ordinary software.

## Benchmark context

NVIDIA reports 82.2% on SWE-bench Verified and 86.8% on CyberGym L1 at roughly half the tokens of comparable open harnesses — attributed to NOOA's structured state reducing prompt bloat.

**Status:** research preview (alpha on PyPI) — not production-ready for critical paths, but the design is immediately useful for prototyping agents that behave like regular code.

**Sources:** [NVIDIA NOOA on GitHub (Apache 2.0)](https://github.com/NVIDIA-NeMo/labs-OO-Agents) · [arXiv: NVIDIA-labs OO Agents](https://arxiv.org/abs/2607.20709) · [nooa on PyPI](https://pypi.org/project/nooa/) · [NVIDIA AI Releases NOOA — MarkTechPost](https://www.marktechpost.com/2026/08/07/nvidia-ai-releases-nooa-an-object-oriented-python-framework/)
]]></content:encoded>
    </item>

    <item>
      <title>Your Database Already Has a Vector Store: Semantic Search in PostgreSQL in 5 Steps With pgvector and Supabase</title>
      <link>https://supabase.com/docs/guides/ai/semantic-search</link>
      <guid isPermaLink="false">2026-08-09-04-pgvector-supabase-vector-store</guid>
      <pubDate>Sun, 09 Aug 2026 12:06:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Vectors</category>
      <category>Embeddings</category>
      <description>pgvector turns any PostgreSQL database into a vector store. Enable the extension, create a vector column, build an HNSW index, and run semantic search — no separate vector service needed.</description>
      <media:content url="https://supabase.com/images/og/supabase-og.png" medium="image" />
      <media:thumbnail url="https://supabase.com/images/og/supabase-og.png" />
      <content:encoded><![CDATA[
**pgvector is a PostgreSQL extension that adds a native vector column type and nearest-neighbor operators — enable it in any Postgres database (Supabase's free tier included) to get semantic search without a separate vector service.**

**What you'll be able to do after this:**
- Store embeddings in the same database that holds the rest of your application data — no extra service, no sync lag, JOIN-able with your users and metadata
- Build an HNSW index that keeps nearest-neighbor queries under 10ms at 1M+ vectors
- Write a complete semantic search pipeline — raw text to ranked results — entirely in PostgreSQL and Python

## Why put vectors in Postgres?

Running a separate vector database doubles your operational surface area and breaks JOIN semantics: you can't filter by user ID, access control, or any column in your app database in a single query. pgvector keeps everything in one place — and on Supabase, you get a free tier with a REST API and Python client out of the box.

## Step 1: Enable pgvector

On Supabase: Dashboard → Database → Extensions → toggle **vector**. Or raw SQL:

```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

## Step 2: Create a table with a vector column

```sql
CREATE TABLE documents (
  id        BIGSERIAL PRIMARY KEY,
  content   TEXT NOT NULL,
  embedding VECTOR(384)   -- match your embedding model's output dimension
);
```

Common dimensions: `384` for `all-MiniLM-L6-v2`, `768` for `all-mpnet-base-v2`, `1536` for OpenAI's text-embedding-3-small.

## Step 3: Insert embeddings from Python

```python
from sentence_transformers import SentenceTransformer
from supabase import create_client

model = SentenceTransformer("all-MiniLM-L6-v2")  # 384 dims, fast, free
sb = create_client(SUPABASE_URL, SUPABASE_KEY)

texts = [
    "Claude Code can spawn parallel subagents for independent tasks.",
    "Use HNSW indexes for fast approximate nearest-neighbor search.",
]
embeddings = model.encode(texts).tolist()

sb.table("documents").insert([
    {"content": t, "embedding": e}
    for t, e in zip(texts, embeddings)
]).execute()
```

## Step 4: Build an HNSW index

```sql
CREATE INDEX ON documents
USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 64);
```

HNSW is the recommended index type for 2026 — it builds a navigable graph so queries skip exhaustive comparison. Benchmarks show 95%+ recall on 1M-vector datasets with sub-10ms p99 latency on a standard Supabase Pro instance.

## Step 5: Query

```python
query_vec = model.encode("How do I run agents in parallel?").tolist()
# Using raw SQL via psycopg2:
cur.execute("""
    SELECT content,
           1 - (embedding <=> %s::vector) AS similarity
    FROM   documents
    ORDER  BY embedding <=> %s::vector
    LIMIT  5;
""", (query_vec, query_vec))
```

The `<=>` operator is cosine distance; `<->` is L2; `<#>` is negative inner product. Pick `<=>` for text embeddings — it's distance-normalized by default.

**Sources:** [Semantic Search — Supabase Docs](https://supabase.com/docs/guides/ai/semantic-search) · [pgvector Extension — Supabase Docs](https://supabase.com/docs/guides/database/extensions/pgvector) · [HNSW Indexes — Supabase Docs](https://supabase.com/docs/guides/ai/vector-indexes/hnsw-indexes)
]]></content:encoded>
    </item>

    <item>
      <title>Your Claude Code Safety Rules Are Suggestions Until You Use Hooks</title>
      <link>https://code.claude.com/docs/en/advanced-usage/hooks</link>
      <guid isPermaLink="false">2026-08-09-03-claude-code-hooks-guardrails</guid>
      <pubDate>Sun, 09 Aug 2026 04:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>CLAUDE.md instructions drift as context grows; hooks fire deterministically every time. PreToolUse blocks before execution; PostToolUse enforces cleanup — three patterns to wire up now.</description>
      <content:encoded><![CDATA[
**CLAUDE.md instructions drift when context grows long — hooks are deterministic shell commands the harness runs on every matching event, unconditionally. That's the difference between a suggestion and a rule.**

Your CLAUDE.md can say "never use `rm -rf`" or "always run lint after editing." The model follows those when it's paying attention. A hook fires whether it's paying attention or not. The two surfaces are complementary: CLAUDE.md sets *intent*, hooks enforce the *constraint*.

## The two hook types you'll use most

**PreToolUse** fires before the tool runs. Exit code `2` = block (Claude sees the stderr output and retries without that action). Any other non-zero = warn but proceed. Use for guards.

**PostToolUse** fires after the tool completes — it can't undo what happened, but it runs unconditionally. Use for formatting, test runs, audit logging.

Configure in `.claude/settings.json` committed to the project root so every teammate gets it:

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [{ "type": "command", "command": "~/.claude/hooks/block-danger.sh" }]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "~/.claude/hooks/auto-format.sh" }]
      }
    ]
  }
}
```

## Three patterns to wire up now

**1. Block dangerous Bash (PreToolUse, matcher `Bash`)**

```bash
#!/usr/bin/env bash
cmd=$(cat | jq -r '.command')
if echo "$cmd" | grep -qE 'rm -rf|git push --force|DROP TABLE'; then
  echo "BLOCKED: $cmd" >&2; exit 2
fi
```

Exit 2 causes Claude to explain what it tried and propose a safer alternative.

**2. Auto-format on every file write (PostToolUse, matcher `Edit|Write`)**

```bash
#!/usr/bin/env bash
file=$(cat | jq -r '.path // empty')
[[ -z "$file" ]] && exit 0
[[ "$file" == *.py ]]                     && { black --quiet "$file"; exit 0; }
[[ "$file" == *.ts || "$file" == *.tsx ]] && npx prettier --write "$file"
```

**3. Run affected tests after edits (PostToolUse, matcher `Edit|Write`)**

```bash
#!/usr/bin/env bash
file=$(cat | jq -r '.path // empty')
[[ -z "$file" ]] && exit 0
pnpm test --passWithNoTests --testPathPattern="$(basename "$file")" 2>&1 | tail -5
```

The model sees hook stdout as context — when `block-danger` fires, Claude explains what it tried and proposes a safer alternative automatically.

**Sources:** [Claude Code Hooks — Anthropic Docs](https://code.claude.com/docs/en/advanced-usage/hooks) · [Claude Code Settings Reference — code.claude.com](https://code.claude.com/docs/en/settings) · [Claude Code Hooks Setup with Examples — kjetilfuras.com](https://kjetilfuras.com/claude-code-hooks/)
]]></content:encoded>
    </item>

    <item>
      <title>Kimi K3 Lands in GitHub Copilot and GitHub Spark Retires August 31</title>
      <link>https://github.blog/changelog/2026-08-06-kimi-k3-is-now-available-in-github-copilot/</link>
      <guid isPermaLink="false">2026-08-09-02-kimi-k3-copilot-spark-retires</guid>
      <pubDate>Sun, 09 Aug 2026 04:06:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Developer Tools</category>
      <category>LLM</category>
      <description>Kimi K3, an open-weight frontier model, is now GA in GitHub Copilot at $3/$15/M on Fireworks. GitHub Spark stops accepting new apps Aug 4 and shuts down fully Aug 31.</description>
      <content:encoded><![CDATA[
**Two GitHub developer platform changes this week: a strong open-weight model joins Copilot's roster, and GitHub's AI web-app builder reaches end of life August 31.**

**Kimi K3 now GA in GitHub Copilot (Aug 6).** Moonshot AI's Kimi K3 — an open-weight model with frontier-level agentic coding scores — is available for Copilot Pro, Pro+, Max, Business, and Enterprise. Pricing: $3/M input, $15/M output, $0.30/M cached, hosted on Fireworks AI. *Copilot Business and Enterprise have it off by default* — admins must enable Kimi K3 in Copilot settings before anyone on the team can select it. (The rollout briefly paused due to a GitHub Actions incident but resumed the same week.)

**GitHub Spark retires August 31.** The AI-powered web app prototyping tool stopped accepting new users and apps on August 4 and shuts down fully August 31. GitHub's position: agentic development tools have advanced to the point where developers can build the same things in Copilot's native workflow — VS Code, Copilot CLI, or the Copilot desktop app. **Action before deadline:** open your Spark workbench → … → *Create repository* to export your app code. If your app uses `llm()`, swap in your own inference provider before shutdown or the AI features stop working.

**Why it matters:** Kimi K3 gives teams a cost-effective open-weight option for long agentic sessions without leaving their Copilot subscription. Spark's retirement is the clearest signal yet that standalone AI-assisted low-code builders are being absorbed into general-purpose coding agents.

**Sources:** [Kimi K3 is now available in GitHub Copilot — GitHub Changelog](https://github.blog/changelog/2026-08-06-kimi-k3-is-now-available-in-github-copilot/) · [Upcoming deprecation of GitHub Spark — GitHub Changelog](https://github.blog/changelog/2026-08-04-upcoming-deprecation-of-github-spark-on-github-com/)
]]></content:encoded>
    </item>

    <item>
      <title>Why Your RAG Loses the Best Answer at Rank 30 — and How a Cross-Encoder Fixes It</title>
      <link>https://sbert.net/examples/sentence_transformer/applications/retrieve_rerank/README.html</link>
      <guid isPermaLink="false">2026-08-09-01-cross-encoder-reranking-rag</guid>
      <pubDate>Sun, 09 Aug 2026 04:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>RAG</category>
      <category>Embeddings</category>
      <description>Bi-encoders retrieve fast but rank loosely. A cross-encoder reranker rescores the top 100 by reading query and document together — the single highest-ROI RAG fix most developers skip.</description>
      <media:content url="https://huggingface.co/front/thumbnails/v2-2.png" medium="image" />
      <media:thumbnail url="https://huggingface.co/front/thumbnails/v2-2.png" />
      <content:encoded><![CDATA[
**Bi-encoders retrieve 100 candidates in milliseconds; cross-encoders score each (query, doc) pair together and rerank precisely — combining both into a pipeline is the single biggest RAG accuracy improvement most developers skip.**

**What you'll be able to do after this:**
- Understand why fast bi-encoder retrieval misranks candidates and where the precision goes
- Add a cross-encoder reranker to any existing RAG pipeline without touching your vector database
- Choose between `cross-encoder/ms-marco-MiniLM-L-6-v2` (fast, English) and `BAAI/bge-reranker-v2-m3` (stronger, multilingual) based on your latency/quality trade-off

## Why bi-encoders misrank

Your vector search encodes the query and each document *independently*, then measures cosine similarity between the resulting vectors. This is fast — millions of comparisons per second — but the two encodings never see each other. The model can't ask "does this passage actually answer this specific question?" It asks "are these vectors similar?" That mismatch is why the correct answer often lands at rank 30 even when it's somewhere in the top 100.

## The two-stage pattern: retrieve wide, rerank precise

1. **Retrieve 100 candidates** with your bi-encoder and vector DB (fast, cheap, unchanged from your current setup)
2. **Rerank to top 5–10** with a cross-encoder (slower, but sees query and document simultaneously)

A cross-encoder takes `[query] [SEP] [document]` as a single input and outputs one relevance score. Attention crosses the boundary — every query token attends to every document token. That's what makes it accurate, and also why you can't run it over millions of documents at retrieval time.

## Implementation

```python
from sentence_transformers import SentenceTransformer, CrossEncoder

# Stage 1 — your existing bi-encoder retrieval (unchanged)
bi_encoder = SentenceTransformer("all-MiniLM-L6-v2")
query_emb = bi_encoder.encode(query, convert_to_tensor=True)
top_100 = vector_db.query(query_emb, n=100)  # your vector DB call

# Stage 2 — cross-encoder reranking
reranker = CrossEncoder("cross-encoder/ms-marco-MiniLM-L-6-v2")
pairs = [(query, doc["text"]) for doc in top_100]
scores = reranker.predict(pairs)  # ~50ms for 100 pairs on CPU

ranked = sorted(zip(scores, top_100), reverse=True)
final_results = [doc for _, doc in ranked[:5]]
```

`cross-encoder/ms-marco-MiniLM-L-6-v2`: 6-layer MiniLM, trained on MS MARCO passage ranking (a massive human-judged relevance dataset), fast enough to score 100 candidates in well under 100ms on a CPU. Step up to `BAAI/bge-reranker-v2-m3` for multilingual support or stronger long-document precision.

## What to expect

Precision@5 typically improves 20–40% on a domain-specific corpus after adding a reranker. The gain is largest when documents are long and variable — exactly where cosine similarity over compressed embeddings struggles most. The canonical end-to-end notebook (Simple English Wikipedia demo) is at the sbert.net Retrieve & Re-Rank page.

**Sources:** [Retrieve & Re-Rank — Sentence Transformers](https://sbert.net/examples/sentence_transformer/applications/retrieve_rerank/README.html) · [CrossEncoder Usage — sbert.net](https://sbert.net/docs/cross_encoder/usage/usage.html) · [Pretrained Cross-Encoder Models — sbert.net](https://sbert.net/docs/cross_encoder/pretrained_models.html)
]]></content:encoded>
    </item>

    <item>
      <title>Stop Forcing One Tool to Do Both Jobs: A RAG vs Fine-Tuning Decision Framework</title>
      <link>https://huggingface.co/blog/mlabonne/sft-llama3</link>
      <guid isPermaLink="false">2026-08-08-07-rag-vs-fine-tuning-decision-framework</guid>
      <pubDate>Sat, 08 Aug 2026 20:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Fine-Tuning</category>
      <category>HuggingFace</category>
      <description>RAG handles what the model knows; fine-tuning handles how it responds. Mixing these up is the most common LLM engineering mistake. A decision framework plus a runnable QLoRA walk-through with Unsloth on a free Colab T4.</description>
      <media:content url="https://huggingface.co/front/thumbnails/v2-2.png" medium="image" />
      <media:thumbnail url="https://huggingface.co/front/thumbnails/v2-2.png" />
      <content:encoded><![CDATA[
**RAG handles "what the model knows" — fine-tuning handles "how it responds." Mixing up these two jobs is the most common LLM engineering mistake and the easiest to fix.**

**What you'll be able to do after this:**
- Identify in under five minutes whether your problem needs RAG, fine-tuning, prompt engineering, or none of the above
- Understand why most production apps use both, and how to divide the work cleanly between them
- Start a standard supervised QLoRA fine-tune on a free Colab T4 using Unsloth

## The decision frame

RAG and fine-tuning solve different problems. Confusing them burns weeks on the wrong tool.

| Problem | Right tool |
|---|---|
| Model doesn't know recent or private information | RAG |
| Answers need to cite sources or be auditable | RAG |
| Model consistently uses wrong format or tone | Fine-tuning |
| Classification labels are inconsistent | Fine-tuning |
| Policy adherence is unstable | Fine-tuning |

**RAG is the default.** For roughly 80% of production use cases, it's the right starting point:
- Update source data without retraining
- Cite which document generated each answer
- Switch base models freely at any time

**Fine-tune when behavior is the failure mode, not knowledge.** If your pipeline retrieves the right documents but the model formats the answer wrong, that's a behavior problem — and behavior lives in the weights, not in retrieval. Fine-tuning teaches the model *how* to respond; RAG teaches it *what* to say.

**Prompt engineering first.** Before reaching for either tool, try few-shot examples in the system prompt. Many "we need to fine-tune" problems dissolve with 3–5 well-chosen examples. Only move to fine-tuning when the examples need to be so numerous or consistent that they'd overflow every context window.

## The hybrid pattern (most production apps)

Most mature applications use both:
- **RAG layer:** current documents, product data, user history, tickets — anything that changes
- **Fine-tuned layer:** consistent output structure, domain vocabulary, classification behavior, stable tone

They compose cleanly: the retrieval system passes relevant chunks into context; the fine-tuned model knows how to turn those chunks into correctly structured responses.

## Start with supervised QLoRA on a free Colab T4

Once fine-tuning is the right tool, the standard path is **supervised fine-tuning (SFT)** with QLoRA. Maxime Labonne's [canonical HuggingFace guide](https://huggingface.co/blog/mlabonne/sft-llama3) walks through the full loop:

```bash
pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
pip install --no-deps trl peft accelerate bitsandbytes
```

Key steps:
1. **Load with 4-bit quantization** — QLoRA compresses the base model from 32-bit to 4-bit, so an 8B model fits on a free T4 GPU (16 GB VRAM)
2. **Add LoRA adapters** — only ~1% of parameters are trained; the rest are frozen, preserving the base model's capabilities
3. **Format in ChatML** — instruction datasets need `<|im_start|>system` / `<|im_start|>user` / `<|im_end|>` wrapping for the model to generalize correctly
4. **SFTTrainer from TRL** — handles the training loop, prompt-token masking, and packing
5. **Export** — save to GGUF or push to the Hub for inference in Ollama or vLLM

Result: an 8B model fine-tuned on your task in under 2 hours, at zero GPU cost.

**Sources:** [Fine-tune Llama 3.1 Ultra-Efficiently with Unsloth — Maxime Labonne, HuggingFace](https://huggingface.co/blog/mlabonne/sft-llama3) · [RAG vs Fine-Tuning for LLMs: A Comprehensive Guide — HuggingFace](https://huggingface.co/blog/airabbitX/rag-vs-fine-tuning-for-llms-a-com) · [Fine-Tuning vs RAG in 2026: A Decision Guide — codercops.com](https://blog.codercops.com/blog/fine-tuning-vs-rag-llm-decision-guide-2026)
]]></content:encoded>
    </item>

    <item>
      <title>GitHub Copilot in VS Code Now Gives Each Agent Session Its Own Git Worktree</title>
      <link>https://github.blog/changelog/2026-07-30-github-copilot-in-visual-studio-code-july-2026-releases/</link>
      <guid isPermaLink="false">2026-08-08-08-github-copilot-vscode-july-worktrees-parallel</guid>
      <pubDate>Sat, 08 Aug 2026 20:06:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Developer Tools</category>
      <category>Agents</category>
      <description>The July 2026 Copilot in VS Code release gives each agent session its own isolated git worktree, adds /side for parallel side chats, and surfaces subagent model and elapsed-time details inline.</description>
      <content:encoded><![CDATA[
**The July VS Code update gives every Copilot agent session an isolated git worktree so parallel sessions can't step on each other — and a /side command lets you open a second chat without disrupting your main task.**

The [July 2026 Copilot in VS Code release](https://github.blog/changelog/2026-07-30-github-copilot-in-visual-studio-code-july-2026-releases/) ships four additions worth knowing:

- **Git worktrees per session:** start a Copilot, Claude, or Codex session in its own worktree — each agent works on an isolated copy of the repo, preventing conflicts when two agents run in parallel
- **Side chats (`/side`):** open a parallel exploration thread that shares the session context but keeps its own history and title, so you can ask a tangential question without losing your main thread
- **Subagent visibility:** each subagent shows its model name, elapsed time, and active tool call inline in the session panel
- **Credit usage:** Copilot Business and Enterprise users can see their billing-cycle credit consumption in the VS Code status menu

**Why it matters:** Git worktree isolation for agents solves the same race condition that `isolation: worktree` in Claude Code's SDK addresses — two agents editing the same files simultaneously is a data-loss scenario. Copilot's built-in worktree support makes parallel coding agents production-safe without extra configuration.

**Sources:** [GitHub Copilot in VS Code, July 2026 releases — GitHub Changelog](https://github.blog/changelog/2026-07-30-github-copilot-in-visual-studio-code-july-2026-releases/)
]]></content:encoded>
    </item>

    <item>
      <title>Four Memory Layers in Claude Code: Stop Putting Everything in CLAUDE.md</title>
      <link>https://code.claude.com/docs/en/memory</link>
      <guid isPermaLink="false">2026-08-08-09-claude-code-memory-layers-claude-md</guid>
      <pubDate>Sat, 08 Aug 2026 20:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Claude Code has four distinct surfaces for persistent context — CLAUDE.md files (three scopes), hooks, skills, and auto-memory. Most developers only use one. A field guide to what belongs where.</description>
      <content:encoded><![CDATA[
**Claude Code has four distinct surfaces for persistent context — CLAUDE.md files, hooks, skills, and auto-memory — and most developers only use one. Knowing which to use triples session effectiveness.**

The common failure mode: a 600-line CLAUDE.md that buries critical rules in the middle, where the "lost in the middle" attention effect means the model reads them but doesn't weight them. The fix is matching each type of persistent information to the surface designed to carry it.

## The four surfaces

### 1. CLAUDE.md files (three scopes, additive)

CLAUDE.md loads into every session's context at startup. Three scope layers stack additively — all contribute simultaneously, with narrower scopes taking precedence on conflicts:

| Scope | Location | Use for |
|---|---|---|
| Project | `.claude/CLAUDE.md` | Codebase conventions, architecture notes, run commands — things Claude would get wrong without them |
| Local | `.claude/CLAUDE.local.md` | Your personal machine overrides, test credentials, local paths — gitignored, never shared |
| User | `~/.claude/CLAUDE.md` | Cross-project preferences you always want: output style, preferred tools, personal workflow rules |

**Keep each file under ~200–300 lines.** The "lost in the middle" effect means accuracy drops 30%+ for content buried in a long context. Front-load the most critical instructions at the top.

### 2. Hooks — enforced, deterministic side effects

If a rule must be enforced regardless of what Claude decides, put it in a hook. Hooks run shell commands on specific events and block or allow based on exit code. They can't be "ignored" the way a CLAUDE.md instruction can:

```json
{
  "hooks": {
    "PostToolUse": [{
      "matcher": "Write|Edit",
      "hooks": [{"type": "command", "command": "node scripts/validate-blog.mjs"}]
    }]
  }
}
```

Use hooks for: linting on save, secret scanning before push, running tests after edits, blocking banned commands.

### 3. Skills (`.claude/skills/`) — reusable multi-step workflows

A skill is a markdown file with instructions Claude loads on demand. Use skills for complex, repeatable multi-step workflows (publish a post, scaffold a feature, run a review) that shouldn't be in every session's context budget.

Unlike CLAUDE.md, skills only load when invoked with the Skill tool or a slash command — zero context cost at startup.

### 4. Auto-memory

When you correct Claude ("always use single quotes in this project"), Claude writes a note to `.claude/memory/` that persists across sessions without any manual CLAUDE.md editing. This is behavioral learning built into the loop.

## What goes where: the cheat sheet

| Type of information | Right surface |
|---|---|
| "How this codebase works" | CLAUDE.md (project scope) |
| "My local environment quirks" | CLAUDE.md (local scope) |
| "Preferences across all my projects" | CLAUDE.md (user scope) |
| "Always lint before commit" | Hooks |
| "Do this 10-step workflow on request" | Skill |
| "Remember my style correction" | Auto-memory |

**Sources:** [How Claude remembers your project — code.claude.com](https://code.claude.com/docs/en/memory) · [Explore the .claude directory — code.claude.com](https://code.claude.com/docs/en/claude-directory) · [Claude Code settings — code.claude.com](https://code.claude.com/docs/en/settings)
]]></content:encoded>
    </item>

    <item>
      <title>Automate Your Team's Agentic Workflows with a Comment: GitHub Copilot Cloud Agent Automations</title>
      <link>https://github.blog/changelog/2026-08-03-trigger-copilot-automations-with-comments/</link>
      <guid isPermaLink="false">2026-08-08-06-github-copilot-comment-automations-agentic-trigger</guid>
      <pubDate>Sat, 08 Aug 2026 12:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Agents</category>
      <category>Developer Tools</category>
      <description>GitHub Copilot can now run a full cloud agent task whenever someone posts a matching comment on an issue or PR — type /docs on a PR and Copilot writes the documentation and opens a follow-up PR.</description>
      <content:encoded><![CDATA[
**Type `/docs` on any PR comment and GitHub's cloud agent writes the documentation, opens a follow-up PR, and closes the loop — no buttons, no dashboard navigation required.**

GitHub shipped comment-triggered Copilot cloud agent automations on Aug 3. Define an automation with a trigger phrase; whenever anyone posts that phrase in an issue or PR comment, Copilot runs the agent task automatically. The result lands as a new PR, a follow-up issue, or a reply comment — depending on what your automation is configured to do.

## Set up a comment trigger

1. Go to your repo → **Agents** → **Automations** → **New automation**
2. Set the trigger type to "Issue or PR comment" and enter the trigger phrase (e.g. `/docs`)
3. Write the agent instructions for what it should do when triggered
4. Save and enable

Now anyone with repo access can fire the automation by commenting the trigger phrase.

## Three workflows worth wiring up now

**Auto-docs on PR merge:**
```
Trigger: /docs
Instructions: Read the diff in this PR. Update or write the relevant
docstrings, README sections, and inline comments to reflect the changes.
Open a new PR targeting this branch with the documentation updates only.
```

**Bug investigation on demand:**
```
Trigger: /investigate
Instructions: Reproduce the reported issue from this issue description.
Search the codebase for the likely root cause. Post a comment with 3
likely causes, the relevant files, and suggested first steps.
```

**Tech-debt tracker:**
```
Trigger: /track-debt
Instructions: Review the changes in this PR. Identify shortcuts, hardcoded
values, missing tests, and deferred work. Create a GitHub issue for each
one, labeled tech-debt, with a clear description and suggested fix.
```

## Also shipping Aug 3: adjustable reasoning level

The same release adds a reasoning level control (low / medium / high) when you start a Copilot cloud agent task. Use `low` for fast, mechanical tasks (renaming, formatting); `high` for open-ended investigation or cross-file refactors that benefit from deeper planning.

Comment automations are available on Copilot Pro, Pro+, Max, Business, and Enterprise. Business/Enterprise requires the Copilot cloud agent policy enabled by an admin.

**Sources:** [Trigger Copilot automations with comments — GitHub Changelog](https://github.blog/changelog/2026-08-03-trigger-copilot-automations-with-comments/) · [Customize reasoning level for Copilot cloud agent — GitHub Changelog](https://github.blog/changelog/2026-08-03-customize-the-reasoning-level-for-copilot-cloud-agent/) · [Copilot cloud agent docs — GitHub Docs](https://docs.github.com/en/copilot/how-tos/use-copilot-agents/cloud-agent)
]]></content:encoded>
    </item>

    <item>
      <title>Claude Code v2.1.225: Gateway Spend Warnings Now Name the Cap, Agent Trust Prompt Added</title>
      <link>https://code.claude.com/docs/en/changelog</link>
      <guid isPermaLink="false">2026-08-08-05-claude-code-v2125-gateway-spend-workspace-trust</guid>
      <pubDate>Sat, 08 Aug 2026 12:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Claude Code</category>
      <category>Developer Tools</category>
      <description>Claude Code v2.1.225 names the specific spend cap and reset time when a gateway limit is hit, and now prompts before claude agents runs in untrusted directories. v2.1.226 is bug-fix only.</description>
      <content:encoded><![CDATA[
**Claude Code v2.1.225 makes gateway spend-limit messages actionable — you now see which cap was hit, when it resets, and the operator's note — instead of a generic "limit reached."**

Two additions in v2.1.225 (Aug 7), no feature changes in v2.1.226 (Aug 8):

**Gateway spend-limit messages.** When a Claude Apps Gateway spend cap is reached, the usage warning now displays the cap name, its reset time, and any message the operator configured — so a CI job that stalls on a budget limit tells you exactly what hit and when it clears, rather than sending you to the gateway dashboard to find out. Requires gateway version 2.1.225+.

**Workspace trust prompt for `claude agents`.** The `claude agents` command now asks whether to trust an untrusted directory before running, matching the existing behavior of `claude`. This prevents an agent session from silently operating on code outside its intended scope when launched from an unfamiliar or shared directory.

**Why it matters:** If you're running Claude Code through the Claude Apps Gateway (which adds SSO and per-user spend caps for teams on AWS or Google Cloud), the improved spend message removes the main friction point when a budget cap trips in CI: you no longer need to context-switch to the gateway dashboard to understand what happened and when you can retry.

**Sources:** [Claude Code changelog — code.claude.com](https://code.claude.com/docs/en/changelog) · [Release v2.1.225 — github.com/anthropics/claude-code](https://github.com/anthropics/claude-code/releases/tag/v2.1.225) · [Claude Apps Gateway — byteiota.com](https://byteiota.com/claude-apps-gateway-sso-spend-caps-claude-code/)
]]></content:encoded>
    </item>

    <item>
      <title>From Text to Vectors: Compute Your First Text Embeddings with sentence-transformers</title>
      <link>https://www.sbert.net/docs/quickstart.html</link>
      <guid isPermaLink="false">2026-08-08-04-sentence-transformers-text-embeddings-foundations</guid>
      <pubDate>Sat, 08 Aug 2026 12:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Embeddings</category>
      <category>HuggingFace</category>
      <description>Three lines of Python turn any text into a 384-dimensional vector — then cosine similarity shows you exactly how closely two sentences mean the same thing, even when they share no words.</description>
      <media:content url="https://www.sbert.net/_static/logo.png" medium="image" />
      <media:thumbnail url="https://www.sbert.net/_static/logo.png" />
      <content:encoded><![CDATA[
**Three lines of Python turn any text into a 384-dimensional vector — and cosine similarity tells you exactly how closely two sentences mean the same thing, even when they share no words.**

**What you'll be able to do after this:**
- Turn raw text into dense vector representations using a pre-trained model — no training required, runs on CPU
- Measure semantic similarity between any two strings using cosine similarity, even when they share no vocabulary
- Understand what an embedding actually *is* and why the geometric distance between vectors captures meaning — the foundation for every RAG pipeline, semantic search engine, and recommendation system

## Install

```bash
pip install sentence-transformers
```

The model used here is `all-MiniLM-L6-v2`: 22 million parameters, 384-dimensional output, roughly 14,000 sentences per second on a standard CPU. It's the standard starting point — small enough to run anywhere, good enough for most retrieval and similarity tasks.

## Compute your first embeddings

```python
from sentence_transformers import SentenceTransformer

model = SentenceTransformer("all-MiniLM-L6-v2")

sentences = [
    "The weather is lovely today.",
    "It's so sunny outside!",
    "He drove to the store yesterday.",
]

embeddings = model.encode(sentences)
print(embeddings.shape)  # (3, 384)
```

Each sentence becomes a 384-dimensional float array. That's it. The model has already learned — from over a billion training pairs — how to map semantically similar text to nearby regions of the vector space.

## Measure similarity

```python
similarities = model.similarity(embeddings, embeddings)
print(similarities)
```

Output (approximate):
```
tensor([[1.00, 0.67, 0.10],
        [0.67, 1.00, 0.09],
        [0.10, 0.09, 1.00]])
```

The two weather sentences score 0.67 — semantically close, despite sharing no words. "He drove to the store" scores ~0.10 against both — unrelated. A cosine score of 1.0 means identical direction in vector space; 0.0 means orthogonal (unrelated); negative means opposite.

## Encode a query against a corpus

The most common real-world use: given a user query, find the most relevant passages from a set of documents.

```python
corpus = [
    "Puppies are adorable little dogs.",
    "The stock market fell sharply today.",
    "Kittens are playful young cats.",
    "Interest rates affect bond prices.",
]
query = "cute baby animals"

corpus_embeddings = model.encode(corpus)
query_embedding = model.encode([query])

scores = model.similarity(query_embedding, corpus_embeddings)[0]
ranked = sorted(zip(scores, corpus), reverse=True)

for score, sentence in ranked:
    print(f"{score:.2f}  {sentence}")
```

Output:
```
0.71  Puppies are adorable little dogs.
0.68  Kittens are playful young cats.
0.04  The stock market fell sharply today.
0.02  Interest rates affect bond prices.
```

The model ranked "cute baby animals" correctly against passages it has never seen, with no keyword overlap required.

## What comes next

This is the foundation. The next step in the curriculum is storing these vectors in a vector database — Chroma, FAISS, or pgvector — so you can search across thousands or millions of them in milliseconds. The `encode()` + similarity pattern you just ran is exactly what happens inside every RAG retrieval step; knowing it hands-on makes the rest of the stack legible.

Over 10,000 pre-trained Sentence Transformer models are available on [Hugging Face](https://huggingface.co/models?library=sentence-transformers) for specialized domains (code, scientific text, multilingual) — all use the same API.

**Sources:** [Quickstart — sbert.net](https://www.sbert.net/docs/quickstart.html) · [all-MiniLM-L6-v2 — Hugging Face](https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2) · [Semantic Textual Similarity — sbert.net](https://www.sbert.net/docs/sentence_transformer/usage/semantic_textual_similarity.html)
]]></content:encoded>
    </item>

    <item>
      <title>Three Claude Code v2.1.224 Settings Every Team Should Know: Session Caps, Repo Permissions, and Archive Plugins</title>
      <link>https://code.claude.com/docs/en/changelog</link>
      <guid isPermaLink="false">2026-08-08-03-claude-code-session-caps-repo-permissions</guid>
      <pubDate>Sat, 08 Aug 2026 04:09:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Claude Code v2.1.224 adds tunable session-wide safety caps, repo-root "Always allow" permission persistence, and zip-based archive plugin distribution — three settings that matter most in team and CI contexts.</description>
      <content:encoded><![CDATA[
**Claude Code v2.1.224 adds session-wide runaway-loop guards (tunable via env vars), repo-root "Always allow" persistence, and zip-based plugin distribution — three settings that matter most in team and CI contexts.**

## 1. Session-wide safety caps — and how to tune them

v2.1.224 introduces per-session limits to stop runaway agentic loops before they rack up large API bills or spin indefinitely:

| Cap | Default | Env var to override |
|---|---|---|
| WebSearch calls | 200 | `CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION` |
| Subagent spawns | 200 | `CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION` |

For most interactive sessions, 200 is far more than enough. For legitimate long-running CI jobs that fan out many subagents — a full-repo migration, a large test-suite analysis — raise the cap before the run:

```bash
export CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION=500
claude -p "Migrate all 380 route handlers to the new API format" \
  --output-format json \
  --allowedTools "Read,Edit,Bash(node:*)"
```

When a cap is hit, Claude Code logs a warning and terminates cleanly with exit code 1 — so your pipeline can catch it and retry with a higher limit if needed.

## 2. "Always allow" rules now save at the repo root

Previously, approving a tool action with "Always allow" stored the rule in your global `~/.claude/settings.json`. As of v2.1.224, it writes to `.claude/settings.json` in the repository root. Concretely:

- Project allowlists travel with the project (commit them, and teammates get them automatically)
- Approvals granted in a git worktree persist across sessions and other worktrees in the same repo
- Per-repo rules no longer bleed into unrelated projects

For teams: commit `.claude/settings.json` with your standard approved tool set. New developers who clone the repo inherit the team's allowlist without having to approve each action from scratch.

## 3. Archive plugin source

v2.1.224 adds a new plugin source type: install a plugin directly from a zip file served over HTTPS, no git repo or npm package required.

```json
// .claude/settings.json
{
  "pluginSources": [
    {
      "type": "archive",
      "url": "https://internal.example.com/plugins/company-standards.zip",
      "sha256": "a1b2c3d4e5f6..."
    }
  ]
}
```

The `sha256` field is optional but strongly recommended for any internal distribution — it pins the exact archive so a compromised URL can't silently swap in a different payload.

**Sources:** [Claude Code changelog — code.claude.com](https://code.claude.com/docs/en/changelog) · [Claude Code Updates Aug 2026 — Releasebot](https://releasebot.io/updates/anthropic/claude-code)
]]></content:encoded>
    </item>

    <item>
      <title>OpenAI Upgrades ChatGPT Free to GPT-5.6 Luna with Unlimited Text Chats</title>
      <link>https://openai.com/index/improving-gpt-5-6-sol-in-chatgpt/</link>
      <guid isPermaLink="false">2026-08-08-02-chatgpt-gpt56-luna-free-default</guid>
      <pubDate>Sat, 08 Aug 2026 04:08:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>LLM</category>
      <category>Developer Tools</category>
      <description>OpenAI makes GPT-5.6 Luna the default for all free ChatGPT users and removes the daily text chat limit — 62% fewer factual errors than GPT-5.5 Instant, with a Think button for harder questions.</description>
      <content:encoded><![CDATA[
**GPT-5.6 Luna — 62% fewer factual errors than GPT-5.5 Instant — is now the default model for all ChatGPT Free users, with text chats uncapped and a Think button for harder questions.**

OpenAI's [August 6 update](https://openai.com/index/improving-gpt-5-6-sol-in-chatgpt/) promotes Luna from its previous position as a mid-tier option to the default experience for everyone on the free plan, replacing GPT-5.5 Instant. Unlimited text conversations are now available (image generation, file uploads, and voice still have limits). The same release upgrades GPT-5.6 Sol — the pro-tier model — with improved factual accuracy and instruction following.

**Why it matters:** Luna's API price sits at $0.20/$1.20 per million input/output tokens. The model your users encounter for free is now the same model that, at the API, is the most cost-efficient frontier option for high-volume agentic pipelines. The free-tier baseline just rose significantly.

**Sources:** [OpenAI announcement](https://openai.com/index/improving-gpt-5-6-sol-in-chatgpt/) · [TechCrunch](https://techcrunch.com/2026/08/06/openai-brings-unlimited-chatgpt-text-chats-to-free-users/) · [The Next Web](https://thenextweb.com/news/chatgpt-free-unlimited-text-chats-gpt-5-6-luna-default)
]]></content:encoded>
    </item>

    <item>
      <title>Measure Before You Improve: Evaluate Any RAG Pipeline with RAGAS</title>
      <link>https://docs.ragas.io/en/stable/getstarted/quickstart/</link>
      <guid isPermaLink="false">2026-08-08-01-ragas-rag-evaluation-faithfulness</guid>
      <pubDate>Sat, 08 Aug 2026 04:07:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>RAG</category>
      <category>Best Practices</category>
      <description>RAGAS gives you four numbers that diagnose any RAG pipeline: faithfulness, context precision, context recall, and answer relevancy — each pointing to a different fix.</description>
      <media:content url="https://docs.ragas.io/en/stable/_static/logo.png" medium="image" />
      <media:thumbnail url="https://docs.ragas.io/en/stable/_static/logo.png" />
      <content:encoded><![CDATA[
**`ragas` gives you four numbers that tell you exactly where your RAG pipeline breaks — before you touch the code, run a benchmark, or guess at what to fix.**

**What you'll be able to do after this:**
- Run automated RAG evaluation with one function call and get per-metric scores for every query in your test set
- Distinguish retrieval failures (low context precision or recall) from generation failures (low faithfulness or answer relevancy) and know which layer to fix
- Drop evaluation into CI so regressions surface before deployment

## Install

```bash
pip install ragas langchain_openai
```

Set `OPENAI_API_KEY` — RAGAS uses an LLM as judge. Any OpenAI-compatible endpoint works.

## Build an evaluation dataset

You need three things per example: the user question, the retrieved context chunks (as a list of strings), and the model's answer. Start with 10–30 representative queries from your actual use case.

```python
from ragas import EvaluationDataset, SingleTurnSample

samples = [
    SingleTurnSample(
        user_input="What is the return policy?",
        retrieved_contexts=[
            "Items can be returned within 30 days of purchase with a receipt.",
            "Sale items are final and cannot be returned.",
        ],
        response="You can return items within 30 days with a receipt. Sale items are non-refundable.",
    ),
    # ... add more samples
]

dataset = EvaluationDataset(samples=samples)
```

## Run the evaluation

```python
from ragas import evaluate
from ragas.metrics import Faithfulness, AnswerRelevancy, ContextPrecision, ContextRecall
from ragas.llms import LangchainLLMWrapper
from langchain_openai import ChatOpenAI

evaluator_llm = LangchainLLMWrapper(ChatOpenAI(model="gpt-4o-mini"))

results = evaluate(
    dataset=dataset,
    metrics=[Faithfulness(), AnswerRelevancy(), ContextPrecision(), ContextRecall()],
    llm=evaluator_llm,
)

print(results.to_pandas()[["faithfulness","answer_relevancy","context_precision","context_recall"]])
```

## Reading the scores

| Metric | What it measures | Low score → fix |
|---|---|---|
| **Faithfulness** | Answer grounded in retrieved context? | Prompt: add citation enforcement; reduce hallucination |
| **Context Precision** | Right chunks ranked first? | Reranker, better embeddings, metadata filters |
| **Context Recall** | Did retrieval find all relevant chunks? | Increase `k`, better chunking, hybrid search |
| **Answer Relevancy** | Does the answer address the question? | Prompt engineering, filter off-topic retrieval |

A score below ~0.7 on any metric usually points to a specific, fixable issue. Fix precision before recall — surfacing the right chunk first is cheaper than retrieving more and hoping a reranker saves you.

## Add a CI gate

```python
# In a CI script or conftest.py
results = evaluate(dataset, metrics=[Faithfulness(), ContextPrecision()], llm=evaluator_llm)
df = results.to_pandas()
assert df["faithfulness"].mean() > 0.75, f"Faithfulness regression: {df['faithfulness'].mean():.2f}"
assert df["context_precision"].mean() > 0.70, f"Precision regression: {df['context_precision'].mean():.2f}"
```

RAGAS also accepts Hugging Face datasets as input, so you can version your eval set alongside your code and track scores across commits.

**Sources:** [RAGAS Quickstart — docs.ragas.io](https://docs.ragas.io/en/stable/getstarted/quickstart/) · [Evaluate a simple RAG system — docs.ragas.io](https://docs.ragas.io/en/stable/tutorials/rag/) · [RAGAS GitHub — vibrantlabsai/ragas](https://github.com/vibrantlabsai/ragas)
]]></content:encoded>
    </item>

    <item>
      <title>Automate Claude Code with --print and --output-format json: Scripting and CI/CD Pipelines</title>
      <link>https://code.claude.com/docs/en/headless</link>
      <guid isPermaLink="false">2026-08-07-08-claude-code-headless-mode-automation</guid>
      <pubDate>Fri, 07 Aug 2026 20:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Run Claude Code non-interactively with -p, get structured JSON output, restrict tools with --allowedTools, and drop AI steps into any CI/CD pipeline — no terminal UI required.</description>
      <content:encoded><![CDATA[
**`claude -p "prompt" --output-format json` turns Claude Code into a scriptable pipeline step — structured output, deterministic exit codes, and schema-constrained responses for CI/CD automation.**

Most Claude Code usage is interactive. But `-p` (non-interactive/print mode) is the bridge between the agent and your toolchain: one command in, parseable data out. Every workflow that runs on a schedule, inside GitHub Actions, or as part of a build pipeline benefits from this.

## The three flags you need

**`-p` / `--print`** — non-interactive mode. Claude reads the prompt, runs the agent loop, and exits. No REPL, no UI.

**`--output-format`** — controls what comes out on stdout:
- `text` (default) — plain text result
- `json` — structured object with `result`, `session_id`, `total_cost_usd`, and `duration_ms`
- `stream-json` — newline-delimited JSON, one event per line; useful for real-time streaming in long-running jobs

**`--allowedTools`** — restrict which tools Claude can use. Critical for CI: only allow what the task actually needs.

## Minimal example

```bash
# One-shot code review, JSON output
claude -p "Review the staged diff for security issues. Be concise." \
  --output-format json \
  --allowedTools "Bash(git diff:*),Read" \
  | jq -r '.result'
```

Exit code `0` = success; any non-zero code = error. Unix-standard, so `set -e` pipelines work correctly.

## GitHub Actions integration

```yaml
- name: AI security scan
  run: |
    REVIEW=$(claude -p "Scan the staged files for hardcoded secrets or SQL injection risks" \
      --output-format json \
      --allowedTools "Read,Bash(git diff:*)" \
      | jq -r '.result')
    echo "## Security Review" >> $GITHUB_STEP_SUMMARY
    echo "$REVIEW" >> $GITHUB_STEP_SUMMARY
```

## Structured output with `--json-schema`

Force Claude to return a JSON object matching your schema:

```bash
claude -p "Analyze the test results in ./test-output.xml" \
  --output-format json \
  --allowedTools "Read" \
  --json-schema '{
    "type": "object",
    "properties": {
      "passed": {"type": "boolean"},
      "failures": {"type": "array", "items": {"type": "string"}},
      "summary": {"type": "string"}
    },
    "required": ["passed", "failures", "summary"]
  }' \
  | jq '.result'
```

The schema-constrained response lands in the `result` field alongside `total_cost_usd` — ready to pipe into `jq`, a script, or a monitoring system.

## What to lock down in CI

- Always pass `--allowedTools` listing only what the task needs — omit Bash entirely for read-only analysis
- Store `ANTHROPIC_API_KEY` in your CI secrets; Claude Code picks it up from the environment
- Use `--max-turns N` to cap the agent loop length for predictable runtime

**Sources:** [Run Claude Code programmatically — code.claude.com](https://code.claude.com/docs/en/headless) · [Headless Mode and CI/CD — SFEIR Institute](https://institute.sfeir.com/en/claude-code/claude-code-headless-mode-and-ci-cd/) · [Claude Code Headless Mode guide — buildthisnow.com](https://www.buildthisnow.com/blog/guide/development/claude-code-headless-mode)
]]></content:encoded>
    </item>

    <item>
      <title>Scale Any vLLM-Backed Model to Production with Ray Serve LLM</title>
      <link>https://docs.ray.io/en/latest/serve/llm/quick-start.html</link>
      <guid isPermaLink="false">2026-08-07-07-ray-serve-llm-vllm-autoscaling</guid>
      <pubDate>Fri, 07 Aug 2026 20:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Self-Hosting</category>
      <category>Developer Tools</category>
      <description>Ray Serve LLM wraps vLLM in a production harness — autoscaling that responds to queue depth, multi-model routing, and a built-in OpenAI-compatible API in under 30 lines of Python.</description>
      <media:content url="https://avatars.githubusercontent.com/u/22125274?v=4" medium="image" />
      <media:thumbnail url="https://avatars.githubusercontent.com/u/22125274?v=4" />
      <content:encoded><![CDATA[
**Ray Serve LLM wraps vLLM in a production harness — queue-depth autoscaling, multi-model routing, and an OpenAI-compatible API endpoint in ~25 lines of Python.**

You've loaded a model with Ollama or spun up a raw vLLM server. That works for one machine and modest load. When you need autoscaling, rolling upgrades, multi-model routing, or true production reliability, Ray Serve LLM is the missing layer.

**What you'll be able to do after this:**
- Deploy any Hugging Face model behind an OpenAI-compatible REST endpoint using Ray Serve LLM and vLLM
- Enable autoscaling that responds to request queue depth (not CPU) — spinning up replicas under load, scaling back down at idle
- Serve multiple models from one cluster and route traffic by model name, with no changes to the client

## Install

```bash
pip install "ray[serve]" vllm
```

## Deploy a model in ~20 lines

```python
# serve_llm.py
from ray import serve
from ray.serve.llm import LLMServer, LLMConfig

config = LLMConfig(
    model_loading_config={
        "model_id": "meta-llama/Llama-3.1-8B-Instruct",
    },
    deployment_config={
        "autoscaling_config": {
            "min_replicas": 1,
            "max_replicas": 4,
            "target_ongoing_requests": 5,
        }
    },
    accelerator_type="A10G",   # or "T4", "A100", etc.
)

llm_server = LLMServer.from_llm_config(config)
serve.run(llm_server)
```

```bash
# Start
serve run serve_llm.py

# Query — standard OpenAI API
curl http://localhost:8000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "meta-llama/Llama-3.1-8B-Instruct",
    "messages": [{"role": "user", "content": "Explain KV caching in one paragraph."}]
  }'
```

## Multi-model serving

Add a second `LLMConfig` entry with a different `model_id` — Ray Serve routes requests by the `model` field in each API call. No load balancer config needed; Ray handles it.

## Why queue depth, not CPU

Traditional autoscalers trigger on CPU or memory. LLMs are I/O-bound: a GPU can be 100% utilized but still have headroom for more requests in the batch. Ray Serve autoscales on `target_ongoing_requests` — the number of requests currently being processed per replica. This correctly captures backpressure and scales before the queue grows too long.

## What this covers beyond raw vLLM

| Feature | Raw `vllm serve` | Ray Serve LLM |
|---|---|---|
| OpenAI-compatible API | Yes | Yes |
| Autoscaling | No | Yes (queue-depth) |
| Multi-model routing | No | Yes |
| Rolling upgrades | No | Yes |
| Multi-node tensor parallel | Yes | Yes |
| Health checks | Basic | Enterprise-grade |

**Sources:** [Serving LLMs — Ray 2.56.0 docs](https://docs.ray.io/en/latest/serve/llm/index.html) · [Ray Serve LLM Quickstart](https://docs.ray.io/en/latest/serve/llm/quick-start.html) · [Announcing Native LLM APIs in Ray Data and Ray Serve — Anyscale blog](https://www.anyscale.com/blog/llm-apis-ray-data-serve)
]]></content:encoded>
    </item>

    <item>
      <title>Claude Code Self-Hosted Environments Are Now in Public Beta: Run Agent Sessions on Your Own Compute</title>
      <link>https://claude.com/blog/run-claude-code-sessions-on-your-own-compute</link>
      <guid isPermaLink="false">2026-08-07-06-claude-code-self-hosted-environments-cross-session</guid>
      <pubDate>Fri, 07 Aug 2026 12:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Claude Code's self-hosted environments (public beta) let Team/Enterprise teams run agent sessions on infrastructure they control — inside the firewall, next to internal services, with no source code leaving the network.</description>
      <content:encoded><![CDATA[
**Claude Code's new self-hosted environments (public beta, Team/Enterprise) run agent sessions on your own compute — inside your firewall, next to internal APIs, keeping source code and artifacts off Anthropic's servers.**

Anthropic's cloud infrastructure covers most workloads, but regulated industries, internal-API-dependent codebases, and strict compliance requirements need agent execution on controlled compute. Self-hosted environments solve this while keeping the full Claude Code cloud UI.

## How it works

You deploy *runners* — long-lived processes that pick up session requests from Claude Code clients (web, mobile, desktop, routines) and start a local Claude Code process per session. Two modes:

- **Fixed**: a set number of runners stay running; sessions distribute across them
- **On-demand**: an orchestrator watches the queue and provisions runners as sessions arrive

Repository checkouts, build artifacts, secrets, and any files a session creates stay on your machines. The session UI runs through Anthropic's cloud; only the agent execution is local.

## Set up (Team or Enterprise plan)

1. Go to your organization's Claude settings → **Environments** → **Self-hosted (beta)**
2. Deploy runners on your infrastructure (the [docs cover Kubernetes and systemd deployments](https://code.claude.com/docs/en/self-hosted-environments))
3. Runners register with a token and start accepting sessions immediately

No changes to how developers start a session — they get the same web/desktop/mobile UI; the agent just executes on your hardware.

## Also shipping in this update: cross-session messaging + spawn cap gone

Two more agent coordination improvements alongside self-hosted environments:

- **Cross-session messaging**: `ListAgents` now discovers Claude Code sessions across machines (macOS and Linux); `SendMessage` delivers work between them without human relay. Background agents can hand off to other sessions directly.
- **Subagent spawn cap removed**: the previous 200-subagent ceiling per session is gone. Long autonomous workflows no longer hit a hard limit — concurrency and depth limits still apply, but sustained multi-hour agent runs are no longer bounded by a count.

**Sources:** [Self-hosted environments for Claude Code — Anthropic blog](https://claude.com/blog/run-claude-code-sessions-on-your-own-compute) · [Self-hosted environments docs — code.claude.com](https://code.claude.com/docs/en/self-hosted-environments) · [Claude Code changelog Aug 2026 — Gradually](https://www.gradually.ai/en/changelogs/claude-code/)
]]></content:encoded>
    </item>

    <item>
      <title>xAI Ships Grok 4.6 Today: Improved Post-Training on the 1.5T Core, Grok 4.7 (2.1T) Next</title>
      <link>https://x.ai/news/grok-4-6</link>
      <guid isPermaLink="false">2026-08-07-05-grok-46-xai-launch</guid>
      <pubDate>Fri, 07 Aug 2026 12:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>LLM</category>
      <category>Developer Tools</category>
      <description>xAI releases Grok 4.6 today — same 1.5T V9 base as Grok 4.5 but significantly improved SFT and RL; 2.1T Grok 4.7 follows in weeks. The coding-agent race now has four serious competitors.</description>
      <content:encoded><![CDATA[
**xAI ships Grok 4.6 today — same 1.5T V9 foundation as Grok 4.5 but with significantly better post-training (SFT + RL); Grok 4.7 at 2.1T follows in weeks.**

Grok 4.6 doesn't increase parameter count — it reuses the 1.5T V9 core that xAI co-built with Cursor since that acquisition and delivers gains through improved supervised fine-tuning and reinforcement learning. API access typically follows X/SuperGrok consumer rollout by a few days. Independent benchmark scores (SWE-bench, LiveCodeBench) usually appear within a week of consumer launch.

Grok 4.7, a 2.1T model, is slated to follow Grok 4.6 by a few weeks and is projected to outperform it on every benchmark while trading slightly more latency for better token efficiency.

The practical context: the AI coding space now has four serious competitors — Claude Code, OpenAI Codex/GPT-5.6, Meta Muse Code (launched Aug 5), and xAI Grok — each shipping faster post-training iteration cycles than new architectures. For developers building model-agnostic tools, OpenRouter and LiteLLM routing matters more than model loyalty.

**Why it matters:** Four competitive AI coding agents on rapid-release cycles means benchmark gaps close within weeks; routing flexibility beats picking a permanent winner.

**Sources:** [Grok 4.6 release confirmed — AIToolsReview](https://aitoolsreview.co.uk/insights/grok-4-6-grok-4-7-release-date) · [Grok 4.6 specs — kie.ai](https://kie.ai/blog/what-is-grok-4-6) · [Grok 4.6 & 4.7 roadmap — tbreak.com](https://tbreak.com/grok-4-6-4-7-xai-release-date-specs/)
]]></content:encoded>
    </item>

    <item>
      <title>Build Your First MCP Server in 20 Lines of Python with FastMCP</title>
      <link>https://gofastmcp.com/getting-started/quickstart</link>
      <guid isPermaLink="false">2026-08-07-04-build-mcp-server-fastmcp-python</guid>
      <pubDate>Fri, 07 Aug 2026 12:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>MCP</category>
      <category>Agents</category>
      <description>Install FastMCP, decorate a Python function with @mcp.tool, and Claude Code discovers it automatically — no boilerplate, no manual JSON Schema. Build tools, resources, and prompts in one file.</description>
      <media:content url="https://opengraph.githubassets.com/1/PrefectHQ/fastmcp" medium="image" />
      <media:thumbnail url="https://opengraph.githubassets.com/1/PrefectHQ/fastmcp" />
      <content:encoded><![CDATA[
**Turn any Python function into an MCP tool Claude can call — FastMCP's decorator API eliminates all the protocol boilerplate so you ship a working server in under 20 lines.**

**What you'll be able to do after this:**
- Write a Python function and expose it as an MCP tool that Claude Code discovers and calls automatically
- Add read-only resources (data the model can browse) and reusable prompt templates alongside your tools
- Connect the server to Claude Code locally (stdio) or run it remotely over HTTP for team-shared tooling

## Why build your own MCP server?

Every capability Claude Code reaches for — file reads, shell commands, web search — is served by an MCP server. Writing your own gives Claude direct, type-safe access to your internal APIs, databases, or proprietary tooling, with inputs validated and documentation auto-generated from Python type hints.

## A complete server in 20 lines

```bash
pip install fastmcp
```

```python
from fastmcp import FastMCP

mcp = FastMCP("my-tools")

@mcp.tool
def get_issue(repo: str, number: int) -> dict:
    """Fetch a GitHub issue by repo and number."""
    import httpx
    r = httpx.get(f"https://api.github.com/repos/{repo}/issues/{number}")
    return r.json()

@mcp.resource("config://defaults")
def defaults() -> str:
    """Return project-wide defaults."""
    return "timeout=30\nmax_retries=3"

if __name__ == "__main__":
    mcp.run()   # stdio by default; pass transport="http" for remote
```

The `@mcp.tool` decorator reads the function's type hints to build the JSON Schema and uses the docstring as the tool description. No separate schema file, no serialization code.

## Wire it into Claude Code

Add the server to `.claude/settings.json`:

```json
{
  "mcpServers": {
    "my-tools": {
      "command": "python",
      "args": ["/path/to/server.py"]
    }
  }
}
```

Claude Code discovers `get_issue` on the next session start. For a remote (HTTP) server shared across a team:

```python
mcp.run(transport="http", host="0.0.0.0", port=8000)
```

then update the settings to `"type": "http", "url": "http://your-server:8000/mcp"`.

## The three MCP primitives

FastMCP covers all three things an MCP server can expose:

| Primitive | Decorator | What it does |
|---|---|---|
| Tool | `@mcp.tool` | Executable function the model can call |
| Resource | `@mcp.resource("uri://…")` | Read-only data the model can browse |
| Prompt | `@mcp.prompt` | Reusable instruction template in Claude Code's prompt picker |

The [FastMCP quickstart](https://gofastmcp.com/getting-started/quickstart) builds all three in a single file — a good first project to clone and extend.

**Sources:** [FastMCP quickstart — gofastmcp.com](https://gofastmcp.com/getting-started/quickstart) · [Official MCP build-server guide — modelcontextprotocol.io](https://modelcontextprotocol.io/docs/develop/build-server) · [How to Build Your First MCP Server — freeCodeCamp](https://www.freecodecamp.org/news/how-to-build-your-first-mcp-server-using-fastmcp/) · [FastMCP on GitHub — PrefectHQ/fastmcp](https://github.com/PrefectHQ/fastmcp)
]]></content:encoded>
    </item>

    <item>
      <title>Give Claude Code the Entire HuggingFace Ecosystem: Three Commands to Unlock Model Search, Training Jobs, and Hub Pushes</title>
      <link>https://huggingface.co/docs/hub/en/agents-skills</link>
      <guid isPermaLink="false">2026-08-07-03-huggingface-skills-claude-code-plugin</guid>
      <pubDate>Fri, 07 Aug 2026 04:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>HuggingFace</category>
      <description>Three Claude Code commands wire in the entire HuggingFace Hub — search models, launch GPU fine-tuning jobs, push checkpoints, and manage datasets from a single plugin marketplace install.</description>
      <content:encoded><![CDATA[
**Three Claude Code commands wire in the entire HuggingFace Hub — search models, launch GPU fine-tuning jobs, push checkpoints, and manage datasets from a single plugin marketplace install.**

Claude Code is great at writing code. HuggingFace Skills makes it great at *running ML workloads* — searching the Hub, submitting training jobs to cloud GPUs, evaluating models, and pushing finished checkpoints, all without leaving your Claude Code session.

## Installing the HuggingFace Skills plugin

HuggingFace publishes a plugin marketplace at `huggingface/skills`. Add it to Claude Code and install the HuggingFace CLI skill:

```bash
# 1. Register the marketplace (one-time)
/plugin marketplace add huggingface/skills

# 2. Install the hf-cli skill (teaches Claude every hf command)
/plugin install hf-cli@huggingface/skills

# 3. (Optional) Install the fine-tuning skill
/plugin install hf-llm-trainer@huggingface/skills
```

The `hf-cli` skill is generated from your locally installed HuggingFace CLI, so it's always current. Once installed, Claude Code understands every `hf` subcommand — search, datasets, models, Spaces, jobs, and buckets.

## What Claude Code can now do

```
You: Search for Llama-3.2 fine-tunes that support 128K context and are < 8B params

Claude: [searches Hub] Found 14 models matching those criteria:
        - meta-llama/Llama-3.2-8B-Instruct (8B, 128K) ✓
        - unsloth/Llama-3.2-8B-Instruct-bnb-4bit (4-bit quant) ✓
        ...

You: Fine-tune the 8B Instruct model on datasets/my-org/support-tickets
     with LoRA on a single H100, push to my-org/support-llama

Claude: [launches job on HuggingFace Jobs, monitors progress, pushes on completion]
```

With the `hf-llm-trainer` skill, Claude understands the full training decision space: which GPU tier to pick for a given model size, LoRA vs full fine-tuning, SFT vs DPO vs GRPO, how to configure Hub auth, and how to structure multi-stage pipelines.

## Available skills

| Skill | What it teaches Claude |
|---|---|
| `hf-cli` | All `hf` CLI commands: Hub search, datasets, models, Spaces, jobs |
| `hf-llm-trainer` | Fine-tuning from 0.5B to 70B params; SFT, DPO, RLVR; GGUF conversion |
| `hf-datasets` | Dataset loading, filtering, format conversion, push to Hub |

Install on demand with `/plugin install <skill>@huggingface/skills`. Each skill is a `SKILL.md` file with YAML frontmatter — you can read and customize them.

## Why this matters for your workflow

The HuggingFace ecosystem is where open-model AI engineering happens: model discovery, dataset curation, training, evaluation, and serving. Skills make all of that available inside the same Claude Code session where you're writing application code — no context switch to a notebook, no separate CLI window, no copy-paste of model IDs.

HuggingFace published a worked example of using this to have Claude fine-tune an open-source LLM end-to-end — from dataset prep to Hub push — without the developer writing a single training script: [We Got Claude to Fine-Tune an Open Source LLM](https://huggingface.co/blog/hf-skills-training).

**Sources:** [HuggingFace Skills docs — huggingface.co/docs/hub/en/agents-skills](https://huggingface.co/docs/hub/en/agents-skills) · [huggingface/skills — GitHub](https://github.com/huggingface/skills) · [We Got Claude to Fine-Tune an Open Source LLM — HuggingFace Blog](https://huggingface.co/blog/hf-skills-training) · [Claude Code Integration — DeepWiki](https://deepwiki.com/huggingface/skills/3.1-claude-code-integration)
]]></content:encoded>
      <media:content url="https://opengraph.githubassets.com/1/huggingface/skills" medium="image" />
      <media:thumbnail url="https://opengraph.githubassets.com/1/huggingface/skills" />
    </item>

    <item>
      <title>Meta Enters the AI Coding Race With Muse Code: Parallel Worktree Subagents Built on Muse Spark 1.2</title>
      <link>https://www.cnbc.com/2026/08/05/meta-debuts-muse-code-to-take-on-anthropic-and-openai-.html</link>
      <guid isPermaLink="false">2026-08-07-02-meta-muse-code-ai-coding-agent</guid>
      <pubDate>Fri, 07 Aug 2026 04:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Developer Tools</category>
      <category>LLM</category>
      <description>Meta launched Muse Code on August 5 for macOS and Linux — an AI coding agent powered by Muse Spark 1.2 that fans out parallel subagents in isolated worktrees. Claude Code now has a fourth major rival.</description>
      <content:encoded><![CDATA[
**Meta's Muse Code, launched August 5 for macOS and Linux on Muse Spark 1.2, is Meta's first dedicated coding agent — parallel isolated-worktree subagents, one-command install, pay-as-you-go or contributor-tier pricing.**

Meta rolled out Muse Code beta on August 5, entering a space already occupied by Claude Code, OpenAI's Codex, and Cursor. The agent installs with a single command on macOS and Linux, connects to any git repo, and runs on Muse Spark 1.2 — a coding-optimized model variant trained specifically for the agent workload.

For large tasks, Muse Code fans out to parallel subagents working in isolated worktrees, so the main checkout is never touched mid-run. The pattern is structurally similar to Claude Code's workflow orchestration and Codex's parallel agent mode — all three converged on isolated-worktree subagents independently.

Pricing: pay-as-you-go at API rates, or a discounted contributor tier where developers consent to training data use.

**Why it matters:** The coding-agent market now has four serious contenders competing on the same architecture — parallel subagents, worktree isolation, agentic workflows. Your CLAUDE.md conventions, context-engineering patterns, and custom slash commands are increasingly portable skills. A competitive field accelerates feature shipping and applies downward pressure on pricing.

**Sources:** [Meta debuts Muse Code to rival Claude Code — CNBC](https://www.cnbc.com/2026/08/05/meta-debuts-muse-code-to-take-on-anthropic-and-openai-.html) · [Meta enters AI coding race — The Next Web](https://thenextweb.com/news/meta-muse-code-ai-coding-agent-launch) · [Meta Muse Code beta — Dataconomy](https://dataconomy.com/2026/08/06/meta-muse-code-launch-ai-coding-agent/) · [Meta Muse Code for macOS/Linux — 9to5Mac](https://9to5mac.com/2026/08/05/meta-launches-muse-code-ai-coding-agent-for-macos-and-linux/)
]]></content:encoded>
    </item>

    <item>
      <title>Teach Any Model to Reason Step-by-Step: GRPO, the RL Algorithm Behind DeepSeek-R1, on a Free Colab T4</title>
      <link>https://unsloth.ai/docs/get-started/reinforcement-learning-rl-guide/tutorial-train-your-own-reasoning-model-with-grpo</link>
      <guid isPermaLink="false">2026-08-07-01-grpo-reasoning-model-fine-tuning-unsloth</guid>
      <pubDate>Fri, 07 Aug 2026 04:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Fine-Tuning</category>
      <category>HuggingFace</category>
      <description>GRPO trains reasoning by scoring groups of sampled responses and rewarding the above-average ones — no value model, no preference pairs. Unsloth's free Colab notebook runs it on a T4 in under 2 hours.</description>
      <content:encoded><![CDATA[
**GRPO is the RL algorithm behind DeepSeek-R1: score groups of responses, reward the better-than-average ones — no value model, no preference pairs. Unsloth's free Colab notebook trains a reasoning model on a T4 in under 2 hours.**

**What you'll be able to do after this:**
- Explain why GRPO is cheaper and simpler than PPO for training reasoning models — and when it beats supervised fine-tuning
- Write a verifiable reward function for your domain (math, code, SQL, structured extraction) and plug it into Unsloth's GRPO trainer
- Run a full GRPO training run on a free Colab T4 to build a model that generates `<think>…</think>` traces before answering

## What GRPO is

When you do supervised fine-tuning (SFT) on a reasoning dataset, the model learns to imitate the reasoning traces in your training data. GRPO (Group Relative Policy Optimization) does something fundamentally different: it **explores** by sampling multiple responses, **evaluates** each one against a reward function, and **reinforces** the above-average ones.

The key insight: instead of needing a value model to estimate expected reward (like PPO does), GRPO uses the **group mean reward as the baseline**. Sample G=8 completions for one prompt, score each, subtract the mean — the ones that did better than average get positive gradients, the rest get negative.

```
Prompt: "Solve for x: 3x + 7 = 22"

G=8 sampled completions:
  C1: "x = 5"          reward=1.0  (correct)
  C2: "x = 5"          reward=1.0  (correct)
  C3: "x = 5"          reward=1.0  (correct)
  C4: "x = 4"          reward=0.0  (wrong)
  C5: "x = 5"          reward=1.0  (correct)
  C6: "x = 6"          reward=0.0  (wrong)
  C7: "x = 5"          reward=1.0  (correct)
  C8: "x = 4"          reward=0.0  (wrong)

Group mean = 0.625
Advantages:
  C1: 0.375  (above mean → reinforce)
  C4: -0.625 (below mean → discourage)
```

No reference model scoring. No separate value head. No paired preference data. Just: sample, score, update.

## Running GRPO with Unsloth on a free Colab T4

Open Unsloth's [GRPO notebook on Colab](https://unsloth.ai/docs/get-started/reinforcement-learning-rl-guide/tutorial-train-your-own-reasoning-model-with-grpo). The key moving parts:

```python
from unsloth import FastLanguageModel
from trl import GRPOConfig, GRPOTrainer

# Load a 1.5B model in 4-bit — fits in 16GB VRAM with GRPO overhead
model, tokenizer = FastLanguageModel.from_pretrained(
    model_name="unsloth/Qwen2.5-1.5B-Instruct",
    max_seq_length=2048,
    load_in_4bit=True,
)
model = FastLanguageModel.get_peft_model(model, r=16, lora_alpha=16)

# Your reward function: the only thing GRPO needs you to write
def reward_correct_answer(completions, ground_truth, **kwargs):
    """Return 1.0 if the completion contains the correct answer, else 0.0."""
    return [
        1.0 if str(gt).strip() in c else 0.0
        for c, gt in zip(completions, ground_truth)
    ]

trainer = GRPOTrainer(
    model=model,
    reward_funcs=reward_correct_answer,  # plug in your domain's verifier
    args=GRPOConfig(
        num_generations=8,       # G — completions per prompt
        max_new_tokens=512,
        max_steps=500,
        learning_rate=5e-6,
        per_device_train_batch_size=1,
        gradient_accumulation_steps=4,
        output_dir="grpo-reasoning-model",
    ),
    train_dataset=dataset,       # needs a "prompt" column
)
trainer.train()
```

Unsloth patches the attention kernels to cut VRAM by ~2×, which is what makes GRPO feasible on a free T4.

## When to use GRPO instead of SFT

| Situation | Better choice |
|---|---|
| You have correct reasoning traces (CoT) | SFT on those traces |
| You can verify correctness automatically (math, code, SQL) | **GRPO** |
| You want the model to discover better reasoning strategies | **GRPO** |
| You're adapting writing style or domain vocabulary | SFT |
| Your task doesn't have a clear correct/incorrect signal | SFT or DPO |

GRPO excels at tasks with **verifiable outcomes**: the reward function is just a unit test. Code that passes tests, math answers that match, SQL that returns the right rows, structured output that satisfies a regex — all clean GRPO targets.

**Sources:** [Train Your Own Reasoning Model with GRPO — Unsloth Docs](https://unsloth.ai/docs/get-started/reinforcement-learning-rl-guide/tutorial-train-your-own-reasoning-model-with-grpo) · [Train your own R1 reasoning model locally — Unsloth Blog](https://unsloth.ai/blog/r1-reasoning) · [GRPOTrainer — TRL / Hugging Face](https://huggingface.co/docs/trl/grpo_trainer) · [DeepSeek-R1 Technical Report — arXiv](https://arxiv.org/abs/2501.12948)
]]></content:encoded>
      <media:content url="https://unsloth.ai/images/unsloth_logo.png" medium="image" />
      <media:thumbnail url="https://unsloth.ai/images/unsloth_logo.png" />
    </item>

    <item>
      <title>What Did Claude Actually Do? Auditing Unattended Runs With JSONL Session Logs</title>
      <link>https://dev.to/boucle2026/how-to-see-everything-claude-code-does-audit-trail-hook-1g9j</link>
      <guid isPermaLink="false">2026-08-06-09-claude-code-jsonl-session-audit-trail</guid>
      <pubDate>Thu, 06 Aug 2026 20:04:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>Claude Code writes every tool call to JSONL logs in ~/.claude/projects/. One PostToolUse hook or three open-source tools turn those logs into a full audit trail for any background run.</description>
      <content:encoded><![CDATA[
**Claude Code writes every tool call to JSONL logs in ~/.claude/projects/. One PostToolUse hook or three open-source tools turn those logs into a full audit trail for any background run.**

When Claude Code finishes a background task — a cron-triggered publish run, a CI pipeline, an overnight refactor — you want to know: which files did it touch? Which commands did it run? The answer lives in ~/.claude/.

## Where the logs are

Claude Code writes session transcripts to:

```
~/.claude/projects/<url-encoded-project-path>/<session-id>.jsonl
```

Each `.jsonl` file is one session, one JSON object per line, appended as the session runs. Crash-safe: a mid-session abort leaves a valid truncated file.

Each line is one event. Tool events look like:

```json
{"type":"tool_use","id":"toolu_01XYZ","name":"Write","input":{"file_path":"/project/src/foo.ts","content":"..."}}
{"type":"tool_result","tool_use_id":"toolu_01XYZ","content":[{"type":"text","text":"File written"}]}
```

## Quick audit with jq

```bash
# All files written in the last session
cd ~/.claude/projects
LATEST=$(ls -t */*.jsonl | head -1)
jq -r 'select(.type == "tool_use" and .name == "Write") | .input.file_path' "$LATEST"

# All bash commands run
jq -r 'select(.type == "tool_use" and .name == "Bash") | .input.command' "$LATEST"

# Token spend per turn
jq 'select(.usage != null) | .usage' "$LATEST"
```

## Real-time audit hook (PostToolUse)

Add this to `.claude/settings.json` to log every tool call as it happens — no after-the-fact parsing:

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": ".*",
        "hooks": [{
          "type": "command",
          "command": "jq -c '{ts: now|todate, tool: .tool_name, input: .tool_input}' >> ~/.claude/session-logs/$(date +%Y-%m-%d).jsonl"
        }]
      }
    ]
  }
}
```

This produces one JSON line per tool call in `~/.claude/session-logs/YYYY-MM-DD.jsonl`:

```json
{"ts":"2026-08-06T20:00:00Z","tool":"Write","input":{"file_path":"/project/content/feed.xml"}}
{"ts":"2026-08-06T20:00:01Z","tool":"Bash","input":{"command":"node scripts/ingest-feed.mjs"}}
```

## Graphical viewers

- **[claude-code-trace](https://github.com/delexw/claude-code-trace)** — TUI/web/desktop viewer. Browse sessions, inspect tool call trees, live-tail an active session. `npx @delexw/claude-code-trace`
- **[cc-audit-log](https://github.com/yurukusa/cc-audit-log)** — scans all sessions under ~/.claude/projects/, classifies actions (file writes, edits, git, bash), outputs a human-readable timeline. `npx cc-audit-log`
- **[claude-code-log](https://github.com/daaain/claude-code-log)** — converts a JSONL transcript to readable HTML or Markdown for sharing a session trace with your team.

For teams running Claude Code in CI or as a scheduled background agent, the PostToolUse hook is a five-minute change that pays off the first time something unexpected happens.

**Sources:** [How to See Everything Claude Code Does — DEV Community](https://dev.to/boucle2026/how-to-see-everything-claude-code-does-audit-trail-hook-1g9j) · [claude-code-trace — GitHub](https://github.com/delexw/claude-code-trace) · [cc-audit-log — GitHub](https://github.com/yurukusa/cc-audit-log) · [Claude Code JSONL format — claude-dev.tools](https://claude-dev.tools/docs/jsonl-format)
]]></content:encoded>
    </item>

    <item>
      <title>Google's AI Leadership Fractures: Hassabis to Chairman, Jeff Dean Exits to Found Discovery Loop</title>
      <link>https://fortune.com/2026/08/05/demis-hassabis-steps-down-google-deepmind-ai-shakeup/</link>
      <guid isPermaLink="false">2026-08-06-07-google-deepmind-leadership-exodus</guid>
      <pubDate>Thu, 06 Aug 2026 20:03:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>LLM</category>
      <category>Developer Tools</category>
      <description>Demis Hassabis steps back from Google DeepMind CEO to chairman/Alphabet chief scientist; Jeff Dean, Sanjay Ghemawat, Oriol Vinyals, and Quoc Le exit to co-found Discovery Loop.</description>
      <content:encoded><![CDATA[
**Two Google AI exits announced August 5: Demis Hassabis steps back as DeepMind CEO to become Alphabet Chief Scientist; Jeff Dean, Sanjay Ghemawat, Oriol Vinyals, and Quoc Le depart to co-found Discovery Loop.**

Two announcements on August 5 signal a structural shift at Google's AI operation.

**Hassabis out as day-to-day CEO.** Demis Hassabis — who co-founded DeepMind in 2010 and has led it through the Gemini era — steps back from the CEO role. Koray Kavukcuoglu, a 13-year DeepMind veteran who served as its CTO and Google's chief AI architect, takes over as CEO. Hassabis becomes Alphabet's Chief Scientist and Chairman of Google DeepMind, focusing on longer-horizon AGI research.

**Jeff Dean exits after 27 years.** Dean — author of the MapReduce and BigTable papers, lead architect of TensorFlow, and Google's chief scientist — is co-founding Discovery Loop with Sanjay Ghemawat (Google Senior Fellow, GFS co-author), Oriol Vinyals (VP of research at Google DeepMind, a technical lead on Gemini), and Quoc Le (Google Brain co-founder, AutoML-Zero). Discovery Loop's mission: automate the experimental loop of scientific research — proposing experiments, running them, evaluating results, iterating. Google is a founding investor and cloud partner.

**Why it matters:** four of Google's most influential AI infrastructure and research figures departing in a single announcement is not routine churn. For teams building on Google infrastructure or Gemini APIs, the leadership transition signals a shift in priorities worth tracking — new AI leadership influences where platform investment goes.

**Sources:** [Hassabis steps down — Fortune](https://fortune.com/2026/08/05/demis-hassabis-steps-down-google-deepmind-ai-shakeup/) · [Jeff Dean launches Discovery Loop — TechCrunch](https://techcrunch.com/2026/08/05/jeff-dean-and-other-top-ai-researchers-are-leaving-google-to-launch-their-own-startup/) · [Kavukcuoglu named CEO — Bloomberg](https://www.bloomberg.com/news/articles/2026-08-05/google-deepmind-boss-hassabis-moves-to-chair-role-in-shakeup/) · [Google shifts AI to California — Bloomberg](https://www.bloomberg.com/news/articles/2026-08-06/google-shifts-ai-power-to-california-in-race-against-anthropic-openai) · [Axios](https://www.axios.com/2026/08/06/googles-ai-leadership-shuffle)
]]></content:encoded>
    </item>

    <item>
      <title>Guaranteed JSON From Claude: Two Native API Patterns That Never Need a Retry Loop</title>
      <link>https://platform.claude.com/docs/en/build-with-claude/structured-outputs</link>
      <guid isPermaLink="false">2026-08-06-08-claude-api-structured-outputs-tool-use</guid>
      <pubDate>Thu, 06 Aug 2026 20:02:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Tutorial</category>
      <category>Agents</category>
      <category>Best Practices</category>
      <description>The Claude API has two orthogonal structured-output patterns: output_config JSON schema mode constrains Claude's text response; strict:true on tool definitions guarantees schema-valid tool inputs.</description>
      <content:encoded><![CDATA[
**The Claude API offers two orthogonal structured-output patterns — output_config JSON schema mode for constrained text responses and strict:true tool definitions for schema-valid tool inputs — that together eliminate JSON parse failures from agentic pipelines.**

**What you'll be able to do after this:**
- Get parseable JSON from Claude every time without try/except retry loops
- Guarantee tool call inputs match your schema exactly via grammar-constrained sampling
- Combine both patterns to build reliable agentic pipelines where Claude calls tools AND returns structured data

## The two patterns

Pattern 1 controls what Claude *says* (its text response). Pattern 2 controls what Claude *does* (its tool call parameters). Both are orthogonal — use either or both together.

**Pattern 1: output_config JSON schema (constrained text responses)**

Pass an `output_config` block with a `json_schema` format. The API compiles your schema into a grammar and samples tokens only from schema-valid continuations — the response is always valid JSON matching your schema.

```python
import anthropic, json

client = anthropic.Anthropic()

schema = {
    "type": "object",
    "properties": {
        "sentiment": {"type": "string", "enum": ["positive", "negative", "neutral"]},
        "confidence": {"type": "number", "minimum": 0, "maximum": 1},
        "key_phrases": {"type": "array", "items": {"type": "string"}, "maxItems": 5}
    },
    "required": ["sentiment", "confidence", "key_phrases"],
    "additionalProperties": False
}

response = client.messages.create(
    model="claude-sonnet-4-6-20260901",
    max_tokens=512,
    output_config={
        "format": {
            "type": "json_schema",
            "json_schema": {
                "name": "sentiment_result",
                "schema": schema
            }
        }
    },
    messages=[{"role": "user", "content": "Analyze sentiment: 'The new API is incredibly fast and docs are clear.'"}]
)

result = json.loads(response.content[0].text)
# {"sentiment": "positive", "confidence": 0.95, "key_phrases": ["incredibly fast", "docs are clear"]}
# sentiment is always "positive", "negative", or "neutral" — no parse error possible
```

**Tradeoff:** complex schemas with many nested objects produce larger grammars that take longer to compile on the first call. Keep schemas focused; subsequent calls with the same schema benefit from server-side caching.

**Pattern 2: strict:true tool use (guaranteed tool call parameters)**

Add `"strict": true` as a top-level property in a tool definition. When Claude calls that tool, its `input` always matches your `input_schema` — no coercion, no extra keys, no missing required fields.

```python
tool = {
    "name": "create_task",
    "description": "Create a task in the task management system",
    "strict": True,
    "input_schema": {
        "type": "object",
        "properties": {
            "title": {"type": "string", "maxLength": 100},
            "priority": {"type": "integer", "minimum": 1, "maximum": 5},
            "assignee_id": {"type": "string", "pattern": "^usr_[a-z0-9]+$"}
        },
        "required": ["title", "priority"],
        "additionalProperties": False
    }
}

response = client.messages.create(
    model="claude-sonnet-4-6-20260901",
    max_tokens=256,
    tools=[tool],
    messages=[{"role": "user", "content": "Create a high-priority task: 'Fix the auth bug', assigned to usr_abc123."}]
)

for block in response.content:
    if block.type == "tool_use" and block.name == "create_task":
        task_data = block.input
        # task_data["priority"] is always 1-5, title always ≤ 100 chars — guaranteed
```

**Combining both patterns**

You can set `strict: true` on tools AND pass `output_config` in the same request. Claude calls tools with guaranteed-valid parameters and returns a structured final response — the right shape for multi-step agentic workflows.

```python
response = client.messages.create(
    model="claude-sonnet-4-6-20260901",
    max_tokens=1024,
    tools=[tool],           # strict: True
    output_config={
        "format": {
            "type": "json_schema",
            "json_schema": {"name": "summary", "schema": summary_schema}
        }
    },
    messages=[{"role": "user", "content": "Create tasks for the auth bug fix and return a summary."}]
)
# Claude calls create_task (guaranteed-valid inputs) then returns structured summary JSON
```

This pattern is available on Claude Sonnet 4.5+, Opus 4.5+, Haiku 4.5+, and later.

**Sources:** [Structured outputs — Claude Platform Docs](https://platform.claude.com/docs/en/build-with-claude/structured-outputs) · [Strict tool use — Claude Platform Docs](https://platform.claude.com/docs/en/agents-and-tools/tool-use/strict-tool-use) · [How to implement tool use — Claude Platform Docs](https://platform.claude.com/docs/en/agents-and-tools/tool-use/implement-tool-use)
]]></content:encoded>
    </item>


    <item>
      <title>Skip Permissions Safely: Run Claude Code in Full-Autonomy Mode Inside a Locked-Down Docker Container</title>
      <link>https://www.docker.com/blog/docker-sandboxes-run-claude-code-and-other-coding-agents-unsupervised-but-safely/</link>
      <guid isPermaLink="false">2026-08-06-06-claude-code-docker-devcontainer-sandbox</guid>
      <pubDate>Thu, 06 Aug 2026 12:06:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>Workflow</category>
      <category>Claude Code</category>
      <category>Best Practices</category>
      <description>A deny-all Docker container makes --dangerously-skip-permissions safe: Claude gets full autonomy inside an isolated filesystem and network wall, never touching your host.</description>
      <media:content url="https://www.docker.com/app/uploads/2025/07/2400x1260_docker-sandboxes-blog.png" medium="image" type="image/png" />
      <media:thumbnail url="https://www.docker.com/app/uploads/2025/07/2400x1260_docker-sandboxes-blog.png" />
      <content:encoded><![CDATA[
**A deny-all Docker container makes `--dangerously-skip-permissions` safe: Claude gets full autonomy inside an isolated filesystem and network wall, never touching your host machine.**

`--dangerously-skip-permissions` removes every approval dialog -- Claude can read, write, and execute without asking. On your laptop that means it can touch all your files and make any network request. Inside a container it can only touch what you explicitly mounted, and only reach the network endpoints you allow.

This matters even more after today's v2.1.223 patches, which fixed two permission-bypass bugs. Container isolation is defense-in-depth: even if a future bypass exists, a sandboxed Claude cannot escape to your host.

## Three paths to a sandboxed session

**Option 1: Docker Desktop sbx launcher (fastest)**

Docker Desktop 4.40+ ships a built-in sandbox launcher. One command gives you an isolated Claude Code environment:

```bash
docker sbx run anthropic/claude-code@latest --dangerously-skip-permissions
```

The launcher mounts your current directory into /workspace, installs a deny-all outbound firewall that permits only the Anthropic API and npm registry, and removes the container on exit.

**Option 2: Anthropic official devcontainer feature (best for VS Code teams)**

Add `.devcontainer/devcontainer.json` to any project:

```json
{
  "name": "Claude Code Sandbox",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "features": {
    "ghcr.io/anthropics/claude-code-features/claude-code:latest": {}
  },
  "postCreateCommand": "npm install --prefix /workspace",
  "remoteEnv": {
    "ANTHROPIC_API_KEY": "${localEnv:ANTHROPIC_API_KEY}"
  }
}
```

Open in VS Code with the Dev Containers extension and run `claude --dangerously-skip-permissions` in the integrated terminal. Your project files are mounted read-write; everything else on your host is unreachable.

**Option 3: Manual Dockerfile with firewall (most control)**

For tighter control -- allow npm and GitHub but block everything else:

```dockerfile
FROM ubuntu:24.04
RUN apt-get update && apt-get install -y nodejs npm nftables curl
RUN npm install -g @anthropic-ai/claude-code
COPY firewall.sh /usr/local/bin/firewall.sh
RUN chmod +x /usr/local/bin/firewall.sh
ENTRYPOINT ["/usr/local/bin/firewall.sh"]
CMD ["claude", "--dangerously-skip-permissions"]
```

```bash
# firewall.sh -- allow only Anthropic + npm, deny all else
nft add table inet filter
nft add chain inet filter output '{ type filter hook output priority 0 ; policy drop ; }'
nft add rule inet filter output ip daddr 18.157.0.0/16 accept
nft add rule inet filter output ip daddr 104.16.0.0/12 accept
nft add rule inet filter output ct state established accept
```

Run it with:

```bash
docker build -f Dockerfile.claude-sandbox -t claude-sandbox .
docker run -it --rm   -v "$(pwd)":/workspace   -e ANTHROPIC_API_KEY="$ANTHROPIC_API_KEY"   --cap-add NET_ADMIN   claude-sandbox
```

## What the container buys you

| Threat | Without container | With container |
|---|---|---|
| Claude reads ~/.ssh | Possible | Blocked -- not mounted |
| Claude exfiltrates to attacker server | Possible | Blocked -- deny-all firewall |
| Permission bypass bug | Exploitable | Contained to workspace only |
| Runaway file deletions | Host filesystem | Only mounted project dir |

The setup takes under 10 minutes. For any team running Claude Code in auto-approve mode on production code, it is the right foundation.

**Sources:** [Docker Sandboxes blog -- Docker.com](https://www.docker.com/blog/docker-sandboxes-run-claude-code-and-other-coding-agents-unsupervised-but-safely/) . [Trail of Bits devcontainer for Claude Code](https://github.com/trailofbits/claude-code-devcontainer) . [Run Claude Code Sandboxed -- DEV Community](https://dev.to/wartzarbee/how-to-run-claude-code-sandboxed-containers-network-walls-and-secret-isolation-2jkn) . [Claude Code on the web docs](https://code.claude.com/docs/en/claude-code-on-the-web)
]]></content:encoded>
    </item>

    <item>
      <title>Claude Code v2.1.223 Patches Two Permission-Layer Flaws That Let Commands Hide From You</title>
      <link>https://code.claude.com/docs/en/changelog</link>
      <guid isPermaLink="false">2026-08-06-05-claude-code-v2123-permission-security</guid>
      <pubDate>Thu, 06 Aug 2026 12:05:00 +0000</pubDate>
      <dc:creator>Chris Harper</dc:creator>
      <category>AI</category>
      <category>News</category>
      <category>Claude Code</category>
      <category>Security</category>
      <description>Today v2.1.223 closes a Bash compound-statement bypass and an invisible Unicode trick that let crafted commands hide parts of themselves from Claude Code approval dialogs.</description>
      <content:encoded><![CDATA[
**Today v2.1.223 closes a Bash compound-statement bypass and invisible Unicode tab-padding that let crafted commands hide parts of themselves from Claude Code approval dialogs -- upgrade immediately if you run any auto-approve hooks.**

Two security fixes shipped in today's release, both targeting the permission layer that decides whether Claude Code needs user approval before running a command.

**Bash compound-statement bypass:** a crafted command using Bash compound-statement syntax could make the permission analyzer see a benign-looking command while Bash executed something else. The fix makes the analyzer fail closed on file-descriptor redirect forms it parses differently than Bash does.

**Invisible Unicode / tab padding:** commands padded with tab characters or invisible Unicode code points could visually hide part of the command in the approval dialog. You would approve what looked like "git status" but Bash would run something more. This closes the same class of bypass that v2.1.211 patched for whitespace characters in June.

These join a string of permission-hardening fixes since June (v2.1.211, v2.1.214, v2.1.216, now v2.1.223). The pattern suggests targeted fuzzing of the permission layer.

**Other changes in v2.1.223:**
- Added /teleport hint in cloud sessions showing the CLI handoff command
- Added owner/* wildcard support in managed marketplace settings
- Added a warning when a background agent's requested model is restricted by policy

**What to do:** run `claude update` or `npm update -g @anthropic-ai/claude-code` to get v2.1.223. If you have allowedTools or auto-approve hooks configured, this update is particularly relevant -- the bypass classes fixed today affect exactly that mode.

**Why it matters:** as Claude Code agents run unattended in CI and cloud sessions, the permission layer is the last defense between Claude and unintended system commands. Keeping it bypass-proof is foundational to safe agentic automation.

**Sources:** [Claude Code changelog -- code.claude.com](https://code.claude.com/docs/en/changelog) . [Releasebot Anthropic Claude Code August 2026](https://releasebot.io/updates/anthropic/claude-code) . [Claude Code permission guide -- petefreitag.com](https://www.petefreitag.com/blog/claude-code-permissions/)
]]></content:encoded>
    </item>

    
    
    
    

    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    


    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    

    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    

    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
        
    
    
    
    
    

    

    

    

    

    
    
        
    
    
    
    
        
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    

    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    

    
    
    
    
    
    


    
    

    
    
    
    
    
    
    
    
    
    
    
    

    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
        
    
    
    
    
    
    
    
    

    
    
    
    
    
    
    
    
    
    
    
    
    

    
    
    
    
    
    
    
    
    
    
    

    
    
    
    
    
    
    
    
    

    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    
    

  </channel>
</rss>